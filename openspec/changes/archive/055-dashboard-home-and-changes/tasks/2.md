---
title: When the dashboard opens, it shows a home of what needs you, and a changes list links every change
verify: pnpm verify
scope:
  - packages/ui/src/router.ts
  - packages/ui/src/data.ts
  - packages/ui/src/app.tsx
  - packages/ui/src/home/**
  - packages/ui/src/changes/**
  - tests/ui-data.test.tsx
  - tests/ui-home.test.tsx
  - tests/ui-changes.test.tsx
entry:
  - packages/ui/src/router.ts
  - packages/ui/src/data.ts
  - packages/ui/src/app.tsx
  - packages/ui/src/change/ChangeView.tsx
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] `Route` gains `{ name: 'home' }` at `#/` and `{ name: 'changes' }` at `#/changes`. The empty hash, `#/`, and any unknown hash resolve to home. `#/report`, `#/graph`, and `#/changes/<key>` are unchanged.
- [ ] The data module loads the inbox for home and the graph for the changes list, preferring inlined documents as it does for the other routes.
- [ ] The nav reads Home · Changes · Report · Graph, in that order.
- [ ] `HomeView` under `packages/ui/src/home/` renders the Dashboard home requirement of the delta. Kinds read `awaiting approval`, `task dead`, `task regressed`, and `change regressed`. Each change links to `#/changes/<id>`. Elapsed time uses `formatSeconds` from `change/format.ts`.
- [ ] `ChangesView` under `packages/ui/src/changes/` renders the Changes list requirement of the delta, using the shared `formatCost` from `packages/ui/src/format.ts` for both cost columns.
- [ ] New `tests/ui-home.test.tsx` and `tests/ui-changes.test.tsx` render each view from inlined documents and assert every scenario of the two requirements. `tests/ui-data.test.tsx` expects home wherever it expected the report for an empty or unknown hash, and adds the two new routes to its round-trip cases.
- [ ] The task's verify command passes, UI files stay at or under 250 lines, and the staged bundle stays under its budget.

## Details

This task's `scope` is authoritative over any injected code-ownership rules.
Change 054 created `packages/ui/src/format.ts`; import `formatCost` from it,
not from a view folder. The inbox's `InboxChangeRef.id` is the change's numeric
id. `/api/changes/<id>` and the `change` route both resolve a numeric id, so
link with it.

Follow the patterns of the existing routes in `app.tsx`: an empty-state
paragraph while a document is null, and `onNavigate` for internal links. Render
commands in `<code>`. Add no clipboard button and no action that runs a
command. Keep new components small, one per file, and give each group a heading
(`h2`) so change 056 can style the hierarchy.
