# Task 1 result — Portable inbox lock fixture

## Changed

- `tests/inbox.test.ts`
  - `setLiveLock` now derives a single `1.pid` path under the fixture change
    (`openspec/changes/005-running-mixed/.run/running/1.pid`) and calls
    `fs.mkdir(path.dirname(lockPath), { recursive: true })` before
    `fs.writeFile`. Payload (`{ pid: process.pid, startedAt }`) and return
    value (`startedAt`) are unchanged.
  - Added regression test `materializes the missing runtime lock directory from
    clean tracked fixture state` inside the bare-CLI inbox integration suite. It
    deletes the copied fixture's ignored `.run/running/` directory, asserts
    `setLiveLock` recreates it and preserves the PID/timestamp payload, and then
    runs the bare CLI with `--json` to confirm the live task still projects.

## Verification

- Reproduced the clean-checkout failure first: with
  `fixture/inbox/openspec/changes/005-running-mixed/.run/running/` removed
  (gitignored, untracked), the two integration cases failed with `ENOENT` at
  `setLiveLock`. After the fix the directory is created recursively and both
  cases pass from that same clean state.
- `node --import tsx --test tests/inbox.test.ts` → 14 pass, 0 fail.
- `pnpm tsc --noEmit` → clean.
- `pnpm lint` → clean (188 files).
- `pnpm test` → 892 pass, 0 fail.

## Deviations / notes

- Acceptance line 4 verified via `git status`: only `tests/inbox.test.ts` is
  modified. No lock marker, placeholder, or `.run/running/` entry is committed;
  the directory remains gitignored and absent from tracked state.
- Acceptance line 5: no production files changed.
- No drift against `openspec/specs/status-inspection/spec.md`; the inbox
  projection/running/JSON contract behavior is untouched.
