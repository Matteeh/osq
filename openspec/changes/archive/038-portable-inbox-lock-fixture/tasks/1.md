---
title: When a clean checkout runs inbox integration tests, runtime lock setup succeeds
verify: node --import tsx --test tests/inbox.test.ts
scope:
  - tests/inbox.test.ts
entry:
  - tests/inbox.test.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] The copied inbox fixture succeeds when no `.run/running/` directory or PID files exist in tracked source state.
- [ ] Live-lock setup creates the target marker directory recursively before writing `1.pid` with the test process PID and existing timestamp payload.
- [ ] Both bare text and `--json` integration cases reach their CLI assertions and retain their existing live-running-task expectations.
- [ ] Runtime PID files remain ephemeral and ignored; no fixture lock marker or placeholder file is committed.
- [ ] Production inbox, state, lock, watcher, CLI, and cursor behavior remains unchanged.

## Details

Update the existing `setLiveLock` helper in `tests/inbox.test.ts` so it
materializes the runtime marker parent before calling `fs.writeFile`. Derive
the directory and `1.pid` path from the same fixture change path, and use the
existing `fs` promises API with recursive directory creation. Keep the current
lock payload, return value, integration assertions, copied fixture contents,
and cleanup behavior intact. Do not add a tracked placeholder under the
ignored running directory and do not change production code.
