## Changed

- `templates/proposal.md`, `templates/openspec/schemas/osq/templates/proposal.md`,
  and `FALLBACK_PROPOSAL_MD` in `src/core/foundation/new.ts` gained the
  `## Decisions` section (comment plus `None`) between `## Surface` and
  `## Contract`; all three are byte-identical.
- `templates/openspec/schemas/osq/schema.yaml` and its repository copy
  `openspec/schemas/osq/schema.yaml` gained the `**Decisions**` instruction
  bullet between the Surface and Contract bullets.
- `templates/openspec/config.yaml` and `openspec/config.yaml` changed only the
  first proposal rule to name `Surface, Decisions, Contract`.
- `MANAGED_PLANNER_BLOCK` in `src/core/foundation/init-blocks.ts`, `PLANNER.md`,
  and `templates/PLANNER.md` gained the decisions bullet directly after the
  Surface bullet under `### Parent spec`; the three managed blocks are equal.
- Regenerated `tests/fixtures/events/verified.jsonl` and
  `tests/fixtures/events/dead.jsonl` with `UPDATE_GOLDEN=1`; only the two
  `proposalWords` measures in each changed.
- Added `tests/decisions-template.test.ts` (9 tests) covering the seeded
  decisions section through `osq new` and the fallback, byte-identity of the
  proposal template, schema, and config trees, the instruction and config rule
  ordering, and the planner bullet.

Task verify (`node --import tsx --test tests/decisions-template.test.ts`, 9/9)
and the proposal verify (`pnpm verify`, exit 0) both pass.

## Deviated

- The task predicted golden `proposalWords` would move 262 -> 272 "with a
  shorter comment"; using the exact comment text supplied in the task it is
  262 -> 311. Only `proposalWords` changed, as acceptance requires.
- `src/core/foundation/new.ts` crossed the 250-line budget when the fallback
  gained the section, so I removed three blank separator lines around
  `legacyTemplateDir`/`targetDir` in `createNewSpec`. No code behavior changed.

Touched: templates/proposal.md, templates/openspec/schemas/osq/templates/proposal.md, tests/decisions-template.test.ts, templates/openspec/schemas/osq/schema.yaml, openspec/schemas/osq/schema.yaml, templates/openspec/config.yaml, openspec/config.yaml, openspec/schemas/osq/templates/proposal.md, src/core/foundation/init-blocks.ts, src/core/foundation/new.ts, PLANNER.md, templates/PLANNER.md, tests/fixtures/events/verified.jsonl, tests/fixtures/events/dead.jsonl
