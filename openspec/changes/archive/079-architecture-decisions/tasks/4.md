---
title: When osq new, an OpenSpec-aware agent, or the planner starts a proposal, every entry point asks for a Decisions section
verify: node --import tsx --test tests/decisions-template.test.ts
verify_starts: red
scope:
  - templates/**
  - PLANNER.md
  - src/core/foundation/init-blocks.ts
  - src/core/foundation/new.ts
  - openspec/config.yaml
  - openspec/schemas/osq/**
  - tests/decisions-template.test.ts
  - tests/fixtures/events/*.jsonl
entry:
  - templates/proposal.md
  - templates/openspec/schemas/osq/schema.yaml
  - src/core/foundation/init-blocks.ts
  - tests/proposal-format.test.ts
  - tests/golden-events.test.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] `templates/proposal.md` gains the Decisions section below between `## Surface` and `## Contract`. `templates/openspec/schemas/osq/templates/proposal.md` and `FALLBACK_PROPOSAL_MD` in `new.ts` hold the same text.
- [ ] In `templates/openspec/schemas/osq/schema.yaml`, the proposal `instruction` gains the Decisions bullet below between the Surface and Contract bullets. The first proposal rule in `templates/openspec/config.yaml` reads `"Follow the proposal template's sections in order: Goal, Verify, Non-goals, Surface, Decisions, Contract, Human steps, Delta."`. No other bytes change in either file.
- [ ] `MANAGED_PLANNER_BLOCK` gains the planner bullet below directly after the Surface bullet under `### Parent spec`. The managed blocks in `PLANNER.md` and `templates/PLANNER.md` equal it byte for byte.
- [ ] `openspec/config.yaml` and every file under `openspec/schemas/osq/` stay byte-identical to their `templates/openspec/` counterparts.
- [ ] New `tests/decisions-template.test.ts` covers "Seeded decisions section" of the modified "One proposal format" and "Planner decisions guidance", and asserts the instruction and config rule name Decisions between Surface and Contract.
- [ ] The golden event fixtures under `tests/fixtures/events/` are regenerated with `UPDATE_GOLDEN=1`, and the only values that change are the `proposalWords` measures.

## Details

Edit `PLANNER.md` and `templates/PLANNER.md` only between `<!-- OSQ:START -->`
and `<!-- OSQ:END -->`. Copy the block by hand; don't run `osq init`. In
`init-blocks.ts` and `new.ts` the constants are template literals, so escape
each backtick as `` \` ``. `tests/proposal-format.test.ts`,
`tests/init-planner.test.ts`, `tests/managed-blocks.test.ts`, and
`tests/new.test.ts` must pass unchanged; a trial of this change passed them.

Regenerate with
`UPDATE_GOLDEN=1 node --import tsx --test tests/golden-events.test.ts`, then
rerun without the variable. A trial changed only `proposalWords`, from 262 to
272 with a shorter comment. If any other field changes, write that in the
result file.

### Template section

```markdown
## Decisions

<!-- One line per accepted ADR that governs a capability this change writes,
saying what it means here, such as "ADR 009: the adapter is the only module
that imports dockerode." Start a line "Departs from ADR <n>:" to record a
departure and its reason. -->
None

```

### Schema instruction bullet

```yaml
      - **Decisions**: one line per accepted ADR that governs a capability the
        change writes, saying what it means for this change. Start a departure
        line with `Departs from ADR <n>:`. Write `None` when no ADR governs it.
```

### Planner bullet

```markdown
- `## Decisions` follows `## Surface`. Give one line per accepted ADR that
  governs a capability the change writes, saying what it means for this
  change, such as `ADR 009: the adapter is the only module that imports
  dockerode.` Name a system-wide ADR only to depart from it; AGENTS.md already
  carries its rule. A departure line starts `Departs from ADR <n>:` and gives
  the reason; a needed departure is a reason for a new ADR. Write `None` when
  no ADR governs the change. Repeat a rule in a task only when that task
  touches the area.
```
