---
title: When a change is digested for approval, it lists the governing ADRs and flags each departure
verify: node --import tsx --test tests/approval-digest-decisions.test.ts
verify_starts: red
scope:
  - src/core/spec/digest-decisions.ts
  - src/core/spec/digest.ts
  - src/core/spec/digest-format.ts
  - src/core/report/approval-flags.ts
  - tests/report-approval-flags.test.ts
  - tests/report-json.test.ts
  - fixture/report/expected.json
  - tests/approval-digest-decisions.test.ts
entry:
  - src/core/spec/digest.ts
  - src/core/spec/digest-flags.ts
  - src/core/foundation/decisions.ts
  - src/core/spec/approve.ts
  - tests/approval-digest.test.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] New `src/core/spec/digest-decisions.ts` implements the spec-lint-and-approve delta's "Approval digest decisions" and "ADR departure flag" with `readDecisions` and `governingAdrs`, and returns the digest's decisions and its `adr_departure` flags.
- [ ] `ApprovalFlagId` gains `adr_departure`. `ApprovalDigest` gains `decisions`, a list of `{ number, rule }`. `buildApprovalDigest` appends the departure flags after the flags from `buildApprovalFlags`.
- [ ] `formatApprovalDigest` prints the `Decisions:` lines after the capabilities and before the human steps only when `decisions` isn't empty.
- [ ] `FLAG_IDS` in `approval-flags.ts` gains `adr_departure` after `verify_starts_conflict`, as the metrics-and-reporting delta's "ADR departure flag outcomes" says.
- [ ] `tests/report-approval-flags.test.ts` and `tests/report-json.test.ts` expect the new `adr_departure` key with zero counts, and `fixture/report/expected.json` gains it. Nothing else in them changes.
- [ ] New `tests/approval-digest-decisions.test.ts` covers every scenario of the three requirements, including one change that records `adr_departure` through `approveSpec` and is counted by the report's approval flag outcomes.

## Details

The written capabilities are the digest's delta capabilities. Read the
proposal's `## Decisions` section in the new module. With no decisions folder
the digest's formatted text and flags stay as they are today, so
`tests/approval-digest.test.ts` passes unchanged. A trial adding the flag id
failed only the two report tests and the fixture in scope. `digest.ts` is 222
lines and must stay within 250, and `buildApprovalDigest` is 58 lines and must
stay within 80. New functions stay within 80 lines and new files within 250.
