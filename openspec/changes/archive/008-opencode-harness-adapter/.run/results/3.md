## Changed

- Updated `src/harness/opencode.ts`:
  - Implemented `OpencodeAdapter.setup` to create `.opencode/agent/` directory recursively if absent.
  - Defined `OPENCODE_AGENT_TEMPLATE` with frontmatter configuring `description`, `mode: subagent`, and `permission` allowing `read`, `edit`, `bash`, `glob`, `grep` and denying `webfetch`, `websearch`.
  - Enclosed the `AGENTS.md` execution and exiting protocol within `<!-- OSQ:START -->` and `<!-- OSQ:END -->` managed block markers.
  - Ensured `setup` is idempotent and preserves user edits outside the managed block markers (frontmatter and surrounding body content).
  - Supported custom agent names configured in `config.opencode.agent` (defaulting to `osq-coder.md`).
- Updated `README.md`:
  - Documented `osq setup` scaffolding behavior for the `opencode` harness.
  - Documented caveat noting that the `--auto` flag approves any action the agent file does not deny.
- Created `tests/opencode-setup.test.ts`:
  - Added tests for all acceptance criteria: directory creation, agent file frontmatter, tool permissions, managed block markers and task execution procedure, idempotency, user edits preservation, README `--auto` documentation, and custom agent name resolution.

## Next Steps

- Proceed to Task 4 (`specs/008-opencode-harness-adapter/tasks/4.md`): Implement `OpencodeAdapter.spawn` to run `opencode run` with `--auto`, `--format json`, `--dir`, configured model, agent, attaching `task.md`, `spec.md`, and named feature docs via `--file`.
