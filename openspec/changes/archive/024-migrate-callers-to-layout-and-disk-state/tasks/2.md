---
title: When change workflow runs, commands use layout resolvers
verify: node --import tsx --test tests/approve.test.ts tests/linter.test.ts tests/new.test.ts
scope: [src/cli/lint.ts, src/core/approve.ts, src/core/linter.ts, src/core/new.ts, tests/approve.test.ts, tests/linter.test.ts, tests/new.test.ts]
entry: [src/core/linter.ts, src/core/approve.ts]
skills: []
tests:
  modify: true
---
## Acceptance
- [ ] src/cli/lint.ts resolves change directories using getChangesDir from src/core/layout.ts
- [ ] src/core/approve.ts resolves target spec directory using getChangesDir
- [ ] src/core/linter.ts checks dependency presence via getChangesDir and getArchiveDir
- [ ] src/core/new.ts uses getChangesDir to locate change folders and calculate next spec number
- [ ] tests/approve.test.ts uses getChangesDir instead of removed config.paths.specs
- [ ] tests/linter.test.ts and tests/new.test.ts pass without config path errors
- [ ] All scoped modules compile cleanly under TypeScript

## Details

1. In `src/cli/lint.ts`:
   - Import `getChangesDir` from `../core/layout.js`.
   - Replace `path.join(cwd, config.paths.specs)` with `getChangesDir(config.paths.openspecRoot, cwd)`.
2. In `src/core/approve.ts`:
   - Import `getChangesDir` from `./layout.js`.
   - Replace `path.join(projectRoot, config.paths.specs)` with `getChangesDir(config.paths.openspecRoot, projectRoot)`.
3. In `src/core/linter.ts`:
   - Import `getChangesDir` and `getArchiveDir` from `./layout.js`.
   - In `checkDependencyExists`, check in `getChangesDir(config.paths.openspecRoot, projectRoot)` and `getArchiveDir(config.paths.openspecRoot, projectRoot)` instead of `config.paths.specs`.
4. In `src/core/new.ts`:
   - Import `getChangesDir` from `./layout.js`.
   - Replace `DEFAULT_CONFIG.paths.specs` with `getChangesDir(DEFAULT_CONFIG.paths.openspecRoot)`.
5. In `tests/approve.test.ts`:
   - Import `getChangesDir` from `../src/core/layout.js`.
   - Replace `DEFAULT_CONFIG.paths.specs` with `getChangesDir(DEFAULT_CONFIG.paths.openspecRoot)`.
6. In `tests/linter.test.ts` and `tests/new.test.ts`:
   - Verify tests run and pass cleanly.
7. Verification:
   - Run `node --import tsx --test tests/approve.test.ts tests/linter.test.ts tests/new.test.ts`.
