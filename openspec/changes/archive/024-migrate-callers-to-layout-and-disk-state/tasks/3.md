---
title: When lifecycle archiving runs, watcher derives locations canonically
verify: node --import tsx --test tests/doctor.test.ts tests/archiver.test.ts tests/migrate.test.ts
scope: [src/watcher/archiver.ts, src/core/doctor.ts, src/core/migrate.ts, tests/doctor.test.ts, tests/archiver.test.ts, tests/migrate.test.ts]
entry: [src/watcher/archiver.ts, src/core/doctor.ts]
skills: []
tests:
  modify: true
---
## Acceptance
- [ ] src/watcher/archiver.ts resolves archive directory using getArchiveDir from src/core/layout.ts
- [ ] src/watcher/archiver.ts derives completed spec state via overloaded deriveSpecState
- [ ] src/core/doctor.ts validates paths and archives using getChangesDir and getArchiveDir
- [ ] src/core/migrate.ts maps legacy specs without reading removed config.paths.specs or archive
- [ ] tests/doctor.test.ts passes all 5 diagnostic checks cleanly
- [ ] tests/archiver.test.ts tests archive derivation with getArchiveDir
- [ ] tests/migrate.test.ts passes clean verification

## Details

1. In `src/watcher/archiver.ts`:
   - Import `getArchiveDir` from `../core/layout.js`.
   - Replace `path.join(projectRoot, config.paths.archive)` with `getArchiveDir(config.paths.openspecRoot, projectRoot)`.
   - Call `await deriveSpecState(projectRoot, specFolderPath)`.
2. In `src/core/doctor.ts`:
   - Import `getChangesDir` and `getArchiveDir` from `./layout.js`.
   - Replace checks on `config.paths.specs` and `config.paths.archive` with validation of `config.paths.openspecRoot`.
   - In `checkOrphanedLocks`, resolve roots using `getChangesDir(config.paths.openspecRoot, projectRoot)`.
   - In `checkArchives`, resolve archive directory using `getArchiveDir(config.paths.openspecRoot, projectRoot)`.
3. In `src/core/migrate.ts`:
   - Import `getChangesDir` and `getArchiveDir` from `./layout.js`.
   - Replace `config.paths.specs` and `config.paths.archive` fallback references with `getChangesDir(config.paths.openspecRoot)` and `getArchiveDir(config.paths.openspecRoot)`.
4. In `tests/archiver.test.ts`:
   - Import `getArchiveDir` from `../src/core/layout.js`.
   - Update tests to assert archive destinations using `getArchiveDir(DEFAULT_CONFIG.paths.openspecRoot, tmpDir)`.
5. Verification:
   - Run `node --import tsx --test tests/doctor.test.ts tests/archiver.test.ts tests/migrate.test.ts`.
