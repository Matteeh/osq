---
title: When test suites execute, assertions verify layout path derivation
verify: node --import tsx --test tests/cut-over.test.ts tests/watcher-preflight.test.ts tests/watcher.test.ts
scope: [tests/cut-over.test.ts, tests/watcher-preflight.test.ts, tests/watcher.test.ts]
entry: [tests/cut-over.test.ts, tests/watcher.test.ts]
skills: []
tests:
  modify: true
---
## Acceptance
- [ ] tests/cut-over.test.ts asserts openspecRoot in DEFAULT_CONFIG.paths instead of removed specs and archive
- [ ] tests/watcher-preflight.test.ts resolves archive directory using getArchiveDir
- [ ] tests/watcher.test.ts resolves archive directory using getArchiveDir
- [ ] node --import tsx --test tests/cut-over.test.ts tests/watcher-preflight.test.ts tests/watcher.test.ts passes
- [ ] Repository-wide pnpm tsc --noEmit produces 0 errors
- [ ] Repository-wide pnpm test passes completely
- [ ] osq status executes without errors and reports active and archived spec counts

## Details

1. In `tests/cut-over.test.ts`:
   - Import `getChangesDir` and `getArchiveDir` from `../src/core/layout.js`.
   - Update tests asserting on `DEFAULT_CONFIG.paths.specs` / `DEFAULT_CONFIG.paths.archive` to assert `openspecRoot === 'openspec'` and that `getChangesDir` / `getArchiveDir` resolve the canonical paths.
2. In `tests/watcher-preflight.test.ts`:
   - Import `getArchiveDir` from `../src/core/layout.js`.
   - Replace `path.join(tmpDir, DEFAULT_CONFIG.paths.archive, folderName)` with `path.join(getArchiveDir(DEFAULT_CONFIG.paths.openspecRoot, tmpDir), folderName)`.
3. In `tests/watcher.test.ts`:
   - Import `getArchiveDir` from `../src/core/layout.js`.
   - Replace `path.join(tmpDir, DEFAULT_CONFIG.paths.archive, folderName)` with `path.join(getArchiveDir(DEFAULT_CONFIG.paths.openspecRoot, tmpDir), folderName)`.
4. Verification:
   - Run `node --import tsx --test tests/cut-over.test.ts tests/watcher-preflight.test.ts tests/watcher.test.ts`.
