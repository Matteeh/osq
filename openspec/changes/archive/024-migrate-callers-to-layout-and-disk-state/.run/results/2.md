# Task 2 — workflow/scaffolding callers use layout resolvers

## Changed

- `src/core/linter.ts`
  - `checkDependencyExists` now takes `(projectRoot, depId, config)` and builds
    `dirsToCheck` from `getChangesDir(config.paths.openspecRoot, projectRoot)` and
    `getArchiveDir(config.paths.openspecRoot, projectRoot)` instead of an ad-hoc
    `path.join(specsDir, 'archive')`.
  - Call site updated to `checkDependencyExists(projectRoot, dep, config)`; the
    previously awkward pass-through of an already-resolved directory is gone.
  - Reordered `./delta.js` / `./layout.js` imports for `biome check`.
- `tests/approve.test.ts`
  - `findSpecFolder` test now resolves `specsDir` via
    `getChangesDir(DEFAULT_CONFIG.paths.openspecRoot, tmpDir)`; import added.
- `tests/linter.test.ts`
  - Added `accepts depends_on naming a change that lives in the archive`, which
    seeds `openspec/changes/archive/042-archived` and asserts the dependency
    resolves with no `missing change` error (covers the `getArchiveDir` branch).
- Also present and verified from the interrupted prior pass (left in place):
  - `src/cli/lint.ts` resolves the change root via
    `getChangesDir(config.paths.openspecRoot, cwd)`.
  - `src/core/approve.ts` resolves the target spec directory via
    `getChangesDir(config.paths.openspecRoot, projectRoot)`.
  - `src/core/new.ts` resolves the default change root via
    `getChangesDir(DEFAULT_CONFIG.paths.openspecRoot, projectDir)` (wrapped for
    formatter); the `options.specsDirName` override is unchanged.

## Acceptance status

- [x] `src/cli/lint.ts` uses `getChangesDir`
- [x] `src/core/approve.ts` uses `getChangesDir`
- [x] `src/core/linter.ts` checks dependencies via `getChangesDir` + `getArchiveDir`
- [x] `src/core/new.ts` uses `getChangesDir`
- [x] `tests/approve.test.ts` no longer references removed `config.paths.specs`
- [x] `tests/linter.test.ts` and `tests/new.test.ts` pass without config path errors
- [x] Scoped modules compile cleanly under TypeScript

Verify command: `node --import tsx --test tests/approve.test.ts tests/linter.test.ts tests/new.test.ts`
→ **38 tests, 38 pass, 0 fail**.

Scoped `tsc --noEmit` filter: 0 diagnostics. `biome check` on all 7 scoped files:
clean.

## Deviated

- Added one extra test to `tests/linter.test.ts` beyond the literal task detail
  (item 6 said only "verify tests pass"). This is inside scope, uses the declared
  `tests.modify: true`, and directly covers acceptance line 3's `getArchiveDir`
  requirement.
- `checkDependencyExists` signature changed shape (now `config` rather than a raw
  directory string); acceptance text only requires the helpers be used, so this
  is a minimal supporting change within scope.

## Drift against features

- None. Matches `cli-foundation` "Canonical OpenSpec path layout" (all callers
  derive from `openspecRoot`, no `paths.specs`/`paths.archive`) and
  `watcher-and-harness` layout cut-over rule for the workflow modules.

## Missing context / out of scope

- `pnpm tsc --noEmit` still reports errors in modules owned by later tasks of this
  change (`src/core/doctor.ts`, `src/core/migrate.ts`, `src/watcher/archiver.ts`,
  and tests `archiver/cut-over/watcher/watcher-preflight` still reading removed
  `paths.specs`/`paths.archive`). Those are proposal items 4–5, not this task.
- There is no test asserting the archive branch of `checkDependencyExists` before
  this task; one was added here.

## Next steps

- All acceptance lines for task 2 are met; nothing left inside this task.
- Task 3/4 should migrate `doctor.ts`, `migrate.ts`, `archiver.ts`, and the
  remaining legacy-path tests to complete the change.
