## Changed

- `src/cli/serve.ts`: `ServeCommandOptions` gains an optional `exportDir`. `serveCommand` now prints through the injected `stdout` before the server branch, and when `exportDir` is set it resolves the directory against `cwd`, calls `exportDashboard` with the loaded `config` plus the injected `cwd`, `uiDir`, `home`, and `now`, prints `exported dashboard to <resolved dir>` and the scrub reminder, and returns without validating `--port`, binding a listener, or launching a browser.
- `src/cli/index.ts`: the `serve` command registers `.option('--export <dir>', ...)` and forwards `exportDir` to `serveCommand`. The existing action `try/catch` still prints `Error: <message>` and sets `process.exitCode = 1` for a refused or failed export.
- `README.md`: the command list gains `osq serve --export <dir>`, and the Delivery Dashboard section gains an `--export ./demo` example plus a sentence that the snapshot needs a static host, cannot open from `file://`, and scrubs only the project root and home paths.
- `tests/serve-export.test.ts`: new test covering export success output through injected `stdout`, the absence of a bound server on the ignored `--port`, no browser launch with `--open`, refusal of a non-empty target without writing, an unchanged serve path (printed loopback URL then abort), and the registered `--export` option.

## Missing context

- None.

## Next

- None; all acceptance lines are covered.

Touched: src/cli/serve.ts, src/cli/index.ts, README.md, tests/serve-export.test.ts
