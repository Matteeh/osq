# Tasks

## 1. Deterministic scope resolver

- [x] 1. When scope entries are resolved, exact paths and glob matches produce one deterministic file set

## 2. Resolver-versioned completion

- [x] 2. When completed tasks use resolver 2, hashing and audits migrate legacy markers through recertification

## 3. Resolved-scope lint

- [x] 3. When a change is linted, resolved scope preserves test compliance and warns about overlap

## 4. Resolver-versioned size evidence

- [x] 4. When scope size is measured and reported, resolver-2 file counts form a visibly separate series
