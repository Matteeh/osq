---
reason: blocked
fingerprint: sha256:8ea9ee2f2fae5dc799ef02c7d6baccb4aef80d8e33f3e1e45219bd27e694990e
---
`pnpm verify` stays red because of the out-of-scope `tests/watcher-preflight.test.ts`
case above. The task's own verify passes. Changing that test would trip the
watcher's `undeclared_test_change` gate, since it is not in this task's `scope`
and only `tests/opencode-spawn.test.ts` and `tests/harness-interactive.test.ts`
are authorized. Widen this task's scope to include
`tests/watcher-preflight.test.ts` (with the version bump) and re-run, or update
that case in a separate task.
