# Tasks

## 1. Doctor

- [x] 1. When doctor runs with the opencode harness, it reports whether the installed opencode is inside the tested range

## 2. Adapter

- [x] 2. When the opencode harness runs a task or a planning session, it uses opencode v2's flags and records the final step's usage
