---
title: When doctor runs with the opencode harness, it reports whether the installed opencode is inside the tested range
verify: node --import tsx --test tests/opencode-doctor.test.ts
verify_starts: red
tests:
  modify: true
scope:
  - src/core/foundation/config-opencode.ts
  - src/core/foundation/harness-catalog.ts
  - tests/opencode-doctor.test.ts
entry:
  - src/core/foundation/config-pi.ts
  - src/core/foundation/harness-catalog.ts
  - src/core/foundation/doctor.ts
skills: []
---
## Acceptance

- [ ] New `src/core/foundation/config-opencode.ts` exports `OPENCODE_TESTED_RANGE`, `assessOpencodeVersion`, and `diagnoseOpencode`, as the cli-foundation delta's "OpenCode diagnostics" says, messages word for word.
- [ ] The opencode entry in `harness-catalog.ts` declares `diagnose: diagnoseOpencode`.
- [ ] New `tests/opencode-doctor.test.ts` covers the three scenarios through `runDoctorChecks` with a fake opencode binary, plus unparseable output warning.

## Details

Model the assessment on `assessPiVersion` and `diagnosePi` in
`config-pi.ts`, which return a `harness-version` diagnosis. Unlike Pi, below
the range is a failure (`ok: false`), not a warning. Point the harness at the
fake binary with `OPENCODE_PATH` or `opencode.bin`.
