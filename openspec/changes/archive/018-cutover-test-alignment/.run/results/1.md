# Task 1 — createNewSpec scaffolds OpenSpec change folders

## Changed

- `tests/new.test.ts`: strengthened the `createNewSpec respects options.specsDirName override`
  case to also assert the override path is scaffolded with `proposal.md` (with the requested
  `title: Custom Spec`) and `tasks/1.md`, proving the override preserves the full OpenSpec layout
  rather than only the folder path.

## State on entry

The core implementation in `src/core/new.ts` was already present in the working tree when this
attempt started (carried over from the 016 OpenSpec cut-over, uncommitted):

- `specsDir` defaults to `path.join(projectDir, options.specsDirName || DEFAULT_CONFIG.paths.specs)`.
- Legacy `specs/_template` copy path retained when present (reads/replaces `spec.md` title).
- Otherwise seeds `proposal.md`, `tasks.md`, and `tasks/1.md` from `templates/`, replacing the
  `title:` line in `proposal.md`.
- `src/cli/new.ts` already consumes the `NewSpecResult` and logs the scaffolded path.

I verified this against every acceptance line rather than reimplementing.

## Acceptance coverage

1. Default target dir is `DEFAULT_CONFIG.paths.specs` (`openspec/changes`) — `src/core/new.ts:106`; test asserts `openspec/changes/001-order-cancellation`.
2. Scaffolds `proposal.md`, `tasks.md`, `tasks/1.md` — `src/core/new.ts:136-165`; test asserts all three exist.
3. `proposal.md` title updated to requested name — `src/core/new.ts:145`; test asserts `title: Order Cancellation` and `title: Custom Spec`.
4. `options.specsDirName` override preserved — test asserts custom path plus scaffolded proposal/task file.
5. `tests/new.test.ts` asserts default scaffolding under `openspec/changes` with `proposal.md` — covered by the first case.

## Verify

```
node --import tsx --test tests/new.test.ts   # 5 pass / 0 fail
pnpm tsc --noEmit                            # clean
pnpm exec biome check tests/new.test.ts src/core/new.ts src/cli/new.ts  # clean
```

## Deviated

None.

## Drift against features/

None. `createNewSpec` is not named in any living capability spec under `openspec/specs/`
(`cli-foundation` owns `templates/**` and config/init/logger only), so this scaffolding helper is
below the capability-drift threshold. No `features/writes` are declared by the parent proposal.

## Missing context

None.

## Next steps

None for this task. Task 2 (watcher/status fixture path alignment) and task 3 (migrate formatting)
remain for the parent change.
