# Task 2 — Test fixtures align with openspec layout paths

## Changed

No new edits were required. Every acceptance line was already satisfied by the test-fixture
path changes carried over in the working tree from the 016 OpenSpec cut-over, so this attempt
verified each line against the current files and the task verify command rather than
reimplementing.

Verified state of the scoped files:

- `tests/watcher-loop-logging.test.ts:193` — broken fixture is created at
  `path.join(tmpDir, 'openspec', 'changes', '099-broken')`; `runWatcherCycle` detects it and
  records exactly one `[error] watcher error:` line.
- `tests/status.test.ts:85,99` — archived fixtures are created under
  `path.join(tmpDir, 'openspec', 'changes', 'archive')`; `getStatusOverview` scans
  `DEFAULT_CONFIG.paths.archive` and reports the correct counts (2 and 1).
- `tests/watcher.test.ts:59,126` — archived folder is located via
  `DEFAULT_CONFIG.paths.archive`; both the single-task and multi-task cycle subtests pass.
- `tests/watcher-preflight.test.ts:393` — archived folder is located via
  `DEFAULT_CONFIG.paths.archive`; all six binary preflight subtests pass.

## Verify

```
node --import tsx --test tests/watcher-loop-logging.test.ts tests/watcher-preflight.test.ts tests/watcher.test.ts tests/status.test.ts
# tests 27 / suites 7 / pass 27 / fail 0
```

Independent checks:

```
pnpm tsc --noEmit                                              # clean
pnpm exec biome check tests/watcher-loop-logging.test.ts \
  tests/watcher-preflight.test.ts tests/watcher.test.ts tests/status.test.ts  # clean
```

## Acceptance coverage

1. `tests/watcher-loop-logging.test.ts` creates the broken change fixture under
   `openspec/changes` — line 193.
2. `tests/status.test.ts` creates archived change fixtures under `openspec/changes/archive` —
   lines 85 and 99.
3. `tests/watcher.test.ts` passes watcher cycle and multi-task execution subtests — both pass.
4. `tests/watcher-preflight.test.ts` passes all binary preflight subtests — all pass.
5. `tests/watcher-loop-logging.test.ts` passes symbol and status logging subtests — all pass.
6. `tests/status.test.ts` passes status overview and state inspection subtests — all pass.

## Deviated

None.

## Drift against features/

None. These are test-fixture path alignments only; no `features/writes` are declared by the
parent proposal and no living capability requirement under `openspec/specs/` names the affected
fixture paths.

## Missing context

None.

## Next steps

None for this task. Task 3 (Biome formatting conformance for `src/core/migrate.ts`) remains for
the parent change.
