---
title: When createNewSpec runs, change folders are created in openspec changes with proposal templates
verify: node --import tsx --test tests/new.test.ts
scope: [src/core/new.ts, src/cli/new.ts, tests/new.test.ts]
entry: [src/core/new.ts]
skills: []
tests:
  modify: true
---
## Acceptance
- [ ] createNewSpec in src/core/new.ts defaults target change directory to DEFAULT_CONFIG.paths.specs (openspec/changes)
- [ ] createNewSpec scaffolds change folders containing proposal.md, tasks.md, and tasks/1.md
- [ ] createNewSpec updates title in proposal.md to match the requested spec name
- [ ] createNewSpec preserves options.specsDirName override when explicitly supplied
- [ ] tests/new.test.ts asserts default scaffolding under openspec/changes with proposal.md

## Details

1. In `src/core/new.ts`:
   - Import `DEFAULT_CONFIG` from `./config.js`.
   - In `createNewSpec`:
     - Default `specsDir` to `path.join(projectDir, options.specsDirName || DEFAULT_CONFIG.paths.specs)`.
     - When determining templates to copy:
       - Check if `specs/_template` exists in `specsDir` (for legacy compatibility if `options.specsDirName` is supplied).
       - If not, or if using default OpenSpec layout, seed the change folder using OpenSpec template documents (`proposal.md`, `tasks.md`, and `tasks/1.md`):
         - Read templates from `templates/proposal.md`, `templates/tasks.md`, or the project's scaffolded templates.
         - Ensure `tasks/` directory is created with starter task `1.md`.
     - Read `proposal.md` (or `spec.md` if legacy) and replace `title: ...` with `title: ${trimmedTitle}`.
2. In `src/cli/new.ts`:
   - Ensure `newCommand` works with the updated `createNewSpec` result and logs the scaffolded path.
3. In `tests/new.test.ts`:
   - Update tests to verify that `createNewSpec(tmpDir, 'Order Cancellation')` scaffolds `openspec/changes/001-order-cancellation/` by default.
   - Assert `proposal.md`, `tasks.md`, and `tasks/1.md` exist inside the scaffolded directory.
   - Verify that passing `options.specsDirName: 'custom-specs'` still creates the change in the custom path.
