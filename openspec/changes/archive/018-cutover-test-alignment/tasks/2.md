---
title: When test fixtures execute, test suites align with openspec layout paths
verify: node --import tsx --test tests/watcher-loop-logging.test.ts tests/watcher-preflight.test.ts tests/watcher.test.ts tests/status.test.ts
scope: [tests/watcher-loop-logging.test.ts, tests/watcher-preflight.test.ts, tests/watcher.test.ts, tests/status.test.ts]
entry: [tests/watcher-loop-logging.test.ts]
skills: []
tests:
  modify: true
---
## Acceptance
- [ ] tests/watcher-loop-logging.test.ts creates broken change fixture under openspec/changes
- [ ] tests/status.test.ts creates archived change fixtures under openspec/changes/archive
- [ ] tests/watcher.test.ts passes all watcher cycle and multi-task execution subtests
- [ ] tests/watcher-preflight.test.ts passes all binary preflight subtests
- [ ] tests/watcher-loop-logging.test.ts passes all symbol and status logging subtests
- [ ] tests/status.test.ts passes all status overview and state inspection subtests

## Details

1. In `tests/watcher-loop-logging.test.ts`:
   - In test `'logs watcher errors at error level on permanent lines'`:
     - Replace `path.join(tmpDir, 'specs', '099-broken')` with `path.join(tmpDir, 'openspec', 'changes', '099-broken')`.
     - Ensure `runWatcherCycle(tmpDir, DEFAULT_CONFIG)` detects the broken folder under `openspec/changes/` and records the error log.
2. In `tests/status.test.ts`:
   - In test `'getStatusOverview returns count of archived change folders'`:
     - Replace `path.join(tmpDir, 'specs', 'archive', ...)` with `path.join(tmpDir, 'openspec', 'changes', 'archive', ...)`.
     - Verify `getStatusOverview` scans `DEFAULT_CONFIG.paths.archive` and reports the correct archived count.
3. In `tests/watcher.test.ts` and `tests/watcher-preflight.test.ts`:
   - With `createNewSpec` creating changes in `openspec/changes/` by default, verify `setupApprovedSpec`, `runWatcherOnce`, and preflight test cases find and execute approved specs without `Spec "001" not found` errors.
4. Verify:
   - Run `node --import tsx --test tests/watcher-loop-logging.test.ts tests/watcher-preflight.test.ts tests/watcher.test.ts tests/status.test.ts`.
