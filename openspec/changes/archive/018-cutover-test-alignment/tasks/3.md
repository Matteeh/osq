---
title: When biome check runs, migrate module formatting conforms to repo style
verify: pnpm exec biome check src/core/migrate.ts
scope: [src/core/migrate.ts]
entry: [src/core/migrate.ts]
skills: []
---
## Acceptance
- [ ] src/core/migrate.ts formats according to repository Biome rules
- [ ] pnpm exec biome check src/core/migrate.ts passes with zero diagnostics
- [ ] pnpm lint passes with zero errors across the repository

## Details

1. In `src/core/migrate.ts`:
   - Adjust formatting on lines assigning `legacyFeatures` and `legacyArchive` to adhere to Biome line-break style:
     ```ts
     const legacyFeatures =
       config.paths.features === 'openspec/specs' ? 'features' : config.paths.features;
     const legacySpecs = config.paths.specs === 'openspec/changes' ? 'specs' : config.paths.specs;
     const legacyArchive =
       config.paths.archive === 'openspec/changes/archive' ? 'specs/archive' : config.paths.archive;
     ```
   - Alternatively, execute `pnpm exec biome check --write src/core/migrate.ts`.
2. Verify:
   - Run `pnpm exec biome check src/core/migrate.ts`.
   - Run `pnpm lint` to confirm all source and test files pass lint checks.
