# Task 1 — Mangled file and fused acceptance line linting

## Changed

- `src/core/linter.ts`
  - Added `CHANGE_FOLDER_IGNORES` (`.run`, `.git`, `.DS_Store`), `PROHIBITED_CONTROL_REGEX` (`/[\x00-\x08\x0B-\x1F\x7F]/`), and `ACCEPTANCE_CHECKBOX_REGEX`.
  - Added `collectArtifactFiles` recursive walk over the change folder (mirrors hasher ignore set), `scanControlCharacters`, and `scanFusedAcceptance`.
  - `lintChangeFolder` now scans every authored file for prohibited control characters before doc resolution, and each task's raw content for fused acceptance lines (`>1` checkbox per line).
- `src/core/init.ts` — appended `- Write files with the file tool, never through a shell echo.` to `MANAGED_PLANNER_BLOCK` under `### Before you write a task`.
- `PLANNER.md` — same sentence added to the managed block.
- `tests/mangled-lint.test.ts` (new) — covers bell `\x07`, backspace `\x08`, carriage return `0x0D`, DEL `0x7F`, `.run/` exclusion, nested-file recursion, fused acceptance lines, single-item-per-line acceptance, clean `\n`/`\t` files, and `approveSpec` refusal without writing `.run/approved`.
- `tests/init-planner.test.ts` — added assertions for the file-tool sentence in `MANAGED_PLANNER_BLOCK` and the repository `PLANNER.md`.

## Deviated

None.

## Notes

- The obsolete `" and "` title warning removal is task 4, not task 1; it is left in place here.
- `osq approve` already routes through `lintChangeFolder` and exits non-zero on failure; the new control-character/fused-line errors flow through that existing path unchanged. `approve.ts` was not modified (outside task 1 scope).

## Verify

`node --import tsx --test tests/mangled-lint.test.ts tests/init-planner.test.ts` — 16 pass, 0 fail.

Also ran `tests/linter.test.ts` + `tests/approve.test.ts` (34 pass) and `pnpm tsc --noEmit` (clean).
