---
reason: blocked
fingerprint: sha256:0a98ec6172ffcbf6c1b24f0836b4e91a8e16c7cc2e8f30cebda5f731c5b78687
---
- The proposal verify `pnpm verify` cannot pass within this task's scope: the only failure is `tests/decisions-read.test.ts`, whose fix requires editing either that preexisting test (outside `scope`, no `tests.modify` → any change is killed as `undeclared_test_change`) or the committed ADR (outside `scope`). Rule 7 also makes the change-level verify a hard per-task gate (`gates.changeVerifyAfterTask` defaults true and is unset in `osq.config.ts`). Blocking is the only in-scope exit; the plan needs a human decision.
