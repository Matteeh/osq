## Changed

- No new code was needed this attempt. The prior attempt's implementation is
  present in the working tree and complete against every acceptance line; I
  reviewed it and ran both verifies green:
  - `src/core/vcs/vcs.ts` — the read-only `Vcs` port (`VcsHead`, `VcsStash`,
    `VcsStatusEntry`, `Vcs`); types only, runs no git, imports neither
    implementation.
  - `src/core/vcs/git-vcs.ts` — `GitVcs` via `execFile` with an argument list,
    the project root as cwd, and `GIT_DIR`/`GIT_INDEX_FILE`/`GIT_WORK_TREE`
    stripped from the child environment. HEAD via `rev-parse --verify --quiet
    HEAD` and `symbolic-ref --quiet --short HEAD`; SHA-256 of `ls-files --stage
    -z`; `stash list --format=%H%x00%gs` parsing `WIP on`/`On` and `(no branch)`;
    `status --porcelain=v1 -z --untracked-files=all` with the rename old path as
    the next `-z` field. Every read bounded by `timeouts.gitSeconds`, default 10
    beside a comment.
  - `src/core/vcs/no-vcs.ts` — `NoVcs`: null root, null commit and branch, empty
    digest, empty lists, reason carried.
  - `src/core/vcs/select.ts` — `selectVcs`, the only importer of the two
    implementations; reasons `git not found`, `not a git repository`, `not the
    repository root` by realpath comparison, else `GitVcs`.
  - `src/core/foundation/config.ts` — one optional `gitSeconds?: number` on
    `OsqTimeouts`, no doc comment.
  - `tests/vcs.test.ts` — 16 tests covering every "Vcs port" and "Vcs selection"
    scenario against temporary repositories, `NoVcs` on this repository's
    `fixture/` (below the root), a staged file changing the index digest, a stash
    made with `-m`, a renamed file in status, the `GIT_DIR` override, and the
    configured/default timeout.
- Verifies run: `node --import tsx --test tests/vcs.test.ts` → 16/16 pass;
  `pnpm verify` → exit 0 (typechecks, build, full suite, lint).

## Missing context

- The previous attempt's `## Blocked` concerned `tests/decisions-read.test.ts`
  failing because the before-approval ADR `decisions/003-git-strategy.md` is an
  accepted `applies_to: all` record. That conflict is resolved in the current
  tree: the test now passes 24/24 and asserts the five accepted ADRs. No plan
  amendment was needed after all.

## Next

None; the task is complete.

Touched: src/core/vcs/vcs.ts, src/core/vcs/git-vcs.ts, src/core/vcs/no-vcs.ts, src/core/vcs/select.ts, src/core/foundation/config.ts, tests/vcs.test.ts
