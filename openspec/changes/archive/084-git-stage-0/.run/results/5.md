## Changed

- `src/harness/prompt.ts`: the closing `CRITICAL:` line of
  `buildExecutorPrompt` now ends with `Never run git.`. `EXECUTOR_STEPS` is
  untouched, so the managed AGENTS.md block is unchanged.
- `tests/executor-no-git.test.ts` (new): asserts `buildExecutorPrompt`'s last
  line ends with the no-git sentence and that agy, opencode, and codex all
  deliver the same final sentence.
- `tests/fixtures/prompts/agy.txt`, `tests/fixtures/prompts/opencode.txt`,
  `tests/fixtures/prompts/codex.txt`: regenerated with
  `UPDATE_GOLDEN=1 node --import tsx --test tests/harness-prompt-injection.test.ts`;
  the golden test then passes without the variable.

Verification: `node --import tsx --test tests/executor-no-git.test.ts` passes
(2/2); `node --import tsx --test tests/harness-prompt-injection.test.ts` passes
(18/18) without `UPDATE_GOLDEN`; the proposal's `pnpm verify` passes through
typechecks, build, the full suite, and lint.

Touched: src/harness/prompt.ts, tests/executor-no-git.test.ts, tests/fixtures/prompts/agy.txt, tests/fixtures/prompts/opencode.txt, tests/fixtures/prompts/codex.txt
