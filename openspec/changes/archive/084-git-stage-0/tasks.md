# Tasks

## 1. Vcs port

- [x] 1. When osq reads git state, one Vcs port answers through GitVcs or NoVcs

## 2. Doctor

- [x] 2. When osq doctor runs, it reports git and warns about git environment variables

## 3. Violation recording

- [x] 3. When a task's agent exits, the watcher records git moves and edits outside scope without changing the outcome

## 4. Relative paths

- [x] 4. When verify output or an archive path is recorded, it is relative to the project root

## 5. Executor prompt

- [x] 5. When an executor prompt is built, its closing line forbids running git
