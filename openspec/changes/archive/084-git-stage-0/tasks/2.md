---
title: When osq doctor runs, it reports git and warns about git environment variables
verify: node --import tsx --test tests/vcs-doctor.test.ts
verify_starts: red
tests:
  modify: true
scope:
  - src/core/vcs/doctor-git.ts
  - src/core/foundation/doctor.ts
  - tests/vcs-doctor.test.ts
  - tests/doctor.test.ts
  - tests/pi/doctor.test.ts
  - tests/codex/adapter.test.ts
entry:
  - src/core/foundation/doctor.ts
  - src/core/foundation/config-doctor.ts
  - src/core/vcs/select.ts
  - tests/doctor.test.ts
skills: []
---
## Acceptance

- [ ] New `src/core/vcs/doctor-git.ts` returns the `git` check, and the `git-env` check when a variable is set, as the cli-foundation delta's "Doctor git check" says, using `selectVcs` and `probeVersion`.
- [ ] `runDoctorChecks` in `doctor.ts` adds them right after the `validator` check, through one spread.
- [ ] New `tests/vcs-doctor.test.ts` covers the three scenarios, `not a git repository` for a plain temporary folder, and doctor's exit code staying 0 with only these warnings.
- [ ] The pinned check-name lists in `tests/doctor.test.ts`, `tests/pi/doctor.test.ts`, and `tests/codex/adapter.test.ts` gain `git` after `validator`. Adjust only those lines, the check count, and the `[ok]` prefix assertion in `doctorCommand`'s test, whose temporary folder is not a repository.

## Details

`doctor.ts` is at its line budget with two lines to spare: one import and one
spread. For "Git absent", pass `gitBinary` naming a path that does not exist,
through an optional dependency on `DoctorDependencies` or through a parameter
of the new check. Keep the new dependency optional so existing callers compile
unchanged. Save and restore `process.env.GIT_DIR` around the test that sets it.
New functions stay within 80 lines.
