# Tasks

## 1. Port and config

- [x] 1. When osq plans a worktree, the port names the default branch and the config places the worktree

## 2. Resolver

- [x] 2. When vcs is enabled, the resolver reports a running change from its osq worktree and the watcher leaves it alone

## 3. Dashboard

- [x] 3. When osq serve starts with osq worktrees, edits in a worktree change folder invalidate that change

## 4. Approve

- [x] 4. When vcs is enabled, osq approve commits the change onto its own branch and worktree and writes nothing to the checkout

## 5. Status

- [x] 5. When a change runs in a worktree, osq status prints its path and warns about an edited checkout copy
