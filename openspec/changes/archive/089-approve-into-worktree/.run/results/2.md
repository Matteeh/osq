## Changed

- `src/core/status/change-locations.ts`: `ChangeTree` gains optional `worktreeFolder`. `changeTrees` returns the project root first, then one tree per `osq/*` worktree from `selectVcs`+`worktreeList` (git kind only, path compared via `fs.realpath`, path equal to the root skipped), each built with `getChangesDir`/`getArchiveDir`/`getRejectedDir` on the worktree path. `listChanges` makes a worktree tree contribute only the folder its `worktreeFolder` names and drops any active project-root folder a worktree tree names. `findChange`/`locateFolder` unchanged.
- `src/watcher/loop.ts`: `runWatcherCycle` skips every active change whose tree carries `worktreeFolder` before any reap, retry, spawn, verify, or archive.
- `tests/change-locations-worktrees.test.ts`: new; covers "Running change in a worktree", "Worktree of another branch", "One tree today", and "Approved change in a worktree" through `runWatcherCycle` with the mock adapter.

Touched: src/core/status/change-locations.ts, src/watcher/loop.ts, tests/change-locations-worktrees.test.ts
