---
title: When osq serve starts with osq worktrees, edits in a worktree change folder invalidate that change
verify: node --import tsx --test tests/serve-sse-worktrees.test.ts
verify_starts: red
scope:
  - src/core/web/web-server.ts
  - src/core/web/web-events.ts
  - src/core/web/web-trees.ts
  - tests/serve-sse-worktrees.test.ts
entry:
  - src/core/web/web-events.ts
  - src/core/web/web-server.ts
  - src/core/status/change-locations.ts
skills: []
---
## Acceptance

- [ ] `startWebServer` passes `trees: await changeTrees(projectRoot, config)` to `createInvalidationHub`.
- [ ] The hub watches the first tree's OpenSpec root plus the change folder of each tree with `worktreeFolder`, and a path inside one of those folders adds that folder's numeric id, as the web-inspection delta's "Invalidation across worktrees" says. Without `trees`, the hub behaves exactly as before.
- [ ] New `tests/serve-sse-worktrees.test.ts` covers both scenarios with the injectable `watch` and `schedule` seams, the "Server wiring" one against a temporary repository with one osq worktree.

## Details

`web-events.ts` has 242 lines. Move `singleChangeTree` and the new
worktree-folder classification into new `src/core/web/web-trees.ts` so both
files stay within 250 lines. `web-trees.ts` must not call `getChangesDir` or
`getArchiveDir`; the reader structural test forbids it. Existing serve tests
construct the hub without `trees` and must pass unchanged.
