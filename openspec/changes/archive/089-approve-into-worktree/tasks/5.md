---
title: When a change runs in a worktree, osq status prints its path and warns about an edited checkout copy
verify: node --import tsx --test tests/status-worktree.test.ts
verify_starts: red
scope:
  - src/core/status/status.ts
  - tests/status-worktree.test.ts
entry:
  - src/core/status/status.ts
  - src/core/status/change-locations.ts
  - src/core/spec/hasher.ts
  - tests/approve-worktree.test.ts
skills: []
---
## Acceptance

- [ ] `getStatusOverview` records, for each change whose tree has `worktreeFolder`, the worktree path and whether the checkout's copy differs from the approved hash, and `formatStatusOverview` prints both lines as the status-inspection delta's "Running change in status" says.
- [ ] The checkout copy is the folder of the same name in the first tree's changes directory, hashed with `hashChangeFolder`.
- [ ] Output for changes outside worktrees is unchanged.
- [ ] New `tests/status-worktree.test.ts` covers all three scenarios through `statusCommand` or `formatStatusOverview(await getStatusOverview(...))`, approving the change with `approveSpec` as `tests/approve-worktree.test.ts` sets it up.

## Details

Add optional fields to `StatusOverview` so every existing caller and test
keeps compiling. Keep `status.ts` within 250 lines.
