---
title: When change folder markers are inspected, state is derived with stale lock reaping
verify: node --import tsx --test tests/state.test.ts tests/lock.test.ts
scope: [src/core/state.ts, src/core/lock.ts, tests/state.test.ts, tests/lock.test.ts]
entry: [src/core/state.ts, src/core/lock.ts]
skills: []
---
## Acceptance
- [ ] deriveTaskState identifies task status (pending, running, done, dead) from .run markers
- [ ] deriveSpecState aggregates task states and determines if spec is ready to run or complete
- [ ] acquireLock writes .run/running/<n>.pid with PID and timestamp exclusively
- [ ] releaseLock removes .run/running/<n>.pid cleanly
- [ ] reapStaleLocks detects dead PID or timeout past config.timeouts.staleLockSeconds
- [ ] Reaped locks create .run/dead/<n>.md with reason crashed or reason timeout
