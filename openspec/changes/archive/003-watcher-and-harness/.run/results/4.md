# Result for Task 4

## Changed
- `src/watcher/archiver.ts`: Implemented `applyDelta` (creating or appending delta markdown into files defined in `features.writes`), `archiveSpecFolder` (moving entire change folder to `specs/archive` preserving all `.run` markers), and `checkAndArchiveSpec` (triggering delta application and archive upon all tasks reaching `done` status).
- `tests/archiver.test.ts`: Added unit tests validating delta writing, directory moving, marker preservation, and execution completion gates.
