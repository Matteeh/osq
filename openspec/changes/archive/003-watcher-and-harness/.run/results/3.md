# Result for Task 3

## Changed
- `src/watcher/runner.ts`: Implemented `runTask`:
  - Enforces change folder integrity against `.run/approved` before spawning, failing with `reason: spec_conflict` on any modification.
  - Spawns task through configured harness adapter within `timeouts.taskTimeoutSeconds`.
  - Verifies presence of `.run/results/<n>.md`, marking `reason: no_result` if missing.
  - Independently executes `task.verify`, writing `.run/dead/<n>.md` with `reason: verify_red` on non-zero exit.
  - On success, writes `.run/done/<n>` marker and updates checkbox `- [x] <n>.` in `tasks.md`.
- `tests/runner.test.ts`: Added comprehensive unit tests covering all verification gate and failure pathways.
