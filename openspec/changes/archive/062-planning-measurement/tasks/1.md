---
title: When the planning observation tests are split by topic, every assertion still passes unchanged
verify: node --import tsx --test tests/plan-telemetry.test.ts tests/planning-observed-records.test.ts tests/planning-observed-match.test.ts tests/planning-observed-codex.test.ts tests/planning-observed-opencode.test.ts tests/planning-observed-claude.test.ts tests/planning-observed-approve.test.ts
scope:
  - tests/planning-observed.test.ts
entry:
  - tests/planning-observed.test.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] `tests/planning-observed.test.ts` is deleted. Its shared constants and helper functions (everything above the first `describe`) move unchanged into `tests/planning-observed-helpers.ts`, exported, with no `.test` suffix so the runner does not load it on its own.
- [ ] Each `describe` block moves, with its `it` bodies byte for byte, into one new file: `planning record source compatibility` to `tests/planning-observed-records.test.ts`; `path containment` and `findPlanningSessions` to `tests/planning-observed-match.test.ts`; `Codex rollout observation` to `tests/planning-observed-codex.test.ts`; `OpenCode observation` to `tests/planning-observed-opencode.test.ts`; `Claude session observation` to `tests/planning-observed-claude.test.ts`; `approval-time observation` and `approve command planning notice` to `tests/planning-observed-approve.test.ts`.
- [ ] The six files together run the same number of tests as the old file, and all pass. Each imports only what it uses, so `pnpm lint` stays clean.

## Details

This task changes no source file and no assertion. Later tasks each own one of
the new files, so moving the tests now keeps those tasks from sharing a file.
Keep `beforeEach` and `afterEach` hooks inside the `describe` blocks they
already belong to. A helper that saves and restores environment variables stays
in the helpers file, and each test file that uses it imports it. Leave
`tests/fixtures/planning-observed/` where it is; the helpers file resolves it
from its own directory exactly as the old file did.
