---
title: When a user reads the README, planning slices, the planning config block, and the planning report are explained
verify: node --import tsx --test tests/planning-docs.test.ts tests/init-planner.test.ts
scope:
  - README.md
entry:
  - README.md
  - src/core/foundation/config-planning.ts
  - src/core/report/planning-slice.ts
tests:
  modify: false
skills: []
---
## Acceptance

- [ ] The approval-observation paragraph under `### Planning` says observed sessions are cut into per-turn slices. Each turn goes to one change: a turn that edits a change folder belongs to it; other turns go to the next change edited before the next approval, else to the change approved next. One long session planning several changes is no longer counted once per change.
- [ ] That paragraph says which readers report per-turn tokens (Claude Code message usage, Codex `token_count`, OpenCode messages), that input excludes cached input, and that a Claude `cost-state` cost counts only when a slice holds the whole session.
- [ ] A short `osq.config.ts` example shows the `planning` block with `idleGapMinutes` and one `prices` entry with placeholder prices, and says prices are USD per million tokens, that osq ships no price table, and that a slice gets a price-table cost only when every turn's model is priced.
- [ ] The `### Metrics & Reporting` paragraph adds the `Planning by change` and `Planning vs execution` sections, active minutes and the idle gap, spec words per changed line, and that any unknown cost reads `not reported`.
- [ ] New `tests/planning-docs.test.ts` asserts that README mentions `planning.idleGapMinutes`, `planning.prices`, `Planning by change`, and `not reported`.

## Details

Match the tone and length of the surrounding README paragraphs; this is user
documentation, not a design note. Do not document the slice record format.
Every existing test is frozen.
