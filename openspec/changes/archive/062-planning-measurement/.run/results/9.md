## Changed

- `README.md`:
  - The approval-observation paragraph under `### Planning` now says each
    observed session is cut into per-turn slices; every turn goes to exactly one
    change (a turn that edits a change folder owns it, otherwise the next change
    edited before the next approval, else the change approved next), so one long
    session planning several changes is no longer counted once per change. It
    names the per-turn token readers (Claude Code message usage, Codex
    `token_count`, OpenCode messages), says input excludes cached input, and
    scopes a Claude `cost-state` cost to a slice holding the whole session.
  - Added a short `osq.config.ts` example with the `planning` block, an
    `idleGapMinutes` value, and one placeholder `prices` entry, plus the notes
    that prices are USD per million tokens, osq ships no price table, and a
    slice gets a price-table cost only when every turn's model is priced.
  - The `### Metrics & Reporting` paragraph now names the `Planning by change`
    and `Planning vs execution` sections, per-change tokens/cost, active minutes
    bounded by `planning.idleGapMinutes`, spec words per changed line, and that
    an unreported cost reads `not reported`. The existing harness-price-table
    disclaimer sentence was preserved verbatim for the frozen README test.
- `tests/planning-docs.test.ts` (new): reads README with whitespace collapsed
  and asserts the `planning.idleGapMinutes` and `planning.prices` keys, the
  `Planning by change` section, and the `not reported` label, plus the
  per-turn slice attribution, reader list, input/cached and cost-state rules,
  config block, price-table rules, and report measures.

## Missing context

None. The task named `src/core/foundation/config-planning.ts` and
`src/core/report/planning-slice.ts` as entries only for context; no code change
was needed because the docs describe already-implemented behavior.

Touched: README.md, tests/planning-docs.test.ts
