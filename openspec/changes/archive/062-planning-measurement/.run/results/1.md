## Changed

- Deleted `tests/planning-observed.test.ts`.
- Added `tests/planning-observed-helpers.ts`: all shared constants and helpers from above the first `describe` (fixture dir, `PASSING_VERIFY`, `LOCAL_VERIFIER`, env keys, `restoreEnv`, `installLocalVerifier`, `createProject`, `createChange`, `captureLogs`, `readManifest`, `renderTree`, `candidate`), exported, no `.test` suffix. Bodies unchanged; only `export` prefixes and two `biome` line wraps were added.
- Added six topic files, each holding its `describe` block with `it` bodies byte for byte:
  - `tests/planning-observed-records.test.ts` — `planning record source compatibility`
  - `tests/planning-observed-match.test.ts` — `path containment` and `findPlanningSessions`
  - `tests/planning-observed-codex.test.ts` — `Codex rollout observation`
  - `tests/planning-observed-opencode.test.ts` — `OpenCode observation`
  - `tests/planning-observed-claude.test.ts` — `Claude session observation`
  - `tests/planning-observed-approve.test.ts` — `approval-time observation` and `approve command planning notice`
- Each file imports only what it uses. The six new test files run 26 tests, the same count as the old file's eight `describe` blocks, and all pass. The task `verify` command passes (41 tests, 0 fail) and `pnpm verify` passes with exit code 0. Body slices were diffed against `git show HEAD:tests/planning-observed.test.ts` and match byte for byte.

Touched: tests/planning-observed.test.ts, tests/planning-observed-helpers.ts, tests/planning-observed-records.test.ts, tests/planning-observed-match.test.ts, tests/planning-observed-codex.test.ts, tests/planning-observed-opencode.test.ts, tests/planning-observed-claude.test.ts, tests/planning-observed-approve.test.ts
