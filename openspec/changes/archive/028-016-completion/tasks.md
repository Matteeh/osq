# Tasks

## 1. Pinned Validator Enforcement with Fail-Not-Skip

- [x] 1. When validator is missing or drifted, osq lint with approve fail citing ADR 004 plus install command with no skipped output across src

## 2. Living Spec Re-seed with Prose Appender Deletion

- [x] 2. When living specs are re-seeded, deterministic delta merge recreates openspec/specs with prose appender deleted proving delta equivalence

## 3. Removal of features.writes with Archive Normalization

- [x] 3. When proposal schema drops features.writes, linter rejects the field while delta specs act as sole writes declaration

## 4. Scaffolding Cleanup with Legacy Template Removal

- [x] 4. When project scaffolds, osq init creates openspec layout only with legacy specs template deleted

## 5. Documentation with Managed Block Alignment

- [x] 5. When documentation with managed instructions block are updated for OpenSpec layout, tests enforce absence of retired paths