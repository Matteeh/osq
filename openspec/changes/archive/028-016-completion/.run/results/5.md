# Task 5 result: OpenSpec layout documentation and managed instructions block

## Changed

- `src/core/init.ts`
  - Renamed `MANAGED_AGENTS_BLOCK` to `MANAGED_AGENTS_MD_BODY` (the name used by
    the change's delta spec and this task) and rewrote its body for the OpenSpec
    layout:
    - "Executing a spec" now reads the task file, parent `proposal.md`, delta
      specs, and named capability docs; writes `.run/results/<n>.md`; runs
      `verify`; stays inside `scope`.
    - New "OpenSpec layout" section: living capability specs under
      `openspec/specs/`, changes under `openspec/changes/<id>-<slug>/`,
      `proposal.md` plus delta specs beside it, the `openspec/schemas/osq/`
      workflow, `tasks/<n>.md` vs `tasks.md`, the full `.run/` marker set
      including `regressed/<n>.md`, and state derived purely from disk.
    - New "Gates and executor permissions" section: approval gate, verification
      gate, and the agent's write restriction to `.run/results/<n>.md` plus
      `scope`.
    - "Exiting" no longer mentions `features/` or "drift against features".
  - Added a deprecated alias `MANAGED_AGENTS_BLOCK = MANAGED_AGENTS_MD_BODY` so
    `src/harness/opencode.ts` (out of scope) and any external importer keep
    compiling.
- `AGENTS.md`
  - Rewrote the "Executing a spec", "Exiting", and "Writing specs" sections for
    the OpenSpec layout and added an "OpenSpec layout" section mirroring the
    managed block: change folders, `.run/` markers (including `regressed/`), the
    two gates, disk-derived state, and executor permissions.
  - Removed all remaining retired references (`features/`, `specs/dead/`, and
    "drift against features").
- `README.md`
  - `.run/` tree now lists `regressed/`.
  - Proposal example drops `features.writes`; prose states the delta specs are
    the authoritative capability-write declaration; change-folder tree label no
    longer says "reads/writes".
  - Agent read list no longer names `features.reads`/`features.writes`.
  - Lint table replaces the two `features.writes` limit rows with "proposal
    declares `features.writes` | reject".
  - New "Gates and permissions" section documents the approval gate, the
    verification gate (including archive-time re-run and `regressed` markers),
    disk-derived state, and executor permissions.
  - Failure-reason paragraph documents `.run/regressed/<n>.md` and
    `.run/regressed/change.md`; `osq migrate` line no longer names the retired
    layout paths.
- `tests/managed-blocks.test.ts` (new)
  - Asserts zero `features/` and zero "drift against features" in both
    `MANAGED_AGENTS_MD_BODY` and `AGENTS.md`.
  - Asserts every `specs/` occurrence is either the living OpenSpec layout
    (`openspec/specs/`) or a change-folder delta spec
    (`specs/<capability>/spec.md`); any legacy root-level `specs/` reference
    fails.
  - Asserts the managed body documents the OpenSpec paths, all `.run/` markers
    including `regressed/<n>.md`, both gates, disk-derived state, and the
    executor permissions, and that `AGENTS.md` mirrors the guidance.
- `tests/setup-block-coexistence.test.ts`
  - Updated to import/compare `MANAGED_AGENTS_MD_BODY`. Its three existing
    cases already prove `osq setup` preserves a foreign `<!-- OPENSPEC:START -->`
    block across repeated runs.
- `tests/doctor.test.ts` was left unchanged; the existing suite passes and
  already covers managed-block drift and the pinned validator.

## Verification

- Task verify `node --import tsx --test tests/managed-blocks.test.ts
  tests/setup-block-coexistence.test.ts tests/doctor.test.ts` => 28 tests, 28
  pass, 0 fail.
- `pnpm tsc --noEmit`: clean.
- `pnpm lint` (biome over `src` + `tests`, 131 files): clean.
- Related suites: `tests/init.test.ts`, `tests/opencode-setup.test.ts`,
  `tests/no-skipped-in-src.test.ts`, `tests/report-cost.test.ts`,
  `tests/changelog-release-docs.test.ts`, `tests/opencode-config.test.ts`,
  `tests/package-hygiene.test.ts`, `tests/init-planner.test.ts`,
  `tests/harness-prompt-injection.test.ts`: all pass.
- Full suite after this task: 636 tests / 482 pass / 154 fail. The 154 failures
  are the pre-existing cross-task blockers (148 validator-unavailable in
  temp-repo suites, 5 `tests/report.test.ts` ENOENT on the deleted
  `specs/_template`, 1 packed-tarball smoke) and are unrelated to this task.

## Deviated

1. The literal string `openspec/specs/` remains in `MANAGED_AGENTS_MD_BODY`
   because `tests/init.test.ts` (out of scope for this task) asserts that the
   refreshed `AGENTS.md` contains `openspec/specs/`. It is the living OpenSpec
   layout path, not the retired root `specs/`. `tests/managed-blocks.test.ts`
   encodes the intended distinction: it accepts `openspec/specs/` and delta
   `specs/<capability>/spec.md`, and rejects any other `specs/` reference.
   `AGENTS.md` itself uses `openspec/specs` (no trailing slash) and contains no
   `specs/` substring at all.
2. `MANAGED_AGENTS_BLOCK` is kept as an alias rather than deleted, because
   `src/harness/opencode.ts` (outside the task scope) imports it.
3. README retains the phrase `features.writes` in one lint-table row describing
   that declaring it is now rejected. It is an accurate description of a
   rejected field, not a reference to the retired `features/` path.
4. `tests/doctor.test.ts` was left untouched even though it is in scope; no
   doctor behavior or expectation needed changing.

## Drift against features/

- Living `cli-foundation` "Scaffolding and project initialization" still states
  `osq init` creates `specs/` and templates. That stale scenario predates this
  task and comes from task 4; the change's delta only ADDs a new requirement and
  does not MODIFY/REMOVE it, so after the delta merge the living spec will still
  contradict the OpenSpec-only scaffold. The spec author should add a MODIFIED
  block for that scenario.
- No other capability doc was consulted or needs updating for this task.

## Missing context / cross-task dependencies

- The full change-level `pnpm verify` cannot pass until the pre-existing
  validator-unavailable failures (148) and the `tests/report.test.ts`
  `specs/_template` ENOENT failures (5) are fixed by the owning tasks. Both were
  already flagged in tasks 3 and 4 as out-of-scope blockers.

## Next steps

- None within task 5; all five acceptance lines are satisfied.
- If work continues: update `tests/report.test.ts` and the packed-tarball smoke
  test for the OpenSpec-only scaffold, and add the shared fake-validator helper
  so the remaining validator-dependent suites and the change-level `pnpm verify`
  can pass.
