# Task 4 result: osq init scaffolds OpenSpec layout only; legacy specs template deleted

## Changed

- `specs/_template/` deleted (tracked files): `spec.md`, `tasks.md`, `tasks/1.md`
  via `git rm -r specs/_template`.
- `src/core/init.ts`:
  - Deleted constants `TEMPLATE_SPEC_MD`, `TEMPLATE_TASKS_MD`, `TEMPLATE_TASK_1_MD`.
  - `dirsToCreate` reduced to the OpenSpec hierarchy only: `openspec`,
    `openspec/schemas`, `openspec/schemas/osq`, `openspec/schemas/osq/templates`,
    `openspec/specs`, `openspec/changes`, `openspec/changes/archive`.
    Removed `specs`, `specs/_template`, `specs/_template/tasks`, `specs/archive`,
    and also `features` and `decisions` to satisfy "scaffold only openspec/
    directories" (see Deviated).
  - `filesToCreate` now only writes `osq.config.ts` and `.env.example`; the three
    `specs/_template/**` writes were removed. Bundled `openspec/**` template
    writes are unchanged.
  - `InitResult.skippedFiles` -> `existingFiles` (carried over from task 1's
    rename that was still uncommitted in the working tree).
- `tests/init.test.ts`:
  - Fresh-repo test now asserts `openspec`/`openspec/specs`/`openspec/changes`
    are created, that every `createdDirs` entry is under `openspec/`, and that
    `openspec/config.yaml` is written.
  - New test `does not create legacy specs/ or specs/_template/ during
    initialization` asserts none of `specs`, `specs/_template`,
    `specs/_template/spec.md`, `specs/_template/tasks.md`,
    `specs/_template/tasks/1.md` exist after `scaffoldProject`.
  - Renamed the overwrite test and updated `skippedFiles` -> `existingFiles`
    assertions in both overwrite/idempotency tests.

## Verification

- Task verify `node --import tsx --test tests/init.test.ts`: 11 tests, 11 pass,
  0 fail.
- `pnpm tsc --noEmit`: clean (previously failed on `tests/init.test.ts`
  `skippedFiles`; now fixed).
- `pnpm exec biome check src/core/init.ts tests/init.test.ts`: clean.

## Deviated

1. Removed `features` and `decisions` from `dirsToCreate` as well. The task
   acceptance ("osq init scaffolds only openspec/ directories and config files")
   and Details ("Retain only `openspec/` directories") require it, even though
   the explicit "Remove:" list only enumerated the `specs/**` entries. A fresh
   `osq init` no longer creates either directory.
2. `src/core/init.ts` had an uncommitted task-1 rename (`skippedFiles` ->
   `existingFiles`) in the working tree; this task's diff therefore includes it.
   That is required for `tests/init.test.ts` to compile and for the no-"skipped"
   invariant from task 1.

## Drift against features/

- The living `cli-foundation` "Scaffolding and project initialization" scenario
  still reads "system creates `openspec/`, `specs/`, config files, templates".
  The change's delta only ADDs the new "OpenSpec layout initialization and
  template retirement" requirement; it does not MODIFY/REMOVE that stale
  scenario. After the delta merge the living spec will still claim `specs/` is
  created, contradicting this task. The spec author should add a MODIFIED block
  for that scenario.
- `tests/report.test.ts` (out of task scope) copies `root/specs/_template` in its
  `createChangeFolder` helper. With the template folder deleted and no longer
  scaffolded, those tests now fail with `ENOENT ... specs/_template` before the
  pre-existing validator failures are even reached. Task 3 fixed the equivalent
  helper in `tests/show.test.ts` but not this one; it needs the same treatment
  (build the folder directly from `openspec/changes` templates).
- `tests/package-install-smoke.test.ts` (out of scope, integration) still expects
  `specs`, `specs/_template*`, `features`, and `decisions` after `npx osq init`.
  It will fail until updated to the OpenSpec-only expectations. It also already
  fails on the deleted template files.
- `tests/linter.test.ts` retains only a stale comment about `specs/_template`; no
  functional dependency.

## Missing context / next steps

- Next acceptance line if work continues: none within task 4; all five
  acceptance items are satisfied.
- Cross-task blocker (unchanged from task 3): `tests/report.test.ts` and the
  packed-tarball smoke test need updating for the OpenSpec-only scaffold, and the
  shared fake-validator helper is still needed so the change-level `pnpm verify`
  can pass. Both are outside task 4's scope.
