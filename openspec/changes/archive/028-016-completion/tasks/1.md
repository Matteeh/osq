---
title: When validator is missing or drifted, osq lint with approve fail citing ADR 004 plus install command with no skipped output across src
verify: node --import tsx --test tests/validator-missing.test.ts tests/no-skipped-in-src.test.ts
scope: [src/core/linter.ts, src/core/approve.ts, src/core/init.ts, src/core/migrate.ts, src/cli/init.ts, tests/validator-missing.test.ts, tests/no-skipped-in-src.test.ts, tests/linter.test.ts]
entry: [src/core/linter.ts]
skills: []
tests.modify: true
---
## Acceptance
- [ ] validateWithOpenSpec fails with an error when validator binary is missing instead of returning empty errors
- [ ] validateWithOpenSpec fails with an error when validator version differs from pinned 1.13.1
- [ ] Error messages for missing binary or drifted version name ADR 004 (decisions/004-pinned-openspec-validator.md) and specify the install command (pnpm add -D @fission-ai/openspec@1.13.1)
- [ ] osq lint and osq approve reject specs and changes when the validator is missing or drifted, exiting with non-zero code
- [ ] All occurrences of the word "skipped" (case-insensitive) are eliminated from src/ (renaming skippedFiles in init.ts and cli/init.ts, skipped in migrate.ts, removing skipped logs in linter.ts)
- [ ] Automated grep test tests/no-skipped-in-src.test.ts asserts that grep -ri "skipped" src/ yields zero matches
- [ ] Automated test tests/validator-missing.test.ts asserts that lint and approve fail when the validator is absent or has a drifted version, verifying the error message names ADR 004 and the install command

## Details

1. In `src/core/linter.ts`:
   - Update `validateWithOpenSpec`:
     - When `!bin` (no local binary found):
       - Do NOT log "openspec validate skipped" or return empty errors.
       - Return `{ errors: ['OpenSpec validator binary is unavailable at node_modules/.bin/openspec. Refer to ADR 004 (decisions/004-pinned-openspec-validator.md) and run: pnpm add -D @fission-ai/openspec@1.13.1'], warnings: [], ran: false, bin: null, version: null }`.
     - When `version` differs from `OPENSPEC_EXPECTED_VERSION`:
       - Do NOT treat this as merely a warning or info log.
       - Record an error in `errors`:
         `OpenSpec validator version ${version} differs from pinned ${OPENSPEC_EXPECTED_VERSION}. Refer to ADR 004 (decisions/004-pinned-openspec-validator.md) and run: pnpm add -D @fission-ai/openspec@1.13.1`.
     - Ensure `OPENSPEC_EXPECTED_VERSION` remains `'1.13.1'`.

2. Eliminate the word "skipped" from `src/`:
   - In `src/core/init.ts`:
     - Rename interface property `skippedFiles` to `existingFiles`.
     - In `scaffoldProject`, update initialization `existingFiles: []` and `.push(...)`.
   - In `src/cli/init.ts`:
     - Change loop from `for (const file of result.skippedFiles)` to `for (const file of result.existingFiles)`.
   - In `src/core/migrate.ts`:
     - Rename `skipped` property on `MigrateResult` to `existing`.
     - Update all `.push(...)` references in `convertChangeDoc`, `moveDirectory`, `migrateFeatureDocs`, `createSampleStub`.
   - In `src/core/linter.ts`:
     - Delete line `logger?.verbose('openspec validate skipped: no local openspec binary found');`.

3. In `tests/no-skipped-in-src.test.ts`:
   - Create a test that reads all files under `src/` (or executes a recursive case-insensitive search for `/skipped/i`).
   - Assert zero occurrences of `skipped` across all files in `src/`.

4. In `tests/validator-missing.test.ts`:
   - Write tests simulating missing validator binary (e.g. configuring a non-existent binary path or temporarily mocking binary resolution).
   - Assert `validateWithOpenSpec` returns errors naming ADR 004 and `pnpm add -D @fission-ai/openspec@1.13.1`.
   - Assert `osq lint` and `osq approve` fail when validator is missing.
   - Assert `osq lint` and `osq approve` fail when validator version differs from `1.13.1`.

5. Verify:
   `node --import tsx --test tests/validator-missing.test.ts tests/no-skipped-in-src.test.ts`