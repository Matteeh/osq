---
title: When living specs are re-seeded, deterministic delta merge recreates openspec/specs with prose appender deleted proving delta equivalence
verify: node --import tsx --test tests/living-specs-delta-equivalence.test.ts tests/archiver.test.ts tests/delta-merge.test.ts
scope: [src/watcher/archiver.ts, openspec/specs/**, tests/living-specs-delta-equivalence.test.ts, tests/archiver.test.ts, tests/delta-merge.test.ts]
entry: [src/watcher/archiver.ts]
skills: []
tests.modify: true
---
## Acceptance
- [ ] Re-seed all 5 living capability specs in openspec/specs/<capability>/spec.md by executing deterministic mergeDelta over all archived deltas in archive order (016 through 027)
- [ ] Remove all legacy openspec/specs/*.md files (cli-foundation.md, metrics-and-reporting.md, spec-lint-and-approve.md, status-inspection.md, watcher-and-harness.md)
- [ ] Zero "Delta from" headers or paragraphs exist across any living spec under openspec/specs/
- [ ] Requirements introduced in 017 (code ownership, test gating) and 020 through 027 are preserved in the re-seeded specs
- [ ] Legacy prose appender applyDelta in src/watcher/archiver.ts is deleted, its identifier is absent from src/, and archiveSpecFolder calls applyOpenSpecDeltas directly
- [ ] Automated test tests/living-specs-delta-equivalence.test.ts verifies openspec/specs equals the cumulative deterministic delta merge of 016..027 and asserts git grep "Delta from" openspec/specs/ is empty
- [ ] tests/archiver.test.ts and tests/delta-merge.test.ts are updated to invoke applyOpenSpecDeltas instead of deleted applyDelta

## Details

1. In `src/watcher/archiver.ts`:
   - Delete the `applyDelta` function completely.
   - Ensure the identifier `applyDelta` does not exist anywhere in `src/watcher/archiver.ts` or `src/`.
   - In `archiveSpecFolder`:
     - Replace `await applyDelta(projectRoot, specFolderPath, config);` with:
       `await applyOpenSpecDeltas(projectRoot, specFolderPath, config);`.
   - Ensure `archiver.ts` remains under the 250-line budget.

2. Re-seed `openspec/specs/`:
   - Write a deterministic script or function to merge deltas:
     - Start from clean/null base specs for all 5 capabilities:
       `cli-foundation`, `metrics-and-reporting`, `spec-lint-and-approve`, `status-inspection`, `watcher-and-harness`.
     - Scan `openspec/changes/archive` for all change folders starting from `016` onward in ascending lexicographic order.
     - For each archive folder, if it contains `specs/<capability>/spec.md`, parse it with `parseDelta` and merge it into the running capability spec with `mergeDelta(base, capability, delta)`.
     - Write the resulting merged Markdown to `openspec/specs/<capability>/spec.md`.
   - Delete all loose markdown files in `openspec/specs/*.md`:
     `openspec/specs/cli-foundation.md`,
     `openspec/specs/metrics-and-reporting.md`,
     `openspec/specs/spec-lint-and-approve.md`,
     `openspec/specs/status-inspection.md`,
     `openspec/specs/watcher-and-harness.md`.
   - Verify `git grep "Delta from" openspec/specs/` returns zero matches.

3. Update existing tests in `tests/archiver.test.ts` and `tests/delta-merge.test.ts`:
   - Replace any imports or calls to `applyDelta` with `applyOpenSpecDeltas`.
   - Ensure tests verify OpenSpec delta merging behavior rather than prose appending.

4. In `tests/living-specs-delta-equivalence.test.ts`:
   - Replay the deterministic merge of all archived delta files from `016` onward in memory.
   - Assert that each living spec file on disk at `openspec/specs/<capability>/spec.md` exactly equals the replay result.
   - Assert that `git grep "Delta from" openspec/specs/` is completely empty.

5. Verify:
   `node --import tsx --test tests/living-specs-delta-equivalence.test.ts tests/archiver.test.ts tests/delta-merge.test.ts`