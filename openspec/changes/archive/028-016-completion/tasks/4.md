---
title: When project scaffolds, osq init creates openspec layout only with legacy specs template deleted
verify: node --import tsx --test tests/init.test.ts
scope: [src/core/init.ts, specs/_template/**, tests/init.test.ts]
entry: [src/core/init.ts]
skills: []
tests.modify: true
---
## Acceptance
- [ ] Delete legacy template folder specs/_template/ and all files inside it (spec.md, tasks.md, tasks/1.md)
- [ ] src/core/init.ts removes specs, specs/_template, specs/_template/tasks, and specs/archive from scaffolded directory list
- [ ] src/core/init.ts removes TEMPLATE_SPEC_MD, TEMPLATE_TASKS_MD, and TEMPLATE_TASK_1_MD writes to specs/_template/
- [ ] osq init scaffolds only openspec/ directories and config files
- [ ] Automated test tests/init.test.ts asserts that specs/ and specs/_template/ are not created during project initialization

## Details

1. Remove `specs/_template/`:
   - Delete `specs/_template/spec.md`, `specs/_template/tasks.md`, `specs/_template/tasks/1.md`.
   - Remove directory `specs/_template/tasks` and `specs/_template`.

2. In `src/core/init.ts`:
   - In `INIT_DIRS`:
     - Remove:
       `'specs'`,
       `'specs/_template'`,
       `'specs/_template/tasks'`,
       `'specs/archive'`.
     - Retain only `openspec/` directories:
       `'openspec'`,
       `path.join('openspec', 'schemas')`,
       `path.join('openspec', 'schemas', 'osq')`,
       `path.join('openspec', 'schemas', 'osq', 'templates')`,
       `path.join('openspec', 'specs')`,
       `path.join('openspec', 'changes')`,
       `path.join('openspec', 'changes', 'archive')`.
   - In `INIT_FILES`:
     - Remove entries targeting `specs/_template/spec.md`, `specs/_template/tasks.md`, and `specs/_template/tasks/1.md`.
     - Retain template writes to `openspec/config.yaml` and `openspec/schemas/osq/**`.
   - Delete constants `TEMPLATE_SPEC_MD`, `TEMPLATE_TASKS_MD`, `TEMPLATE_TASK_1_MD` if no longer referenced.

3. In `tests/init.test.ts`:
   - Update tests asserting on `scaffoldProject` output:
     - Assert `specs/` is NOT in `createdDirs`.
     - Assert `specs/_template` is NOT created.
     - Assert only `openspec/` hierarchy is scaffolded.

4. Verify:
   `node --import tsx --test tests/init.test.ts`