---
title: When documentation with managed instructions block are updated for OpenSpec layout, tests enforce absence of retired paths
verify: node --import tsx --test tests/managed-blocks.test.ts tests/setup-block-coexistence.test.ts tests/doctor.test.ts
scope: [README.md, AGENTS.md, src/core/init.ts, tests/managed-blocks.test.ts, tests/setup-block-coexistence.test.ts, tests/doctor.test.ts]
entry: [src/core/init.ts]
skills: []
tests.modify: true
---
## Acceptance
- [ ] README.md is updated for the OpenSpec layout: change folder structure, .run/ markers including regressed, two gates (approval and verification), state derivation from disk, and executor permissions
- [ ] AGENTS.md and MANAGED_AGENTS_MD_BODY in src/core/init.ts are rewritten for OpenSpec layout without mentioning features/, specs/, or "drift against features"
- [ ] Managed block documents execution guidelines: read task file, parent proposal.md, delta specs, write .run/results/<n>.md, verify inside scope
- [ ] Automated test tests/managed-blocks.test.ts asserts zero occurrences of features/, specs/ (as path references), or "drift against features" in AGENTS.md and MANAGED_AGENTS_MD_BODY
- [ ] osq setup on this repository leaves OpenSpec's own managed block intact, verified by tests/setup-block-coexistence.test.ts

## Details

1. In `src/core/init.ts`:
   - Update `MANAGED_AGENTS_MD_BODY`:
     - Detail the OpenSpec layout:
       - Living capability specs live at `openspec/specs/<capability>/spec.md`.
       - Changes live at `openspec/changes/<id>-<slug>/`.
       - Change document is `proposal.md`; delta specs live under `specs/<capability>/spec.md`.
       - `.run/` markers track state: `running/<n>.pid`, `done/<n>`, `dead/<n>.md`, `regressed/<n>.md`, `approved`.
       - Two gates: Approval gate (`osq approve`, `.run/approved`) and Verification gate (watcher runs task `verify` command).
       - State is derived purely from disk markers.
       - Agent protocol: read task file, parent `proposal.md`, delta specs in `specs/`; stay strictly within `scope`; run `verify` before exit; write `.run/results/<n>.md` with sections (changed, deviated, missing context, next acceptance line); never edit living specs, `tasks.md`, or marker files.
     - Ensure the text contains zero occurrences of:
       - `features/`
       - `specs/` (as path prefix for changes or features)
       - `drift against features`
   - Synchronize `AGENTS.md` with the updated managed block.

2. In `README.md`:
   - Review and align all documentation sections to reference `openspec/changes/` and `openspec/specs/`.
   - Document `.run/regressed/<n>.md` and archive-time verification gate.
   - Ensure consistency with OpenSpec layout and execution guarantees.

3. In `tests/managed-blocks.test.ts`:
   - Write a test verifying `MANAGED_AGENTS_MD_BODY` and `AGENTS.md`:
     - Assert that `/features\//` matches 0 times.
     - Assert that `/drift against features/` matches 0 times.
     - Assert that `/specs\/(?!archive)/` (legacy specs/ path) matches 0 times outside allowed OpenSpec delta spec descriptions (`specs/<capability>/spec.md`).

4. In `tests/setup-block-coexistence.test.ts`:
   - Verify that running `osq setup` on a repository containing both an OpenSpec managed block (`<!-- OPENSPEC:START -->`) and an osq managed block leaves both blocks intact.

5. Verify:
   `node --import tsx --test tests/managed-blocks.test.ts tests/setup-block-coexistence.test.ts tests/doctor.test.ts`