---
title: When the smoke test installs the packed tarball, dependencies resolve normally
verify: node --import tsx --test tests/package-install-smoke.test.ts
scope: [tests/package-install-smoke.test.ts]
entry: [tests/package-install-smoke.test.ts]
skills: []
tests:
  modify: true
---
## Acceptance
- [ ] Header comment in tests/package-install-smoke.test.ts states the package under test comes from the freshly packed local tarball while its dependencies come from wherever pnpm normally gets them
- [ ] Environment configuration in tests/package-install-smoke.test.ts drops npm_config_offline and npm_config_registry while preserving npm_config_audit, npm_config_fund, and NO_COLOR
- [ ] Tarball installation command in tests/package-install-smoke.test.ts uses --prefer-offline instead of --offline
- [ ] Smoke test passes locally with the pnpm store empty
- [ ] Result artifact .run/results/1.md documents the verification procedure for the empty-store case

## Details

1. In `tests/package-install-smoke.test.ts`:
   - Replace the file header comment with:
     ```ts
     /**
      * The guarantee this test makes is: the package under test comes from the freshly
      * packed local tarball, its dependencies come from wherever pnpm normally gets them.
      */
     ```
   - Update `offlineEnv` (rename to `smokeEnv` or maintain the environment object):
     - Remove `npm_config_offline: 'true'`.
     - Remove `npm_config_registry: 'http://127.0.0.1:1/'`.
     - Retain `npm_config_audit: 'false'`, `npm_config_fund: 'false'`, and `NO_COLOR: '1'`.
   - In the consumer setup hook, update the installation command to pass `--prefer-offline`:
     - Change `['add', tarballPath, '--offline']` to `['add', tarballPath, '--prefer-offline']`.
2. Verification:
   - Run `node --import tsx --test tests/package-install-smoke.test.ts`.
   - Verify the empty-store case locally (e.g. running pnpm with `--store-dir` directed to a fresh empty temporary directory) to confirm that installation does not fail on missing pre-cached registry metadata.
   - Document in `.run/results/1.md` the exact command and verification output proving the empty-store case succeeded.
