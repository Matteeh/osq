---
title: When any test spawns the package manager, it is npm
verify: node --import tsx --test tests/package-hygiene.test.ts
scope: [tests/bin-execution.test.ts, tests/package-hygiene.test.ts, tests/package-install-smoke.test.ts]
entry: [tests/bin-execution.test.ts, tests/package-install-smoke.test.ts]
skills: []
tests:
  modify: true
---
## Acceptance
- [ ] tests/bin-execution.test.ts replaces execFileAsync('pnpm', ['build']) with execFileAsync('npm', ['run', 'build'])
- [ ] tests/package-install-smoke.test.ts replaces pnpm build with npm run build and pnpm add with npm install
- [ ] tests/package-hygiene.test.ts includes a grep-based test asserting that no test under tests/ passes 'pnpm' as a spawn command
- [ ] Test suite passes on a machine without pnpm on PATH

## Details

1. In `tests/bin-execution.test.ts`:
   - In `before`: replace `await execFileAsync('pnpm', ['build'], { cwd: repoRoot })` with `await execFileAsync('npm', ['run', 'build'], { cwd: repoRoot })`.
2. In `tests/package-install-smoke.test.ts`:
   - Replace `await run('pnpm', ['build'], repoRoot)` with `await run('npm', ['run', 'build'], repoRoot)`.
   - Replace `await run('pnpm', ['add', tarballPath, '--prefer-offline'], consumerDir)` with `await run('npm', ['install', tarballPath, '--prefer-offline'], consumerDir)`.
3. In `tests/package-hygiene.test.ts`:
   - Add a test suite asserting that no test under `tests/` passes `'pnpm'` as the command argument to child process spawning calls (`execFile`, `execFileAsync`, `spawn`, `spawnSync`, `exec`, `execSync`, or test helper `run`).
   - Read all `.ts` and `.js` files under `tests/` and assert that no process execution call passes string literal `'pnpm'` as the command parameter.
4. Verification:
   - Run `node --import tsx --test tests/package-hygiene.test.ts`.
   - Run `node --import tsx --test tests/bin-execution.test.ts`.
   - Confirm tests pass when `PATH` does not include `pnpm`.
