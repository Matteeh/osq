# Task 1 result: packed tarball deps resolve normally

## Changed

`tests/package-install-smoke.test.ts` (only file touched):

- Replaced the file header comment with the required guarantee statement:
  the package under test comes from the freshly packed local tarball, its
  dependencies come from wherever pnpm normally gets them.
- Renamed `offlineEnv` to `smokeEnv`, dropped `npm_config_offline: 'true'`
  and `npm_config_registry: 'http://127.0.0.1:1/'`, retained
  `npm_config_audit: 'false'`, `npm_config_fund: 'false'`, `NO_COLOR: '1'`.
- Updated the `run()` helper to pass `smokeEnv`.
- Changed the consumer install from `pnpm add <tarball> --offline` to
  `pnpm add <tarball> --prefer-offline` and updated the adjacent comment.
- (Pre-existing, not from this task: the `openspec/changes/001-smoke` /
  `proposal.md` expectations were already modified in the working tree by the
  spec-layout migration. Left as-is.)

## Verification

Task verify command (populated store):

```
node --import tsx --test tests/package-install-smoke.test.ts
```

Result: `tests 3`, `pass 3`, `fail 0`, `cancelled 0`.

Empty-store case (acceptance line 4). A pnpm shim was placed first on `PATH`
so every `pnpm` invocation in the test ran against a fresh, empty store
(`--store-dir` must be passed as `--store-dir=<dir>`, the space form is not
parsed before a script name):

```
rm -rf /tmp/opencode/osq-empty-store && mkdir -p /tmp/opencode/osq-empty-store
printf '#!/bin/sh\nexec /home/mathias/.nvm/versions/node/v24.21.0/bin/pnpm --store-dir=/tmp/opencode/osq-empty-store "$@"\n' > /tmp/opencode/osq-bin/pnpm
chmod +x /tmp/opencode/osq-bin/pnpm
# store confirmed empty before the run:
ls -A /tmp/opencode/osq-empty-store   # -> (no output)

PATH=/tmp/opencode/osq-bin:$PATH node --import tsx --test tests/package-install-smoke.test.ts
```

Output:

```
▶ packed tarball consumer smoke test
  ✔ installs the packed tarball into an isolated temporary project offline (2.053404ms)
  ✔ npx osq init scaffolds configuration, template directories, and AGENTS.md (232.365165ms)
  ✔ npx osq new smoke creates a valid change specification (221.169165ms)
✔ packed tarball consumer smoke test (2664.468916ms)
ℹ tests 3
ℹ suites 1
ℹ pass 3
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 2730.945347
```

After the run `/tmp/opencode/osq-empty-store/v11` existed, proving pnpm
populated the previously empty store from the registry instead of failing on
missing pre-cached metadata. The old configuration (`npm_config_offline=true`
plus a dead registry) is the failure this removes.

Also ran: `pnpm exec tsc --noEmit` (exit 0) and
`pnpm exec biome check tests/package-install-smoke.test.ts` (clean).

## Deviated

None.

## Drift against features/

None. `openspec/specs/cli-foundation.md` contains no requirement about offline
installation, matching the proposal's Delta statement that no living-document
delta is applied.

## Missing context

None.

## Next

Tasks 2 and 3 are still pending and both also touch
`tests/package-install-smoke.test.ts`:
- Task 2: replace remaining `pnpm` spawns with `npm` (`pnpm build` ->
  `npm run build`, `pnpm add ... --prefer-offline` -> `npm install ...
  --prefer-offline`) and add the grep-based `tests/package-hygiene.test.ts`.
- Task 3: move pack/install setup out of `before` into the first `it`, and make
  the `run()` helper report command, args, and cwd on failure.
