# Task 2 result

## Changed

The interactive-spawn implementation already existed in the working tree
(committed in `e51fa90` "osq: 031 planner in osq: interactive planning command,
brief writer, and planner config"), so acceptance lines 1–4 were already
satisfied. This run closed the two remaining gaps:

- `tests/harness-interactive.test.ts`: added two subprocess-based tests that
  spawn the adapter in a child Node process with piped stdio and assert the
  fake harness's stdout and stderr tokens surface uncaptured, proving
  `stdio: 'inherit'` for both `OpencodeAdapter` and `AgyAdapter`. Added a
  `runInteractiveInSubprocess` helper that writes a `.mts` runner (so tsx keeps
  ESM / top-level await) and JSON-encodes the absolute adapter module path.
- `src/harness/opencode.ts`: `spawnInteractive` now resolves `code ?? 1`
  (was `?? 0`) on close without an exit code, matching the task detail
  "defaulting to 1 if closed without code".
- `src/harness/agy.ts`: same `code ?? 1` default.

## Deviated

- The `HarnessAdapter` port keeps `spawnInteractive?` as optional rather than a
  required method. This preserves compilation of the existing test stub
  adapters (`LifecycleStubAdapter`, `FileMutatingAdapter`, `HeartbeatStubAdapter`,
  `ScopeWritingAdapter`, `BlockingAdapter`) and matches the committed design;
  `src/cli/plan.ts` already guards with `if (!adapter.spawnInteractive)`. The
  acceptance line only requires the port to declare the method with
  `prompt`/`cwd`/`model`/`agent`, which it does.
- No other deviations.

## Drift against features/

None. Changes align with:
- `watcher-and-harness` — Interactive harness adapter spawning (inherited stdio,
  exit-code resolution, no stdout capture).
- `cli-foundation` — Interactive planning command consumes the port via
  `planCommand`.

## Missing context

None.

## Next steps

None for task 2; all acceptance lines are satisfied. Verification:
- `node --import tsx --test tests/harness-interactive.test.ts` → 5/5 pass.
- `pnpm tsc --noEmit` clean, `pnpm lint` clean.
- Full `pnpm test` → 688/688 pass, no regressions from the exit-code default.
