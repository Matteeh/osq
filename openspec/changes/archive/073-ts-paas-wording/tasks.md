# Tasks

## 1. Managed blocks

- [x] 1. When planners and executors read the managed blocks, they get the ts-paas rules, and the plan prompt labels its spec list

## 2. Proposal seed

- [x] 2. When osq new seeds a proposal, its Human steps read None and the schema says they never include osq approve

## 3. Pre-spawn words

- [x] 3. When a task's pre-spawn verify runs, the watch log and osq show state its start in words
