## Changed

- `templates/proposal.md`, `templates/openspec/schemas/osq/templates/proposal.md`, and the `FALLBACK_PROPOSAL_MD` template in `src/core/foundation/new.ts`: the `## Human steps` body is now the single line `None`.
- `src/core/foundation/new.ts`: `FALLBACK_PROPOSAL_MD` is now exported for the seed test.
- `templates/openspec/schemas/osq/schema.yaml` and `openspec/schemas/osq/schema.yaml`: the `**Human steps**` instruction now reads "everything a task must not do itself, never including `osq approve`; write `None` when there is nothing."
- Mirrored the schema and proposal template into `openspec/schemas/osq/`; `diff -r` confirms the two schema directories are byte-identical.
- Added `tests/proposal-seed-human-steps.test.ts`: `createNewSpec` seeds `## Human steps` as `None` with no `osq approve`, `FALLBACK_PROPOSAL_MD` matches, and the schema instruction says Human steps never include `osq approve`.
- Regenerated `tests/fixtures/events/verified.jsonl` and `tests/fixtures/events/dead.jsonl`; the only field that changed is `proposalWords` (276 → 262). No other bytes changed.

Touched: src/core/foundation/new.ts, templates/proposal.md, templates/openspec/schemas/osq/schema.yaml, templates/openspec/schemas/osq/templates/proposal.md, openspec/schemas/osq/schema.yaml, openspec/schemas/osq/templates/proposal.md, tests/proposal-seed-human-steps.test.ts, tests/fixtures/events/verified.jsonl, tests/fixtures/events/dead.jsonl
