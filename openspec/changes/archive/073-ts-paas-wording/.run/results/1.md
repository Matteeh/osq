## Changed

- `src/core/foundation/init-blocks.ts`: executor step 3 now explains that a `verify` naming a file the task creates fails until that file exists, so starting red is expected; the managed `AGENTS.md` ownership line is addressed to executors and names planners as the writers of `tasks.md` and the task files; the managed planner block's interactive steps 2 and 3 became one step 2 that writes the folder unless the human asks to review first; "Either way" gained the finishing-message bullet after the `osq approve` line; "Parent spec" gained the `## Human steps`/`osq approve`, cross-capability delta, and REMOVED-plus-ADDED bullets.
- `src/cli/plan-queue.ts`: `buildBaseOpeningPrompt` prints `All living specs. Read the ones this change writes or whose code it uses.` as its own paragraph between `## Capability Specs` and the unchanged path list.
- Refreshed the managed blocks in `AGENTS.md`, `PLANNER.md`, `templates/PLANNER.md`, and `.opencode/agent/osq-coder.md` to the new constants.
- Added `tests/managed-wording.test.ts`: asserts each new planner/executor rule in the constants, that `scaffoldProject` replaces both stale managed blocks on a temp project, and that `buildBaseOpeningPrompt` carries the label sentence ahead of every living spec path.
- Regenerated `tests/fixtures/prompts/{agy,codex,opencode}.txt`; only executor step 3 differs.

## Deviated

- None.

## Missing context

- The acceptance lines quote whole sentences without specifying line wrapping. I wrapped the planner prose at the block's existing width and normalized whitespace in the new test, so the quoted sentences match regardless of where the constant breaks them.

## Next

- None. Both the task's `verify` (`node --import tsx --test tests/managed-wording.test.ts`) and the proposal's `verify` (`pnpm verify`) exit 0.

Touched: src/core/foundation/init-blocks.ts, src/cli/plan-queue.ts, AGENTS.md, PLANNER.md, templates/PLANNER.md, .opencode/agent/osq-coder.md, tests/managed-wording.test.ts, tests/fixtures/prompts/agy.txt, tests/fixtures/prompts/codex.txt, tests/fixtures/prompts/opencode.txt
