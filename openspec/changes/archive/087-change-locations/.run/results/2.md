## Changed

- `src/core/lifecycle/done.ts`: `markTaskDoneManual` now resolves its change
  with `findChange(projectRoot, config, specIdOrPrefix)` and reads
  `folderPath` from the result. Dropped the `findSpecFolder` and `getChangesDir`
  imports and the local `specsDir` value.
- `src/core/lifecycle/retry.ts`: `retrySpec` resolves its change through
  `findChange` and uses `folderPath`. Kept `getChangeRunDir` from the layout
  module and dropped only `getChangesDir` and `findSpecFolder`.
- `src/core/lifecycle/reject.ts`: `rejectSpec` resolves the source with
  `findChange`, binding its `folderPath` to `sourcePath`, and keeps building the
  destination with `getRejectedDir`. Dropped `getChangesDir` and
  `findSpecFolder` and kept `getChangeRunDir`.
- `src/core/spec/approve.ts`: `approveSpec` now finds the change with
  `findChange` and takes `specsDir` from the returned `tree.changesDir` for the
  planning-observation lookup. `findSpecFolder` is untouched and still exported
  for `plan` and `lint`; the `getChangesDir` import is gone.
- `src/cli/approve.ts`: the failure path uses `findChange(cwd, config, specId)`
  and reads its `folderPath`. Dropped the `findSpecFolder` and `getChangesDir`
  imports.

## Deviated

None.

## Missing context

None.

## Next

None; task 2 is complete.

Touched: src/core/lifecycle/done.ts, src/core/lifecycle/retry.ts, src/core/lifecycle/reject.ts, src/core/spec/approve.ts, src/cli/approve.ts
