---
title: When the watcher reads changes, it lists them through the change locations module, and no reader lists them on its own
verify: node --import tsx --test tests/change-locations-readers.test.ts
verify_starts: red
scope:
  - src/watcher/loop.ts
  - src/watcher/baseline.ts
  - tests/change-locations-readers.test.ts
entry:
  - src/core/status/change-locations.ts
  - src/watcher/loop.ts
  - tests/line-budget.test.ts
skills: []
---
## Acceptance

- [ ] `runWatcherCycle` lists active changes with `listChanges`, keeping its numeric order, and runs each one with its tree's root as `projectRoot`. With one tree that is the same value as today.
- [ ] `findLatestArchivedSpec`, the chokidar watch in `startWatcher`, and the idle status label use `listChanges`, `changeTrees`, and `changesDirLabel`.
- [ ] `changeFolders` in `baseline.ts` uses `listChanges` for the active and archived changes.
- [ ] New `tests/change-locations-readers.test.ts` covers "A reader lists changes on its own". It scans every `.ts` file under `src/`, and fails naming each file outside the requirement's allowed list that calls `getChangesDir` or `getArchiveDir`.
- [ ] `pnpm verify` passes, including the watcher and baseline tests, unchanged.

## Details

Model the file scan on `tests/line-budget.test.ts`. The idle status row prints
the changes directory relative to the project root, which is
`changesDirLabel`. `runWatcherCycle` is grandfathered at 184 lines, and
`loop.ts` is on the line-budget allow list. Watch every tree's changes
directory, which is one today.
