---
title: When report, doctor, or the web server reads changes, it lists them through the change locations module
verify: node --import tsx --test 'tests/report*.test.ts' 'tests/doctor*.test.ts' 'tests/web*.test.ts' tests/planning-price-gaps.test.ts tests/disclosures-inbox-show.test.ts
verify_starts: green
scope:
  - src/core/report/report.ts
  - src/core/report/recent-disclosures.ts
  - src/core/foundation/doctor.ts
  - src/core/foundation/doctor-prices.ts
  - src/core/web/web-events.ts
entry:
  - src/core/status/change-locations.ts
  - src/core/report/report.ts
  - src/core/web/web-events.ts
skills: []
---
## Acceptance

- [ ] `getMetricsReport` and `getRepositoryRecord` in `report.ts` list changes through `listChanges`. The legacy `specs/` fallback in `getMetricsReport` stays as it is.
- [ ] `recent-disclosures.ts` picks its recent archived changes from `listChanges`.
- [ ] Doctor's locks, archives, and done-markers checks, and `doctor-prices.ts`, list changes through `listChanges`.
- [ ] `createInvalidationHub` in `web-events.ts` takes its changes, archive, and rejected directories from `changeTrees`, and behaves as before with one tree.
- [ ] The verify's tests pass unchanged.

## Details

Each of `report.ts` and `recent-disclosures.ts` has its own file-local
`listRecentArchiveFolders(archiveDir, limit)`. Keep its recency rule, and feed
it the archived folders instead of a directory. `doctor.ts` has 249 of 250
lines, so the migration must not add net lines. `createInvalidationHub` is
synchronous. If `changeTrees` cannot be awaited there, resolve the trees where
the hub's options are built, or state the reason under `## Deviated`.
`getMetricsReport` is grandfathered at 654 lines.
