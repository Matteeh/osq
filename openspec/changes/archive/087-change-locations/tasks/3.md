---
title: When status, the inbox, or the queue reads changes, it lists them through the change locations module
verify: node --import tsx --test 'tests/status*.test.ts' 'tests/inbox*.test.ts' 'tests/queue*.test.ts' tests/next-step.test.ts tests/show-next-step.test.ts tests/report-queue.test.ts tests/function-budget.test.ts
verify_starts: green
tests:
  modify: true
scope:
  - src/core/status/status.ts
  - src/core/status/next-step.ts
  - src/core/status/inbox-projection.ts
  - src/core/status/queue-state.ts
  - src/core/status/queue-report-detail.ts
  - tests/function-budget.test.ts
entry:
  - src/core/status/change-locations.ts
  - src/core/status/status.ts
skills: []
---
## Acceptance

- [ ] `getStatusOverview` lists active changes with `listChanges`, keeping its proposal check, its order, and its output.
- [ ] `readNextStep` decides whether a folder is archived with `locateFolder`.
- [ ] `inbox-projection.ts` collects landed items from the archived changes `listChanges` returns.
- [ ] `queue-state.ts` scans the three locations through `listChanges`, keeping its `brief.md` filter and its grouping. `queue-report-detail.ts` finds a failure's folder with `findChange` or `listChanges`.
- [ ] If `getStatusOverview` is 80 lines or fewer afterwards, its key is removed from `GRANDFATHERED` in `tests/function-budget.test.ts`. That file changes in no other way.
- [ ] The verify's other tests pass unchanged.

## Details

`status.ts` has 247 of 250 lines and `queue-state.ts` 249, so the migration
must not add net lines to either. `status.ts` computes the archive and
rejected folder names relative to the changes directory to skip them. The
resolver's filters make that unnecessary. `collectLandedItems` takes a
directory today, so give it the archived folders instead, or the archive
directory from the change's tree.
