---
title: When a lifecycle command names a change, it finds it through the change locations module
verify: node --import tsx --test 'tests/retry*.test.ts' tests/reject.test.ts tests/done-manual.test.ts tests/approve.test.ts tests/approve-confirm.test.ts
verify_starts: green
scope:
  - src/core/lifecycle/done.ts
  - src/core/lifecycle/retry.ts
  - src/core/lifecycle/reject.ts
  - src/core/spec/approve.ts
  - src/cli/approve.ts
entry:
  - src/core/status/change-locations.ts
  - src/core/spec/approve.ts
skills: []
---
## Acceptance

- [ ] `done`, `retry`, `reject`, and `approveSpec` find their change with `findChange` instead of `getChangesDir` and `findSpecFolder`, and use its `folderPath`.
- [ ] The error path in `src/cli/approve.ts` finds the folder with `findChange`.
- [ ] `findSpecFolder` stays exported from `spec/approve.ts` unchanged, for `plan` and `lint`.
- [ ] The verify's tests pass unchanged.

## Details

`reject` keeps building its destination with `getRejectedDir`. `retrySpec` is
grandfathered at 155 lines and stays over 80. Drop imports that become unused.
