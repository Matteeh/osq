## Changed

- New `src/core/foundation/traceability-block.ts`: `TRACEABILITY_START_MARKER`/`TRACEABILITY_END_MARKER`, `traceabilityScope`, `renderTraceabilityBlock(file, config)` for `AGENTS.md` and `PLANNER.md` with the exact delta text, `writeTraceabilityBlocks`, and `checkTraceabilityBlocks`. The block is inserted directly after the `<!-- OSQ:END -->` line (or at the end of a file without a managed block) and removed with the exact reverse, so a stop-opt-in restores bytes.
- `init.ts`: added `updatedTraceability` to `InitResult` and one `writeTraceabilityBlocks` call after `writeRulesBlock`.
- `doctor-managed.ts`: `checkManagedBlocks` now appends `checkTraceabilityBlocks` problems as their own group, so the exact `<file> traceability block is …; run \`osq init\`` messages survive.
- New `tests/trace-blocks.test.ts`: the two delta scenarios, a two-name list, `'all'`, second-init no-op, removal whose bytes equal the pre-opt-in files, and each of the three doctor messages plus the doctor integration.
- `README.md`: a `### Traceability` section next to the architecture decisions documenting the config, the table format, the `@matteeh/osq/testing` helper with `run`/`then`/`each` and every failure message, the tags and forms lint reads, the `## Scenarios` task section, `OSQ_CHANGE`, lint findings under `warn`/`require`, and the report and show lines.
- `AGENTS.md`: `## Layout` lists `trace/` under `src/core/` and `src/testing/`; nothing inside the managed block changed.

## Deviated

- The task says the write call goes "after `writeRulesBlock`"; I placed it after `updatePlannerMd` as well. Calling it immediately after `writeRulesBlock` runs before `PLANNER.md` exists, so the block landed before the appended managed block and violated "directly after `<!-- OSQ:END -->`". The call is still after `writeRulesBlock`, and both files now hold the block in the required position.

## Missing context

- The task did not say whether `init` should report the traceability write like it reports `updatedProjectRules`; `src/cli/init.ts` is outside `scope`, so `InitResult.updatedTraceability` is set but not printed.

Touched: src/core/foundation/traceability-block.ts, src/core/foundation/init.ts, src/core/foundation/doctor-managed.ts, tests/trace-blocks.test.ts, README.md, AGENTS.md
