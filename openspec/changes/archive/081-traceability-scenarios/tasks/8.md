---
title: When a capability is opted in, osq report lists its traceability gaps and osq show lists each task's scenarios
verify: node --import tsx --test tests/trace-report.test.ts
verify_starts: red
scope:
  - src/core/report/report-traceability.ts
  - src/core/report/report.ts
  - src/cli/report.ts
  - src/core/status/show.ts
  - tests/trace-report.test.ts
entry:
  - src/core/report/report-dependencies.ts
  - src/core/report/report.ts
  - src/cli/report.ts
  - src/core/status/show.ts
  - src/core/trace/scenario-index.ts
  - tests/dependencies-report.test.ts
skills: []
---
## Acceptance

- [ ] New `src/core/report/report-traceability.ts` derives the metrics-and-reporting delta's "Traceability gaps in report" from the living specs, the scenario index, and `readCapabilityOwnership`. `MetricsReport` gains optional `traceability`.
- [ ] `getMetricsReport` fills it only when a capability is opted in. `toStableMetrics` copies it only when present, and `formatMetricsReport` prints the section only when present.
- [ ] `getSpecDetailsFromFolder` gives each task its scenario pairs from the scenario test files in its resolved scope, and `formatSpecDetails` prints the status-inspection delta's "Scenarios in show" line after the `Dependencies added:` line, from a small formatter beside `formatDependenciesAdded`.
- [ ] New `tests/trace-report.test.ts` covers both scenarios of "Traceability gaps in report" and "Scenarios in show" on temporary projects, through `getMetricsReport`, the report command's JSON and text output, and `getSpecDetailsFromFolder` with `formatSpecDetails`.
- [ ] The same test asserts that with nothing opted in the report JSON has no `traceability` key and the text has no `Traceability:` line. `tests/report-json.test.ts` and `fixture/report/expected.json` pass unchanged.

## Details

`report-dependencies.ts` and change 080's task 4 are the model for an optional
report section in its own module. `report.ts` and `show.ts` are on the
line-budget allow list, and `getMetricsReport`, `formatMetricsReport`,
`toStableMetrics`, `buildSpecDetails`, and `formatSpecDetails` are
grandfathered. Keep each to the call and a few wiring lines. Build the import
graph once per report run with `buildImportGraph`, skipping
`config.paths.openspecRoot`, and once per show run only when the change has a
task whose scope holds a scenario test file. The index persists nothing. New
functions stay within 80 lines and new files within 250.
