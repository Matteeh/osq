---
title: When a scenario has AND lines or a table under a THEN, osq parses them as outcomes and lint accepts the table
verify: node --import tsx --test tests/scenario-outcomes.test.ts
verify_starts: red
scope:
  - src/core/spec/delta.ts
  - tests/scenario-outcomes.test.ts
entry:
  - src/core/spec/delta.ts
  - src/core/spec/linter.ts
  - tests/delta-merge.test.ts
  - tests/impact-lint.test.ts
  - tests/helpers.ts
skills: []
---
## Acceptance

- [ ] `DeltaScenario` gains `outcomes: readonly ScenarioOutcome[]`, and `parseScenario` fills it as the spec-lint-and-approve delta's "Scenario outcomes" says. `ScenarioOutcome` is exported and holds `text` and an optional `rows` of `Record<string, string>`.
- [ ] `then` and `when` keep their current values, and `tests/delta-merge.test.ts` passes unchanged.
- [ ] New `tests/scenario-outcomes.test.ts` covers both scenarios of "Scenario outcomes", plus an indented table, blank lines between an outcome and its table, a missing cell, and a table under an AND line.
- [ ] The same test lints a temporary project through `lintChangeFolder` whose change delta adds the pricing sample's "Volume pricing" requirement with its table, and asserts the change is valid with no finding that mentions the table, as "Scenario tables accepted" says.
- [ ] The same test runs the local `node_modules/.bin/openspec validate pricing --type spec` on a temporary copy of the sample living spec, with `OPENSPEC_TELEMETRY=0`, `--strict`, `--json`, and `--no-interactive`, and asserts it is valid.

## Details

Use this sample living spec text, which task 5 also puts in its fixture:

```
# pricing Specification

## Purpose
Prices quotes by volume tier and percentage code, in integer cents.

## Requirements
### Requirement: Volume pricing
The engine SHALL price every unit in a quote at the tier its quantity falls in.
Follows ADR 001.

#### Scenario: Volume discount tiers
- **WHEN** a quote is calculated for a quantity
- **THEN** the unit price follows this table

| quantity | unit price |
|---|---|
| 1 | 10.00 |
| 99 | 10.00 |
| 100 | 9.00 |
| 499 | 9.00 |
| 500 | 8.00 |

### Requirement: Percentage codes
The engine SHALL take a percentage code off the tiered subtotal, not the list
price. Follows ADR 001.

#### Scenario: A percentage code comes off the tiered subtotal
- **WHEN** a quote for 200 units uses the code SAVE10
- **THEN** the subtotal is 1800.00
- **AND** the discount is 180.00
- **AND** the total is 1620.00
```

`tests/impact-lint.test.ts` shows how to scaffold a lint project with
`installFakeValidator`. The fake validator doesn't read tables, so the real
validator run proves OpenSpec accepts them. A trial of `openspec validate`
1.13.1 accepted this text. `delta.ts` is on the line-budget allow list. Keep
`parseScenario` within 80 lines by putting the table reading in its own
function. New functions stay within 80 lines.
