---
title: When a capability is opted in, osq init writes the traceability instruction blocks and doctor checks them
verify: node --import tsx --test tests/trace-blocks.test.ts
verify_starts: red
scope:
  - src/core/foundation/traceability-block.ts
  - src/core/foundation/init.ts
  - src/core/foundation/doctor-managed.ts
  - README.md
  - AGENTS.md
  - tests/trace-blocks.test.ts
entry:
  - src/core/foundation/rules-block.ts
  - src/core/foundation/init.ts
  - src/core/foundation/doctor-managed.ts
  - tests/rules-block.test.ts
  - tests/managed-blocks.test.ts
skills: []
---
## Acceptance

- [ ] New `src/core/foundation/traceability-block.ts` renders, writes, and checks both blocks as the cli-foundation delta's "Traceability instruction blocks" says, with the exact block text given there.
- [ ] `scaffoldProject` in `init.ts` writes both blocks through one call after `writeRulesBlock`, and `checkManagedBlocks` in `doctor-managed.ts` adds the block problems to its result.
- [ ] New `tests/trace-blocks.test.ts` covers both scenarios, a list of two capabilities, `'all'`, a second `osq init` changing nothing, a block removed when the config stops opting in, and each of the three doctor messages.
- [ ] `README.md` documents the `traceability` config, the scenario table format, `@matteeh/osq/testing` with `run`, `then`, and `each` and its failure messages, the tags and the forms lint reads, the `## Scenarios` task section, `OSQ_CHANGE`, the lint findings and their modes, and the report and show lines, near the architecture decisions documentation.
- [ ] AGENTS.md's `## Layout` lists `src/testing/` as the scenario test helper and `trace/` under `src/core/` as traceability. Nothing inside the `<!-- OSQ:START -->` block changes.

## Details

`rules-block.ts` is the model: a separately marked block, rendered from config,
written by init, and checked by doctor. This block goes directly after the
managed block's `<!-- OSQ:END -->` line. `tests/managed-blocks.test.ts` and
`tests/rules-block.test.ts` must pass unchanged, which proves the managed
blocks didn't change. `init.ts` is 168 lines and `doctor-managed.ts` 99. osq
itself opts nothing in, so this repository's PLANNER.md gains no block. New
functions stay within 80 lines and new files within 250.
