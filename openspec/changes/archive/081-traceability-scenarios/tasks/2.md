---
title: When osq.config.ts sets traceability, osq resolves and validates it
verify: node --import tsx --test tests/traceability-config.test.ts
verify_starts: red
scope:
  - src/core/foundation/config.ts
  - src/core/foundation/config-traceability.ts
  - src/core/foundation/config-user.ts
  - tests/traceability-config.test.ts
entry:
  - src/core/foundation/config.ts
  - src/core/foundation/config-gates.ts
  - src/core/foundation/config-user.ts
  - tests/config.test.ts
skills: []
---
## Acceptance

- [ ] New `src/core/foundation/config-traceability.ts` exports `TraceabilityConfig` with `capabilities: 'all' | readonly string[]` and `mode: TraceabilityMode`, `DEFAULT_TRACEABILITY_CONFIG`, and `validateTraceabilityConfig`, as the cli-foundation delta's "Traceability configuration" says.
- [ ] `OsqConfig` gains `traceability?: TraceabilityConfig`, `DEFAULT_CONFIG` holds the default, and `defineConfig` always sets it through `validateTraceabilityConfig`.
- [ ] `OsqUserConfig` accepts `traceability?: Partial<TraceabilityConfig>`.
- [ ] New `tests/traceability-config.test.ts` covers both scenarios, `'all'`, a list, a partial block, an invalid capabilities value, and loading `traceability` from an `osq.config.ts` in a temporary project through `loadConfig`.

## Details

Model the validator on `validateGatesConfig` in `config-gates.ts`.
`config.ts` is 239 lines and must stay within 250, so add only the import, the
field, the default, and the one call. A trial adding the field and default
broke no test and no typecheck. New functions stay within 80 lines and new
files within 250.
