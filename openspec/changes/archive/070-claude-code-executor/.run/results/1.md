## Changed
- Copied the change's captured run byte-for-byte to `tests/fixtures/claude/run.jsonl`.
- Added `src/harness/claude/claude-tools.ts` with `claudeToolSummary` (Bash/Read/Edit/Write/Glob/Grep shapes, JSON fallback, 60-char cut) and `toProjectRelative`.
- Added `src/harness/claude/claude-stream.ts` exporting `createClaudeStreamState`, `processClaudeStdoutLine`, `ClaudeStreamContext`, `ClaudeStreamState`, and a `ClaudeEventStreamParser` mirroring the Pi/Codex modules. It translates `assistant` `tool_use`/`text` blocks, `user` `tool_result` blocks into `file_changed` for successful remembered `Edit`/`Write`, and `result.modelUsage` into per-model `tokens` events, recording `subtype`/`isError`/`result` on the state.
- Added `tests/claude/stream.test.ts` plus hand-written fixtures `tests/claude/fixtures/stream-multi-usage.jsonl`, `stream-skipped.jsonl`, and `stream-errored-write.jsonl`.

## Deviated
- None.

## Missing context
- None.

Touched: src/harness/claude/claude-stream.ts, src/harness/claude/claude-tools.ts, tests/fixtures/claude/run.jsonl, tests/claude/stream.test.ts, tests/claude/fixtures/stream-multi-usage.jsonl, tests/claude/fixtures/stream-skipped.jsonl, tests/claude/fixtures/stream-errored-write.jsonl
