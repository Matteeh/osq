## Changed
- Added a `claude` entry to the README's "Available adapters" list describing the Claude Code harness adapter and its fresh headless `claude -p` tasks.
- Added a `### Claude Code` README section (after Pi, before Commands) in the Pi section's shape: a `harness: 'claude'` config example with a placeholder model and commented `claude.bin`/`claude.model`/`claude.sandbox`; binary and model precedence including the `OSQ_MODEL` executor-only fallback; the `2.1.278` minimum enforced by doctor's `harness-version` and watcher preflight; login versus `ANTHROPIC_API_KEY` with `--bare` and the `harnessAuth` record; the stripping flags (`--tools Bash,Read,Edit,Write,Glob,Grep`, `--strict-mcp-config`, `--disable-slash-commands`, `--setting-sources ""`, `--no-session-persistence`, `{"autoMemoryEnabled":false}`) with the 29,500 → 13,300 token measurement and that no config key re-enables them; the `dontAsk` baseline with `Bash(git:*)` denial and honest unconfined-Bash limit; and `claude.sandbox` with startup failure, no network, and the `bubblewrap`/`socat` requirement on Linux and WSL2.
- Added `tests/claude/readme.test.ts`, which extracts the `### Claude Code` section from `README.md` and asserts every required token plus the adapter-list entry.

## Missing context
- None.

Touched: README.md, tests/claude/readme.test.ts
