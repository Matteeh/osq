# Task 1 result (retry attempt 2) — dead/regressed retry retention and prior context

## Context

This is a re-execution after the watcher recorded `scope_regression` for task 1:
task 2 (rejection transition) legitimately modified two files that also appear
in task 1's `scope` — `src/cli/index.ts` and `src/core/layout.ts`. The retry
renamed `dead`/`regressed`/`done` markers into attempt-suffixed history and
appended `retry` with `attempt: 2` before this run started. No code defect was
implied by that regression.

## Changed

None. The attempt-1 implementation already present in the tree is complete and
consistent with task 2's additions, so no source edit was necessary:

- `src/core/retry.ts` — validation (target, approval hash, running markers,
  active failure) before mutation; single target-wide ordinal across retained
  `dead/<t>.<n>.md` and `regressed/<t>.<n>.md`; renames active failures into
  history (never deletes); renames `done/<n>` for a task regression; appends one
  typed `retry` event (`target`, `reason`, next `attempt`, timestamp).
- `src/core/approve.ts` — no `retainDeadMarkers`; approval only refreshes the
  seal and manifest.
- `src/cli/retry.ts` + `src/cli/index.ts` — `retry <id> <target>` registered,
  coexists with task 2's `reject <id> --reason`.
- `src/harness/types.ts` — `retry` event member, `attempt` on `started`,
  `attempt`/`priorFailureReason` on `SpawnTaskOptions`, shared
  `priorContextLines`.
- `src/harness/{agy,opencode,codex-prompt}.ts` — one consistent prior-context
  section naming prior attempt, failure reason, and existing result path.
- `src/watcher/attempt.ts` + `src/watcher/spawn.ts` — retry context rebuilt from
  the append-only event stream before spawn; `started` carries `attempt`;
  options forwarded to every adapter including mock.
- `src/core/layout.ts` — task 1's helpers and task 2's `getRejectedDir`
  coexist.

## Verification

- Task verify
  (`node --import tsx --test tests/retry*.test.ts tests/dead-marker-retention.test.ts tests/golden-events.test.ts tests/harness-prompt-injection.test.ts`):
  33/33 pass, 0 fail.
- `pnpm verify` (tsc --noEmit + full suite + biome lint): exit 0.

All seven acceptance lines remain satisfied and are covered by existing tests;
no failing behavior was found to fix.

## Deviated

None beyond the deviations recorded in the attempt-1 result (direct
`fs.appendFile` for the typed event because `src/core` may not import
`src/harness`; `src/watcher/attempt.ts` extracted to keep `spawn.ts` under the
200-line lifecycle budget).

## Drift against features/

None. Living capability specs were not touched by this run; approved deltas are
applied by the watcher on archive.

## Missing context

None.

## Next steps

None for this task. Task 3 (rejected state projections) remains unchecked and
can consume `getRejectedDir` and the `rejected` event type added by task 2.
