## Changed

- Added the `## Surface` section between `## Non-goals` and `## Contract` to
  `templates/proposal.md`, `templates/openspec/schemas/osq/templates/proposal.md`,
  `openspec/schemas/osq/templates/proposal.md`, and the `FALLBACK_PROPOSAL_MD`
  literal in `src/core/foundation/new.ts` (backticks escaped there).
- Added the Surface bullet between the Non-goals and Contract bullets in the
  proposal `instruction` of `templates/openspec/schemas/osq/schema.yaml` and its
  `openspec/schemas/osq/schema.yaml` copy.
- Changed the first proposal rule in `templates/openspec/config.yaml` and
  `openspec/config.yaml` to name `Surface` in the section order.
- Added the Surface bullet as the second bullet under `### Parent spec` in
  `MANAGED_PLANNER_BLOCK` (`src/core/foundation/init-blocks.ts`) and the two
  managed blocks in `PLANNER.md` and `templates/PLANNER.md`; all three are
  byte-identical.
- `tests/proposal-format.test.ts`: added `Surface` after `Non-goals` in
  `SECTIONS`, asserted `MANAGED_PLANNER_BLOCK` names `## Surface`, and added a
  test that the canonical template's Surface section contains the seven
  categories and a `None` line.
- Regenerated `tests/fixtures/events/verified.jsonl` and `dead.jsonl` with
  `UPDATE_GOLDEN=1`; only `repoLines` (495 to 518) and `proposalWords` (235 to
  276) changed.

## Missing context

- None.

## Next

- None; task 1 is complete.

Touched: templates/proposal.md, templates/openspec/schemas/osq/templates/proposal.md, templates/openspec/schemas/osq/schema.yaml, templates/openspec/config.yaml, templates/PLANNER.md, PLANNER.md, openspec/config.yaml, openspec/schemas/osq/schema.yaml, openspec/schemas/osq/templates/proposal.md, src/core/foundation/init-blocks.ts, src/core/foundation/new.ts, tests/proposal-format.test.ts, tests/fixtures/events/verified.jsonl, tests/fixtures/events/dead.jsonl
