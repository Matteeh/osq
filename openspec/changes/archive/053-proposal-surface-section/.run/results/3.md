## Changed

- No new file edits were needed this attempt. The task's implementation is
  already present in the working tree from the prior attempt and matches the
  acceptance lines:
  - `src/core/spec/linter.ts`: `PROPOSAL_SURFACE_ERROR`, `HTML_COMMENT_REGEX`,
    `readSurfaceSection`, and `proposalSurfaceIsEmpty`; `lintChangeFolder`
    rejects a `proposal.md` whose `## Surface` section is absent or holds only
    comments/whitespace, placed next to the frontmatter `verify` check and gated
    to `resolvedDoc.kind === 'proposal'`. The heading is matched on its own line
    in `linter.ts`; `parser.ts` is untouched.
  - `tests/proposal-surface-lint.test.ts` (new): missing, empty, comment-only,
    `None`, list, and seeded comment + `None` through `lintChangeFolder`, plus
    missing, comment-only, `None`, list, and a fresh `createNewSpec` proposal
    through `approveSpec`.
  - `## Surface` holding `None.` added after `## Non-goals` in
    `tests/plan-prompt-lifecycle.test.ts`, `tests/mangled-lint.test.ts`,
    `tests/proposal-writes-schema.test.ts`, `tests/openspec-version.test.ts`,
    and `tests/instruction-delta-lint.test.ts` (same `None.` form task 2 used).
  - `README.md`: `## Surface` between `## Non-goals` and `## Contract` in the
    `## Change folder` example, plus one sentence naming the categories and that
    `None` is valid.

- This attempt ran the task verify and the proposal verify to green: the task
  verify passed 114/114, and `pnpm verify` passed twice in a row
  (1327 + 69 tests, 0 failures, lint clean).

## Deviated

- The prior `change_verify_red` was not caused by this task's code. The dead
  marker `3.1.md` shows `pnpm verify` failing only at
  `tests/package-hygiene.test.ts` with `npm pack --dry-run` reporting
  `encountered unexpected EOF` on `dist/cli/show.d.ts`. That is a preexisting
  race: `tests/package-install-smoke.test.ts` runs `npm run build` in the repo
  root while `tests/package-hygiene.test.ts` packs the same tree, so a
  concurrent `tsc` write can truncate a `dist` file mid-read. Both files are
  outside this task's scope, so the flake was not patched; both verify commands
  now pass and `dist` is freshly rebuilt.

## Next

- None; both the task verify and the proposal verify pass.

Touched: src/core/spec/linter.ts, tests/proposal-surface-lint.test.ts, README.md, tests/plan-prompt-lifecycle.test.ts, tests/mangled-lint.test.ts, tests/proposal-writes-schema.test.ts, tests/openspec-version.test.ts, tests/instruction-delta-lint.test.ts
