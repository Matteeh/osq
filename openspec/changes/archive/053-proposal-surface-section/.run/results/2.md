## Changed

- Added a `## Surface` section holding `None.` to every inline proposal
  builder in the eight scoped test files.
  - `tests/runner-test-gating.test.ts`: `proposalFor` (no Non-goals, so the
    section sits at the end of the body).
  - `tests/scope-recertification.test.ts`: `proposalMd`, after `## Non-goals`,
    before `## Delta`.
  - `tests/scope-resolver-upgrade.test.ts`: `proposalMd`, same placement.
  - `tests/archive-verification.test.ts`: `proposal`, after `## Non-goals`.
  - `tests/manifest.test.ts`: `PROPOSAL`, after `## Non-goals`.
  - `tests/runner-scope-hashes.test.ts`: `PROPOSAL`, after `## Non-goals`.
  - `tests/cut-over.test.ts`: `proposalMd`, after `## Non-goals` and before the
    optional `## Human steps`.
  - `tests/queue-watch.test.ts`: `proposalMd`, after `## Non-goals`.

## Deviated

- The acceptance says the section holds `None`; the surrounding proposals use
  `None.` as their existing body text, so the new section uses `None.` to match.
  The subsection holds the single word `None`, which is what the rule checks.

## Missing context

- None; I found every inline `proposal.md` writer in scope via `## Goal` and
  `proposal.md` search, and each is covered. Raw strings passed directly to
  `parseSpecMd`/`extractHumanSteps` (archive-verification line 182, cut-over
  lines 277-294) are not written proposal files, so they were left unchanged.

## Next

- None; task 2 is complete.

Touched: tests/runner-test-gating.test.ts, tests/scope-recertification.test.ts, tests/scope-resolver-upgrade.test.ts, tests/archive-verification.test.ts, tests/manifest.test.ts, tests/runner-scope-hashes.test.ts, tests/cut-over.test.ts, tests/queue-watch.test.ts
