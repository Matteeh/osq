---
title: When osq new, an OpenSpec-aware agent, or the planner starts a proposal, every entry point asks for a Surface section
verify: node --import tsx --test tests/proposal-format.test.ts tests/new.test.ts tests/init.test.ts tests/init-planner.test.ts tests/managed-blocks.test.ts tests/schema-instructions.test.ts tests/golden-events.test.ts tests/openspec-differential.test.ts tests/parser.test.ts
scope:
  - templates/**
  - PLANNER.md
  - src/core/foundation/init-blocks.ts
  - src/core/foundation/new.ts
  - openspec/config.yaml
  - openspec/schemas/osq/**
  - tests/proposal-format.test.ts
  - tests/fixtures/events/*.jsonl
entry:
  - templates/proposal.md
  - templates/openspec/schemas/osq/schema.yaml
  - src/core/foundation/init-blocks.ts
  - tests/proposal-format.test.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] `templates/proposal.md` gains the Surface section below between `## Non-goals` and `## Contract`. `templates/openspec/schemas/osq/templates/proposal.md` and `FALLBACK_PROPOSAL_MD` in `new.ts` hold the same text.
- [ ] In `schema.yaml`, the proposal `instruction` gains the Surface bullet below between the Non-goals and Contract bullets. The first proposal rule in `templates/openspec/config.yaml` reads `"Follow the proposal template's sections in order: Goal, Verify, Non-goals, Surface, Contract, Human steps, Delta."`. No other bytes change in either file.
- [ ] `MANAGED_PLANNER_BLOCK` gains the Surface bullet below as the second bullet under `### Parent spec`. The managed blocks in `PLANNER.md` and `templates/PLANNER.md` equal it byte for byte.
- [ ] `openspec/config.yaml` and every file under `openspec/schemas/osq/` stay byte-identical to their `templates/openspec/` counterparts.
- [ ] `tests/proposal-format.test.ts` adds `Surface` after `Non-goals` in its section list. It asserts that `MANAGED_PLANNER_BLOCK` names `## Surface`, and that the template's Surface section contains the seven category names and a `None` line. Its other assertions stay.
- [ ] `tests/fixtures/events/verified.jsonl` and `dead.jsonl` are regenerated with `UPDATE_GOLDEN=1`. The only values that change are the `repoLines` and `proposalWords` measures.
- [ ] The task's verify command passes.

## Details

This task's `scope` is authoritative over any injected code-ownership rules.
`templates/**` covers both proposal templates, `schema.yaml`, `config.yaml`,
and `templates/PLANNER.md`. Change only the parts named here.

Edit `PLANNER.md` and `templates/PLANNER.md` only between `<!-- OSQ:START -->`
and `<!-- OSQ:END -->`. Copy the block by hand, and don't run `osq init`. In
`init-blocks.ts` and `new.ts`, the constants are template literals, so escape
each backtick as `` \` ``. `tests/init-planner.test.ts` and
`tests/new.test.ts` must pass unchanged. The first slices the block between
`### Tasks` and `### Parent spec`, and the second checks that the existing
headings appear in order.

The golden event test seeds a project with `scaffoldProject` and `osq new`, so
its `measures` events count these files. Regenerate with
`UPDATE_GOLDEN=1 node --import tsx --test tests/golden-events.test.ts`, then
rerun without the variable. If any field other than `repoLines` or
`proposalWords` changes, stop and write that in `.run/results/1.md`.

### Template section

```markdown
## Surface

<!-- User-facing names this change adds, changes, or removes: commands, flags,
config keys, frontmatter fields, document sections, dead reasons, and event
types. Replace None with one line per name, such as
"- Added: `osq init --refresh-schema` (flag)". -->
None

```

The blank line after `None` separates the section from `## Contract`, as the
other sections are separated.

### Schema instruction bullet

```yaml
      - **Surface**: the user-facing names the change adds, changes, or removes:
        commands, flags, config keys, frontmatter fields, document sections,
        dead reasons, and event types. Write `None` when there are none.
```

### Planner bullet

```markdown
- `## Surface` follows `## Non-goals` and lists the user-facing names the change
  adds, changes, or removes: commands, flags, config keys, frontmatter fields,
  document sections, dead reasons, and event types. Write `None` when there are
  none; `osq lint` rejects a proposal without the section.
```
