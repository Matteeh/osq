# Task 1 retry result — final-tree verification repair

Status: complete against the task verify command.
Verify: `node --import tsx --test tests/scope-recertification.test.ts tests/runner-scope-hashes.test.ts tests/watcher-loop-logging.test.ts tests/import-graph.test.ts tests/line-budget.test.ts` → 39 pass / 0 fail.
Also green: `pnpm tsc --noEmit`.

## Changed

This retry was limited to repairing the three behavioral suites whose dynamically
authored fixtures still seeded the rejected template sentinel. No `src/` file was
touched.

- `tests/scope-recertification.test.ts`: `PASSING` is now `node verify.cjs`;
  added a `LOCAL_VERIFIER` script and wrote it to each temporary project root in
  `setupProject` before `approveSpec`. `FAILING` (a failing diagnostic) and the
  timeout command were left unchanged — they are authored after approval and are
  not the placeholder.
- `tests/runner-scope-hashes.test.ts`: `PASSING_VERIFY` is now `node verify.cjs`
  and `PROPOSAL` interpolates it instead of hard-coding the sentinel; added
  `LOCAL_VERIFIER` written in the `Pre-spawn scope comparison` beforeEach.
- `tests/watcher-loop-logging.test.ts`: `writeTask1` now seeds `node verify.cjs`;
  added `LOCAL_VERIFIER` plus a `useLocalVerifier(projectRoot, specFolder)`
  helper that writes the verifier and rewrites the freshly created proposal's
  `verify:` line, since `createNewSpec` intentionally seeds the sentinel. Called
  it at all seven `createNewSpec` sites.

## Deviated

None. The earlier attempt's note about keeping direct `runTask` protection is
required by this task's Details ("Keep the same audit as direct `runTask`
protection before lock acquisition"), so it is not a deviation.

## Notes

- The replacement set is exactly the literal `node -e "process.exit(0)"` sentinel
  commands, per the Details' "replace only" instruction. The failing and timeout
  fixture commands remain as-is; they are not the sentinel and never reach lint.
- The local verifier mirrors the established `PASSING_VERIFY` / `LOCAL_VERIFIER`
  pattern in `tests/linter.test.ts` and checks for `openspec` in its own project
  root.

## Drift against features/

No `features/` directory exists; the capability docs live under `openspec/specs/`.

## Next

All task 1 acceptance lines are green. Unfinished work: none; no acceptance line
is outstanding.
