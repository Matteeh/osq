# Task 3 retry result — final-tree verification repair for the retry suites

Status: complete against the task verify command.

Verify: `node --import tsx --test tests/retry*.test.ts tests/harness-prompt-injection.test.ts tests/import-graph.test.ts`
→ 47 pass / 0 fail, exit 0. Also green: `pnpm tsc --noEmit`, `pnpm exec biome check`
on both edited files.

## Why the prior attempt was verify_red

The recertification implementation from attempt 2 is intact and its own suites
(`tests/retry-recertification.test.ts`, `tests/harness-prompt-injection.test.ts`,
`tests/import-graph.test.ts`) still pass. The red came entirely from the
archive-time re-run of this task's verify after task 4's verify-command trust
lint landed: the two pre-existing retry suites named by this task's verify
command still seeded the planning sentinel `node -e "process.exit(0)"` as their
proposal/task verify, so `approveSpec` now hard-fails at lint before any retry
behavior runs.

## Changed (this retry)

Only the two failing pre-existing suites named by this verify command were
touched; no source, no assertion, and no behavior changed.

- `tests/retry.test.ts`: `VERIFY` is now `node verify.cjs`; added the
  established `LOCAL_VERIFIER` script; the `beforeEach` writes `verify.cjs` into
  the temporary project root and rewrites the freshly created proposal's
  `verify:` line (because `createNewSpec` intentionally seeds the rejected
  sentinel) before approval.
- `tests/retry-attempts.test.ts`: same `VERIFY`/`LOCAL_VERIFIER` constants; the
  scaffolded task's `verify:` frontmatter uses `VERIFY`, the proposal `verify:`
  line is rewritten, and `verify.cjs` is written into the temp root before
  approval. The verifier resolves against its own execution root and is also
  the command `runTask` executes, so it exits 0 deterministically.

The local verifier mirrors `tests/linter.test.ts` and
`tests/watcher-loop-logging.test.ts`: it asserts `openspec` exists in its own
project root and exits 0/1, using no network, TTY, or repository-wide suite.

## Changed (carried from attempt 2 — source recertification work still in place)

- `src/core/retry.ts`: recertification branch (validate approval/running/failure
  first; run the task verify through task 1's shared core executor under
  `verifyTimeoutSeconds`; recompute current literal scope; pass renames only
  `regressed/<n>.md` and refreshes canonical `done/<n>` metadata; failure/timeout
  requeues both markers; one typed `recertification` event).
- `src/cli/retry.ts`: distinct `Recertified …` / `Requeued …` / `Retried …`
  reporting; no new flag.
- `src/harness/types.ts` (+ `agy.ts`, `opencode.ts`, `codex-prompt.ts`): typed
  `recertification` lifecycle event and bounded failed-output prior context.
- `src/watcher/attempt.ts`: `readRetryContext` reconstructs attempt, reason, and
  output from the latest transitioning event; passed recertifications do not
  advance attempts.
- `src/watcher/spawn.ts`: forwards the requeued output as `priorFailureOutput`.

## Deviated

- This repair edited `tests/retry.test.ts` and `tests/retry-attempts.test.ts`,
  which are not listed in this task's `scope` but are named by its verify
  command (`tests/retry*.test.ts`). Task 1's and task 7's repairs followed the
  same rule for their own verify suites; keeping these files frozen would leave
  this task's required verify permanently red. Only the sentinel fixture command
  was replaced with a deterministic local verifier; every assertion and the
  behavioral coverage are unchanged.
- The prior attempt's existing deviations remain: the recertification mutation
  lives inside the single `retrySpec` flow to stay within the 250-line source
  budget, and `RetryResult.attempt` on a passed recertification is the retained
  ordinal while no event records it.

## Drift against features/

No `features/` directory exists; the proposal's `features.reads` resolve to
`openspec/specs/`. Behavior matches the `watcher-and-harness` delta requirements
`Scope recertification lifecycle event`, `Preserving retry transition`, `Done
marker scope hash frontmatter`, and `Retried executor context`, and the
`cli-foundation` `Explicit retry command` rule. No capability spec was modified.

## Missing context

- The repository-wide suite remains red only from other out-of-scope test files
  that still seed the sentinel and call `approveSpec` (documented in result 4/6/7).
  They are outside this task's verify command and were left untouched.
- Differing paths and attribution for a recertification event are read from the
  regression marker body/frontmatter rather than recomputed, matching the
  acceptance wording.

## Next

All seven task 3 acceptance lines are implemented and covered; the task verify
command is green. No acceptance line is outstanding.
