# Task 5 result — planner final-tree and file-ownership discipline

Status: complete against the task verify command.

Verify: `node --import tsx --test tests/init-planner.test.ts tests/managed-blocks.test.ts`
→ 22 pass / 0 fail. Also `pnpm tsc --noEmit` clean.

## Changed

- `PLANNER.md`, `templates/PLANNER.md`, `src/core/init.ts` (`MANAGED_PLANNER_BLOCK`):
  added two bullets to the existing `### Tasks` guidance, byte-identical across
  all three sources.
  - Final-tree rule: every task `verify` must stay re-runnable against the
    completed change's final tree, because the watcher and archive
    recertification run it there after later tasks land; mid-change-only
    commands are a planning failure. This supplements, not replaces, the
    existing real-entry-point rule.
  - Ownership rule: a file belongs to one task; a later task may extend it only
    when it must, is ordered after the owner, and the proposal names the shared
    file. No automatic overlap lint introduced.
- `tests/init-planner.test.ts`:
  - Added a `tasksGuidance` helper slicing `### Tasks` … `### Parent spec` so
    the new rules are asserted to sit inside the task-planning guidance rather
    than as detached prose.
  - Added a test for the final-tree rule and one for the ordered ownership rule.
  - Strengthened the existing block-replacement test, the `scaffoldProject`
    initialization test, and the byte-equality test with assertions that the new
    rules are present.
- `tests/managed-blocks.test.ts`: added a `planner managed block coexistence`
  suite with a temp-dir fixture that
  - runs `updatePlannerMd` twice over a `PLANNER.md` containing foreign text and
    a foreign `<!-- OPENSPEC:START -->` block, asserting both survive unchanged
    alongside a single osq block, and
  - updates a file containing a foreign block plus a stale osq block, asserting
    only the osq block is replaced while the foreign block and epilogue survive.

## Deviated

None. All edits are confined to the five declared scope files.

## Drift against features/

No `features/` directory exists; the proposal's `features.reads` resolve to the
living specs under `openspec/specs/`. Changed behavior matches this change's
delta requirement `Final-tree planner discipline` and the `cli-foundation`
capability rule `Planner protocol rules in documentation and templates`.

## Acceptance

All six acceptance lines are implemented and covered by the strengthened tests.
No acceptance line is outstanding.
