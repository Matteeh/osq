# Task 2 result — archive scope recertification gate (repair)

Status: complete and green against the task verify command.
Verify: `node --import tsx --test tests/archive-verification.test.ts tests/archiver.test.ts tests/scope-recertification.test.ts` → 30 pass / 0 fail.
Also green: `pnpm tsc --noEmit`, `pnpm lint`.

## Why the prior attempt was verify_red

The archive gate itself was already implemented and correct, but every test in
`tests/archive-verification.test.ts` calls `approveSpec`, which now runs the
verify-command trust lint. The fixture still used the planning sentinel
`node -e "process.exit(0)"` / `node -e "process.exit(1)"`, so approval failed
with placeholder errors before any archive behavior was exercised.

## Changed

- `tests/archive-verification.test.ts` (only file edited this attempt):
  - Replaced `PASSING`/`FAILING` sentinels with real local fixture commands
    `node verify-pass.cjs` and `node verify-fail.cjs`.
  - `beforeEach` writes both deterministic scripts into the scaffolded
    execution root (`process.exit(0)` / `process.exit(1)`), so approve-time lint
    resolves existing repository paths instead of rejecting the sentinel.
  - Updated the change-level failure marker assertion to match the new failing
    command (`verify-fail.cjs`) instead of the removed inline `process.exit(1)`.
- `src/watcher/archiver.ts` (unchanged this attempt): the archive scope
  recertification gate from the prior attempt is present — `checkAndArchiveSpec`
  runs `auditScopeRegressions` over `listCanonicalDoneNumbers(runDir)` after the
  done-state eligibility check and before the first archive verify, returning
  `false` when any stale task is recorded.

## Deviated

None. Edits stayed inside `src/watcher/archiver.ts` and
`tests/archive-verification.test.ts`; `tests/archiver.test.ts` and task 1's
`tests/scope-recertification.test.ts` remain frozen.

## Drift against features/

No `features/` directory exists; the proposal's `features.reads` resolve to the
living specs under `openspec/specs/`. Behavior matches the delta requires
`Archive scope recertification audit` (watcher-and-harness) and all seven task
acceptance lines.

## Missing context

None. All seven acceptance lines are covered by the current suite:
final-task drift through `checkAndArchiveSpec`, multiple stale tasks in one
audit, repeated-check idempotency, and the unchanged clean-archive sequence.

## Next

No further work required for this task.
