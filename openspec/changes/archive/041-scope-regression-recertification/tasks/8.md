---
title: When verify-command trust lint is active, every approved test fixture uses a real local verifier
verify: pnpm verify
scope:
  - tests/*.test.ts
  - tests/codex/*.test.ts
entry:
  - src/core/linter.ts
  - tests/linter.test.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] Every test-authored proposal or task passed to `lintChangeFolder` or `approveSpec` replaces the planning sentinel with a deterministic local verify command backed by a file created in that fixture's temporary project root before lint or approval.
- [ ] The 24 failing suites recorded by the change-level regression are migrated, and a fresh full-suite run contains no approval or lint failure caused by `node -e "process.exit(0)"` in a passing fixture.
- [ ] Sentinel text remains in generated templates, explicit placeholder-rejection cases, and inert command or event payloads that never become an authored change, so rejection and serialization coverage is preserved.
- [ ] Fixtures that exercise verification failure or timeout retain those semantics through explicit local failing or hanging commands rather than being converted to passing verification.
- [ ] Only fixture constants, temporary-root setup, and narrowly reusable test helpers change; behavioral assertions, earlier task test files, and every file under `src/` remain frozen.
- [ ] `pnpm verify` passes on the completed final tree.

## Details

This is the single cleanup for task 4's incomplete fixture migration. Start
from the active change-level `verify_red` output, not from a blind repository
replacement. The currently failing files are:

- `tests/approve.test.ts`
- `tests/codex/watcher.test.ts`
- `tests/cut-over.test.ts`
- `tests/dead-marker-retention.test.ts`
- `tests/golden-events.test.ts`
- `tests/harness-generic-workflows.test.ts`
- `tests/manifest.test.ts`
- `tests/measures.test.ts`
- `tests/reject.test.ts`
- `tests/runner-already-running.test.ts`
- `tests/runner-checkbox-projection.test.ts`
- `tests/runner-done-dead-events.test.ts`
- `tests/runner-lifecycle-logging.test.ts`
- `tests/runner-lifecycle-pid.test.ts`
- `tests/runner-outcome-logging.test.ts`
- `tests/runner-synthesized-result.test.ts`
- `tests/runner-terminal-status.test.ts`
- `tests/runner-test-gating.test.ts`
- `tests/runner.test.ts`
- `tests/status.test.ts`
- `tests/watcher-build-identity.test.ts`
- `tests/watcher-heartbeat.test.ts`
- `tests/watcher-preflight.test.ts`
- `tests/watcher.test.ts`

For each authored change that is expected to pass lint, use the established
`node verify.cjs` plus `LOCAL_VERIFIER` pattern from `tests/linter.test.ts` and
write the verifier beneath the same temporary project root before calling
`lintChangeFolder` or `approveSpec`. If a suite needs distinct passing, failing,
or timeout commands, create explicit local scripts that preserve those outcomes.
When `createNewSpec` deliberately seeds the sentinel, rewrite proposal and task
frontmatter during fixture setup before approval.

Do not replace matching strings that are merely expected event data, adapter
input, direct verification-executor commands, generated template assertions, or
the subject of placeholder rejection tests. Do not alter behavioral assertions
to accept the new lint failure. The task's two scope globs are an entry-count
compression for the exact 24-file list above, not permission to modify other
tests. Run the full task verify until TypeScript, all tests, and Biome are green.
