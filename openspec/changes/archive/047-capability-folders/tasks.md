# Tasks

## 1. Package root

- [x] 1. When core code needs the package root, it imports it from one module

## 2. Capability folders

- [x] 2. When core modules and harness adapters move into capability folders, every import, the public entry point, and the architecture tests stay green

## 3. Function budget

- [x] 3. When a source function exceeds the function budget, the suite fails and the grandfathered list is explicit
