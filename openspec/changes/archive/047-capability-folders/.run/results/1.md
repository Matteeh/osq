# Task 1 result

## Changed

- Added `src/core/package-root.ts` exporting `PACKAGE_ROOT`
  (`path.resolve(fileURLToPath(new URL('../../', import.meta.url)))`) and
  `TEMPLATES_ROOT` (`path.join(PACKAGE_ROOT, 'templates')`), carrying the
  tsx/`dist/` explanatory comment formerly in `manifest.ts`.
- `src/core/planning.ts`: now imports `PACKAGE_ROOT` from `./package-root.js`;
  dropped the local declaration and the `fileURLToPath` import (`path` remains
  in use).
- `src/core/manifest.ts`: now imports `PACKAGE_ROOT` from `./package-root.js`;
  dropped the local declaration, its comment, and the `fileURLToPath` import.
- `src/core/web-static.ts`: `resolveUiDir` joins the imported `PACKAGE_ROOT`
  with the existing `ui/dist` subpath; dropped the `fileURLToPath` import.
- `src/core/init.ts` and `src/core/new.ts`: now re-export `TEMPLATES_ROOT` from
  `./package-root.js` (plus a local import, since each still uses the binding
  internally); dropped the `fileURLToPath` imports.
- Added `tests/package-root.test.ts`: asserts `PACKAGE_ROOT` contains
  `package.json`, `TEMPLATES_ROOT` contains `PLANNER.md`, both re-exports from
  `init.ts`/`new.ts` equal `TEMPLATES_ROOT`, and that no other `.ts` file under
  `src/core/` (recursive, excluding any base-named `package-root.ts`) contains
  both `new URL(` and `import.meta.url`.

## Deviated

- None from the task body. `init.ts` and `new.ts` keep a local `import` of
  `TEMPLATES_ROOT` alongside the re-export because both still reference the
  binding in their own bodies.

## Missing context

- None.

## Verification

- `node --import tsx --test tests/package-root.test.ts tests/serve.test.ts tests/import-graph.test.ts tests/line-budget.test.ts`
  passes: 40 tests, 0 failures.
- `pnpm lint` passes (296 files).
- `pnpm tsc --noEmit` reports no errors in `src/`; its only errors are
  pre-existing `packages/ui` and `tests/ui-*.tsx` JSX/module-resolution
  failures unrelated to this task.
