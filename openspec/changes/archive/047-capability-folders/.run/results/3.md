# Task 3 result

## Changed

- Added `tests/function-budget.test.ts`:
  - `MAX_LINES = 80`, recursive walk of non-declaration `.ts` files under
    `src/`, modeled on `tests/line-budget.test.ts`.
  - Parses with the installed `typescript` dev dependency through
    `ts.createSourceFile` and `ts.forEachChild`; no dependency added.
  - Measures function declarations, method declarations, and arrow or function
    expressions bound to a `const` (nested functions included), keyed
    `<path relative to src, POSIX>#<name>`. Length is inclusive from
    `node.getStart(sf)` (leading comments excluded) to `node.getEnd()`.
  - `GRANDFATHERED` holds exactly the 23 keys from the task body. The primary
    `assert.deepEqual(violations, [], ...)` reports `<key> has <n> lines (max 80)`
    for every non-grandfathered over-budget function; a second
    `assert.deepEqual(stale, [], ...)` names any key that no longer matches an
    over-budget function.
- Rewrote only the `## Layout` fence and the prose under it in `AGENTS.md`:
  the seven `src/core/` capability folders with their capabilities, the four
  harness folders plus shared root files, and the three sentences (ownership
  folder plus one budget sentence per budget test naming its explicit exempt
  list). Every other section, including the duplicated `Executing a spec` and
  `Exiting` sections, is untouched.

## Measurement

The measured over-budget set and the per-function line counts match the
planner's list exactly (all 23 keys, same counts), so the grandfather list is
stored verbatim and no source was edited to fit.

## Verification

- `node --import tsx --test tests/function-budget.test.ts tests/import-graph.test.ts tests/line-budget.test.ts`
  passes: 15 tests, 0 failures.
- `npx tsc --noEmit -p packages/ui/tsconfig.cli.json` exits 0.
- `npx biome check tests/function-budget.test.ts` reports no findings.
