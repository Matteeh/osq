---
title: When an executor can't finish within its scope, the protocol tells it to write ## Blocked, and the planner rules drop the expected retry
verify: node --import tsx --test tests/executor-planner-wording.test.ts
verify_starts: red
scope:
  - src/core/foundation/init-blocks.ts
  - "*.md"
  - templates/PLANNER.md
  - .opencode/agent/osq-coder.md
  - tests/fixtures/prompts/
  - tests/managed-blocks.test.ts
  - tests/human-steps-guidance.test.ts
  - tests/executor-planner-wording.test.ts
entry:
  - src/core/foundation/init-blocks.ts
  - tests/managed-blocks.test.ts
  - tests/human-steps-guidance.test.ts
  - tests/harness-prompt-injection.test.ts
  - tests/init-planner.test.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] Step 2 in `EXECUTOR_STEPS` reads: ``2. Can't finish within your task's `scope`, or too big for one pass? Write what you need under `## Blocked` in `.run/results/<n>.md`, and exit without code.``
- [ ] `RESULT_HEADINGS` gains `## Blocked`, with purpose "for the human: what you need before this task can be finished within its scope.", between `## Outside scope` and `## Next`. The other headings don't change.
- [ ] In `MANAGED_PLANNER_BLOCK`, the bullet that starts "A file belongs to one task." becomes the text under Details, and no other planner line changes.
- [ ] `AGENTS.md`, `.opencode/agent/osq-coder.md`, `PLANNER.md`, and `templates/PLANNER.md` hold the new constants between their `OSQ:START` and `OSQ:END` markers, and `tests/fixtures/prompts/*.txt` hold what `tests/harness-prompt-injection.test.ts` now renders.
- [ ] In `README.md`, the "Automatic retry" bullet adds `blocked` to the reasons that wait for you, and the "Scope recertification" bullet says the watcher recertifies a task by itself when a later task whose scope covers the file changed it, the recorded hashes show nothing else did, and the verify passes. Every other change still halts until `osq retry <id> <n>`.
- [ ] `tests/managed-blocks.test.ts` changes only its expected step 2 and heading list. `tests/human-steps-guidance.test.ts` drops its assertion on the old expected-retry line and asserts that the block doesn't contain `list the expected \`osq retry\``.
- [ ] New `tests/executor-planner-wording.test.ts` asserts step 2's exact text, the heading order with `## Blocked`, the new planner bullet in `MANAGED_PLANNER_BLOCK` and both `PLANNER.md` copies (with line wrapping collapsed), and that `AGENTS.md` contains `## Blocked`.

## Details

The new planner bullet, wrapped like its neighbours:

```
- A file belongs to one task. Before each later task, the watcher re-hashes the
  resolved `scope` of every done task. When a later task must extend a file,
  order it after the owner, put the file in its `scope` too, and name the shared
  file in the proposal; the watcher then recertifies the owner by itself when
  the owner's `verify` still passes. Any other change to a done task's files
  halts the change until a human runs `osq retry`. Globs resolve again at every
  audit, so a broad glob also captures files that later tasks create.
```

Leave the `## Human steps` subsection bullet ("A step during the run, such as an
expected `osq retry`, goes under Before approval.") as it is.

Refresh each repository copy by replacing the text between the markers with the
constant. `CLAUDE.md` is a symlink to `AGENTS.md`; don't replace it with a
file. `tests/doctor.test.ts` runs `osq doctor` on this repository, and
`tests/init-planner.test.ts` checks the three planner copies byte for byte;
both must pass. `tests/auto-retry-docs.test.ts` reads the README's gate bullets,
so add to them instead of rewording what is there. The `*.md` scope entry
covers only the repository root's Markdown files.
