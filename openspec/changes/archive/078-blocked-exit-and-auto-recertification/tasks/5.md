---
title: When osq report runs, it counts automatic recertifications apart from human ones and blocked deaths by reason
verify: node --import tsx --test tests/report-auto-recertification.test.ts
verify_starts: red
scope:
  - src/core/report/report-scope.ts
  - src/core/report/report-events.ts
  - src/core/report/report.ts
  - fixture/report/expected.json
  - tests/report-json.test.ts
  - tests/report-scope-regressions.test.ts
  - tests/report-auto-recertification.test.ts
entry:
  - src/core/report/report-events.ts
  - src/core/report/report.ts
  - src/cli/report.ts
  - tests/report-scope-regressions.test.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] `ScopeRegressionHistory` gains `recertifiedAutomatically`, counted as the metrics-and-reporting delta's "Scope regression history" says, and a passed recertification with `automatic: true` no longer counts in `recertifiedByHuman`.
- [ ] The text report prints `    Recertified automatically: <n>` right after `    Recertified by human: <n>`.
- [ ] New `src/core/report/report-scope.ts` holds the scope regression and recertification counting that `observeTaskStream` in `report-events.ts` does today, and `observeTaskStream` calls it.
- [ ] `fixture/report/expected.json` gains `"recertifiedAutomatically": 0` in its key order, and `tests/report-json.test.ts` and `tests/report-scope-regressions.test.ts` change only their expected scope regression objects, which gain `recertifiedAutomatically`.
- [ ] New `tests/report-auto-recertification.test.ts` covers the "Automatic and human recertifications", "No scope regression history", and "Blocked deaths" scenarios through `getMetricsReport` and `formatMetricsReport` on a temp project, and the JSON through `osq report --json`.

## Details

`report-events.ts` is 248 lines and must stay within 250, and
`observeTaskStream` is 79 lines against a limit of 80; moving the scope counters
into `report-scope.ts` keeps both within their limits. `src/cli/report.ts`
spreads `scopeRegressions` into the stable JSON, so it needs no change.
`deadByReason` already counts every dead reason, so `blocked` needs no new
code; the test proves it. The web report's scope panel stays as it is.
