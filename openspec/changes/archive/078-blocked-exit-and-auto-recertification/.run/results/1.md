## Changed

- `src/core/foundation/init-blocks.ts`: rewrote step 2 of `EXECUTOR_STEPS` to the blocked-exit wording; added `## Blocked` to `RESULT_HEADINGS` between `## Outside scope` and `## Next` with the required purpose; replaced the "A file belongs to one task." planner bullet in `MANAGED_PLANNER_BLOCK` with the new ownership/recertification/glob text (the expected `osq retry` line is gone).
- Refreshed the managed blocks between the OSQ markers from the new constants in `AGENTS.md`, `.opencode/agent/osq-coder.md`, `PLANNER.md`, and `templates/PLANNER.md`. `CLAUDE.md` is the untouched symlink to `AGENTS.md`.
- `README.md`: added `blocked` to the reasons that wait for a human in the "Automatic retry" bullet, and rewrote the "Scope recertification" bullet to state the watcher recertifies by itself when a later task whose scope covers the changed file alone changed it and the verify still passes, with every other change still halting until `osq retry <id> <n>`.
- `tests/managed-blocks.test.ts`: added `## Blocked` to the expected heading list (it had no step-2 expectation to change).
- `tests/human-steps-guidance.test.ts`: dropped the positive assertion on the old expected-retry line and now asserts the block does not contain ``list the expected `osq retry` ``.
- New `tests/executor-planner-wording.test.ts`: asserts step 2's exact text, the heading order and `## Blocked` purpose, the new planner ownership bullet (line wrapping collapsed) in `MANAGED_PLANNER_BLOCK` and both `PLANNER.md` copies, and that `AGENTS.md` contains `## Blocked`.
- Regenerated `tests/fixtures/prompts/agy.txt`, `opencode.txt`, and `codex.txt` from the updated executor prompt.

## Deviated

- The planner ownership bullet says "order that later task after the owner" where the task's Details text says "order it after the owner". `tests/init-planner.test.ts` is an entry file but not in `scope`, and it asserts the raw `### Tasks` guidance contains `later task after the owner`; the exact Details wording would have made `pnpm verify` fail and there was no way to update that test within scope. The meaning is unchanged and the new `executor-planner-wording.test.ts` asserts the delivered wording.

## Missing context

- `tests/managed-blocks.test.ts` has no step-2 expectation, so acceptance line 6's "changes only its expected step 2 and heading list" reduced to the heading list. Step 2's exact text is asserted by the new `tests/executor-planner-wording.test.ts`.
- `tests/init-planner.test.ts` would need a follow-up (its `later task after the owner` assertion) if the exact task Details wording for the ownership bullet is ever wanted.

## Next

None.

Touched: src/core/foundation/init-blocks.ts, AGENTS.md, .opencode/agent/osq-coder.md, PLANNER.md, templates/PLANNER.md, README.md, tests/managed-blocks.test.ts, tests/human-steps-guidance.test.ts, tests/executor-planner-wording.test.ts, tests/fixtures/prompts/agy.txt, tests/fixtures/prompts/opencode.txt, tests/fixtures/prompts/codex.txt
