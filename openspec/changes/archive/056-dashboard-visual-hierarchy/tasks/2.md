---
title: When the report charts many changes, the cost chart becomes horizontal bars and no axis label overlaps
verify: pnpm verify
scope:
  - packages/ui/src/report/**
  - tests/ui-report.test.tsx
entry:
  - packages/ui/src/report/CostChart.tsx
  - packages/ui/src/report/WritesChart.tsx
  - packages/ui/src/report/scales.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] `CostChart` draws one horizontal row per change in descending change number, including archived changes without a landed time. The row label is the change number and slug, shortened with an ellipsis past 28 characters. Execution and planning cost are stacked left to right, and the formatted cost with coverage sits at the row's end. The chart's height grows with the row count.
- [ ] The planning boundary stays marked. Rows before the first valid planning record have no planning segment, and a labelled rule separates them from later rows.
- [ ] `WritesChart` labels its x axis with change numbers only. It thins the labels so that consecutive printed labels are at least 24px apart, and gives every band an SVG `<title>` with the full folder key.
- [ ] Every chart element that shows a shortened or omitted change name carries the full name in an SVG `<title>`. Chart colors come from the palette properties of change 056 task 1, set through `style` rather than presentation attributes.
- [ ] `tests/ui-report.test.tsx` adds a 50-change fixture, 37 without a landed time. It asserts 50 cost rows, a title on every shortened label, and writes-axis labels at least 24px apart. Its other assertions change only where they pinned the old vertical layout.
- [ ] The task's verify command passes, and every file under `packages/ui/src/report/` stays at or under 250 lines. Split `CostChart.tsx` if it would grow past that.

## Details

This task's `scope` is authoritative over any injected code-ownership rules.
Keep the chart's question heading, text alternative, and `n of m` coverage
rules from the `Delivery report visualization` requirement. Use the shared
`formatCost` from `packages/ui/src/format.ts`. Keep the SVG
application-owned; no chart library.
