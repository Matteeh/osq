# Tasks

## 1. Palette and hierarchy

- [x] 1. When the dashboard shows a status, a table, or a heading, one palette, one badge, and one type scale set the hierarchy

## 2. Report charts

- [x] 2. When the report charts many changes, the cost chart becomes horizontal bars and no axis label overlaps

## 3. Graph

- [x] 3. When the graph renders, every change gets a mark in change-number order, lane names show in full, and enabled edges are visible
