# Tasks

## 1. Loading

- [x] 1. When a config file fails to import or validate, loadConfig throws naming the file, and the osq import resolves to the running osq

## 2. Command line

- [x] 2. When a command's config fails to load, osq prints the error and exits 1
