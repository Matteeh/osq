---
title: When a command's config fails to load, osq prints the error and exits 1
verify: node --import tsx --test tests/cli-config-errors.test.ts
verify_starts: red
scope:
  - src/cli/bin.ts
  - src/cli/run.ts
  - src/cli/init.ts
  - tests/cli-config-errors.test.ts
entry:
  - src/cli/bin.ts
  - src/cli/index.ts
  - src/cli/init.ts
  - src/core/foundation/config.ts
skills: []
---
## Acceptance

- [ ] New `src/cli/run.ts` exports `runCli(argv)`, which builds the program with `createProgram(resolvePackageVersion())`, awaits `parseAsync(argv)`, and handles a `ConfigLoadError` as the cli-foundation delta's "Config error exit" says. `bin.ts` calls it and does nothing else.
- [ ] `initCommand` calls `loadConfig` without falling back to `DEFAULT_CONFIG`.
- [ ] New `tests/cli-config-errors.test.ts` covers both scenarios of "Config error exit" through `runCli`, plus one command whose action throws another error, which still rejects.

## Details

In the test, stub `process.exit` and `console.error`, `process.chdir` into the
temporary project for the call, and restore all three afterwards. Write the
broken config as `export default { vcs: { author: 'osq' } }` passed through
`defineConfig` imported from `@matteeh/osq`, which resolves through the alias
task 1 added.
