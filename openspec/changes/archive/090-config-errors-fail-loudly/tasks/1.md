---
title: When a config file fails to import or validate, loadConfig throws naming the file, and the osq import resolves to the running osq
verify: node --import tsx --test tests/config-load-errors.test.ts
verify_starts: red
scope:
  - src/core/foundation/config.ts
  - src/core/foundation/config-file.ts
  - src/core/foundation/package-root.ts
  - tests/config-load-errors.test.ts
entry:
  - src/core/foundation/config.ts
  - src/core/foundation/package-root.ts
  - src/core/foundation/doctor.ts
  - src/core/foundation/init.ts
skills: []
---
## Acceptance

- [ ] `config.ts` exports `ConfigLoadError`, and `loadConfig` rejects with it as the cli-foundation delta's "Config file errors" says, word for word. The `DEBUG_OSQ` branch goes.
- [ ] jiti imports the config file with `alias: { '@matteeh/osq': PACKAGE_ENTRY }`, where `PACKAGE_ENTRY` is a new export of `package-root.ts`: the running osq's `index` without an extension, one level above `core/`.
- [ ] A project with no config file loads the defaults exactly as before.
- [ ] New `tests/config-load-errors.test.ts` covers the four scenarios of "Config file errors" in temporary projects, the doctor one through `runDoctorChecks`, and the "Empty project" scenario of the proposal.

## Details

`config.ts` has 244 of 250 lines; if the change does not fit, move the file
import into new `src/core/foundation/config-file.ts`. Build the scaffolded
config with `scaffoldProject` so the test uses the exact text `osq init`
writes. `tests/package-root.test.ts` allows `new URL(..., import.meta.url)`
only in `package-root.ts`; derive `PACKAGE_ENTRY` there, from `import.meta.url`
two levels up, which is `src/` under `tsx` and `dist/` once built.
