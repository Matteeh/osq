---
title: When an ADR is added to osq's decisions, the own-records test checks validity instead of a pinned list
verify: node --import tsx --test tests/decisions-own.test.ts
verify_starts: red
tests:
  modify: true
scope:
  - tests/decisions-own.test.ts
  - tests/decisions-read.test.ts
entry:
  - tests/decisions-read.test.ts
  - src/core/foundation/decisions.ts
  - src/core/foundation/rules-block.ts
skills: []
---
## Acceptance

- [ ] New `tests/decisions-own.test.ts` covers both scenarios of the cli-foundation delta's "osq's own decision records validate", using `readDecisions`, `readLivingCapabilityNames`, `validateDecisions`, `checkProjectRules`, and `writeRulesBlock`.
- [ ] "One more ADR" copies `decisions/`, `AGENTS.md`, and the living specs into a temporary folder, adds an accepted ADR with `applies_to: all` and a short rule at the next free number, and refreshes the copy. It never writes to the repository.
- [ ] The "osq's own decision records" describe block is removed from `tests/decisions-read.test.ts`, and the rest of that file is unchanged, apart from imports the removal leaves unused.

## Details

Import `readLivingCapabilityNames` from wherever the removed block imports it.
Resolve the repository root the way `tests/decisions-read.test.ts` does. Pass
`DEFAULT_CONFIG` throughout, and use `fs.cp` with `recursive: true` for the copy.
