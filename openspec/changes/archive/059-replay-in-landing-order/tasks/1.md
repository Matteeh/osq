---
title: When the replay test rebuilds living specs, it merges archived deltas in the order the changes landed
verify: node --import tsx --test tests/living-specs-delta-equivalence.test.ts tests/import-graph.test.ts
scope:
  - tests/living-specs-delta-equivalence.test.ts
entry:
  - tests/living-specs-delta-equivalence.test.ts
  - src/core/web/web-data-lifecycle.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] `archivedChangeFolders` returns archives from `FIRST_ARCHIVED_DELTA` onward in landing order. Those whose `readLandedAt` is null come first in folder-name order. The rest follow, sorted by that timestamp and then by folder name.
- [ ] The test imports `readLandedAt` from `src/core/web/web-data-lifecycle.ts` and doesn't parse `change.jsonl` itself.
- [ ] A new case in the same file builds a temporary archive directory with two changes that write one capability, where the higher-numbered change carries the earlier `archived` timestamp. It asserts that the ordering puts the higher-numbered change first, and that a folder without an event sorts before both.
- [ ] Every existing assertion in the file stays, and the cumulative re-seed test passes against this repository's living specs, where 058 landed before 057.
- [ ] The task's verify command passes.

## Details

This task's `scope` is authoritative over any injected code-ownership rules.
To test the ordering against a temporary directory, give `archivedChangeFolders`
the archive directory as a parameter, defaulting to `ARCHIVE_DIR`. On this
repository, archives 016 to 037 have no `archived` event, and 038 onward all
have one. Don't edit any living spec or archive to make the test pass.
