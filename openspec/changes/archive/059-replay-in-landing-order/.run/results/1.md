## Changed

`tests/living-specs-delta-equivalence.test.ts` now replays archived deltas in
landing order:

- Imports `readLandedAt` from `src/core/web/web-data-lifecycle.js` and `os`.
- `archivedChangeFolders(archiveDir = ARCHIVE_DIR)` returns folders from
  `FIRST_ARCHIVED_DELTA` onward; folders with a null `readLandedAt` come first
  in folder-name order, the rest follow by timestamp then folder name.
- Added a `writeArchivedChange` helper that builds one temporary archived
  change carrying a single capability delta and an `archived` event.
- Added a case that builds a temporary archive with `059-no-event`,
  `060-later-landed` (09:00Z) and `061-earlier-landed` (08:00Z), all writing
  `cli-foundation`, and asserts the order is
  `['059-no-event', '061-earlier-landed', '060-later-landed']`.

No living spec or archive was edited.

## Deviated

None.

## Missing context

None.

Touched: tests/living-specs-delta-equivalence.test.ts
