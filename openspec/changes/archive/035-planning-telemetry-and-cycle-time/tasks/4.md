---
title: When a completed change is archived, its archive time is recorded
verify: node --import tsx --test tests/archiver.test.ts
scope:
  - src/watcher/archiver.ts
  - tests/archiver.test.ts
entry:
  - src/watcher/archiver.ts
  - src/harness/types.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] archiveSpecFolder appends exactly one typed archived event to `.run/events/change.jsonl` only after the change folder has been relocated successfully.
- [ ] The archived event uses the append-only event writer, carries an ISO timestamp as the authoritative archive time, and is never written to a numbered task event file.
- [ ] Failed archive verification or failed relocation emits no archived event and retains existing regressed diagnostics and archive behavior.
- [ ] Existing change-level verify_ran history remains intact and direct and watcher-driven archive tests observe the same event contract.

## Details

Reuse `archiveSpecFolder`, `checkAndArchiveSpec`, and `appendHarnessEvent`; task 1
already adds the typed `archived` event variant required here. Append against
the returned archive target after `fs.rename` and final task-checkbox projection
have succeeded. Do not use directory metadata, task completion time, final
verification time, or a mutable manifest field as an archive-time proxy.

Extend the existing archiver tests to parse `change.jsonl` and assert ordering,
cardinality, destination path, and absence on blocked archival. Preserve delta
application, collision suffixes, `.run/` history, and archive verification.
Run the task verify and `pnpm verify` before exit.
