---
title: When a change is shown, its planning sessions and wall times are listed
verify: node --import tsx --test tests/show.test.ts
scope:
  - src/core/show.ts
  - tests/show.test.ts
entry:
  - src/core/show.ts
  - src/cli/show.ts
  - src/core/planning.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] SpecDetails exposes planning sessions correlated by sessionId and ordered by plan_started timestamp for active and archived changes.
- [ ] Each valid session retains start time, harness, model, optional agent, nullable exitCode, and nullable wallSeconds without exposing token data or harness artifact paths in show output.
- [ ] formatSpecDetails prints a `Planning Sessions:` section before `Event Timeline:` and includes every session's harness, model, exit result, and formatted wall time.
- [ ] Missing files, malformed lines, unknown records, and unmatched starts do not block existing task details or event timelines; unavailable exit and wall values render explicitly.

## Details

Reuse `getSpecDetails`, `formatSpecDetails`, `formatDuration`, and the shared
planning-log parser/correlator from task 1. Do not fold planning lifecycle
records into the task `timeline`; they remain a separate list sourced only from
`.run/plan.jsonl`. Preserve all existing active/archive resolution, task state,
dead diagnostics, result display, and task-event ordering.

Extend `tests/show.test.ts` through `getSpecDetails` and `showCommand` with two
complete sessions plus a malformed line and an unmatched start. Existing tests
are editable only for the new stable field and rendered section; do not weaken
their prior assertions. Run the task verify and `pnpm verify` before exit.
