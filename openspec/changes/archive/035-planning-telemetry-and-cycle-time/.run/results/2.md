# Task 2 result

## Changed

- `src/core/manifest.ts`: added `planningSessions: number` to `ManifestData`, and
  `buildManifest` now counts valid `plan_started` records via `readPlanRecords`
  (reusing the task-1 parser) before returning. A missing/empty/malformed log
  yields zero because `readPlanRecords` already normalizes those cases.
- `tests/manifest.test.ts`: extended coverage through `approveSpec`:
  - existing approval test now asserts `planningSessions === 0` with no log,
  - counts valid starts for new + resumed sessions with an orphan exit ignored,
  - missing, empty, and partially malformed logs accepted without blocking approval,
  - before/after approval assertion that appending only `.run/plan.jsonl` changes
    `planningSessions` while `approveSpec().hash` (hashChangeFolder output) is unchanged.

## Verification

- `node --import tsx --test tests/manifest.test.ts`: 5/5 pass.
- `pnpm verify`: exit 0 (tsc, tests, lint).

## Deviated

- None.

## Drift against features/

- None. No hasher exclusions or approval marker behavior changed; `.run/` remains
  outside the approved hash, confirmed by the repeated-approval hash assertion.

## Missing context

- None. `ManifestData` has a single constructor (`buildManifest`), so the new
  required field did not require changes to other consumers.

## Next steps

- Task 2 acceptance complete. Next: task 3 (or whichever follows) per `tasks.md`.
