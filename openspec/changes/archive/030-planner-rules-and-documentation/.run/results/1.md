# Task 1 result

## Changed

- `PLANNER.md`: extended the managed planner block (`<!-- OSQ:START -->`..`<!-- OSQ:END -->`).
  - `### Tasks` now carries the slicing rule (verify exercises the slice through the real entry
    point, wiring included; out-of-scope work means widen or merge; an executor "outside its
    scope" result is a planning failure) and the detail rule (acceptance lines and reuse names,
    no signature blocks, numbered steps, or line numbers, full signatures only for ports).
  - `### Parent spec` now states every proposal declares a change-level `verify` as the first
    thing written after the goal.
  - Retained `Write files with the file tool, never through a shell echo.` under
    `### Before you write a task`.
- `src/core/init.ts`: mirrored the same edits in `MANAGED_PLANNER_BLOCK`. `updatePlannerMd` and
  `scaffoldProject` continue to write the block unchanged in behavior.
- `templates/PLANNER.md`: created new template containing the managed block byte-for-byte.
- `tests/init-planner.test.ts`: added `extractManagedBlock` helper plus tests for the slicing,
  detail, and change-level verify rules; strengthened the scaffold test to compare the written
  block to `MANAGED_PLANNER_BLOCK`; added a byte-for-byte equality test across `PLANNER.md`,
  `MANAGED_PLANNER_BLOCK`, and `templates/PLANNER.md`.

## Deviated

None. All acceptance lines satisfied and all edits stayed inside scope.

## Verify

- `node --import tsx --test tests/init-planner.test.ts` -> 10 pass, 0 fail.
- `pnpm tsc --noEmit` clean.
- `pnpm exec biome check src/core/init.ts tests/init-planner.test.ts` clean.
- Regression run of `tests/init.test.ts tests/managed-blocks.test.ts tests/doctor.test.ts
  tests/manifest.test.ts tests/line-budget.test.ts tests/changelog-release-docs.test.ts`
  -> 47 pass, 0 fail.

## Drift against features/

None. `cli-foundation` planner-scaffolding/file-tool requirements stay satisfied; no living spec
was edited.

## Missing context

None.
