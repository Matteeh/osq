---
title: When planner protocol is updated, PLANNER.md, initialization templates, and byte-equality tests reflect slicing, detail, file tool, and change-level verify rules
verify: node --import tsx --test tests/init-planner.test.ts
scope: [PLANNER.md, src/core/init.ts, templates/PLANNER.md, tests/init-planner.test.ts]
entry: [src/core/init.ts]
skills: []
tests.modify: true
---
## Acceptance
- [ ] PLANNER.md encodes slicing, detail, files, and change-level verify rules between OSQ start and end markers
- [ ] MANAGED_PLANNER_BLOCK in src/core/init.ts matches PLANNER.md managed block content byte-for-byte
- [ ] templates/PLANNER.md matches the managed planner block byte-for-byte
- [ ] scaffoldProject and updatePlannerMd in src/core/init.ts continue to write the updated block on project initialization
- [ ] tests/init-planner.test.ts asserts byte-for-byte equality across PLANNER.md, MANAGED_PLANNER_BLOCK, and templates/PLANNER.md

## Details

Update the managed planner block in `PLANNER.md`, `MANAGED_PLANNER_BLOCK` in `src/core/init.ts`, and `templates/PLANNER.md`.
Reuse `OSQ_START_MARKER`, `OSQ_END_MARKER`, `MANAGED_PLANNER_BLOCK`, `updatePlannerMd`, `updateManagedBlock`, `scaffoldProject`, and `TEMPLATES_ROOT` in `src/core/init.ts`.
Under `### Before you write a task`, ensure `Write files with the file tool, never through a shell echo.` is retained.
Under `### Tasks`, add the slicing rule stating every task's `verify` exercises its slice through the real entry point, wiring included, and if closing the loop requires a file outside the task's scope, the scope is wrong, widen it or merge the task, and an executor result stating outside this task's scope is a planning failure. Add the detail rule stating task bodies carry acceptance lines and names of existing code to reuse, without signatures blocks, numbered implementation steps, or line numbers, with full signatures written only for ports.
Under `### Parent spec`, add the change-level verify rule stating every proposal declares one as the first thing written after the goal.
Create `templates/PLANNER.md` containing the updated managed block.
In `tests/init-planner.test.ts`, reuse existing test helpers and temporary directories to verify that `PLANNER.md`, `MANAGED_PLANNER_BLOCK`, and `templates/PLANNER.md` match byte-for-byte between markers.
