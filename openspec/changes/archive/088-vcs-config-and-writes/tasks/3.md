---
title: When doctor runs with vcs enabled, it warns about a missing prepare command, commit hooks, and signing
verify: node --import tsx --test tests/vcs-doctor-warnings.test.ts
verify_starts: red
scope:
  - src/core/vcs/doctor-git.ts
  - tests/vcs-doctor-warnings.test.ts
entry:
  - src/core/vcs/doctor-git.ts
  - src/core/vcs/vcs.ts
  - tests/vcs-doctor.test.ts
skills: []
---
## Acceptance

- [ ] `checkGit` in `doctor-git.ts` appends the `vcs-prepare`, `git-hooks`, and `git-signing` warnings after the `git` line, as the cli-foundation delta's "Doctor version control warnings" says. It reads hooks and `commit.gpgsign` through the `Vcs` port's `hookNames` and `configValue`.
- [ ] New `tests/vcs-doctor-warnings.test.ts` covers the delta's three scenarios through `runDoctorChecks` in temporary repositories, plus `git-signing` with `commit.gpgsign` set to true in the repository's own config, and the exit code staying 0.
- [ ] `tests/vcs-doctor.test.ts` and `tests/doctor.test.ts` pass unchanged.

## Details

`doctor.ts` spreads whatever `checkGit` returns, so it needs no change. Each
message names what it found and what to do: set `vcs.prepare` to the install
command, check that each hook runs unattended and leaves the tree as verified,
and check that signing runs without a prompt.
