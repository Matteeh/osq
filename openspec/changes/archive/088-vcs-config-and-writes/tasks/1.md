---
title: When osq.config.ts sets a vcs block, osq validates it and exposes it on the loaded config
verify: node --import tsx --test tests/vcs-config.test.ts
verify_starts: red
scope:
  - src/core/foundation/config-vcs.ts
  - src/core/foundation/config-agents.ts
  - src/core/foundation/config.ts
  - src/core/foundation/config-user.ts
  - src/index.ts
  - tests/vcs-config.test.ts
entry:
  - src/core/foundation/config.ts
  - src/core/foundation/config-gates.ts
  - src/core/foundation/config-user.ts
skills: []
---
## Acceptance

- [ ] New `src/core/foundation/config-vcs.ts` exports `VcsConfig`, `DEFAULT_VCS_CONFIG`, and `validateVcsConfig`, as the cli-foundation delta's "Version control configuration" says, and `defineConfig` sets `vcs` from it.
- [ ] `OsqTimeouts` gains optional `gitCommitSeconds`, and `config-vcs.ts` exports `DEFAULT_GIT_COMMIT_SECONDS`, 120.
- [ ] `AgyConfig` and `OpencodeConfig` move into new `src/core/foundation/config-agents.ts`, and `config.ts` re-exports them, so every existing import keeps working.
- [ ] `OsqUserConfig` accepts a partial `vcs` block, and `src/index.ts` exports the `VcsConfig` type.
- [ ] New `tests/vcs-config.test.ts` covers the delta's three scenarios through `loadConfig` in temporary projects, plus trimming of `worktreeRoot` and `prepare`, an empty `prepare` failing, and a non-boolean `enabled` failing.

## Details

Model the validator on `validateGatesConfig` in `config-gates.ts`. A trial
showed the move takes `config.ts` from 249 to 243 lines with `vcs` and
`gitCommitSeconds` wired in, and breaks no test. Keep `config.ts` within 250
lines. The author form is a name, a space, and an email in angle brackets. The
error messages are the delta's, word for word.
