# Tasks

## 1. Config

- [x] 1. When osq.config.ts sets a vcs block, osq validates it and exposes it on the loaded config

## 2. Git writes

- [x] 2. When osq needs a git write, the Vcs port offers exactly stage 1's writes and nothing on the never list

## 3. Doctor warnings

- [x] 3. When doctor runs with vcs enabled, it warns about a missing prepare command, commit hooks, and signing
