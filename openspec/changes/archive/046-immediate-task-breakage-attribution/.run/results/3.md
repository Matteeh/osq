# Task 3 — When archive behavior is isolated, tests select the intended verification boundary

## Changed

- `tests/archive-verification.test.ts`:
  - Added a local `ARCHIVE_ONLY_CONFIG: OsqConfig = Object.freeze({ ...DEFAULT_CONFIG,
    gates: { changeVerifyAfterTask: false } })`, derived from `DEFAULT_CONFIG`.
  - Clean two-task case keeps `DEFAULT_CONFIG` (default-on gate) and now expects
    three change-target `verify_ran` events — one after each task plus the
    established archive-time one — asserting `command`, `exitCode`, `duration`
    for every event. Task-stream assertions are unchanged.
  - `blocks archiving when the change-level verify fails` and `leaves the
    transient plan-prompt.md in place when archive verification fails` now pass
    `ARCHIVE_ONLY_CONFIG` to both `runTask` calls and
    `checkAndArchiveSpec`, so the deliberately red proposal verify is first
    met by the archive gate; the existing archive-time `verify_red` marker/event
    assertions are unchanged.
  - Final-task scope-drift case passes `ARCHIVE_ONLY_CONFIG` to all three
    `runTask` calls and `checkAndArchiveSpec`, so the zero change-target
    verification assertion still means the ordinary archive verifier never ran
    after the scope audit halted archival.
- `tests/plan-prompt-lifecycle.test.ts`:
  - Added the same local `ARCHIVE_ONLY_CONFIG` derived from `DEFAULT_CONFIG`.
  - `runSingleTask` now accepts an optional config (default `DEFAULT_CONFIG`).
  - The failing archive prompt-lifecycle case runs the task with
    `ARCHIVE_ONLY_CONFIG`, reaches archive verification, and keeps its existing
    prompt-retention and active-change assertions.

## Verification

Task verify command passes: 36 tests, 0 failures across
archive-verification, plan-prompt-lifecycle, golden-events, import-graph, and
line-budget. Proposal-level `pnpm verify` is green on the final tree:
`typecheck:cli`, `typecheck:ui`, `build`, full `test` (TS + UI), and `lint` all
pass. No production source or archive implementation was touched.

## Deviated

- `checkAndArchiveSpec` also receives the local `ARCHIVE_ONLY_CONFIG` in the
  isolated scenarios, matching the task detail's "leaving `checkAndArchiveSpec`
  on the same local config". The archiver never reads `gates`, so archive
  behavior is identical to `DEFAULT_CONFIG`.

## Missing context

None. All six acceptance lines are satisfied.

## Next steps

None for this task; the change no longer has the out-of-scope regression that
task 1 and task 2 recorded.
