# Task 1 — Import boundaries and `WatchCommandOptions` relocation

Status: complete. All acceptance lines satisfied; verify command passes.

## Changed

- `src/watcher/dev.ts`
  - Removed `import type { WatchCommandOptions } from '../cli/watch.js';` (the sole `watcher -> cli` edge).
  - Declared and exported `WatchCommandOptions` in the watcher tier.
- `src/cli/watch.ts`
  - Removed the local interface declaration.
  - Imports `type { WatchCommandOptions }` from `../watcher/dev.js` and re-exports it so existing importers (e.g. `tests/watcher-dev.test.ts`) keep working.
- `tests/import-graph.test.ts` (new)
  - Recursively lists `src/**/*.ts`, extracts relative static (`import`/`export ... from`) and dynamic `import(...)` specifiers, resolves them, and enforces: `core` stays inside `core`, `harness` imports neither `watcher` nor `cli`, `watcher` imports no `cli`.
  - Also asserts the two structural acceptance lines (interface declared in `dev.ts`, imported from `dev.js` in `cli/watch.ts`).

## Deviated

- The task detail's literal `WatchCommandOptions` body omits `once`, but `buildWorkerInvocation` in `src/watcher/dev.ts` reads `options.once`. I kept the fields listed in the task (`dir`, `harness`, `verbose`, `quiet`, `symbols`, `allowStale`, `dev`) and additionally retained `once?: boolean` so `pnpm tsc --noEmit` stays green. Behavior is unchanged.
- The test separately asserts acceptance lines 1–2 via source text in addition to the graph scan described in step 3; this is additive and does not alter the specified checks.

## Drift against features/

- None observed. The change implements the `watcher-and-harness` "Architectural import graph boundaries" rule and resolves the reverse `WatchCommandOptions` import described in the proposal contract.

## Missing context

- None.

## Verification

- `node --import tsx --test tests/import-graph.test.ts` — 6/6 pass.
- `pnpm tsc --noEmit` — clean.
- `pnpm lint` — clean.
- Full suite `pnpm test` — 477 pass, 0 fail.

## Next

- No further acceptance line for this task. Remaining work belongs to the other tasks of change 021 (splitting `src/watcher/runner.ts` into `lock.ts`, `spawn.ts`, `heartbeat.ts`, `verify.ts`, `outcome.ts`).
