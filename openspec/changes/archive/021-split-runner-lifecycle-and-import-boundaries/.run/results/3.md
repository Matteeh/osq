# Task 3 — Dedicated outcome module extraction

Status: complete. All acceptance lines satisfied; verify command passes.

## Changed

- `src/watcher/outcome.ts` (new, 181 lines)
  - Moved out of `runner.ts`: `RunTaskFailureReason`, `RunTaskResult`,
    `tickTaskCheckboxContent`, `tickTaskCheckbox`, `recordLifecycleEvent`,
    `recordDeadEvent`, `recordDoneEvent`, and `formatTaskOutcomeLine`.
  - `recordLifecycleEvent`, `recordDeadEvent`, and `recordDoneEvent` are now
    exported (previously module-private in `runner.ts`).
  - Added exported marker writers `writeDeadMarker(runDir, taskNumber, content)`
    and `writeDoneMarker(runDir, taskNumber)` for consistent
    `.run/dead/<n>.md` / `.run/done/<n>` writes, per the task detail.
- `src/watcher/runner.ts`
  - Removed the moved definitions and now imports them from `./outcome.js`.
  - Re-exports `RunTaskFailureReason` and `RunTaskResult` from `./outcome.js`
    so `tests/runner-test-gating.test.ts` (external caller, out of scope) keeps
    importing them from `runner.js`.
  - Replaced every inline `fs.mkdir(deadDir) + fs.writeFile(deadDir/<n>.md)`
    site with `writeDeadMarker`, and the done marker write with
    `writeDoneMarker`; removed the now-unused `doneDir` local. `deadDir` is
    retained only for the pre-done `unlink` cleanup.
  - Dropped now-unused `DeadEventData` / `DoneEventData` / `HarnessEvent`
    imports; kept `appendHarnessEvent` for `result_written`/`verify_ran`.
  - Still 612 lines; the remaining decomposition is owned by tasks 4–5.
- `tests/runner-outcome-logging.test.ts`
  - Imports `formatTaskOutcomeLine` (plus the other moved helpers) from
    `../src/watcher/outcome.js`; `runTask` stays on `../src/watcher/runner.js`.
  - Added an `Outcome module shape` suite: type/value import from `outcome.js`
    and a `< 200` line budget assertion for `outcome.ts`.
- `tests/runner-checkbox-projection.test.ts`
  - Imports `tickTaskCheckbox` and `tickTaskCheckboxContent` from
    `../src/watcher/outcome.js`; `runTask` stays on `runner.js`.
- `tests/runner-done-dead-events.test.ts` — unchanged (it only imports
  `runTask`); it is part of the verify command and passes.

## Deviated

- Acceptance only lists the moved exports, but the task detail also asks to
  "export helper functions to write dead markers and done markers
  consistently", so `writeDeadMarker`/`writeDoneMarker` were added and wired
  through `runner.ts`. Marker contents are byte-for-byte unchanged.
- Added the `Outcome module shape` tests to a scoped test file (additive) to
  cover the export and line-budget acceptance lines at runtime.

## Drift against features/

- None observed. This implements the `watcher-and-harness`
  "Runner lifecycle modularization" rule for the outcome phase; the outcome
  module stays inside `src/watcher` and imports only `src/core`, `src/harness`,
  and node builtins, so the task-1 import-graph boundaries hold.

## Missing context

- None.

## Verification

- `node --import tsx --test tests/runner-outcome-logging.test.ts tests/runner-checkbox-projection.test.ts tests/runner-done-dead-events.test.ts` — 38/38 pass.
- `pnpm tsc --noEmit` — clean.
- `pnpm lint` — clean.
- Full suite `pnpm test` — 482 pass, 0 fail.

## Next

- No further acceptance line for this task. Remaining work is change 021 tasks 4–5:
  `verify.ts` extraction, `spawn.ts` extraction, reducing `runner.ts` below 200
  lines, and adding the line-count assertion to `tests/import-graph.test.ts`.
