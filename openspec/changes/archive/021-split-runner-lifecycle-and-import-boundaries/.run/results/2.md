# Task 2 — Task lock and heartbeat module extraction

Status: complete. All acceptance lines satisfied; verify command passes.

## Changed

- `src/watcher/heartbeat.ts` (new, 199 lines)
  - Moved out of `runner.ts`: `TaskHeartbeatStats`, `HeartbeatAccumulator`,
    `heartbeatAccumulators` map, `createHeartbeatAccumulator`,
    `relativizeToolSummary`, `computeTaskHeartbeatStats`,
    `clearTaskHeartbeatStats`, `formatTokens`, `formatTaskStatusRow`,
    `formatTaskHeartbeatLine`.
  - Imports `asRecord` from `../harness/stream.js`; no watcher-tier back imports.
  - Doc comments on the moved helpers were condensed to fit the <200 line budget.
- `src/watcher/lock.ts` (new, 18 lines)
  - `acquireTaskLock(runDir, taskNumber)` and `releaseTaskLock(runDir, taskNumber)`
    delegate to `acquireLock`/`releaseLock` from `../core/lock.js`; re-exports the
    core `LockResult` type through the acquire return type.
- `src/watcher/runner.ts`
  - Dropped the direct `../core/lock.js` import and the duplicated heartbeat
    definitions.
  - Imports `clearTaskHeartbeatStats`, `computeTaskHeartbeatStats`,
    `formatTaskHeartbeatLine`, `formatTaskStatusRow` from `./heartbeat.js`.
  - Imports `acquireTaskLock`, `releaseTaskLock` from `./lock.js` and now calls
    those wrappers at lock acquire (line ~438) and release in `finally` (~765).
  - `ELLIPSIS`/`truncateToWidth` stay behind because `formatTaskStartedLine`
    (not in this task's move list) still uses them.
- `tests/watcher-heartbeat.test.ts`
  - `computeTaskHeartbeatStats` now imported from `../src/watcher/heartbeat.js`.
  - Added `Watcher lifecycle modules` suite: a real acquire/contend/release round
    trip through `acquireTaskLock`/`releaseTaskLock`, and a `<200` line budget
    assertion for `lock.ts` and `heartbeat.ts`.
- `tests/runner-terminal-status.test.ts`
  - Heartbeat type/helpers now imported from `../src/watcher/heartbeat.js`;
    `runTask` still from `../src/watcher/runner.js`.

## Deviated

- `formatTaskStartedLine` and `formatTaskOutcomeLine` remain in `runner.ts`
  because they are assigned to tasks 5 and 3 respectively. `runner.ts` is still
  767 lines; the remaining reduction is owned by tasks 3–5, not this task.
- Added two behavioral/structural tests to `watcher-heartbeat.test.ts` (in scope)
  beyond the literal import-path edits, per the rule to test each acceptance line.
  These are additive and do not change runner behavior.

## Drift against features/

- None observed. This implements the `watcher-and-harness` "Runner lifecycle
  modularization" rule (lock/heartbeat phases under 200 lines) and keeps all
  watcher imports inside `src/watcher`, so the task-1 import-graph test still
  holds.

## Missing context

- None.

## Verification

- `node --import tsx --test tests/watcher-heartbeat.test.ts tests/runner-terminal-status.test.ts tests/runner-already-running.test.ts` — 37/37 pass.
- `pnpm tsc --noEmit` — clean.
- `pnpm lint` — clean.
- Full suite `pnpm test` — 479 pass, 0 fail.

## Next

- No further acceptance line for this task. Remaining work belongs to change 021
  tasks 3–5: `outcome.ts`, `verify.ts`, and `spawn.ts` extraction, plus reducing
  `runner.ts` below 200 lines and adding the line-count assertion to
  `tests/import-graph.test.ts`.
