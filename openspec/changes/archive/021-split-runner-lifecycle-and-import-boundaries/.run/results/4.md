# Task 4 — Dedicated verify module extraction

Status: complete. All acceptance lines satisfied; verify command passes.

## Changed

- `src/watcher/verify.ts` (new, 195 lines)
  - Moved out of `runner.ts`: `extractFinalTextFromStream`, `synthesizeResultFile`,
    `snapshotTestFiles`, `findUndeclaredTestChanges`, plus the private
    `TEST_DIR_NAME` constant and `hashFileContent` helper.
  - `snapshotTestFiles` and `findUndeclaredTestChanges` are now exported
    (previously module-private in `runner.ts`).
  - Added exported `runVerificationGate(projectRoot, verifyCommand,
    verifyTimeoutSeconds)` returning `{ passed, timedOut, error? }`. It spawns
    the command detached with `shell: true`, collects stdout/stderr, enforces
    the timeout with process-group `SIGTERM` and delayed `SIGKILL` cleanup, and
    returns the failure diagnostic.
  - Doc comments were condensed so the file lands at 195 lines (< 200 budget).
- `src/watcher/runner.ts`
  - Removed the moved definitions and the now-unused `spawn`, `createHash`,
    `Dirent`, and `asRecord` imports.
  - Imports `extractFinalTextFromStream`, `findUndeclaredTestChanges`,
    `runVerificationGate`, `snapshotTestFiles`, `synthesizeResultFile` from
    `./verify.js`.
  - Replaced the ~70-line inline verify `Promise` block with a
    `runVerificationGate` call; the dead-marker/event construction is
    byte-for-byte unchanged (same `reason: verify_red`, `timed_out: true`, and
    diagnostic text).
  - Now 410 lines; the remaining reduction to < 200 belongs to task 5
    (`spawn.ts` extraction and `runner.ts` pipeline slimming).
- `tests/runner-synthesized-result.test.ts`
  - Imports `runTask` from `../src/watcher/runner.js` and
    `extractFinalTextFromStream`, `synthesizeResultFile`, `snapshotTestFiles`,
    `findUndeclaredTestChanges`, `runVerificationGate` from
    `../src/watcher/verify.js`.
  - Added an `verify.ts exports the synthesis and gating helpers` shape test, a
    `< 200` line budget assertion, a snapshot/diff behavior test, and
    `runVerificationGate` pass/fail/timeout tests.

## Deviated

- The task detail lists the four moved functions plus `runVerificationGate`;
  I also moved the two private helpers (`TEST_DIR_NAME`, `hashFileContent`) that
  the snapshot functions depend on. This keeps `verify.ts` self-contained and
  removes the duplicate hashing path from `runner.ts`.
- `runner.ts` retains its `fs`/`path` imports and the result-existence check
  around synthesis (lines ~268–300). Full `ensureTaskResult` extraction is owned
  by task 5, so only delegation—not relocation—is done here, matching scope.
- Added behavioral tests for `runVerificationGate` and the snapshot helpers to
  the in-scope test file (additive) to cover acceptance lines at runtime.

## Drift against features/

- None observed. This implements the `watcher-and-harness` "Runner lifecycle
  modularization" rule for the verify phase. The new module stays inside
  `src/watcher` and imports only `src/harness` and node builtins, so the task-1
  import-graph boundaries hold. Public runner behavior and marker contents are
  unchanged.

## Missing context

- None.

## Verification

- `node --import tsx --test tests/runner-synthesized-result.test.ts tests/runner-test-gating.test.ts` — 22/22 pass.
- `pnpm tsc --noEmit` — clean.
- `pnpm lint` — clean.
- Full suite `pnpm test` — 487 pass, 0 fail.
- `wc -l src/watcher/verify.ts` — 195.

## Next

- No further acceptance line for this task. Remaining work is change 021 task 5:
  create `src/watcher/spawn.ts` (`formatTaskStartedLine` + agent spawn/lifecycle),
  reduce `runner.ts` below 200 lines, and add the `< 200` line-count assertion
  for all six lifecycle modules to `tests/import-graph.test.ts`.
