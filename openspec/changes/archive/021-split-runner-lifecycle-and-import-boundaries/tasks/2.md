---
title: When heartbeat monitoring or task locks are exercised, runner delegates to dedicated modules
verify: node --import tsx --test tests/watcher-heartbeat.test.ts tests/runner-terminal-status.test.ts tests/runner-already-running.test.ts
scope: [src/watcher/lock.ts, src/watcher/heartbeat.ts, src/watcher/runner.ts, tests/watcher-heartbeat.test.ts, tests/runner-terminal-status.test.ts]
entry: [src/watcher/heartbeat.ts, src/watcher/lock.ts]
skills: []
tests:
  modify: true
---
## Acceptance
- [ ] src/watcher/lock.ts provides acquireTaskLock and releaseTaskLock under 200 lines
- [ ] src/watcher/heartbeat.ts contains TaskHeartbeatStats, computeTaskHeartbeatStats, clearTaskHeartbeatStats, relativizeToolSummary, formatTokens, formatTaskStatusRow, and formatTaskHeartbeatLine under 200 lines
- [ ] src/watcher/runner.ts imports lock and heartbeat helpers from lock.ts and heartbeat.ts
- [ ] tests/watcher-heartbeat.test.ts imports computeTaskHeartbeatStats from ../src/watcher/heartbeat.js
- [ ] tests/runner-terminal-status.test.ts imports heartbeat helpers from ../src/watcher/heartbeat.js
- [ ] lock.ts and heartbeat.ts each contain fewer than 200 lines

## Details

1. Create `src/watcher/lock.ts`:
   - Import `acquireLock` and `releaseLock` from `../core/lock.js`.
   - Export helper `acquireTaskLock(runDir: string, taskNumber: string | number)` returning the lock result.
   - Export helper `releaseTaskLock(runDir: string, taskNumber: string | number)` releasing the lock.
   - File length must be under 200 lines (target ~50 lines).
2. Create `src/watcher/heartbeat.ts`:
   - Move from `src/watcher/runner.ts`:
     - `TaskHeartbeatStats` interface
     - `HeartbeatAccumulator` interface, `heartbeatAccumulators` map, `createHeartbeatAccumulator`
     - `relativizeToolSummary`
     - `computeTaskHeartbeatStats`
     - `clearTaskHeartbeatStats`
     - `formatTokens`
     - `formatTaskStatusRow`
     - `formatTaskHeartbeatLine`
   - File length must be under 200 lines (target ~180 lines).
3. In `src/watcher/runner.ts`:
   - Import lock functions from `./lock.js`.
   - Import heartbeat types and functions from `./heartbeat.js`.
   - Remove duplicate definitions of moved heartbeat functions from `runner.ts`.
4. In `tests/watcher-heartbeat.test.ts`:
   - Update import of `computeTaskHeartbeatStats` to `../src/watcher/heartbeat.js`.
5. In `tests/runner-terminal-status.test.ts`:
   - Update imports of `type TaskHeartbeatStats`, `computeTaskHeartbeatStats`, `formatTaskHeartbeatLine`, `formatTaskStatusRow`, `formatTokens`, `relativizeToolSummary` to `../src/watcher/heartbeat.js`.
6. Verification:
   - Run `node --import tsx --test tests/watcher-heartbeat.test.ts tests/runner-terminal-status.test.ts tests/runner-already-running.test.ts`.
