---
title: When tasks execute, runner coordinates lifecycle modules with all files under 200 lines
verify: node --import tsx --test tests/runner.test.ts tests/import-graph.test.ts
scope: [src/watcher/spawn.ts, src/watcher/runner.ts, tests/import-graph.test.ts]
entry: [src/watcher/spawn.ts, src/watcher/runner.ts]
skills: []
tests:
  modify: true
---
## Acceptance
- [ ] src/watcher/spawn.ts manages adapter spawning, lifecycle events, and formatTaskStartedLine under 200 lines
- [ ] src/watcher/runner.ts orchestrates lifecycle sequence across lock.ts, heartbeat.ts, spawn.ts, verify.ts, and outcome.ts in under 200 lines
- [ ] tests/import-graph.test.ts asserts lock.ts, spawn.ts, heartbeat.ts, verify.ts, outcome.ts, and runner.ts each have fewer than 200 lines
- [ ] Full test suite passes via pnpm test
- [ ] Full lint passes via pnpm lint

## Details

1. Create `src/watcher/spawn.ts`:
   - Move from `src/watcher/runner.ts`:
     - `formatTaskStartedLine`
     - Agent process execution coordination: calls `adapter.spawn`, hooks `onSpawn` to emit the `started` event with build info from `resolveBuildInfo`, emits `exited` event upon completion, and handles crash/timeout exits.
   - File length must be under 200 lines (target ~170 lines).
2. In `src/watcher/runner.ts`:
   - Import from `./lock.js`, `./heartbeat.js`, `./spawn.js`, `./verify.js`, `./outcome.js`.
   - Reduce `runTask` to the pure sequential pipeline:
     1. Validate approval hash and task markdown existence.
     2. Acquire lock via `acquireTaskLock`.
     3. Start heartbeat timer via `startTaskHeartbeat`.
     4. Capture test snapshot via `snapshotTestFiles`.
     5. Spawn agent via `spawnTaskAgent`.
     6. Check test modification gating via `findUndeclaredTestChanges`.
     7. Ensure result exists or synthesize via `ensureTaskResult`.
     8. Execute zero-trust verification via `runVerificationGate`.
     9. Record done marker, event, and tick checklist via `recordTaskCompletion`.
     10. In finally: stop heartbeat, clear stats, release lock.
   - File length must be under 200 lines (target ~120 lines).
3. In `tests/import-graph.test.ts`:
   - Add assertion verifying line count of each file in `[ 'lock.ts', 'spawn.ts', 'heartbeat.ts', 'verify.ts', 'outcome.ts', 'runner.ts' ]` under `src/watcher/` is strictly less than 200 lines.
4. Verification:
   - Run `node --import tsx --test tests/runner.test.ts tests/import-graph.test.ts`.
   - Run `pnpm tsc --noEmit && pnpm test && pnpm lint`.
