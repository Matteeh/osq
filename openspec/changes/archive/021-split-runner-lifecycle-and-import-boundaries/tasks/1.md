---
title: When import boundaries are checked, internal packages conform to directional layer constraints
verify: node --import tsx --test tests/import-graph.test.ts
scope: [src/watcher/dev.ts, src/cli/watch.ts, tests/import-graph.test.ts]
entry: [src/watcher/dev.ts, tests/import-graph.test.ts]
skills: []
---
## Acceptance
- [ ] WatchCommandOptions interface is declared in src/watcher/dev.ts instead of src/cli/watch.ts
- [ ] src/cli/watch.ts imports WatchCommandOptions from ../watcher/dev.js
- [ ] tests/import-graph.test.ts verifies src/core imports from no other directory in src
- [ ] tests/import-graph.test.ts verifies src/harness imports from neither src/watcher nor src/cli
- [ ] tests/import-graph.test.ts verifies src/watcher imports from no module in src/cli
- [ ] tests/import-graph.test.ts passes with zero import violations

## Details

1. In `src/watcher/dev.ts`:
   - Declare and export `WatchCommandOptions` directly in this file:
     ```ts
     export interface WatchCommandOptions {
       dir?: string;
       harness?: string;
       verbose?: boolean;
       quiet?: boolean;
       symbols?: boolean;
       allowStale?: boolean;
       dev?: boolean;
     }
     ```
   - Remove `import type { WatchCommandOptions } from '../cli/watch.js';`.
   - Ensure `src/watcher/dev.ts` contains zero imports from `../cli/*`.
2. In `src/cli/watch.ts`:
   - Import `type { WatchCommandOptions }` from `../watcher/dev.js`.
   - Re-export `WatchCommandOptions` so external callers of `src/cli/watch.ts` continue to see the type.
3. In `tests/import-graph.test.ts`:
   - Recursively scan all `.ts` files under `src/`.
   - Parse import/export declarations matching relative specifiers (`./*` or `../*`) and dynamic `import(...)`.
   - Resolve the target file path relative to the importing file:
     - For any file under `src/core/**`: assert resolved path is inside `src/core/**`.
     - For any file under `src/harness/**`: assert resolved path is not inside `src/watcher/**` and not inside `src/cli/**`.
     - For any file under `src/watcher/**`: assert resolved path is not inside `src/cli/**`.
   - Collect and fail the test if any violations are found.
4. Verification:
   - Run `node --import tsx --test tests/import-graph.test.ts`.
