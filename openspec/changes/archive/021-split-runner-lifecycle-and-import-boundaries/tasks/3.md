---
title: When task completion outcomes are recorded, runner delegates to a dedicated outcome module
verify: node --import tsx --test tests/runner-outcome-logging.test.ts tests/runner-checkbox-projection.test.ts tests/runner-done-dead-events.test.ts
scope: [src/watcher/outcome.ts, src/watcher/runner.ts, tests/runner-outcome-logging.test.ts, tests/runner-checkbox-projection.test.ts, tests/runner-done-dead-events.test.ts]
entry: [src/watcher/outcome.ts]
skills: []
tests:
  modify: true
---
## Acceptance
- [ ] src/watcher/outcome.ts exports RunTaskFailureReason, RunTaskResult, and formatTaskOutcomeLine under 200 lines
- [ ] src/watcher/outcome.ts exports recordLifecycleEvent, recordDeadEvent, and recordDoneEvent
- [ ] src/watcher/outcome.ts exports tickTaskCheckboxContent and tickTaskCheckbox
- [ ] src/watcher/runner.ts delegates outcome logging and checkbox ticking to outcome.ts
- [ ] tests/runner-outcome-logging.test.ts imports formatTaskOutcomeLine from ../src/watcher/outcome.js
- [ ] tests/runner-checkbox-projection.test.ts imports tickTaskCheckbox and tickTaskCheckboxContent from ../src/watcher/outcome.js
- [ ] outcome.ts contains fewer than 200 lines

## Details

1. Create `src/watcher/outcome.ts`:
   - Move from `src/watcher/runner.ts`:
     - `RunTaskFailureReason` type
     - `RunTaskResult` interface
     - `tickTaskCheckboxContent`
     - `tickTaskCheckbox`
     - `recordLifecycleEvent`
     - `recordDeadEvent`
     - `recordDoneEvent`
     - `formatTaskOutcomeLine`
   - Export helper functions to write dead markers and done markers consistently.
   - File length must be under 200 lines (target ~150 lines).
2. In `src/watcher/runner.ts`:
   - Import outcome functions, lifecycle event helpers, and checkbox projection functions from `./outcome.js`.
   - Remove duplicate definitions from `runner.ts`.
   - Re-export `RunTaskFailureReason` and `RunTaskResult` from `runner.ts` for existing external callers.
3. In `tests/runner-outcome-logging.test.ts`:
   - Update import of `formatTaskOutcomeLine` to `../src/watcher/outcome.js`.
4. In `tests/runner-checkbox-projection.test.ts`:
   - Update imports of `tickTaskCheckbox` and `tickTaskCheckboxContent` to `../src/watcher/outcome.js`.
5. Verification:
   - Run `node --import tsx --test tests/runner-outcome-logging.test.ts tests/runner-checkbox-projection.test.ts tests/runner-done-dead-events.test.ts`.
