---
title: When task results are synthesized or verified, runner delegates to a dedicated verify module
verify: node --import tsx --test tests/runner-synthesized-result.test.ts tests/runner-test-gating.test.ts
scope: [src/watcher/verify.ts, src/watcher/runner.ts, tests/runner-synthesized-result.test.ts]
entry: [src/watcher/verify.ts]
skills: []
tests:
  modify: true
---
## Acceptance
- [ ] src/watcher/verify.ts exports extractFinalTextFromStream and synthesizeResultFile under 200 lines
- [ ] src/watcher/verify.ts exports snapshotTestFiles and findUndeclaredTestChanges
- [ ] src/watcher/verify.ts exports runVerificationGate for executing verification commands with timeout and process group cleanup
- [ ] src/watcher/runner.ts delegates text extraction, result synthesis, test gating checks, and verification execution to verify.ts
- [ ] tests/runner-synthesized-result.test.ts imports extractFinalTextFromStream and synthesizeResultFile from ../src/watcher/verify.js
- [ ] verify.ts contains fewer than 200 lines

## Details

1. Create `src/watcher/verify.ts`:
   - Move from `src/watcher/runner.ts`:
     - `extractFinalTextFromStream`
     - `synthesizeResultFile`
     - `snapshotTestFiles`
     - `findUndeclaredTestChanges`
   - Extract independent verification command execution into `runVerificationGate`:
     - Spawns `taskData.verify` detached with shell: true.
     - Enforces `verifyTimeoutSeconds` with process group SIGTERM and SIGKILL cleanup.
     - Collects stdout and stderr, returning pass/fail with error diagnostic.
   - File length must be under 200 lines (target ~180 lines).
2. In `src/watcher/runner.ts`:
   - Import synthesis, test snapshotting, and verification helpers from `./verify.js`.
   - Remove duplicate definitions from `runner.ts`.
3. In `tests/runner-synthesized-result.test.ts`:
   - Update imports of `extractFinalTextFromStream` and `synthesizeResultFile` to `../src/watcher/verify.js`.
4. Verification:
   - Run `node --import tsx --test tests/runner-synthesized-result.test.ts tests/runner-test-gating.test.ts`.
