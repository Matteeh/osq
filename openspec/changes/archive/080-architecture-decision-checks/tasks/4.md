---
title: When tasks added dependencies, osq show lists them per task and osq report per change
verify: node --import tsx --test tests/dependencies-report.test.ts
verify_starts: red
scope:
  - src/core/status/show.ts
  - src/core/report/report-dependencies.ts
  - src/core/report/report.ts
  - src/cli/report.ts
  - README.md
  - tests/dependencies-report.test.ts
entry:
  - src/core/report/report-scope.ts
  - src/core/report/report.ts
  - src/cli/report.ts
  - src/core/status/show.ts
  - tests/report-json.test.ts
skills: []
---
## Acceptance

- [ ] New `src/core/report/report-dependencies.ts` derives the list for the metrics-and-reporting delta's "Dependencies added per change" from task event streams, and `HistoryMetrics` gains optional `dependencies`.
- [ ] `getMetricsReport` fills it, `toStableMetrics` in `src/cli/report.ts` copies it only when it isn't empty, and `formatMetricsReport` prints the section only when it isn't empty.
- [ ] `formatSpecDetails` prints the status-inspection delta's "Dependencies added in show" line after the `Instructions changed after approval:` line, from a small formatter beside `formatStuck`.
- [ ] `README.md` documents ADR `checks` and `denies`, the `denied_dependency` dead reason and its automatic retry, the `adr_check_modified` flag, and the new show and report lines, next to the architecture decisions documentation change 079 added.
- [ ] New `tests/dependencies-report.test.ts` covers every scenario of both requirements on temporary projects with hand-written `dependencies_added` events, through `getMetricsReport`, the report command's JSON and text output, and `formatSpecDetails`.

## Details

`src/core/report/report-scope.ts` is the model for a report section derived in
its own module. `report.ts` and `show.ts` are on the line-budget allow list,
and `getMetricsReport`, `formatMetricsReport`, `toStableMetrics`, and
`formatSpecDetails` are grandfathered; keep each to the call and the few
wiring lines. `tests/report-json.test.ts` and `fixture/report/expected.json`
belong to task 2 and must pass unchanged: the fixture has no
`dependencies_added` event, so the omitted field keeps its JSON the same. New
functions stay within 80 lines and new files within 250.
