---
title: When a task may modify an accepted ADR's check file, the approval digest flags it and osq report counts the flag
verify: node --import tsx --test tests/adr-check-flag.test.ts
verify_starts: red
scope:
  - src/core/spec/digest-decisions.ts
  - src/core/spec/digest.ts
  - src/core/report/approval-flags.ts
  - tests/report-approval-flags.test.ts
  - tests/report-json.test.ts
  - fixture/report/expected.json
  - tests/adr-check-flag.test.ts
entry:
  - src/core/spec/digest-decisions.ts
  - src/core/spec/digest.ts
  - src/core/report/approval-flags.ts
  - tests/approval-digest-decisions.test.ts
  - tests/runner-test-gating.test.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] `ApprovalFlagId` gains `adr_check_modified`, and `collectDigestDecisions` raises it as the spec-lint-and-approve delta's "ADR check modification flag" says, from task projections holding each task's number, `tests.modify`, and resolved scope paths.
- [ ] `buildApprovalDigest` passes those projections and appends the new flags after the departure flags.
- [ ] `FLAG_IDS` in `approval-flags.ts` gains `adr_check_modified` after `adr_departure`, as the metrics-and-reporting delta's "ADR check flag outcomes" says.
- [ ] `tests/report-approval-flags.test.ts` and `tests/report-json.test.ts` expect the new key with zero counts, and `fixture/report/expected.json` gains it. Nothing else in them changes.
- [ ] New `tests/adr-check-flag.test.ts` covers both scenarios of "ADR check modification flag" and "Check flag counted". It approves a flagged change with `approveSpec`, reads the report's approval flag outcomes, and runs `runTask` with a fake adapter that edits the check file without `tests.modify` to show the `undeclared_test_change` death.

## Details

Only accepted ADRs count. A proposed or superseded ADR's check files raise
nothing. `tests/runner-test-gating.test.ts` shows how to run a task whose agent
edits a preexisting test. `digest.ts` is 233 lines and must stay within 250,
and `buildApprovalDigest` must stay within 80 lines. A trial adding the flag id
failed only the two report tests and the fixture in scope. New functions stay
within 80 lines and new files within 250.
