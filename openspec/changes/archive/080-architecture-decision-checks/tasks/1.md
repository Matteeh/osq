---
title: When an ADR names checks or denied packages, osq reads them and doctor fails on a missing check file
verify: node --import tsx --test tests/adr-checks-denies.test.ts
verify_starts: red
scope:
  - src/core/foundation/decisions.ts
  - src/core/foundation/decisions-validate.ts
  - src/core/foundation/doctor-decisions.ts
  - decisions/README.md
  - tests/adr-checks-denies.test.ts
entry:
  - src/core/foundation/decisions.ts
  - src/core/foundation/doctor-decisions.ts
  - tests/decisions-read.test.ts
  - tests/rules-block.test.ts
skills: []
---
## Acceptance

- [ ] `Adr` gains `checks: readonly string[]` and `denies: readonly string[]`, read as the cli-foundation delta's "Decision checks and denied packages" says, and `malformed: readonly string[]`, naming each of `checks` and `denies` whose value is present but not a list of non-empty strings.
- [ ] `validateDecisions` reports one error per malformed field, naming the ADR's path and the field.
- [ ] `validateDecisions` and its helpers move to new `src/core/foundation/decisions-validate.ts`, and `decisions.ts` re-exports `validateDecisions` and `DecisionProblems`, so every existing import keeps working.
- [ ] `checkDecisions` fails on each missing check file of an accepted ADR, as "Decision check files in doctor" says, with the message `<adr path>: check file <file> does not exist`.
- [ ] `decisions/README.md`'s `## Format` section describes `checks` and `denies`, and says that only accepted ADRs' checks and denials take effect.
- [ ] New `tests/adr-checks-denies.test.ts` covers every scenario of both requirements on temporary projects through `readDecisions`, `validateDecisions`, and `runDoctorChecks`, plus a missing check file on a superseded ADR passing doctor.

## Details

`decisions.ts` is 231 lines and must stay within 250, which is why validation
moves out. `tests/decisions-read.test.ts` and `tests/rules-block.test.ts` must
pass unchanged; every existing test writes ADRs as files, so the new fields
need no test change. Resolve check files against the project root. New
functions stay within 80 lines and new files within 250.
