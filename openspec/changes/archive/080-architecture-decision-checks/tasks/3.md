---
title: When an agent adds a package to a scoped package.json, the watcher records it and kills the task if an accepted ADR denies it
verify: node --import tsx --test tests/denied-dependency.test.ts
verify_starts: red
scope:
  - src/watcher/dependencies.ts
  - src/watcher/measures.ts
  - src/watcher/task-verify.ts
  - src/watcher/runner.ts
  - src/watcher/failure-reason.ts
  - src/watcher/auto-retry.ts
  - src/harness/types.ts
  - tests/denied-dependency.test.ts
entry:
  - src/watcher/task-verify.ts
  - src/watcher/measures.ts
  - src/watcher/blocked.ts
  - src/core/foundation/decisions.ts
  - src/core/run/scope.ts
  - tests/verify-path-missing.test.ts
skills: []
---
## Acceptance

- [ ] New `src/watcher/dependencies.ts` reads the scoped `package.json` names and implements the watcher-and-harness delta's "Dependencies added" and "Denied dependency" with `resolveScope`, `readDecisions`, and `appendHarnessEvent`.
- [ ] `gatherStartMeasures` adds `dependencies` to the start event as "Dependency baseline" says, through one call into the new module. `MeasuresEventData` gains the optional field.
- [ ] `HarnessEventType` and `OsqEventData` gain `dependencies_added` with a typed `DependenciesAddedEventData` holding `added: { file: string; name: string }[]`. `RunTaskFailureReason` gains `denied_dependency`.
- [ ] `checkBlockedFirst` takes the runner's task verify options object instead of its first four arguments, and runs the dependency check after the blocked check and before `checkMissingVerifyPaths`. The runner's call becomes `checkBlockedFirst(verifyCtx, fail)`, on one line.
- [ ] `ELIGIBLE_AUTO_RETRY_REASONS` gains `denied_dependency` after `verify_path_missing`, as the modified "Automatic retry" says.
- [ ] New `tests/denied-dependency.test.ts` covers every scenario of the three added requirements and "Denied dependency is retried". Its fake agent edits `package.json`, and it asserts the event data, the marker lines, and the retry's `priorFailureOutput`.

## Details

Build the test as `tests/verify-path-missing.test.ts` does: a fake adapter that
edits files and records each spawn's options, and an automatic retry through
the watcher cycle. For "Manifest outside scope", put a `package.json` that is
not valid JSON outside the scope and show that nothing fails.

`src/watcher/runner.ts` is 199 lines and `tests/import-graph.test.ts` keeps it
strictly under 200. The call must not add a line, and `verifyCtx` already holds
`projectRoot`, `specFolderPath`, `taskNumber`, `taskData`, and `config`.
`measures.ts` is 222 lines and must stay within 250. A trial of the baseline,
event, reason, and eligibility broke no test outside the report. The golden
event scenario has no `package.json` in scope, so its fixtures don't change.
New functions stay within 80 lines and new files within 250.
