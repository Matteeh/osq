---
title: When a planner or user reads the docs, verify_starts and gates.preSpawnVerify are explained
verify: node --import tsx --test tests/pre-spawn-docs.test.ts tests/init-planner.test.ts tests/proposal-format.test.ts tests/doctor.test.ts
scope:
  - README.md
  - src/core/foundation/init-blocks.ts
  - PLANNER.md
  - templates/PLANNER.md
entry:
  - README.md
  - src/core/foundation/init-blocks.ts
  - PLANNER.md
  - templates/PLANNER.md
  - tests/init-planner.test.ts
tests:
  modify: false
skills: []
---
## Acceptance

- [ ] The managed planner block (`MANAGED_PLANNER_BLOCK`) gains, under `### Tasks`, one bullet. It says the watcher runs each task's `verify` once before the first attempt and expects it to fail, and that a task whose `verify` should already pass before any change, such as a refactor, declares `verify_starts: green` (or `any` when either is fine).
- [ ] `PLANNER.md` and `templates/PLANNER.md` carry that block byte for byte, so `tests/init-planner.test.ts` still passes.
- [ ] README's "Gates and permissions" list gains a **Pre-spawn verify check** bullet after the change-verification bullet. It covers the first-attempt-only run, `verify_starts` (`red` default, `green`, `any`), `gates.preSpawnVerify` (`warn` default, `fail`, `off`), where a mismatch shows (the task's `verify_ran` event, `osq show`, `osq report`), and the extra verify it costs.
- [ ] README's dead-reasons sentence adds `verify_precondition`, and the files-list line for `tasks/1.md` adds `verify_starts`.
- [ ] A new `tests/pre-spawn-docs.test.ts` asserts that README mentions `gates.preSpawnVerify`, `verify_starts`, and `verify_precondition`, and that `MANAGED_PLANNER_BLOCK` contains `verify_starts: green`.

## Details

Keep the new planner bullet to three or four lines in the style of the bullets
around it. It is an instruction to planners, not a design note. Write the
README bullet in the style of the "Change verification after every task" bullet.
Say plainly that a mismatch warns by default and that `fail` kills the task
before the agent spawns. Every existing test is frozen.
