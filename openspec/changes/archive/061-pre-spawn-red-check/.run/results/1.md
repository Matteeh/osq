## Changed

- `src/core/foundation/config-gates.ts`: exported `PreSpawnVerifyMode` (`'warn' | 'fail' | 'off'`), added optional `preSpawnVerify` to `GatesConfig`, defaulted it to `'warn'` in `DEFAULT_GATES_CONFIG`, and extended `validateGatesConfig` to resolve the default, reject an invalid mode with a `gates.preSpawnVerify` diagnostic, and return the resolved mode alongside `changeVerifyAfterTask`. The non-object gates error no longer claims every gate is boolean.
- `src/core/spec/parser.ts`: exported `VerifyStarts` and `readVerifyStarts`, added `verifyStarts` to `TaskData`, and made `parseTaskMd` return `green`/`any` for those frontmatter values and `red` otherwise.
- `tests/archive-verification.test.ts`: `verifyRanEvents` now also drops events with `data.phase === 'pre_spawn'`.
- `tests/runner-test-gating.test.ts`: every `verify_ran` predicate also ignores `data.phase === 'pre_spawn'`.
- `tests/retry-watch.test.ts`: the written `osq.config.ts` now sets `gates: { preSpawnVerify: 'off' }`.
- `tests/pre-spawn-config.test.ts` (new): tests for the config default/override/rejection and the parser start-state values.

## Deviated

None.

## Missing context

None.

Touched: src/core/foundation/config-gates.ts, src/core/spec/parser.ts, tests/archive-verification.test.ts, tests/retry-watch.test.ts, tests/runner-test-gating.test.ts, tests/pre-spawn-config.test.ts
