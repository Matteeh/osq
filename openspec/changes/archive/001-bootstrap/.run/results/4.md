# Result for Task 4

## Changed
- `src/cli/index.ts`: Configured Commander CLI program metadata and registered `init` and `new <name>` commands.
- `package.json` & `tsconfig.build.json`: Configured build target producing `./dist/cli/index.js` matching binary descriptor.
- `tests/cli.test.ts`: Added unit tests verifying command registration and argument requirements.
