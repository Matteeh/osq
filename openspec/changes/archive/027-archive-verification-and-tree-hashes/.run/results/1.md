---
task: 1
verify: node --import tsx --test tests/golden-events.test.ts
status: done
---

## Changed

- `src/harness/types.ts`: `VerifyRanEventData` now declares `command: string`, `exitCode: number`, `duration: number` (with `verifyCommand?`/`output?` retained as optional legacy fields).
- `src/watcher/verify.ts`: `runVerificationGate` now captures the process exit code, combined stdout/stderr output, and wall-clock duration, and — when an optional `{ specFolderPath, taskNumber }` context is supplied — is the single code path that appends the `verify_ran` event with `command`, `exitCode`, `duration`, and `output` (output omitted when empty). Process-group kill logic extracted to a `killTree` helper to stay under the line budget. Module is 197 lines.
- `src/watcher/runner.ts`: removed the pre-verification `appendHarnessEvent(..., verify_ran)` call and now passes `{ specFolderPath, taskNumber }` to the unified gate. Module is 195 lines. Dropped the now-unused `appendHarnessEvent` import.
- `tests/golden-events.test.ts`: `maskEvent` masks any `duration` field to `0`; added an explicit unit test asserting duration masking preserves `command` and `exitCode`.
- `tests/fixtures/events/verified.jsonl` / `dead.jsonl`: regenerated via `UPDATE_GOLDEN=1` and now carry `{"command":...,"exitCode":0|1,"duration":0}` on the `verify_ran` line.

## Deviated

- Task details suggested `runVerificationGate` return `{ passed, exitCode, duration, output, timedOut, error? }`. Kept the existing backward-compatible return shape (`{ passed, timedOut, error? }`) because `tests/runner-synthesized-result.test.ts` asserts `deepEqual(pass, { passed: true, timedOut: false })` and that file is outside this task's scope. Capture + emission still happen inside the gate, satisfying the acceptance lines.
- Per the line budget, `verify.ts` JSDoc/blank-line trimming and a compact `killTree` helper were used instead of the richer return object.

## Drift against features/

None. `watcher-and-harness` "Typed event hygiene and single emission path" is advanced: only `src/watcher/verify.ts` now emits `verify_ran`.

## Missing context

- Task details' step 1 snippet made `command`/`exitCode`/`duration` required; this was adopted. `output` remains conditional so the golden fixtures match the exact expected line.
- Pre-existing `.run/results` directory did not exist; created it for this file.

## Next acceptance line

All acceptance lines for task 1 are satisfied. Task 2 (done-marker scope hashes / pre-spawn comparison) is next and is not started.
