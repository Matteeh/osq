---
title: When task verification executes, verify_ran events record exitCode with duration via unified emission path matching golden fixtures
verify: node --import tsx --test tests/golden-events.test.ts
scope: [src/harness/types.ts, src/watcher/verify.ts, src/watcher/runner.ts, tests/golden-events.test.ts, tests/fixtures/events/verified.jsonl, tests/fixtures/events/dead.jsonl]
entry: [src/watcher/verify.ts]
skills: []
tests.modify: true
---
## Acceptance
- [ ] src/harness/types.ts updates VerifyRanEventData to include exitCode and duration
- [ ] src/watcher/verify.ts runs verification gate and captures exitCode, duration, stdout, and stderr
- [ ] Single verification emission path emits verify_ran event with exitCode and duration
- [ ] src/watcher/runner.ts invokes the unified verification gate rather than pre-emitting verify_ran
- [ ] tests/golden-events.test.ts masks duration to 0 and asserts golden stream invariants
- [ ] Golden event fixtures verified.jsonl and dead.jsonl match the updated verify_ran schema
- [ ] src/watcher/runner.ts and src/watcher/verify.ts remain strictly under 200 lines

## Details

1. In `src/harness/types.ts`:
   - Update `VerifyRanEventData`:
     ```typescript
     export interface VerifyRanEventData {
       command: string;
       exitCode: number;
       duration: number;
       verifyCommand?: string;
       output?: string;
     }
     ```
   - Ensure backwards compatibility if any existing callers access legacy fields.

2. In `src/watcher/verify.ts`:
   - Extend `runVerificationGate` or create a wrapper that:
     - Measures elapsed wall-clock duration: `const duration = Number(((Date.now() - startMs) / 1000).toFixed(2));`.
     - Captures process exit code (`child.on('close', (code) => ...)`), capturing combined stdout and stderr.
     - Handles timeout, setting `exitCode = 1` or appropriate non-zero code.
     - Calls `appendHarnessEvent(specFolderPath, taskNumber, { type: 'verify_ran', timestamp: new Date().toISOString(), data: { command: verifyCommand, exitCode, duration, output } })`.
     - Returns `{ passed: boolean, exitCode: number, duration: number, output: string, timedOut: boolean, error?: string }`.
   - Ensure the module stays strictly under 200 lines.

3. In `src/watcher/runner.ts`:
   - Replace the early manual `appendHarnessEvent(..., { type: 'verify_ran' })` call with the unified call in `src/watcher/verify.ts`.
   - Ensure `runner.ts` stays strictly under 200 lines.

4. In `tests/golden-events.test.ts`:
   - Update `maskEvent` so that any `duration` field in event data is masked to `0` (similar to `elapsedSeconds`):
     ```typescript
     if ('duration' in record) record.duration = 0;
     ```
   - Re-run `UPDATE_GOLDEN=1 node --import tsx --test tests/golden-events.test.ts` to update `tests/fixtures/events/verified.jsonl` and `tests/fixtures/events/dead.jsonl`.
   - Verify both golden event fixtures match the new line:
     `{"type":"verify_ran","timestamp":"[TIMESTAMP]","data":{"command":"node -e \"process.exit(0)\"","exitCode":0,"duration":0}}`
     and for dead:
     `{"type":"verify_ran","timestamp":"[TIMESTAMP]","data":{"command":"node -e \"process.exit(1)\"","exitCode":1,"duration":0}}`

5. Verify:
   `node --import tsx --test tests/golden-events.test.ts`
