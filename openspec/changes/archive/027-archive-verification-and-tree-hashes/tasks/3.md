---
title: When scope comparison or verification fails, the engine writes regressed markers, emits regressed events, reporting regressed status
verify: node --import tsx --test tests/state-snapshot.test.ts tests/regression-status.test.ts
scope: [src/core/layout.ts, src/core/state.ts, src/core/status.ts, src/watcher/outcome.ts, src/harness/types.ts, tests/state-snapshot.test.ts, tests/regression-status.test.ts]
entry: [src/core/state.ts]
skills: []
tests.modify: true
---
## Acceptance
- [ ] src/harness/types.ts adds regressed to HarnessEventType and defines RegressedEventData
- [ ] src/core/layout.ts adds getRegressedMarkerPath for task and change regression markers
- [ ] src/watcher/outcome.ts exports writeRegressedMarker and recordRegressedEvent
- [ ] src/core/state.ts supports regressed in TaskStatus and SpecStatus derived from regressed markers
- [ ] src/core/status.ts formats tasks and active spec overview with regressed indicators
- [ ] tests/state-snapshot.test.ts tests pure state derivation for regressed markers
- [ ] tests/regression-status.test.ts tests regressed marker formatting and event emission

## Details

1. In `src/harness/types.ts`:
   - Add `'regressed'` to `HarnessEventType`.
   - Define `RegressedEventData`:
     ```typescript
     export interface RegressedEventData {
       task?: string;
       exitCode?: number;
       duration?: number;
       command?: string;
       differingPaths?: string[];
       reason?: string;
     }
     ```
   - Add `regressed: RegressedEventData;` to `OsqEventData`.

2. In `src/core/layout.ts`:
   - Define `REGRESSED_DIR = 'regressed';`
   - Add `getRegressedMarkerPath(changeFolder: string, target: string): string` returning `path.join(changeFolder, RUN_DIR, REGRESSED_DIR, `${target}.md`)`.

3. In `src/watcher/outcome.ts`:
   - Add `'regressed'` to `RunTaskFailureReason`.
   - Export `writeRegressedMarker(runDir: string, target: string, content: string): Promise<void>` writing `.run/regressed/<target>.md`.
   - Export `recordRegressedEvent(specFolderPath: string, target: string, data: RegressedEventData): Promise<void>` appending `regressed` event to `.run/events/<target>.jsonl`.
   - Update `formatTaskOutcomeLine` to support `'regressed'` with failure symbol `?` and `[regressed]` label.
   - Keep `src/watcher/outcome.ts` strictly under 200 lines.

4. In `src/core/state.ts`:
   - Extend `TaskStatus` with `'regressed'`.
   - Extend `SpecStatus` with `'regressed'`.
   - Add `readonly regressedMarkers: ReadonlyMap<string, string>;` to `ChangeFolderSnapshot`.
   - In `deriveTaskStateFromSnapshot`: if `snapshot.regressedMarkers.has(taskNumber)`, assign `status: 'regressed'`.
   - In `deriveSpecState`: if any task has `status === 'regressed'` or `snapshotOrRoot.regressedMarkers.has('change')`, assign `status: 'regressed'`.
   - In `readChangeFolder`: populate `regressedMarkers` from `.run/regressed/` files.
   - Keep `src/core/state.ts` at or under 250 lines.

5. In `src/core/status.ts`:
   - In `formatStatusLine`: if `task.status === 'regressed'`, use indicator `[!]` and label `[regressed]`.
   - In `formatStatusOverview`: if spec status is `regressed`, line shows `[regressed]`.

6. Tests:
   - Update `tests/state-snapshot.test.ts` to assert that `regressedMarkers` derive task and spec `status: 'regressed'`.
   - Create `tests/regression-status.test.ts` to assert `formatStatusLine`, `formatStatusOverview`, `writeRegressedMarker`, and `recordRegressedEvent` behavior.

7. Verify:
   `node --import tsx --test tests/state-snapshot.test.ts tests/regression-status.test.ts`
