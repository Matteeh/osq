---
title: When tasks terminate, dedicated outcome helpers write markers
verify: node --import tsx --test tests/runner-outcome-logging.test.ts tests/lock.test.ts tests/runner-done-dead-events.test.ts
scope: [src/core/lock.ts, src/watcher/outcome.ts, src/watcher/runner.ts, src/watcher/loop.ts, tests/runner-outcome-logging.test.ts, tests/lock.test.ts, tests/runner-done-dead-events.test.ts]
entry: [src/watcher/outcome.ts, src/core/lock.ts]
skills: []
tests:
  modify: true
---
## Acceptance
- [ ] src/watcher/outcome.ts exports writeDoneMarker and writeDeadMarker as sole marker writers
- [ ] src/core/lock.ts refactors reapStaleLocks to detect and return reaped locks without writing markers
- [ ] src/watcher/loop.ts invokes writeDeadMarker and recordDeadEvent for each lock returned by reapStaleLocks
- [ ] src/watcher/runner.ts routes all failure marker writes through writeDeadMarker in outcome.ts
- [ ] tests/lock.test.ts verifies reapStaleLocks returns reaped lock info without creating dead markers
- [ ] tests/runner-outcome-logging.test.ts and tests/runner-done-dead-events.test.ts pass cleanly
- [ ] src/watcher/outcome.ts and src/core/lock.ts remain under 200 lines

## Details

1. In `src/core/lock.ts`:
   - Refactor `reapStaleLocks`: When a stale lock is detected and unlinked, do not create or write `.run/dead/<n>.md`.
   - Return reaped lock descriptors containing `{ taskNumber: string, reason: 'crashed' | 'timeout', pid: number, startedAt: number }`.
   - Remove dead directory path and file creation from `reapStaleLocks`.
2. In `src/watcher/outcome.ts`:
   - Ensure `writeDoneMarker(runDir: string, taskNumber: string): Promise<void>` and `writeDeadMarker(runDir: string, taskNumber: string, content: string): Promise<void>` are exported.
   - Centralize marker formatting and disk writes in these functions.
3. In `src/watcher/loop.ts`:
   - When calling `reapStaleLocks`, iterate over the returned reaped locks.
   - Call `writeDeadMarker` and `recordDeadEvent` through `src/watcher/outcome.ts` for each reaped task.
4. In `src/watcher/runner.ts`:
   - Verify that all failure paths (`no_result`, `crashed`, `timeout`, `verify_red`, `spec_conflict`, `undeclared_test_change`) write their dead markers strictly through `writeDeadMarker`.
5. In `tests/lock.test.ts`:
   - Update tests to assert that `reapStaleLocks` detects expired locks and unlinks pid files without creating dead markers in `.run/dead`.
6. Verification:
   - Run `node --import tsx --test tests/runner-outcome-logging.test.ts tests/lock.test.ts tests/runner-done-dead-events.test.ts`.
