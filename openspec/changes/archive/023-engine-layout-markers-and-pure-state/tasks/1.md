---
title: When runner outcomes occur, invariant assertions verify every failure reason
verify: node --import tsx --test tests/runner-done-dead-events.test.ts
scope: [tests/runner-done-dead-events.test.ts]
entry: [tests/runner-done-dead-events.test.ts]
skills: []
tests:
  modify: true
---
## Acceptance
- [ ] tests/runner-done-dead-events.test.ts tests done event alongside done marker on success
- [ ] tests/runner-done-dead-events.test.ts parameterizes failure checks over every reason in RunTaskFailureReason
- [ ] Parameterized test verifies undeclared_test_change writes matching dead marker and event
- [ ] Parameterized test verifies no_result, crashed, timeout, verify_red, and spec_conflict write matching markers and events
- [ ] Test verifies lock collision under already_running writes neither dead marker nor dead event
- [ ] Every dead marker is asserted to contain valid YAML frontmatter matching the failure reason
- [ ] tests/runner-done-dead-events.test.ts passes completely

## Details

1. In `tests/runner-done-dead-events.test.ts`:
   - Import `RunTaskFailureReason` from `src/watcher/outcome.js`.
   - Keep the existing `done` test verifying that a successful task appends exactly one `done` event and writes the `.run/done/<n>` marker.
   - Refactor the dead failure tests into a parametrized test table covering all possible values of `RunTaskFailureReason`:
     - `no_result`: mock adapter configured with `writeResult: false`.
     - `crashed`: mock adapter configured with non-zero exit code.
     - `timeout`: mock adapter configured with `timedOut: true`.
     - `verify_red`: failing verify command (`exit 1`).
     - `spec_conflict`: tampered task file content or missing approved marker.
     - `undeclared_test_change`: preexisting test modification without `tests.modify: true`.
   - For each failure reason, assert:
     - The dead marker file `.run/dead/<n>.md` exists and contains frontmatter `reason: <reason>`.
     - Exactly one dead event is appended to `.run/events/<n>.jsonl` with `{ task: '1', reason: '<reason>' }`.
   - Retain the test asserting that `already_running` writes no dead marker and no dead event.
2. Verification:
   - Run `node --import tsx --test tests/runner-done-dead-events.test.ts`.
