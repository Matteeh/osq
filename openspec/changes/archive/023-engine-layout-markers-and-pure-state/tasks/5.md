---
title: When source files are inspected, line budget enforces size constraints
verify: node --import tsx --test tests/line-budget.test.ts
scope: [tests/line-budget.test.ts]
entry: [tests/line-budget.test.ts]
skills: []
---
## Acceptance
- [ ] tests/line-budget.test.ts scans all TypeScript source files under src/
- [ ] tests/line-budget.test.ts defines an allow list containing report.ts, show.ts, opencode.ts, agy.ts
- [ ] Every non-allow-listed file under src/ is asserted to have 250 or fewer lines
- [ ] Test fails with diagnostic naming any disallowed file exceeding 250 lines
- [ ] tests/line-budget.test.ts passes cleanly against existing codebase
- [ ] verify command node --import tsx --test tests/line-budget.test.ts succeeds
- [ ] tests/line-budget.test.ts remains under 100 lines

## Details

1. Create `tests/line-budget.test.ts`:
   - Recursively discover all `.ts` files under `src/`.
   - Exclude generated files or type declaration files (`*.d.ts`).
   - Define the explicit allow list: `report.ts`, `show.ts`, `opencode.ts`, `agy.ts`.
   - For every discovered file:
     - If the basename is in the allow list, skip enforcement.
     - Otherwise, count lines (`content.split('\n').length`).
     - If lines > 250, record a failure naming the file path and line count.
   - Assert that no violations exist.
2. Verify all current non-allow-listed source files in `src/` satisfy the constraint.
3. Verification:
   - Run `node --import tsx --test tests/line-budget.test.ts`.
