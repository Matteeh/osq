# Task 2 result

## Changed

- `src/core/lock.ts`: `reapStaleLocks` is now detect-only. It no longer derives the dead directory, creates `.run/dead/`, or writes the marker body; it unlinks the expired `.pid` and returns `ReapedLock[]` (`taskNumber`, `reason`, `pid`, `startedAt`). File now 121 lines.
- `src/watcher/outcome.ts`: added exported `formatReapedMarker(reason, pid, startedAt, reapedAt?)` so reaper marker bodies are formatted in one place next to `writeDeadMarker`/`writeDoneMarker`. `writeDeadMarker` and `writeDoneMarker` remain the only modules that touch `.run/dead` and `.run/done` in `src/`. File now 198 lines.
- `src/watcher/loop.ts`: after `reapStaleLocks`, each returned lock is persisted via `writeDeadMarker(runDir, taskNumber, formatReapedMarker(...))` and `recordDeadEvent(folderPath, taskNumber, reason)`.
- `tests/lock.test.ts`: replaced the two reaper tests with detection tests asserting the returned descriptor shape (`deepEqual`), `.pid` unlink, and that neither `.run/dead/<n>.md` nor the `.run/dead` directory is created. Added a local `exists` helper.
- `src/watcher/runner.ts`: no change needed; every failure path already routes through the shared `fail()` helper, which calls `writeDeadMarker`, and `spawn.ts`/`outcome.ts` keep all marker writes on those two writers.

## Deviated

- Added `formatReapedMarker` to `outcome.ts` (not named in the task) to honor the "centralize marker formatting" intent while keeping `writeDeadMarker`'s signature exactly as specified. To stay under the 200-line budget, two verbose doc comments in `outcome.ts` were condensed.

## Drift against features

- Aligns with `watcher-and-harness`: "Consolidated marker writing and pure state derivation" — reaper detection is now I/O-free with respect to markers, and marker/event emission stays in the watcher loop through `outcome.ts`.

## Missing context

- `pnpm test` (full suite) has one **pre-existing, unrelated** failure: `tests/harness.test.ts` → `MockAdapter setup and spawn simulates task execution and emits events` expects `started`/`exited` events from `MockAdapter.spawn`, but an earlier task removed those emissions from `src/harness/mock.ts` (confirmed via `git diff src/harness/mock.ts`) and moved them into `src/watcher/spawn.ts`. `tests/harness.test.ts` is outside this task's scope, so it was left untouched.
- The task verify command does not exercise `loop.ts`; the reaper wiring was additionally validated with a throwaway inline script that wrote a dead-pid lock, ran `runWatcherCycle`, and observed the expected `.run/dead/<n>.md` body and `dead` event.

## Verification

- `node --import tsx --test tests/runner-outcome-logging.test.ts tests/lock.test.ts tests/runner-done-dead-events.test.ts` — 34/34 pass.
- `pnpm tsc --noEmit` — clean; `pnpm lint` — clean.
- `pnpm test` — only the unrelated pre-existing `tests/harness.test.ts` failure.

## Next steps

- Task complete; every acceptance line satisfied. Follow-up (outside scope): update `tests/harness.test.ts` to assert lifecycle events through `spawnTaskAgent` rather than `MockAdapter.spawn`.
