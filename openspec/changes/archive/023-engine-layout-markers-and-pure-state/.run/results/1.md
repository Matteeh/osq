# Task 1 result

## Changed

- `tests/runner-done-dead-events.test.ts` fully refactored:
  - Parameterized dead-outcome suite over `RunTaskFailureReason` via a `DEAD_SCENARIOS` table covering `no_result`, `crashed`, `timeout`, `verify_red`, `spec_conflict` (tampered task and missing approval), and `undeclared_test_change`.
  - Each scenario asserts `result.success === false`, `result.reason`, the `.run/dead/<n>.md` marker, valid YAML frontmatter `reason: <reason>` via `parseFrontmatter`, and exactly one matching `dead` event `{ task: '1', reason }`.
  - Added a coverage guard test asserting every dead reason is represented and `already_running` is excluded from the table.
  - Retained the `done` test (marker + exactly one `done` event) and additionally asserts no dead marker.
  - Retained/strengthened the `already_running` test to assert neither a dead marker nor a dead event is written.
  - Added a local `MutatingMockAdapter` to simulate an agent editing a preexisting `tests/` file for the `undeclared_test_change` scenario.
  - Imports `RunTaskFailureReason` from `src/watcher/outcome.js` as specified.

## Deviated

- The `verify_red` scenario uses the failing verify command `exit 1` (as prescribed in the task Details) instead of the previous `node -e "process.exit(1)"`. The quoted command makes the runner's dead marker invalid YAML, so `parseFrontmatter` cannot recover `reason`; `exit 1` keeps the marker valid and lets the frontmatter invariant be asserted as required.

## Drift against features

- `watcher-and-harness` marker/event parity is now enforced by this parameterized suite.
- Surfaced (but not fixed, outside scope `tests/**`): `src/watcher/runner.ts` writes the `verify_red` marker as `command: "${taskData.verify}"` without escaping. A verify command containing double quotes produces unparseable YAML frontmatter. A follow-up in watcher scope could YAML-escape that value.

## Missing context

- None.

## Next steps

- Task complete; all acceptance lines satisfied. `node --import tsx --test tests/runner-done-dead-events.test.ts` passes (11/11). `pnpm tsc --noEmit` and `pnpm biome check tests/runner-done-dead-events.test.ts` pass.
