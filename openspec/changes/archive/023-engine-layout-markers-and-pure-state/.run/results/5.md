# Task 5 result

## Changed

- `tests/line-budget.test.ts` (new, 68 lines, under the 100-line limit):
  - Recursively discovers every `.ts` file under `src/` via `fs.readdir({ withFileTypes: true })`, excluding `*.d.ts`.
  - Enforces `MAX_LINES = 250` using `source.split('\n').length` (the expression named in the task Details).
  - Defines a single explicit `ALLOW_LIST` set that contains the four named modules (`report.ts`, `show.ts`, `opencode.ts`, `agy.ts`) and the six additional pre-existing oversized modules (see Deviated).
  - Collects every non-allow-listed file over the budget and asserts `violations` deep-equals `[]`, with an `AssertionError` message listing `src/<path> has <n> lines (max 250)`.

## Deviated

- **Allow list extended beyond the four named modules.** The acceptance line "passes cleanly against existing codebase" cannot hold with only `report.ts`, `show.ts`, `opencode.ts`, `agy.ts`: `wc -l` shows six further pre-existing files over 250 lines that no task in change 023 owns and that are outside this task's `scope`:
  - `src/core/linter.ts` 634, `src/watcher/loop.ts` 400, `src/core/migrate.ts` 346, `src/core/delta.ts` 303, `src/core/parser.ts` 284, `src/harness/types.ts` 257.
  - These are grandfathered in the same `ALLOW_LIST` (documented in a comment) so the ratchet takes effect now: any *new* non-allow-listed file over 250 lines still fails. Removing an entry once its module is split tightens the budget. The four named modules remain present, satisfying "an allow list containing report.ts, show.ts, opencode.ts, agy.ts".

## Drift against features

- `watcher-and-harness` delta scenario "Source file size within budget" states that **all** files except `report.ts`, `show.ts`, `opencode.ts`, and `agy.ts` contain ≤250 lines. The current tree does not satisfy this: six additional modules exceed 250 lines (listed above). The test therefore allow-lists them rather than reporting the scenario as met. The enforced invariant is effectively "no newly oversized source module", not the literal four-exception set.
- No other feature drift.

## Missing context

- The plan for change 023 lists only five tasks and none refactors `linter.ts`, `loop.ts`, `migrate.ts`, `delta.ts`, `parser.ts`, or `types.ts`. The planner appears to have assumed those were already within budget.
- Unrelated to this task: `pnpm tsc --noEmit` is already broken by tasks 3 and 4 (out-of-scope `config.paths.specs`/`config.paths.archive` consumers and old `deriveSpecState` call sites). Task 5's verify command does not invoke `tsc`, so it is unaffected.

## Verification

- `node --import tsx --test tests/line-budget.test.ts` — 1/1 pass.
- Failure path proven by temporarily removing `parser.ts` from the allow list: run failed with `src/core/parser.ts has 285 lines (max 250)`; entry restored and suite re-run green.
- `pnpm exec biome check tests/line-budget.test.ts` — clean.
- `wc -l tests/line-budget.test.ts` — 68.

## Next steps

- Every acceptance line is satisfied by the task verify command.
- Follow-up (outside this task's scope): split the six grandfathered modules and then remove them from `ALLOW_LIST` so the delta scenario's literal four-exception set holds.
- Follow-up (outside this task's scope): repair the `tsc` break introduced by tasks 3 and 4.
