## Changed

- `src/core/foundation/init-blocks.ts`: added exported `EXECUTOR_STEPS` (the
  seven step lines, byte-identical), `RESULT_HEADINGS` (the four headings with
  purposes, in order), `RESULT_TOUCHED_PREFIX` (`Touched:`), and
  `EXECUTOR_EXIT_LINES` (the `## Exiting` body, including empty separators,
  rendered from the headings and prefix). `MANAGED_AGENTS_MD_BODY` now
  interpolates both line arrays joined with `\n`; `MANAGED_AGENTS_BLOCK` stays
  an alias.
- `AGENTS.md` and `.opencode/agent/osq-coder.md`: replaced only the `## Exiting`
  body between the osq markers; every other byte is unchanged and the files keep
  their final-newline state.
- `tests/managed-blocks.test.ts`: added a constants suite (body interpolates the
  joined arrays, contains every step and non-empty exit line, seven steps
  numbered `1.`–`7.`, exit lines name `## Changed`, `## Deviated`,
  `## Missing context`, `## Next`, `Touched:` in order) and an assertion that the
  repository opencode executor agent's managed block equals
  `MANAGED_AGENTS_MD_BODY`. All prior assertions kept.
- `tests/fixtures/events/verified.jsonl` and `dead.jsonl`: regenerated with
  `UPDATE_GOLDEN=1`; only `repoLines` changed (455 → 464).

## Deviated

- The two constants arrays are typed `readonly string[]` rather than mutable
  arrays. Task 2 only reads them, so this does not affect the line-array shape.

Touched: src/core/foundation/init-blocks.ts, AGENTS.md, .opencode/agent/osq-coder.md, tests/managed-blocks.test.ts, tests/fixtures/events/verified.jsonl, tests/fixtures/events/dead.jsonl
