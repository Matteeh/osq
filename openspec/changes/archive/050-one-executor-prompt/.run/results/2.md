## Changed

- Added `src/harness/prompt.ts` with the synchronous `buildExecutorPrompt`
  (header, task/parent/title/scope/entry/verify/result lines, prior context,
  `Delta Specs:`, `Living Capability Specs:`, the managed `EXECUTOR_STEPS` under
  `Rules:`, capability rules, the managed `EXECUTOR_EXIT_LINES` under `Exiting:`,
  and the concrete-result `CRITICAL:` line). Moved `deltaSpecPaths` and a now
  synchronous `livingSpecPaths` there from `codex-prompt.ts`; proposal reads are
  read synchronously from `proposal.md` with `parseSpecMd`, and `Parent Spec:`
  always names `proposal.md`.
- `src/harness/agy/agy.ts`, `src/harness/opencode/opencode.ts`,
  `src/harness/codex/codex-prompt.ts`: prompt builders now return
  `buildExecutorPrompt(options)` only; `buildCodexPrompt` stays async; argv,
  flags, and OpenCode `--file` attachments unchanged. Removed the now-unused
  imports. `buildOpencodeArgs` keeps its body over 80 lines.
- `tests/harness-prompt-injection.test.ts`: added a golden test that builds the
  fixture change (`tasks/3.md`, alpha/beta deltas, `proposal.md` with
  `features.reads: [alpha]`, living `openspec/specs/alpha/spec.md`), compares the
  prompt each harness actually delivers (`-p`, `run`, last arg) with
  `tests/fixtures/prompts/{agy,opencode,codex}.txt`, rewrites them under
  `UPDATE_GOLDEN=1`, and asserts each equals `buildExecutorPrompt`. Added
  managed-step/exit-line, delta/living presence and absence, no-`features/`, and
  `Parent Spec:` naming `proposal.md` without a proposal test. Replaced the old
  numbered-rule assertions.
- Updated `tests/opencode-spawn.test.ts` and `tests/codex/adapter.test.ts` to
  assert the new `CRITICAL:` line and `One attempt.` instead of the old rule
  lines and `Execution procedure (one attempt only)`; other assertions kept.
- Generated `tests/fixtures/prompts/agy.txt`, `opencode.txt`, and `codex.txt`.
  Each equals the task's golden prompt byte for byte with no trailing newline.

## Missing context

- None blocking. `EXECUTOR_STEPS`/`EXECUTOR_EXIT_LINES` were already present from
  task 1 as expected.

## Deviated

- `deltaSpecPaths` and `livingSpecPaths` are exported from `prompt.ts` as they
  were from `codex-prompt.ts`; no caller imports them directly today.

Touched: src/harness/prompt.ts, src/harness/agy/agy.ts, src/harness/opencode/opencode.ts, src/harness/codex/codex-prompt.ts, tests/harness-prompt-injection.test.ts, tests/opencode-spawn.test.ts, tests/codex/adapter.test.ts, tests/fixtures/prompts/agy.txt, tests/fixtures/prompts/opencode.txt, tests/fixtures/prompts/codex.txt
