---
title: When osq installs the executor protocol, the managed AGENTS block is assembled from exported step and exit constants that name the result headings
verify: node --import tsx --test tests/managed-blocks.test.ts tests/init.test.ts tests/opencode-setup.test.ts tests/setup-block-coexistence.test.ts tests/doctor.test.ts tests/golden-events.test.ts tests/line-budget.test.ts tests/function-budget.test.ts
scope:
  - src/core/foundation/init-blocks.ts
  - AGENTS.md
  - .opencode/agent/osq-coder.md
  - tests/managed-blocks.test.ts
  - tests/fixtures/events/*.jsonl
entry:
  - src/core/foundation/init-blocks.ts
  - tests/managed-blocks.test.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] `init-blocks.ts` exports `EXECUTOR_STEPS` (the seven step lines of `## Executing a task`, byte-identical to today's), `RESULT_HEADINGS` (the four headings below with their purposes, in order), `RESULT_TOUCHED_PREFIX` (`Touched:`), and `EXECUTOR_EXIT_LINES` (the `## Exiting` body lines, rendered from `RESULT_HEADINGS` and `RESULT_TOUCHED_PREFIX`).
- [ ] `MANAGED_AGENTS_MD_BODY` interpolates `EXECUTOR_STEPS` and `EXECUTOR_EXIT_LINES`, each joined with `\n`, and equals the AGENTS block below byte for byte.
- [ ] The text between the osq markers in the root `AGENTS.md` and in `.opencode/agent/osq-coder.md` equals `MANAGED_AGENTS_MD_BODY`.
- [ ] `tests/managed-blocks.test.ts` asserts that `MANAGED_AGENTS_MD_BODY` contains every `EXECUTOR_STEPS` line and every non-empty `EXECUTOR_EXIT_LINES` line, that `EXECUTOR_STEPS` has seven lines numbered `1.` to `7.`, and that the exit lines name `## Changed`, `## Deviated`, `## Missing context`, `## Next`, and `Touched:` in that order.
- [ ] `tests/managed-blocks.test.ts` asserts that the managed block in the repository's `.opencode/agent/osq-coder.md` equals `MANAGED_AGENTS_MD_BODY`, and keeps every existing assertion.
- [ ] `tests/fixtures/events/verified.jsonl` and `dead.jsonl` are regenerated with `UPDATE_GOLDEN=1`, and the only values that change are the `repoLines` measures of the seeded project.
- [ ] The task's verify command passes.

## Details

This task's `scope` is authoritative over any injected code-ownership rules.

Only the `## Exiting` section changes. Every other line of the block keeps its
current bytes, including the seven steps. `MANAGED_AGENTS_BLOCK` stays an alias
of `MANAGED_AGENTS_MD_BODY`. `OPENCODE_AGENT_TEMPLATE` in
`src/harness/opencode/opencode.ts` already embeds `MANAGED_AGENTS_BLOCK`, so it
picks up the change without an edit.

Edit the root `AGENTS.md` and `.opencode/agent/osq-coder.md` only between
`<!-- OSQ:START -->` and `<!-- OSQ:END -->`, and keep each file's final-newline
state. Copy the block by hand; do not run `osq init` or `osq setup`, which write
files outside this scope. `CLAUDE.md` is a symlink to `AGENTS.md`; leave it
alone.

In `init-blocks.ts` the constants are template literals or string arrays, so
escape each backtick in a template literal as `` \` ``, and keep
`${OSQ_START_MARKER}` and `${OSQ_END_MARKER}` in place of the marker lines.
Task 2 imports `EXECUTOR_STEPS` and `EXECUTOR_EXIT_LINES` into the executor
prompt, so keep those names and their line-array shape. `EXECUTOR_EXIT_LINES`
includes the empty separator lines exactly as the block shows them.

The golden event test seeds a project with `osq init`, so its `measures` events
count the managed block's lines. Regenerate both fixtures with
`UPDATE_GOLDEN=1 node --import tsx --test tests/golden-events.test.ts`, then
rerun without the variable. If any field other than `repoLines` changes, stop
and write that in `.run/results/1.md`.

### Result headings

| Heading | Purpose |
|---|---|
| `## Changed` | what you changed. |
| `## Deviated` | where you departed from the task, and why. |
| `## Missing context` | what you needed that the task files did not give you. |
| `## Next` | for unfinished work, the acceptance line to pick up next. |

Each renders as the bullet line ``- `<heading>`: <purpose>``.

### AGENTS block

```markdown
<!-- OSQ:START -->
## Executing a task

You were handed one task, `tasks/<n>.md`, from a change under `openspec/changes/`.

1. Read your task file, its parent `proposal.md`, then only the delta specs and capability specs it names. Nothing else.
2. Too big for one pass? Write why in `.run/results/<n>.md`, exit without code.
3. Read a previous result file for this task if present. Run the task's `verify`. Start from what fails.
4. Tests for each acceptance line before implementing.
5. Minimal code to pass. Write only `.run/results/<n>.md` and files inside the task's `scope`; the task's `scope` wins over any other ownership rule you were given.
6. New test files are always allowed. Change a preexisting test only when the task sets `tests.modify: true` and the file is inside `scope`; any other test change kills the task.
7. Run the task's `verify` command before exiting. Then run the proposal's `verify`; the watcher runs both itself and kills the task if either fails.

## Exiting

Write `.run/results/<n>.md` first, with these headings in this order. Leave out any that would be empty.

- `## Changed`: what you changed.
- `## Deviated`: where you departed from the task, and why.
- `## Missing context`: what you needed that the task files did not give you.
- `## Next`: for unfinished work, the acceptance line to pick up next.

End the file with one line, `Touched: <path>, <path>`, listing every file you changed other than the result file, relative to the project root.

Then exit. One attempt. Do not ask questions.

## Where things live

- Living capability specs live under `openspec/specs/` as `<capability>/spec.md`. Never edit them; the watcher applies approved deltas at archive.
- In-flight changes live under `openspec/changes/<id>-<slug>/`: `proposal.md`, delta specs as `specs/<capability>/spec.md`, and one `tasks/<n>.md` per task.
- `tasks.md` and every file under `.run/` except your result file belong to the watcher and the human. Never edit them.
- Approval gate: only `osq approve`, run by a human, writes `.run/approved`. Verification gate: only the watcher's own `verify` run marks a task done.

## Planning a change

Planners follow `PLANNER.md`. When `osq plan` started you, `plan-prompt.md` in the selected change folder is your complete prompt; read it and follow it exactly.

- Write only inside that change folder.
- Run `osq lint <slug>` and fix every finding before you finish.
- Never run `osq approve`; approval belongs to a human.
<!-- OSQ:END -->
```
