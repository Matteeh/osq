# Tasks

## 1. Managed executor protocol

- [x] 1. When osq installs the executor protocol, the managed AGENTS block is assembled from exported step and exit constants that name the result headings

## 2. Shared executor prompt

- [x] 2. When the watcher spawns agy, codex, or opencode, one shared builder writes the prompt and a golden test pins it per harness

## 3. Agent file diagnostics

- [x] 3. When the harness is opencode, doctor checks the executor agent file's managed block and names osq setup as the fix
