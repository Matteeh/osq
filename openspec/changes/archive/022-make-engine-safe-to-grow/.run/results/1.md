# Task 1 — PLANNER.md managed instructions block

## Changed

- `src/core/init.ts`
  - Added exported `MANAGED_PLANNER_BLOCK` wrapped in `${OSQ_START_MARKER}` / `${OSQ_END_MARKER}`.
  - Added exported `updatePlannerMd(targetDir)`; creates `PLANNER.md` when missing, replaces between valid markers, otherwise appends preserving existing content.
  - Added `updatedPlannerMd: boolean` to `InitResult` and invoked `updatePlannerMd` from `scaffoldProject`.
  - Extracted `pathExists` and a shared `updateManagedBlock(filePath, title, block)` helper; `updateAgentsMd` now delegates to it.
- `PLANNER.md` (repo root): planning protocol wrapped in `<!-- OSQ:START -->` / `<!-- OSQ:END -->`, matching `MANAGED_PLANNER_BLOCK`.
- `tests/init-planner.test.ts`: covers fresh creation, marker replacement preserving preamble/appendix, marker-absent append, `scaffoldProject` reporting `updatedPlannerMd`, and the repository root file.

## Deviated

- To respect the `src/core/init.ts` < 250 line budget (final: 249), `updateAgentsMd` was refactored onto a shared `updateManagedBlock` helper instead of duplicating the insert/replace logic.
- The managed block is a condensed version of the previous `PLANNER.md` prose because the full text would not fit the line budget. Behavior and guidance are preserved, wording is tightened.

## Drift against features/

- None. `openspec/changes/022-make-engine-safe-to-grow/specs/cli-foundation/spec.md` "Planner instruction scaffolding" (PLANNER.md created/updated with the managed block) is satisfied by `src/core/init.ts` and `tests/init-planner.test.ts`.

## Verification

- `node --import tsx --test tests/init-planner.test.ts` — 5 pass, 0 fail.
- `node --import tsx --test tests/init.test.ts` — 10 pass, 0 fail (AGENTS.md behavior unchanged).
- `pnpm biome check src/core/init.ts tests/init-planner.test.ts` — clean.
- `pnpm tsc --noEmit` — clean.

## Next steps

- None. All acceptance lines for task 1 are met.
