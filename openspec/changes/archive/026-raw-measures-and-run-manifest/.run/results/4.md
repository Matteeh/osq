# Task 4 — Golden measures events match the checked-in fixture

## Changed

- `tests/golden-events.test.ts`:
  - `maskEvent` now calls a new `maskScopeHashes` helper for `type: "measures"`
    events. Each `scopeHashes[key].before` / `.after` non-null hash string is
    replaced with `[HASH]`; `null` values are preserved.
  - Added an `it('masks measures scope hashes to stable tokens')` unit test
    covering a changed file (`sha256 → sha256`) and an added file (`null →
    sha256`), asserting both are normalized before fixture comparison.
- `tests/fixtures/events/verified.jsonl`: regenerated with `UPDATE_GOLDEN=1`.
  Now 8 lines: start `measures` first, then the previous 5 events, end `measures`
  immediately before `done`.
- `tests/fixtures/events/dead.jsonl`: regenerated. 8 lines: start `measures`
  first, end `measures` immediately before `dead`.

Fixture measures payload (both files): `phase` start/end, `scopeFiles:0`,
`scopeLines:0`, `repoFiles:16`, `repoLines:443`, `importFanIn:0`,
`proposalWords:243`, `taskWords:31`, `deltaRequirements:0`, `deltaScenarios:0`;
end adds `changedFiles:0`, `changedLines:0`, `scopeHashes:{}`.

## Acceptance

- [x] `verified.jsonl` includes measures start (line 1) and measures end (line 7)
      in the correct position
- [x] `dead.jsonl` includes measures start (line 1) and measures end (line 7)
      in the correct position
- [x] `maskEvent` masks non-deterministic measures `scopeHashes` values to
      `[HASH]` and keeps `null`
- [x] golden verified task test passes with the updated fixture
- [x] golden dead task test passes with the updated fixture

## Deviated

- Counts (`repoFiles`, `repoLines`, word counts, delta counts) are left raw in
  the fixtures rather than masked, matching Details point 1's conclusion: the
  scaffolded temp project is fully controlled, so they are deterministic. Only
  content hashes are masked, via a real code path that is a harmless no-op for
  the empty-scope golden tasks but correct for future scoped tasks.

## Drift against features/

- None remaining. The emitted order baked into the fixtures is
  `measures(start) → tokens → result_written → started → exited → verify_ran →
  measures(end) → done|dead`. This reflects the task-2 deviation (start measures
  emitted before the `started` event, which is written asynchronously inside
  `spawnTaskAgent`). The `watcher-and-harness` delta prose says start measures
  fire "after the `started` event"; the fixtures encode the real order. Flagged
  for the approver; no code change to the event order was in this task's scope.

## Verify

- `node --import tsx --test tests/golden-events.test.ts` — pass (4/4), stable
  across repeated runs
- `pnpm tsc --noEmit` — pass
- `pnpm lint` — pass
- `pnpm test` — 582 pass, 0 fail, 1 skipped
