# Task 1 — Run manifest at approval

## Changed

- `src/core/manifest.ts` (new, 138 lines): `ManifestData` interface, `buildManifest`, `writeManifest`, plus helpers `resolveConfigPath`, `hashFileContent`, `resolveOsqVersion`, `resolveModel`, `resolveCreatedAt`.
- `src/core/approve.ts`: import and call `buildManifest` / `writeManifest` after the `approved` marker is written.
- `tests/manifest.test.ts` (new): covers hashes, metadata, and the missing-file `null` hash case.

## Acceptance

- [x] `ManifestData` interface with hashes, osqVersion, harness, model, effort, createdAt, approvedAt
- [x] `buildManifest` hashes AGENTS.md, PLANNER.md, first config file, and each `features.reads`/`features.writes` spec (deduplicated)
- [x] Each hash is `sha256:<hex>` from UTF-8 content, `null` on missing file
- [x] `writeManifest` writes `.run/manifest.json` as indented JSON
- [x] `approveSpec` calls both after writing the approved marker
- [x] `tests/manifest.test.ts` asserts hashes and metadata
- [x] `src/core/manifest.ts` is under 250 lines

## Deviated

- Task Details said to import `resolveBuildInfo` from `../watcher/build.js`. That import violates the living `watcher-and-harness` requirement *Architectural import graph boundaries* ("no module in `src/core` imports from any directory outside `src/core`"), enforced by `tests/import-graph.test.ts`. To stay inside the task `scope` (which excludes `src/watcher/build.ts`) I implemented a local `resolveOsqVersion` in `manifest.ts` that reads the osq package `package.json` version, falling back to `unknown`. All acceptance lines and the capability rule ("Run manifest at approval") are satisfied; the full suite stays green.
- `createdAt` is derived via `resolveChangeDoc` (proposal.md, else fallback spec.md) rather than hard-coding proposal.md; same effective behavior.

## Drift against features/

- None in behavior. The delta spec source comment names only `src/core/approve.ts, src/core/manifest.ts`, consistent with keeping the version read inside core rather than depending on `src/watcher`.

## Missing context

- The change's capability rule for metrics mentions a typed `MeasuresEventData` interface; that belongs to tasks 2–3, not this task.

## Next

- Task 1 complete. Next acceptance line for the change is task 2: "When a task starts, a measures event is emitted with scope plus repo baselines" (single `emitMeasures` path in `src/watcher/measures.ts`).

## Verify

- `node --import tsx --test tests/manifest.test.ts` — pass (2/2)
- `pnpm verify` (tsc + 559 tests + lint) — pass
