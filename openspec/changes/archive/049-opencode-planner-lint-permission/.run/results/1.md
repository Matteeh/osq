# Task 1 result

## Changed

- `src/harness/opencode/opencode.ts`: rewrote `OPENCODE_PLANNER_AGENT_TEMPLATE` to
  allow `read`, `edit`, `glob`, `grep`; deny `webfetch` and `websearch`; drop the
  non-OpenCode `write` and `git` keys; and give `bash` the ordered pattern map
  (`*` deny, the three lint allows, then shell-operator denies including the
  `"*\n*"` YAML escape). Setup logic unchanged.
- `.opencode/agent/osq-planner.md`: hand-written to hold the same bytes as the
  template.
- `tests/opencode-planner-setup.test.ts`: asserts template/repo byte identity,
  the permission map and key order, the last-match-wins allow/deny command set,
  existing body and repeated-setup byte identity, and that a pre-existing planner
  agent file is left untouched.

## Verification

- Task verify: pass (28 tests).
- Proposal verify `pnpm verify`: pass.
