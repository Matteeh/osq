---
title: When the harness is pi, doctor reports Pi's version and credentials, and the README explains Pi
verify: node --import tsx --test tests/pi/*.test.ts tests/doctor.test.ts tests/doctor-agent-file.test.ts tests/harness-generic-workflows.test.ts tests/harness-architecture.test.ts tests/line-budget.test.ts
scope:
  - src/core/foundation/doctor.ts
  - README.md
entry:
  - src/core/foundation/doctor.ts
  - src/core/foundation/harness-catalog.ts
  - src/core/foundation/config-pi.ts
  - README.md
tests:
  modify: false
skills: []
---
## Acceptance

- [ ] `runDoctorChecks` runs the selected catalog entry's optional `diagnose` hook after a passing `harness` check and appends each returned diagnosis as a check with the same `name`, `ok`, `warning`, and `message`. `doctor.ts` names no harness, and an entry without the hook adds no checks.
- [ ] With harness `pi` and a fake `pi` whose `--version` prints `0.87.1`, `harness-version` passes without a warning. With `0.88.0` it passes with a warning naming `0.88.0` and `>=0.87.0 <0.88.0`, and the report stays `ok`.
- [ ] With `pi.provider: 'deepseek'` and a fake auth check that prints `{"status":"not_ready","provider":"deepseek","reason":"credentials_not_configured"}` and exits 1, `harness-auth` fails naming `deepseek` and `credentials_not_configured`, and the report is not `ok`. With `ready` it passes. Without `pi.provider`, the fake records no `auth` invocation.
- [ ] The README lists `pi` under available adapters and adds a Pi section covering: a config example with placeholder provider and model; the precedence of `pi.bin`, `OSQ_PI_PATH`, and `pi`; `pi.model` and `OSQ_MODEL`; `pi.thinking` as effort; the install command `npm install -g @earendil-works/pi-coding-agent`; the tested range; `pi auth check`; that `osq setup` writes no Pi files because Pi reads `AGENTS.md` (and that an `AGENTS.override.md` shadows it); the flags osq passes, including extensions, skills, and prompt templates off; that Pi has no sandbox, no permission prompts, and open network; and that Pi cannot be the planner.

## Details

This task's `scope` wins over any injected code-ownership rule. Task 1 already
added the catalog's `diagnose` hook, Pi's implementation of it in
`config-pi.ts`, and the fake `pi` under `tests/pi/`. Reuse them, and change no
file task 1 owns. Doctor passes `config`, `projectRoot`, and the probed version
to the hook through `findHarness`. Put new tests in a new file under
`tests/pi/`, and drive doctor through `runDoctorChecks` with its injectable
`loadConfig` seam, with `pi.bin` pointing at the fake. Every existing test is
frozen, and doctor output for other harnesses must not change. Model README
examples on the Codex section, with placeholder models rather than
recommendations.
