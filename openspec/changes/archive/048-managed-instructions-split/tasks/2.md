---
title: When osq new seeds a proposal, the template follows the planner's parent-spec rules
verify: node --import tsx --test tests/new.test.ts tests/parser.test.ts tests/queue-plan.test.ts tests/linter.test.ts tests/approve.test.ts tests/golden-events.test.ts tests/function-budget.test.ts tests/line-budget.test.ts
scope:
  - templates/proposal.md
  - src/core/foundation/new.ts
  - tests/new.test.ts
  - tests/fixtures/events/*.jsonl
entry:
  - src/core/foundation/new.ts
  - templates/proposal.md
  - tests/new.test.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] `templates/proposal.md` equals the proposal seed below byte for byte, ending with one trailing newline.
- [ ] `FALLBACK_PROPOSAL_MD` in `new.ts` holds the same text, so an unreadable template seeds the same sections.
- [ ] `tests/new.test.ts` asserts that a change created by `createNewSpec` has `## Goal`, `## Verify`, `## Non-goals`, `## Contract`, `## Human steps`, and `## Delta` in that order, a `### Requirement:` placeholder under `## Contract`, no `|` table row, and no `features.writes`.
- [ ] Existing `new.test.ts` assertions still pass unchanged, and the frozen `tests/parser.test.ts` template test still finds the `Code ownership` example with exactly `src/core/example.ts` and `src/cli/example.ts`.
- [ ] `tests/fixtures/events/verified.jsonl` and `dead.jsonl` are regenerated with `UPDATE_GOLDEN=1`; only the `repoLines` and `proposalWords` measures change.
- [ ] The task's verify command passes.

## Details

This task's `scope` is authoritative over any injected code-ownership rules.
`seedProposal` rewrites only `title:` and `depends_on:` in the frontmatter;
keep those two lines exactly as the seed shows them. `FALLBACK_PROPOSAL_MD` is a
template literal, so escape each backtick as `` \` ``.

Do not change `seedProposal`, `createNewSpec`, `FALLBACK_TASKS_MD`, or
`FALLBACK_TASK_1_MD`. `createNewSpec` is on the function-budget grandfather
list, and the budget test fails if it drops to 80 lines or fewer.

The golden event test seeds a project with `osq new`, so its `measures` events
count the seeded proposal. Task 1 already regenerated these fixtures for the
new managed blocks; regenerate them again with
`UPDATE_GOLDEN=1 node --import tsx --test tests/golden-events.test.ts`, then
rerun without the variable. If any field other than `repoLines` or
`proposalWords` changes, stop and write that in `.run/results/2.md`.

### Proposal seed

````markdown
---
title: Change title
depends_on: []
verify: node -e "process.exit(0)"
features:
  reads: []
---
## Goal

What problem this change solves and why.

## Verify

`node -e "process.exit(0)"`

Replace this planning sentinel, here and in the frontmatter, with the command
that verifies the completed change's final tree, and say what it proves.

## Non-goals

- What this change deliberately does not do.

## Contract

### Requirement: <requirement name>

The system SHALL <observable behavior>.

#### Scenario: <scenario name>
- **WHEN** <condition>
- **THEN** <outcome>

## Human steps

- Review the proposal, delta specs, and task bodies, then run `osq approve <id>`
  yourself.

## Delta

Delta specs live beside the proposal as `specs/<capability>/spec.md`. Each holds
the exact text the capability spec will contain after the change, under
`## ADDED Requirements`, `## MODIFIED Requirements`, `## REMOVED Requirements`,
or `## RENAMED Requirements`; a modified requirement repeats its full text. List
each delta here in one line and name any file two tasks share.

A delta that introduces a new capability also declares the files it owns in a
`### Requirement: Code ownership` block. Its `<!-- source: ... -->` comment lists
the owned path globs, comma-separated, and its scenario restates them:

```markdown
### Requirement: Code ownership
<!-- source: src/core/example.ts, src/cli/example.ts -->
The <capability> capability SHALL own <subsystems>.

#### Scenario: Codebase ownership boundaries
- **WHEN** file ownership is resolved for <capability>
- **THEN** system maps `src/core/example.ts` and `src/cli/example.ts` to <capability>
```
````
