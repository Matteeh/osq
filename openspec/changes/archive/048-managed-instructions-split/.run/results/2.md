# Task 2 result

## Changed

- `templates/proposal.md`: replaced with the task's proposal seed. Byte-for-byte
  equal to lines 47-104 of `tasks/2.md` plus one trailing newline (verified with
  a direct slice/compare of the task file).
- `src/core/foundation/new.ts`: `FALLBACK_PROPOSAL_MD` now holds the same text
  with every backtick escaped as `` \` ``. A direct evaluation of the template
  literal confirms it equals `templates/proposal.md` byte for byte, trailing
  newline included. `seedProposal`, `createNewSpec`, `FALLBACK_TASKS_MD`, and
  `FALLBACK_TASK_1_MD` are untouched.
- `tests/new.test.ts`: added one test asserting the seeded proposal has
  `## Goal`, `## Verify`, `## Non-goals`, `## Contract`, `## Human steps`, and
  `## Delta` in that order, a `### Requirement:` / `#### Scenario:`
  placeholder under `## Contract`, no table row, and no `features.writes`.
  All preexisting assertions kept unchanged.
- `tests/fixtures/events/verified.jsonl`, `dead.jsonl`: regenerated with
  `UPDATE_GOLDEN=1` and re-run without it.

## Deviated

None. The task's `scope` was authoritative; no file outside it was modified.

## Drift against features/

None. `templates/proposal.md` and the fallback match the `cli-foundation`
`Proposal seed template` delta. The frozen `tests/parser.test.ts` template test
still finds the `Code ownership` example with exactly `src/core/example.ts` and
`src/cli/example.ts`.

## Golden fixture diff

Both fixtures changed only in the two permitted fields, on the start and end
`measures` events:

- `repoLines`: 474 -> 455
- `proposalWords`: 247 -> 235

Every other field is byte-identical.

## Verification

- Task verify: 107 tests, 0 failures.
- Proposal verify `pnpm verify`: exit code 0 (CLI/UI typechecks, build, full
  suite, lint).

## Missing context

None.
