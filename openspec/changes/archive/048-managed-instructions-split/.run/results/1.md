# Task 1 result

## Changed

- `src/core/foundation/init-blocks.ts`: replaced `MANAGED_AGENTS_MD_BODY` with the
  executor-only AGENTS block (`## Executing a task`, `tests.modify: true`, the
  `scope`-wins sentence, both gates, `## Where things live`, and the
  `## Planning a change` pointer to `PLANNER.md`) and `MANAGED_PLANNER_BLOCK`
  with the planner block (`### Interactive planning`, `### Working from the
  handoff`, `### Either way`, `### Tasks` with the between-task watcher rules,
  `### Parent spec`). `MANAGED_AGENTS_BLOCK` stays an alias and
  `MANAGED_CLAUDE_PLAN_COMMAND` is untouched.
- `AGENTS.md`, `PLANNER.md`: spliced the new constants between the existing osq
  markers, preserving all text outside the markers and each file's final
  newline.
- `templates/PLANNER.md`: now exactly `MANAGED_PLANNER_BLOCK` (no trailing
  newline, unchanged from before).
- `tests/managed-blocks.test.ts`: executor-protocol assertions now cover
  `## Executing a task`, `tests.modify: true`, the proposal's `verify`, the
  `scope`-wins sentence, `Approval gate:`/`Verification gate:`, and
  `openspec/specs/` + `openspec/changes/`; dropped the `.run/` marker list,
  `derived purely from the marker files on disk`, and `Executing a spec`.
- `tests/init.test.ts`: `## Executing a task` expectations; added a `PLANNER.md`
  assertion on the AGENTS block.
- `tests/opencode-setup.test.ts`: `## Executing a task` expectations.
- `tests/init-planner.test.ts`: added the between-task watcher-rule assertions
  inside the `### Tasks` slice (`change-level \`verify\``, `osq retry`,
  `Globs resolve again`, `tests.modify: true`) and the two planning-mode
  headings. Every preexisting assertion kept.
- `tests/fixtures/events/verified.jsonl`, `dead.jsonl`: regenerated with
  `UPDATE_GOLDEN=1`. The only measure that changed is the seeded project's
  `repoLines` (468 -> 474); `proposalWords` stayed 247 and every other field is
  byte-identical.

## Verification

- Byte equality confirmed against the task file's two fenced blocks directly
  (not just via the suite).
- Task verify passed: 102 tests, 0 failures.
- Proposal verify `pnpm verify` (typechecks, build, full suite, lint) passed.

## Drift against features/

None. The blocks match the `cli-foundation` delta exactly; no other capability
spec's behavior changed.

## Missing context

None. The task's `scope` was authoritative throughout.

## Next steps

- Task 2 owns the `osq new` proposal seed and regenerates the same two golden
  fixtures after its seed change. The watcher is expected to halt once for scope
  recertification of this task's fixtures; the human then runs
  `pnpm osq retry 048 1`.
