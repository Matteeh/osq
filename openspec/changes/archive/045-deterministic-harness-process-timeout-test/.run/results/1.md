# Task 1 result

## Changed

`tests/harness-process.test.ts` only:

- Removed `mock` from the `node:test` import (no remaining use).
- Split the `DEFAULT_KILL_GRACE_PERIOD_MS` assertion into its own compact synchronous test.
- Rewrote the kill-grace case to use real timers: the child registers a no-op
  `SIGTERM` handler before writing `READY`, then keeps an interval alive. The test
  calls `spawnWithTimeout` with `timeoutSeconds: 1` and `killGracePeriodMs: 100`,
  awaits the result, and asserts only `timedOut === true` and `signal === 'SIGKILL'`.
- Removed the readiness promise, fake-clock ticks, `try`/`finally` reset, and
  elapsed-time reasoning.

All acceptance lines satisfied:

- `DEFAULT_KILL_GRACE_PERIOD_MS` remains `5000` with its own test.
- Handler is registered before `READY`.
- Real timers; focused timeout/grace values; awaited result; no `mock.timers`.
- Asserts `timedOut` and `SIGKILL` only.
- Remaining cases and `src/harness/process.ts` untouched.

## Deviated

None.

## Drift against features/

The change delta and its `source` comments reference `features/watcher-and-harness.md`,
but no `features/` directory exists in this checkout. The living capability spec is
`openspec/specs/watcher-and-harness/spec.md`; the modified requirement there already
matches the authored delta. No authored artifact was edited.

## Verification

Ran the task verify command. `tests/harness-process.test.ts` passed three consecutive
runs, then `tests/import-graph.test.ts` and `tests/line-budget.test.ts` passed.
`git diff --stat` shows only `tests/harness-process.test.ts` changed.
