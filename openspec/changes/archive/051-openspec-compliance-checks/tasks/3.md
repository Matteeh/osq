---
title: When an OpenSpec-aware agent or osq new starts a proposal, both use osq's sections, and this repository validates under that schema
verify: node --import tsx --test tests/proposal-format.test.ts tests/schema-instructions.test.ts tests/init.test.ts tests/new.test.ts tests/golden-events.test.ts tests/openspec-differential.test.ts tests/linter.test.ts
scope:
  - templates/openspec/schemas/osq/templates/proposal.md
  - templates/openspec/schemas/osq/schema.yaml
  - templates/openspec/config.yaml
  - openspec/config.yaml
  - openspec/schemas/osq/**
  - tests/proposal-format.test.ts
  - tests/fixtures/events/*.jsonl
entry:
  - templates/proposal.md
  - templates/openspec/schemas/osq/schema.yaml
  - templates/openspec/config.yaml
  - tests/proposal-format.test.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] `templates/openspec/schemas/osq/templates/proposal.md` is byte-identical to `templates/proposal.md`.
- [ ] The `proposal` artifact's `instruction` in `schema.yaml` equals the instruction below. Every other artifact, `apply`, and the rest of the file keep their bytes.
- [ ] `rules.proposal` in `templates/openspec/config.yaml` equals the two rules below. `context` and every other rule keep their bytes.
- [ ] The repository has `openspec/config.yaml`, plus `openspec/schemas/osq/` holding `schema.yaml`, `README.md`, and `templates/{proposal,spec,tasks}.md`. Each is byte-identical to its counterpart under `templates/openspec/`.
- [ ] New `tests/proposal-format.test.ts` asserts the two byte equalities above and that both file sets match. It asserts that the proposal instruction and rules name Goal, Verify, Non-goals, Contract, Human steps, and Delta in order, with no `What Changes` or `Capabilities`. It also asserts that `MANAGED_PLANNER_BLOCK` names `## Goal`, `## Non-goals`, and `## Human steps`.
- [ ] `tests/fixtures/events/verified.jsonl` and `dead.jsonl` are regenerated with `UPDATE_GOLDEN=1`, and only the `repoLines` measures change.
- [ ] The task's verify command passes. That includes the differential test, whose fixtures now validate under the rewritten schema, and `osq lint` on this repository's in-flight changes.

## Details

This task's `scope` is authoritative over any injected code-ownership rules.
Do not change `templates/proposal.md`, `src/core/foundation/new.ts`, or
`PLANNER.md`. osq's section set wins, and those files already define it. Copy
the edited template files into `openspec/` by hand; `osq init` would not
overwrite them.

`tests/schema-instructions.test.ts` pins the tasks and apply instructions, the
config `context`, and the tasks rules. It must pass unchanged.

The golden event test seeds a project with `scaffoldProject`, which copies these
templates, so its `measures` events count their lines. Regenerate with
`UPDATE_GOLDEN=1 node --import tsx --test tests/golden-events.test.ts`, then
rerun without the variable. If any field other than `repoLines` changes, stop
and write that in `.run/results/3.md`.

### Proposal instruction

```yaml
    instruction: |
      Create the proposal a human reads before approving the change. Follow the
      template's frontmatter and sections exactly.

      Frontmatter:
      - `verify`: the change-level command that verifies the completed change's
        final tree. `osq lint` rejects the seeded planning sentinel.
      - `features.reads`: the living capabilities the change reads.

      Sections, in this order:
      - **Goal**: the problem this change solves and why now.
      - **Verify**: the change-level `verify` command again, and what it proves.
      - **Non-goals**: what the change deliberately does not do.
      - **Contract**: requirements, each with `#### Scenario:` blocks using
        WHEN/THEN bullets.
      - **Human steps**: everything a task must not do itself, including running
        `osq approve <id>`.
      - **Delta**: one line per delta spec, and any file two tasks share.

      Each capability the change adds or modifies needs a delta spec file at
      `specs/<capability-path>/spec.md`. Run `openspec list --specs` to review the
      project's capability inventory before introducing a near-duplicate. Never
      run `osq approve` yourself; a human owns that gate.
```

### Proposal rules

```yaml
  proposal:
    - Follow the proposal template's sections in order: Goal, Verify, Non-goals, Contract, Human steps, Delta.
    - Declare the change-level verify command in frontmatter; the human gate reads the proposal before approving.
```
