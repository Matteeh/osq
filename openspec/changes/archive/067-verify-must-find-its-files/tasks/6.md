---
title: When osq show prints a pre-spawn result that recorded missing paths, the line names them
verify: node --import tsx --test tests/show-pre-spawn-missing.test.ts
scope:
  - src/core/status/show.ts
  - tests/show-pre-spawn-missing.test.ts
entry:
  - src/core/status/show.ts
  - tests/show-pre-spawn.test.ts
tests:
  modify: false
skills: []
---
## Acceptance

- [ ] `formatPreSpawnVerify` appends `, missing <path>, <path>` in recorded order when the latest pre-spawn event's `missingPaths` is a non-empty array of strings.
- [ ] An empty, absent, or malformed `missingPaths` leaves the line exactly as today.
- [ ] New `tests/show-pre-spawn-missing.test.ts` builds a change with pre-spawn events the way `tests/show-pre-spawn.test.ts` does. It asserts the line `      Pre-spawn verify: exit 0, expected red, matched, missing tests/a.test.ts, tests/b.test.ts` for a run with two missing paths, and the unchanged line for a run with none.

## Details

`tests/show-pre-spawn.test.ts` and `tests/show-retries.test.ts` stay frozen and
green.
