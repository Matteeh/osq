---
title: When a planner reads PLANNER.md or README, they learn the verify_starts rules, the verify_path_missing reason, and the verify_starts_conflict flag
verify: node --import tsx --test tests/verify-starts-docs.test.ts
scope:
  - PLANNER.md
  - templates/PLANNER.md
  - src/core/foundation/init-blocks.ts
  - README.md
  - tests/verify-starts-docs.test.ts
entry:
  - PLANNER.md
  - src/core/foundation/init-blocks.ts
  - README.md
  - tests/init-planner.test.ts
tests:
  modify: false
skills: []
---
## Acceptance

- [ ] The managed planner block, identical in `PLANNER.md`, `templates/PLANNER.md`, and `MANAGED_PLANNER_BLOCK`, replaces "use `any` when either start is fine" with guidance saying three things: a task whose verify names a test it creates declares `verify_starts: red`; a new test file may share a verify with existing tests; and `any` is only for a task that can honestly start either way.
- [ ] The README's automatic retry bullet and its dead reasons list name `verify_path_missing`, and the dead reasons text says what it means: a path the task verify names did not exist after the agent exited.
- [ ] The README's approval digest bullet names `verify_starts_conflict` in plain words with the other flags and no longer says "five".
- [ ] The README's pre-spawn verify bullet says missing named paths are recorded and do not count as a mismatch for a `red` task.
- [ ] New `tests/verify-starts-docs.test.ts` asserts each of the above, reading the files the way `tests/auto-retry-docs.test.ts` does.

## Details

`tests/init-planner.test.ts` asserts byte equality of the three planner block
copies, and `tests/pre-spawn-docs.test.ts` expects `verify_starts: green` to stay
in the block. Both stay frozen and green, as do `tests/auto-retry-docs.test.ts`
and `tests/approval-digest-docs.test.ts`.
