---
title: When archive verification finds a path its command names missing, the change regresses without running that command
verify: node --import tsx --test tests/archive-verify-path-missing.test.ts
scope:
  - src/watcher/archiver.ts
  - src/watcher/archive-verify.ts
  - tests/archive-verify-path-missing.test.ts
entry:
  - src/watcher/archiver.ts
  - src/core/spec/verify-paths.ts
  - tests/archive-verification.test.ts
tests:
  modify: false
skills: []
---
## Acceptance

- [ ] `verifyArchiveStep` moves from `archiver.ts` into new `src/watcher/archive-verify.ts`, and `checkAndArchiveSpec` calls it from there. `archiver.ts` ends shorter than its current 249 lines.
- [ ] Before running a task or change-level command, the archive step checks the paths it names through `src/core/spec/verify-paths.ts`. When any is missing, it writes the regressed marker with `reason: verify_path_missing`, the command, and each missing path on its own line, records the regressed event with that reason, does not run the command, and leaves the change unarchived.
- [ ] Commands naming no missing path follow the existing archive verification path unchanged.
- [ ] New `tests/archive-verify-path-missing.test.ts` builds a done change whose task verify names a file that has been deleted. It asserts the regressed marker and event carry `verify_path_missing` and the path, that no `verify_ran` event is written for that command, and that the change stays active. A second case with every named path present archives as before.

## Details

Build the done change the way `tests/archive-verification.test.ts` does. That
file stays frozen and green. Keep the existing `verify_red` regressed marker text
for a command that runs and fails.
