---
title: When osq report runs, it counts pre-spawn runs with missing paths and by declared start, and reports verify_starts_conflict outcomes
verify: node --import tsx --test tests/report-pre-spawn-starts.test.ts tests/report-json.test.ts tests/report-approval-flags.test.ts
scope:
  - src/core/report/report-pre-spawn.ts
  - src/core/report/report.ts
  - src/cli/report.ts
  - src/core/report/approval-flags.ts
  - fixture/report/**
  - tests/report-json.test.ts
  - tests/report-approval-flags.test.ts
  - tests/report-pre-spawn-starts.test.ts
entry:
  - src/core/report/report.ts
  - src/core/report/report-events.ts
  - src/cli/report.ts
  - tests/report-pre-spawn.test.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] New `src/core/report/report-pre-spawn.ts` reads one task's events and returns the pre-spawn runs with a non-empty `missingPaths` and, for each of `red`, `green`, and `any` by the event's `expected`, the runs and how many passed (exit code 0). `report.ts` sums them over every task stream. `report-events.ts` does not change.
- [ ] `history.preSpawnVerify` gains `missingPathRuns` and `byStart: { red, green, any }`, each `{ runs, passed }`, and `osq report --json` carries both through `toStableMetrics`.
- [ ] The text report keeps `Pre-spawn verify mismatches: <m> of <r> runs` and adds `Pre-spawn verify with missing paths: <n> of <r> runs` and `Pre-spawn verify by declared start: red <p> of <r> passed, green <p> of <r> passed, any <p> of <r> passed`.
- [ ] `FLAG_IDS` in `approval-flags.ts` gains `verify_starts_conflict` after `unknown_capability`, so the approval flag section and JSON report it like the other flags.
- [ ] New `tests/report-pre-spawn-starts.test.ts` writes pre-spawn events with each declared start, passing and failing exit codes, and some with `missingPaths`. It asserts the JSON counts and both text lines. A change whose manifest records `verify_starts_conflict` appears under that flag in the approval flag section.
- [ ] `fixture/report/expected.json`, `tests/report-json.test.ts`, and `tests/report-approval-flags.test.ts` are updated only for the new keys and the new flag id.

## Details

Measured in a scratch worktree: these fields and the new flag id break only
`tests/report-json.test.ts` (the byte-for-byte fixture and the shape test),
`tests/report-approval-flags.test.ts`, and `fixture/report/expected.json`.
Regenerate the fixture from the real report command the way
`tests/report-json.test.ts` compares it, and review the diff to confirm that only
the new keys changed. `tests/report-pre-spawn.test.ts` stays frozen and green.
