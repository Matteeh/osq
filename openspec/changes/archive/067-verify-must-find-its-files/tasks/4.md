---
title: When a task declares green or any while its verify names a test it creates, the approval digest raises verify_starts_conflict
verify: node --import tsx --test tests/approval-digest-verify-starts.test.ts
scope:
  - src/core/spec/digest.ts
  - src/core/spec/digest-flags.ts
  - src/core/spec/digest-verify-starts.ts
  - tests/approval-digest-verify-starts.test.ts
entry:
  - src/core/spec/digest.ts
  - src/core/spec/digest-flags.ts
  - src/core/spec/verify-starts.ts
  - tests/approval-digest.test.ts
tests:
  modify: false
skills: []
---
## Acceptance

- [ ] `ApprovalFlagId` gains `verify_starts_conflict`, emitted last in the fixed flag order after `unknown_capability`.
- [ ] `ApprovalFlagTask` carries the task's declared `verifyStarts` and its declared scope entries, and `buildApprovalDigest` fills them from the parsed task.
- [ ] New `src/core/spec/digest-verify-starts.ts` turns each contradiction from the shared rule in `src/core/spec/verify-starts.ts` into one flag, labelled `verify starts conflict in task <n>`, with an excerpt naming the path and the declared start. `buildApprovalFlags` appends them, and `digest-flags.ts` stays under 250 lines.
- [ ] New `tests/approval-digest-verify-starts.test.ts` builds changes in a temp project the way `tests/approval-digest.test.ts` does. It asserts that a task declaring `any` or `green` whose verify names a missing file inside its own scope raises one `verify_starts_conflict` flag. The same task declaring `red` raises none, and a task whose verify names a file an earlier task's scope covers raises none, whatever it declares.
- [ ] Approval records the new id in the manifest's `approvalFlags` like the existing flags, asserted through `approveSpec` in the same test file.

## Details

Do not compute the contradiction here; call the shared rule so lint and the
digest agree. `src/core/report/approval-flags.ts` does not change in this task;
task 5 adds the id to the report. `tests/approval-digest.test.ts`,
`tests/approval-digest-docs.test.ts`, `tests/show-digest.test.ts`, and
`tests/approve-confirm.test.ts` stay frozen and green.
