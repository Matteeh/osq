## Changed

- `src/core/foundation/init-blocks.ts`: replaced `MANAGED_PLANNER_BLOCK`'s
  "use `any` when either start is fine" sentence with guidance that a task whose
  verify names a test the task creates declares `verify_starts: red`, that a new
  test file may share the verify with existing tests, and that `any` is only for
  a task that can honestly start either way. `verify_starts: green` stays.
- `PLANNER.md` and `templates/PLANNER.md`: same block text, keeping the three
  copies byte-for-byte equal.
- `README.md`: added `verify_path_missing` to the automatic retry bullet and to
  the dead reasons list with its meaning (a path the verify names did not exist
  after the agent exited); added `verify_starts_conflict` to the approval digest
  bullet and changed "five things" to "six things"; told the pre-spawn bullet
  that a named path that does not exist yet is recorded as `missingPaths` and
  does not count as a mismatch for a `red` task.
- `tests/verify-starts-docs.test.ts`: new test asserting each acceptance line,
  reading README the way `tests/auto-retry-docs.test.ts` does (whitespace
  collapsed, section slices).

## Deviated

None.

## Missing context

None; the task named the exact files and reference test.

## Next

All acceptance lines are covered. Nothing outstanding.

Touched: PLANNER.md, templates/PLANNER.md, src/core/foundation/init-blocks.ts, README.md, tests/verify-starts-docs.test.ts
