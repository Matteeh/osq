# Tasks

## 1. Focused test config

- [x] 1. When osq.config.ts sets traceability.focusedTests, osq validates the command

## 2. Collect and run

- [x] 2. When a task's scoped tests name opted-in scenarios, osq collects and runs their test files and classifies the result

## 3. Watcher gate

- [x] 3. When a focused run fails a collected scenario, the watcher ends the attempt without running the full verify

## 4. Show and docs

- [x] 4. When a task had focused runs, osq show lists each with its outcome and duration
