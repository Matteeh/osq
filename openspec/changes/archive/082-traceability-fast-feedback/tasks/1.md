---
title: When osq.config.ts sets traceability.focusedTests, osq validates the command
verify: node --import tsx --test tests/focused-config.test.ts
verify_starts: red
scope:
  - src/core/foundation/config-traceability.ts
  - tests/focused-config.test.ts
entry:
  - src/core/foundation/config-traceability.ts
  - tests/traceability-config.test.ts
skills: []
---
## Acceptance

- [ ] `TraceabilityConfig` gains optional `focusedTests: string`, and `validateTraceabilityConfig` reads it as the cli-foundation delta's "Focused test command" says, keeping it out of the result when unset.
- [ ] New `tests/focused-config.test.ts` covers both scenarios, a valid command kept verbatim, a non-string value, and loading it from an `osq.config.ts` in a temporary project through `loadConfig`.
- [ ] `tests/traceability-config.test.ts` passes unchanged.

## Details

Leaving the key out when unset keeps `defineConfig({})` and every existing
deep-equality check of `traceability` unchanged. New functions stay within 80
lines.
