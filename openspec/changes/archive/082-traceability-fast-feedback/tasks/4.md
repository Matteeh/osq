---
title: When a task had focused runs, osq show lists each with its outcome and duration
verify: node --import tsx --test tests/focused-show.test.ts
verify_starts: red
scope:
  - src/core/status/show.ts
  - README.md
  - tests/focused-show.test.ts
entry:
  - src/core/status/show.ts
  - tests/dependencies-report.test.ts
skills: []
---
## Acceptance

- [ ] `formatSpecDetails` prints the status-inspection delta's "Focused runs in show" line after the `Scenarios:` line, from a small formatter beside `formatTaskScenarios`.
- [ ] `README.md` documents `traceability.focusedTests` with the reference command `node --test --test-reporter=tap {files}`, which files it runs, the three outcomes, that only a failing scenario test ends an attempt, the `focused_ran` event, and the show line, in the traceability documentation change 081 added.
- [ ] New `tests/focused-show.test.ts` covers the scenario with hand-written `focused_ran` events through `getSpecDetailsFromFolder` and `formatSpecDetails`, plus a `problem` entry and a task without focused events printing no line.

## Details

`show.ts` is on the line-budget allow list and `formatSpecDetails` is
grandfathered, so add only the call and its line. Format the duration with two
decimals. New functions stay within 80 lines.
