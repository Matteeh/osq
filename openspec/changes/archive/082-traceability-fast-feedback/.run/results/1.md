## Changed

- `src/core/foundation/config-traceability.ts`: `TraceabilityConfig` gains the
  optional `focusedTests?: string`. `validateTraceabilityConfig` now rejects a
  present value that is not a string containing `{files}` with
  `traceability.focusedTests must be a command containing {files}`, and spreads
  the key into the resolved block only when it is set, so the default block and
  every existing deep-equality check are unchanged.
- `tests/focused-config.test.ts` (new): covers unset-by-default, a valid
  reference command kept verbatim, the missing-placeholder rejection, a
  non-string rejection, and loading `focusedTests` from a temporary
  `osq.config.ts` through `loadConfig`.

Task verify (`node --import tsx --test tests/focused-config.test.ts`) and the
proposal verify (`pnpm verify`) both pass. `tests/traceability-config.test.ts`
is untouched and passes.

Touched: src/core/foundation/config-traceability.ts, tests/focused-config.test.ts
