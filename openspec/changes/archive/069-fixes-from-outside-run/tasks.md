# Tasks

## 1. Build identity

- [x] 1. When osq runs inside another project, records name osq's build and the project's commit separately

## 2. Capability flag

- [x] 2. When a delta creates a capability on purpose, approval marks it new and raises no unknown_capability flag

## 3. Fingerprints

- [x] 3. When the same node:test failure repeats, its dead markers share a fingerprint
