## Changed

- `src/core/spec/digest-capability.ts` (new): exports the pure `resemblingCapability(name, living)` (first living name in name order that resembles the new one, else null), plus `namesResemble` and `readLivingCapabilityNames(projectRoot, openspecRoot)`. Resemblance covers the three conditions: equal after hyphens/underscores are removed, word subset either direction, and both names at least five chars with Levenshtein distance at most two.
- `src/core/spec/digest.ts`: `ApprovalDigestCapability` gains `creates`. `buildApprovalDigest` reads living capability names once, computes `creates` (no living spec, non-empty delta purpose, no resemblance), passes the living list to the flags, and `formatApprovalDigest` prints `  <name> (new capability):` for a created capability.
- `src/core/spec/digest-flags.ts`: `ApprovalFlagInput` gains `livingCapabilities`. `unknownCapabilityFlags` no longer stats the filesystem; it skips a capability that has a living spec or is deliberately created, and otherwise labels the flag `unknown capability <name> resembles <living>` when a resemblance exists, else `unknown capability <name> without a Purpose`. Excerpt still names the missing living spec path and flag order is unchanged.
- `tests/approval-digest.test.ts`: added `creates: false` to the clean digest's capability shape and updated the two `brand-new-capability` labels to the `without a Purpose` form.
- `tests/approval-digest-capability.test.ts` (new): unit-tests `resemblingCapability` for each of the three conditions, null for short dissimilar names, and first-in-name-order; drives `buildApprovalDigest`/`formatApprovalDigest` for a deliberate new name (no flag, `(new capability)`), a delta without `## Purpose` (flag `without a Purpose`), `watcher-harness` beside living `watcher-and-harness` (flag `resembles watcher-and-harness`), and `cli` beside living `api` (no flag).
- `README.md`: the `unknown_capability` sentence now describes the missing-`## Purpose` and resembling-name conditions and the `(new capability)` heading marker.

## Missing context

None.

Touched: README.md, src/core/spec/digest.ts, src/core/spec/digest-flags.ts, src/core/spec/digest-capability.ts, tests/approval-digest.test.ts, tests/approval-digest-capability.test.ts
