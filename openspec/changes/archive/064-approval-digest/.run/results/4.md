## Changed

- `README.md`:
  - "Gates and permissions" gains an **Approval digest** bullet right after the
    approval gate. It says what the digest shows (goal, one line per task with its
    resolved-scope file count, delta requirements added/modified/removed), names
    the five flags in plain words (`shared_file`, `sensitive_path`,
    `verify_without_test`, `removed_requirement`, `unknown_capability`), says flags
    never block by default and print last as information, and that the approval
    line names them.
  - The `## Commands` block note for `osq approve` now mentions printing the
    digest, and `osq show` mentions `--json`.
  - New `### Approval` subsection under the CLI reference lists
    `osq approve <id> --confirm` and `osq show <id> --json` and states that
    `--confirm` asks only when flags fire, defaults to no, and refuses without a
    terminal; parallel text covers the default non-blocking path.
  - The "Metrics & Reporting" paragraph mentions the `Approval flags` report
    section, its shown/confirmed split, and that it counts only changes approved
    after this release because older manifests carry no recorded flags.
- `tests/approval-digest-docs.test.ts` (new): four tests asserting the Approval
  digest bullet follows the approval gate, the five flag names and the
  non-blocking/approval-line claims, the two documented commands and `--confirm`
  behavior, and the `Approval flags` report section plus its cutoff.

## Deviated

- Kept the digest-bullet wording at prose length and named the sensitive-path
  kinds rather than listing every pattern, as the Details section asked.
- Formatted the new test with `biome check --write` so `pnpm lint` passes.

## Missing context

- The exact README phrasing for the digest, the flags, and the report cutoff was
  not specified; the new tests pin the chosen phrases.

Touched: README.md, tests/approval-digest-docs.test.ts
