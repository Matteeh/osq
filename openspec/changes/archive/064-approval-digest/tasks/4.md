---
title: When a user reads the README, the approval digest, its flags, --confirm, and the flag report are explained
verify: node --import tsx --test tests/approval-digest-docs.test.ts
scope:
  - README.md
entry:
  - README.md
  - src/core/spec/digest-flags.ts
tests:
  modify: false
skills: []
---
## Acceptance

- [ ] README's "Gates and permissions" list gains an **Approval digest** bullet after the approval gate: what the digest shows, the five flags in plain words, that flags never block by default, and that the approval line names them.
- [ ] The CLI reference lists `osq approve <id> --confirm` and `osq show <id> --json`. It says `--confirm` asks only when flags fire, defaults to no, and refuses without a terminal.
- [ ] The "Metrics & Reporting" paragraph mentions the `Approval flags` section and that it counts only changes approved after this release.
- [ ] New `tests/approval-digest-docs.test.ts` asserts that README mentions `--confirm`, `shared_file`, `sensitive_path`, and `Approval flags`.

## Details

Match the tone and length of the surrounding README text. Do not list every
sensitive file pattern; name the kinds. Every existing test is frozen.
