---
title: When osq lint runs without ids beside a rejected change, it lints only active changes
verify: node --import tsx --test tests/lint-skip-rejected.test.ts
verify_starts: red
scope:
  - src/cli/lint.ts
  - tests/lint-skip-rejected.test.ts
entry:
  - src/cli/lint.ts
  - src/core/status/layout.ts
  - tests/watcher-loop-logging.test.ts
skills: []
---
## Acceptance

- [ ] `listChangeFolders` in `src/cli/lint.ts` keeps directories whose name passes `isActiveChangeFolderName` from `src/core/status/layout.ts`, and still sorts them with plain `sort`.
- [ ] New `tests/lint-skip-rejected.test.ts` scaffolds a project with `openspec/changes/rejected/009-nope/proposal.md` beside one change that lints clean, calls `lintCommand` with no ids and an injected `exit`, and asserts that `exit` was not called with 1, that `result.valid` is true, and that no logged error or warning names `rejected`.

## Details

`isActiveChangeFolderName` already exists in `src/core/status/layout.ts`; import
it, do not redefine it. Explicit ids still resolve through `findSpecFolder`
unchanged.

The active change must lint clean on its own. `createNewSpec` seeds the planning
sentinel verify, which lint rejects, so replace it and write a valid task the
way `useLocalVerifier` and `writeTask1` do in
`tests/watcher-loop-logging.test.ts`. Call `installFakeValidator` and
`scaffoldProject` first, and pass `cwd` and a capturing `logger` to
`lintCommand`.
