---
title: When the changes directory holds archive and rejected, the watcher cycle skips both
verify: node --import tsx --test tests/watcher-skip-rejected.test.ts
verify_starts: red
scope:
  - src/core/status/layout.ts
  - src/watcher/loop.ts
  - tests/watcher-skip-rejected.test.ts
entry:
  - src/core/status/layout.ts
  - src/watcher/loop.ts
  - tests/watcher-loop-logging.test.ts
skills: []
---
## Acceptance

- [ ] `src/core/status/layout.ts` exports a pure `isActiveChangeFolderName(name: string): boolean` that returns false for names starting with `_` or `.` and for the archive and rejected folder names, using the same `ARCHIVE_DIR` and `REJECTED_DIR` constants as `getArchiveDir` and `getRejectedDir`, and true for every other name.
- [ ] `runWatcherCycle` in `src/watcher/loop.ts` filters its `readdir` entries through `isActiveChangeFolderName` and still sorts them with `compareNumericPrefix`.
- [ ] New `tests/watcher-skip-rejected.test.ts` scaffolds a project, creates `openspec/changes/archive/` and `openspec/changes/rejected/009-nope/proposal.md`, then creates and approves one change on `MockAdapter`. It runs one `runWatcherCycle` and asserts that no logged error contains `watcher error` and that `summary.tasksRun` is 1.
- [ ] The same test file asserts that `isActiveChangeFolderName` returns false for `archive`, `rejected`, `_draft`, and `.DS_Store`, and true for `001-example`.

## Details

Copy the setup from `tests/watcher-loop-logging.test.ts`: `installFakeValidator`,
`scaffoldProject`, `createNewSpec`, its `useLocalVerifier` and `writeTask1`
helpers, `approveSpec`, and its `CaptureLogger`. Because `createNewSpec` numbers
after the rejected folder, the new change is not `001`; approve it by the id at
the start of the returned `folderName`.

`layout.ts` must not import from `state.ts`. A real change folder with no
proposal must still log a watcher error; do not change the per-folder
`try`/`catch`.
