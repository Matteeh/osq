---
title: When the final planning tree is verified, cross-workflow expectations and golden events match it
verify: pnpm verify
scope:
  - tests/harness-generic-workflows.test.ts
  - tests/fixtures/events/
entry:
  - src/core/manifest.ts
  - src/core/init.ts
  - tests/golden-events.test.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] The generic harness workflow expects manifest planner attribution only from recorded planning sessions, never from planner configuration or executor identity, while retaining task 1's explicit-session coverage.
- [ ] The verified and dead golden event fixtures retain their established event shapes and update only repository file and line measures caused by task 2's managed-instruction scaffold growth.
- [ ] The complete change passes type checking, every test, and source/test lint through the final `pnpm verify` entrypoint without further production changes.

## Details

This is the final-tree compatibility owner for two existing artifacts invalidated
by earlier coherent slices. Preserve the `session: true` adjustment already made
by task 1 in `tests/harness-generic-workflows.test.ts`; update only the manifest
planner expectations required by task 3. Regenerate the two event fixtures only
through the established `UPDATE_GOLDEN=1` golden-events test path and confirm
that their only differences are the scaffold-derived `repoFiles` and
`repoLines` measures documented by task 2. Do not edit production files. Run
the task verify before exit.
