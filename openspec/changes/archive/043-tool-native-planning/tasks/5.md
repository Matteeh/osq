---
title: When planning history is shown or reported, owned and observed sessions contribute to one record
verify: node --import tsx --test tests/report*.test.ts tests/show.test.ts
scope:
  - src/core/report.ts
  - src/cli/report.ts
  - src/core/show.ts
  - tests/report-planning.test.ts
  - tests/report-json.test.ts
  - tests/show.test.ts
  - fixture/report/**
entry:
  - src/core/report.ts
  - src/core/show.ts
  - src/core/planning.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] Report planning aggregation consumes task 3's correlated records and treats observed, explicit owned, and source-less legacy starts identically under the established session-count, incomplete-exit, wall-time, usage, cost, and numeric-coverage rules.
- [ ] The planning block adds one deterministic `changesWithPlanningRecords` count of distinct active and archived changes containing at least one valid `plan_started`; repeated sessions count once, while malformed lines and exit-only records do not count.
- [ ] Text output states exactly `<n> changes have a planning record` and stable JSON carries the same integer without changing existing planning totals, `n of m sessions reported usage`, queue spend, cycle, history, coverage, or other report shapes.
- [ ] `osq show <id>` lists observed records through the same start-time ordering and harness, nullable model, agent, exit, and wall-time fields as owned records; it does not expose transcript text, brief hashes, tokens, or cost that owned inspection currently omits.
- [ ] A source-less legacy record remains readable, a valid unmatched start retains unavailable exit fields, and missing or malformed logs leave task details, recertification history, results, and the event timeline intact.
- [ ] Mixed active/archive fixtures include several records on one change, one observed record on another, an incomplete owned start, and malformed/exit-only lines; focused show, text, stable JSON, and checked-in report fixture assertions remain deterministic.

## Details

Reuse `readPlanningSessions` and the source-compatible parsing established by
task 3. Do not parse plan JSONL independently in show or report and do not add a
second observed-session projection. The report's existing definition of a
session is a valid start; matched exits alone supply wall time and usage. Count
covered changes while visiting canonical active and archived change folders,
not from filesystem timestamps, manifests, brief attribution, or queue state.

Add `changesWithPlanningRecords` beside the existing planning aggregate fields
and render the exact sentence after the session usage-coverage line. Preserve
deterministic change ordering and every stable JSON field already asserted by
the fixture. Update only the checked-in report fixture bytes required by the
new integer and the task-3 source-compatible parser.

Keep the `Planning Sessions` show section in its established position before
the event timeline. Observed records need no visual badge: treating them the
same as owned sessions is the contract. Continue suppressing usage and artifact
hashes from this surface. Extend `tests/show.test.ts` with mixed source values
and `tests/report-planning.test.ts` with the distinct-change cases; update
`tests/report-json.test.ts` and `fixture/report/**` only for the stable shape.
Run the task verify and `pnpm verify` before exit.
