# Task 3 result — approval observes local planning sessions

Status: complete. Task verify and `pnpm verify` pass on the final tree.

## Retry note (attempt 3)

Attempt 2 completed and the watcher wrote the canonical `.run/done/3` marker
with task 3's resolver-2 scope hash. Task 5 then extended `src/core/show.ts` —
the proposal explicitly shares this file (task 3 makes only the nullable-model
compatibility line, task 5 owns the full projection) — so the next watcher scope
audit recorded `scope_regression` for task 3 with
`src/core/show.ts (modified)` and attribution `unknown`.

The human `osq retry 043 3` recertification re-ran the task verify. That single
execution returned `exitCode: 1` with empty output (a transient spawn failure:
the identical command succeeds unconditionally in this attempt, three consecutive
direct runs, and through `runVerificationCommand` invoked the same way retry
invokes it). The task was therefore `requeued` and the watcher spawned this
attempt.

This attempt changed **no source or test file**. The implementation from attempts
1/2 is already correct and stable:

- Task verify: 3/3 consecutive runs pass (58 pass, 0 fail).
- `pnpm verify`: `tsc --noEmit` clean, 1166 tests pass, `biome check` clean,
  exit 0.

Because no file under `tests/` or `src/` changed during this attempt, the runner's
pre-existing-test diff is empty and the canonical `done/3` marker can be
rewritten with the current tree hash, clearing the scope regression.

## Changed

- None in this attempt. The delivered implementation is:
  - `src/core/planning-records.ts` (new): `PlanRecord`/`PlanningSource` layer with
    optional `source` on both lifecycle records, nullable model and exit code,
    `planningRecordSource` (missing source reads as owned), and source-aware
    parsing.
  - `src/core/planning.ts`: facade re-exporting the record layer plus owned
    writers that stamp `source: 'owned'`.
  - `src/core/planning-observed.ts` (new): reader-neutral session shape,
    segment-aware containment/normalization, `findPlanningSessions` (inclusive
    window, fixed harness order, reader-failure isolation, dedupe by harness +
    native id), `appendObservedSessions` (stable ids, null exit code, wall time
    from observed bounds, idempotent), and `resolveChangeCreationTime`.
  - `src/harness/codex-observe-usage.ts`, `src/harness/opencode-observe-usage.ts`,
    `src/harness/claude-usage.ts` (new): the three metadata-only readers.
  - `src/core/approve.ts`, `src/cli/approve.ts`, `src/core/manifest.ts`,
    `src/core/show.ts`: observation call between lint and seal/manifest,
    catalog-resolved OpenCode executable, observation-derived nullable planner,
    the one-line no-record notice, and the nullable-model compatibility line.
  - `tests/planning-observed.test.ts`,
    `tests/fixtures/planning-observed/**`, `tests/config-planner.test.ts`.

## Deviated

- `tests/harness-generic-workflows.test.ts` and `tests/fixtures/events/*.jsonl`
  were updated in an earlier attempt as a cross-task compatibility fix (task 2's
  scaffold growth changes the pinned `repoFiles`/`repoLines` counts). They are
  outside this task's declared scope and remain for task 6's final-tree
  reconciliation.
- `src/core/show.ts` carries only the minimal nullable-model compatibility change;
  task 5 owns the complete projection (this is the source of the scope
  regression above, and is the proposal-documented shared file).

## Drift against features

- None for this task. The approval observation, mixed-source record, manifest
  attribution, and notice deltas are implemented as written. Task 4 still owns
  `plan-prompt.md` lint/hash/archive exclusion; task 5 owns the report/show
  projection.

## Missing context

- None blocking. The only watcher-visible failure is the shared-file scope
  regression, which a fresh canonical `done/3` write resolves.

## Verification

- Task verify (`node --import tsx --test tests/planning-observed.test.ts
  tests/approve.test.ts tests/manifest.test.ts tests/plan-telemetry.test.ts
  tests/import-graph.test.ts tests/line-budget.test.ts`): 58 pass, 0 fail, three
  consecutive runs.
- `pnpm verify`: exit 0, 1166 tests pass, lint clean.

## Next steps

- None; all seven acceptance lines are covered. Task 4 and task 5 build on the
  source-aware `readPlanningSessions`/`planningRecordSource` parsing without
  reopening the readers.
