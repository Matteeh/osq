# Task 2 result

## Changed

- `src/core/init-blocks.ts` (new): canonical `OSQ_START_MARKER`, `OSQ_END_MARKER`,
  `MANAGED_AGENTS_MD_BODY` (now with a `Planning a change` section naming
  `plan-prompt.md`, change-folder-only writes, `osq lint <slug>`, and never
  `osq approve`), the `MANAGED_AGENTS_BLOCK` alias, `MANAGED_PLANNER_BLOCK`
  (new `### Working from the handoff` rules), `MANAGED_CLAUDE_PLAN_COMMAND`,
  and `CLAUDE_PLAN_COMMAND_PATH`.
- `src/core/init-managed.ts` (new): `inspectManagedBlock`, one marker
  parser/replacer (no second parser) that repairs missing, partial, reversed,
  and duplicated blocks, plus `updateAgentsMd`, `updatePlannerMd`, and
  `updateClaudePlanCommand`.
- `src/core/init.ts`: re-exports the canonical values, adds
  `InitResult.updatedClaudePlanCommand`, and `scaffoldProject` now creates
  `.claude/commands/osq-plan.md` (with its parent directory), classifies it in
  `createdFiles`/`existingFiles`, and leaves existing config, environment,
  schema, and consumer template files untouched.
- `src/core/doctor-managed.ts` (new) and `src/core/doctor.ts`: the
  managed-instructions check compares extracted block bytes with the installed
  canonical values for `AGENTS.md`, `PLANNER.md`, and
  `.claude/commands/osq-plan.md`, and fails actionably for a missing file,
  missing block, partial pair, reversed pair, duplicates, or stale content. An
  unmanaged `AGENTS.md` is now a failure.
- `templates/PLANNER.md`, `PLANNER.md`, and `AGENTS.md`: regenerated so the
  repository copies match the installed constants (root `AGENTS.md` received its
  managed block).
- `.claude/commands/osq-plan.md` (new): consumer copy matching the installed
  constant; uses `$ARGUMENTS` to resolve the change folder by slug.
- `README.md`: planning guidance now covers default prompt handoff, explicit
  `--session`, `--print`, the installed Claude/Codex entry points, approval-time
  observation, planning-tool-owned model choice, and that the generated config
  contains no required planner model; the init, command-list, and doctor lines
  were updated.
- Tests: `tests/init.test.ts`, `tests/init-planning-guidance.test.ts` (new),
  `tests/init-planner.test.ts`, `tests/managed-blocks.test.ts`, and
  `tests/doctor.test.ts`.

## Deviated

- Did not regenerate `tests/fixtures/events/verified.jsonl` and `dead.jsonl`.
  They are outside this task's `scope` and outside its verify command, but the
  scaffold growth (larger `AGENTS.md`/`PLANNER.md` blocks plus the new Claude
  command) changes the measured repository size those fixtures pin
  (`repoFiles` 13 -> 14, `repoLines` 439 -> 468). Regenerate them with
  `UPDATE_GOLDEN=1 node --import tsx --test tests/golden-events.test.ts` before
  the change-level `pnpm verify` / archive.

## Drift against features/

- None. The `cli-foundation` managed-planning-entry-point delta is implemented
  as written. No living capability spec was edited.

## Missing context

- `tests/fixtures/events/**` and `tests/golden-events.test.ts` have no owning
  task in change 043, yet any change to the managed `AGENTS.md`/`PLANNER.md`
  bytes or scaffold file set necessarily changes the repository size pinned by
  those golden fixtures. This is a planning gap, not a task 2 defect.

## Next steps

- Acceptance lines 1-7 are implemented and the task verify is green. The next
  owner action is to regenerate the golden event fixtures (see Deviated) so the
  change-level `pnpm verify` passes and the change can archive.
