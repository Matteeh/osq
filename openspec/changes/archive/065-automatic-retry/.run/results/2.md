## Changed

- Added `src/watcher/fingerprint.ts` (61 lines) with `stripAnsi`, `normalizeFailureBody`, and `markerFingerprint`, plus `addFingerprint`, which inserts the `fingerprint: sha256:<hex>` frontmatter line in place and leaves every other byte untouched. `markerFingerprint` hashes the frontmatter `reason`, a newline, and the normalized body after the frontmatter.
- `src/watcher/outcome.ts` (199 lines, still under 200): `writeDeadMarker` gains an optional `projectRoot` parameter and writes `addFingerprint(content, projectRoot)` through the existing `writeMarkerFile`.
- `src/watcher/runner.ts` (197 lines): the `fail` helper passes `projectRoot` to `writeDeadMarker`. No new lines.
- `src/watcher/spawn.ts` (187 lines): both `spawnTaskAgent` crash/timeout dead marker writes pass `projectRoot`.
- Added `tests/dead-fingerprint.test.ts` (new file) proving normalization of ANSI, timestamps, durations, PIDs, and project-root paths; fingerprint equality for volatile-only differences and inequality across reasons and failing test names; frontmatter insertion with byte preservation and the no-frontmatter block; and two real `runTask` deaths (`verify_red` and a crashed agent whose error embeds the project root) carrying a re-derivable `sha256:` fingerprint.

## Deviated

- The `no_result` dead marker is written by `ensureTaskResult`, a third spawn.ts function that the task does not name. Threading `projectRoot` through it would have pushed the one-line object literal at `runner.ts:170` past the 100-column formatter limit, wrapping it four lines and breaching runner.ts's 200-line budget. It therefore still records a fingerprint from its reason and body but without project-root substitution. The acceptance line names only `runTask` and `spawnTaskAgent`, both of which pass `projectRoot`.

## Missing context

None.

## Next

None: all acceptance lines covered. `loop.ts`'s stale-lock reaper still calls `writeDeadMarker` without `projectRoot` as task 2 directs; task 4 owns that call site.

Touched: src/watcher/fingerprint.ts, src/watcher/outcome.ts, src/watcher/runner.ts, src/watcher/spawn.ts, tests/dead-fingerprint.test.ts
