## Changed

- `README.md`:
  - Added an **Automatic retry** bullet to `## Gates and permissions` after
    the pre-spawn verify bullet. It names the six eligible reasons, says every
    other reason waits for the human, documents `gates.autoRetries` (default 1,
    `0` turns retries off, a manual `osq retry` grants one more), and states
    that each retry's prompt carries the previous dead marker's body.
  - Added a **Stuck tasks** bullet next: the fingerprint over reason and body,
    the ignored volatile details (ISO timestamps, durations, PIDs, ANSI codes,
    absolute project paths), the stuck stop (`stuck: true`, one `stuck` event,
    one line), the inbox/`osq --json` `stuck` field, and that
    `osq retry <id> <n>` still works.
  - Extended the dead-reasons paragraph to note every dead marker carries a
    `fingerprint` and a stopped-on marker carries `stuck: true`.
  - Extended the inbox **Needs you** bullet to document the optional
    `stuck: { fingerprint }` field on `task-dead` items.
  - Extended the Metrics & Reporting paragraph to mention the
    `Automatic retries` section.
- Added `tests/auto-retry-docs.test.ts`: six assertions covering the named
  surface (`gates.autoRetries`, `stuck`, `fingerprint`, `Automatic retries`),
  the automatic-retry bullet's reasons/count/prompt, the fingerprint and stuck
  explanation, the dead-reasons frontmatter, the inbox `task-dead` `stuck`
  field, and the report section mention. Formatting was fixed with biome.

## Deviated

None. `fixture/report/expected.json`, the approval-digest README additions, and
other modified files shown by `git status` predate this task (other changes in
flight in the worktree); only the five README passages and the new test file
are mine.

## Missing context

None.

## Next

None: all five acceptance lines are covered. The task verify
(`node --import tsx --test tests/auto-retry-docs.test.ts`) passes all six tests,
and the proposal's `pnpm verify` (typechecks, UI build, full suite, lint) exits 0.

Touched: README.md, tests/auto-retry-docs.test.ts
