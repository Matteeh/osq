---
title: When a dead task is retried manually or automatically, the next prompt carries the retained marker's body
verify: node --import tsx --test tests/retry-prior-output.test.ts
scope:
  - src/watcher/attempt.ts
  - tests/retry-attempts.test.ts
entry:
  - src/watcher/attempt.ts
  - src/watcher/fingerprint.ts
  - src/harness/types.ts
  - src/harness/prompt.ts
  - tests/retry-attempts.test.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] `readRetryContext` sets `output` on each `retry` event to the body after the frontmatter of `.run/dead/<n>.<attempt - 1>.md`, the marker that retry retained, with ANSI codes stripped through `stripAnsi`. When that file is missing, as after retrying a regression, `output` is cleared as today.
- [ ] Requeued recertification output keeps precedence exactly as today when it is the latest transition.
- [ ] The rendered prompt bounds the output at 2,000 characters through the existing `priorContextLines`, with no second bound added.
- [ ] New `tests/retry-prior-output.test.ts` builds an executor prompt with `buildExecutorPrompt` after a manual `retrySpec` and after an automatic one (`{ automatic: true }`), each over a marker with ANSI codes. It asserts both prompts contain the stripped body. A 5,000-character body is cut to the existing bound.
- [ ] In `tests/retry-attempts.test.ts`, only the expected context objects gain the `output` now read from the retained marker.

## Details

Use `stripAnsi` from `src/watcher/fingerprint.ts`, added by task 2. A worktree
measurement showed that this change breaks only the context equality in
`tests/retry-attempts.test.ts`. Other tests are frozen.
