---
title: When retrySpec is asked for an automatic retry, the shared transition records automatic true on the retry event
verify: node --import tsx --test tests/retry-automatic.test.ts
scope:
  - src/core/lifecycle/retry.ts
  - src/core/lifecycle/retry-transition.ts
entry:
  - src/core/lifecycle/retry.ts
  - src/cli/retry.ts
  - tests/retry.test.ts
  - tests/dead-marker-retention.test.ts
tests:
  modify: false
skills: []
---
## Acceptance

- [ ] The marker retention (renaming active dead, regressed, and done markers to the next ordinal) and the event append move out of `retry.ts` into new `src/core/lifecycle/retry-transition.ts`. `retrySpec` calls them, so there is still one transition, and its behavior for every existing caller is byte-identical.
- [ ] `retrySpec(projectRoot, specIdOrPrefix, target, config, options?)` accepts optional `{ automatic?: boolean }`. When `automatic` is true, the appended `retry` event's data adds `automatic: true`. Otherwise the event carries no `automatic` key.
- [ ] Recertification of a scope regression never carries `automatic`, whatever the option says.
- [ ] New `tests/retry-automatic.test.ts` retries a dead task with and without the option on an approved fixture change. It asserts the retained marker name, the `automatic` field (present and true, or absent), and that the approval-hash and running-lock refusals apply to automatic calls too.
- [ ] `retry.ts` and `retry-transition.ts` each stay under 250 lines, without the `biome-ignore format` line `retry.ts` needs today.

## Details

`retry.ts` is at 246 lines, so the move comes before any new option. Keep the
approval check, the running check, and the reason and ordinal computation in
`retrySpec`; move only the rename and append work. `tests/retry.test.ts`,
`tests/retry-attempts.test.ts`, `tests/retry-watch.test.ts`, and
`tests/dead-marker-retention.test.ts` cover the manual path and stay frozen
and green.
