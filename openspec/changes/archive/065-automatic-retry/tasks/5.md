---
title: When a dead task is stuck, the inbox and osq --json mark it stuck with its fingerprint
verify: node --import tsx --test tests/inbox-stuck.test.ts
scope:
  - src/core/status/state.ts
  - src/core/status/inbox.ts
  - src/core/status/inbox-text.ts
entry:
  - src/core/status/state.ts
  - src/core/status/inbox.ts
  - src/cli/inbox.ts
  - tests/inbox.test.ts
tests:
  modify: false
skills: []
---
## Acceptance

- [ ] A dead task whose active marker frontmatter has `stuck: true` and a `fingerprint` derives `stuck: <fingerprint>` on its task state. Every other task state is unchanged.
- [ ] `NeedsYouItem` gains optional `stuck: { fingerprint }`, set only on `task-dead` items for stuck tasks. `osq --json` output for every other item is byte-identical to today.
- [ ] A stuck item's text row reads `  <id>: <title> — task <n>: <task title> — stuck: same failure twice; amend the spec or osq reject <id> --reason <text> — osq retry <id> <n>`. Every other row is unchanged.
- [ ] The row rendering (`needsYouLine`, `runningLine`, `landedLine`, `formatInboxText`) moves to new `src/core/status/inbox-text.ts`, and `inbox.ts` re-exports `formatInboxText`, so both files stay under 250 lines. `state.ts` also stays under 250.
- [ ] New `tests/inbox-stuck.test.ts` builds a change with one stuck and one plain dead task. It asserts both JSON items, the stuck row, and the unchanged plain row, through the real bare `osq` CLI in text and `--json` modes.

## Details

`state.ts` and `inbox.ts` are both at 249 lines, so make room before adding.
The item's `command` stays `osq retry <id> <n>`, keeping the action command
contract; the suggestion sits in the row text. `tests/inbox.test.ts` stays
frozen and green.
