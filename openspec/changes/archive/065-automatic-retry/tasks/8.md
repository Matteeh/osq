---
title: When a user reads the README, automatic retries, fingerprints, stuck tasks, and the retry report are explained
verify: node --import tsx --test tests/auto-retry-docs.test.ts
scope:
  - README.md
entry:
  - README.md
  - src/watcher/auto-retry.ts
tests:
  modify: false
skills: []
---
## Acceptance

- [ ] README's "Gates and permissions" list gains an **Automatic retry** bullet covering:
  - which reasons are retried, and that others always wait for a human
  - the `gates.autoRetries` count, its default of 1, and that a manual `osq retry` grants one more
  - that `0` turns it off
  - that each retry's prompt carries the previous dead marker
- [ ] The same bullet, or the next one, explains the fingerprint and the stuck stop: which details are ignored, that the inbox and `osq --json` show `stuck`, and that `osq retry` still works.
- [ ] The dead-reasons paragraph notes that dead markers carry a `fingerprint` and may carry `stuck: true`. The inbox section documents the optional `stuck` field on `task-dead` items.
- [ ] The "Metrics & Reporting" paragraph mentions the `Automatic retries` section.
- [ ] New `tests/auto-retry-docs.test.ts` asserts that README mentions `gates.autoRetries`, `stuck`, `fingerprint`, and `Automatic retries`.

## Details

Match the tone and length of the surrounding README text. Every existing test
is frozen.
