---
title: When the baseline gate is configured, osq validates the command and can key the tree it would verify
verify: node --import tsx --test tests/baseline-key.test.ts
verify_starts: red
scope:
  - src/core/foundation/config-gates.ts
  - src/watcher/baseline-key.ts
  - tests/baseline-key.test.ts
entry:
  - src/core/foundation/config-gates.ts
  - src/core/vcs/vcs.ts
  - src/core/vcs/select.ts
  - src/core/run/manifest.ts
skills: []
---
## Acceptance

- [ ] `GatesConfig` gains optional `baselineVerify`, and `validateGatesConfig` reads it as the cli-foundation delta's "Baseline verify configuration" says, leaving it out of the result when unset.
- [ ] New `src/watcher/baseline-key.ts` exports `readBaselineKey(vcs: Vcs, projectRoot: string): Promise<BaselineKey | null>`, with `BaselineKey` holding `commit` and `treeDigest`, as the watcher-and-harness delta's "Baseline key" says.
- [ ] New `tests/baseline-key.test.ts` covers both configuration scenarios through `loadConfig` in a temporary project, and all three key scenarios against temporary git repositories it creates, using `selectVcs`.

## Details

Read status and HEAD through the `Vcs` port, and hash contents with
`hashFileContent` from `src/core/run/manifest.ts`. A path is inside a `.run/`
folder when any of its segments is `.run`. Leaving the key out when unset keeps
every existing deep-equality check of `gates` unchanged. In tests, set
`user.name` and `user.email` in each temporary repository. New functions stay
within 80 lines.
