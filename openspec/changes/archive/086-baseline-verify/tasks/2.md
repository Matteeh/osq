---
title: When a change's first task is about to start, the watcher settles the baseline and halts on a red tree
verify: node --import tsx --test tests/baseline-verify.test.ts
verify_starts: red
scope:
  - src/watcher/baseline.ts
  - src/watcher/runner.ts
  - src/watcher/failure-reason.ts
  - src/harness/types.ts
  - tests/baseline-verify.test.ts
  - README.md
  - osq.config.ts
entry:
  - src/watcher/runner.ts
  - src/watcher/task-verify.ts
  - src/watcher/instructions-drift.ts
  - src/watcher/baseline-key.ts
  - src/core/run/verification.ts
  - src/core/status/layout.ts
  - tests/focused-verify.test.ts
skills: []
---
## Acceptance

- [ ] New `src/watcher/baseline.ts` implements the watcher-and-harness delta's "Baseline verify before a change's first task" and "Baseline reuse", selecting through `selectVcs` and keying through `readBaselineKey`.
- [ ] `runTask` calls it right after building `verifyCtx` and before `runPreSpawnVerify` and, on a red baseline, returns through its `fail` with `baseline_red` and the halt line as the outcome's extra text. The call and its check are the only lines added there.
- [ ] `baseline_red` joins `RunTaskFailureReason`, and `src/harness/types.ts` gains the `baseline_ran` event type with `BaselineRanEventData` in `OsqEventData`.
- [ ] New `tests/baseline-verify.test.ts` runs changes through `runWatcherOnce` with a counting fake adapter and `node -e` baseline commands. It covers every scenario of both requirements and the proposal's "Red before the change", including that the default `gates.autoRetries` leaves a `baseline_red` task dead.
- [ ] README.md's Gates and permissions gains a Baseline verify bullet, its automatic retry bullet lists `baseline_red` among the reasons that wait, and its dead reasons list names `baseline_red`. osq's own `osq.config.ts` sets `gates: { baselineVerify: 'pnpm verify' }`.

## Details

Read task streams as `hasEvent` in `instructions-drift.ts` does, skipping
`change.jsonl`. List active and archived change folders with `getChangesDir`
and `getArchiveDir`. Run the command with `runVerificationCommand` and a null
change folder, and append events with `appendHarnessEvent` to the `change`
stream. Model the dead marker's output on `verifyRedFailure` in
`task-verify.ts`. Never use the word "skipped" in `src/`, because
`tests/no-skipped-in-src.test.ts` forbids it. `runTask` is grandfathered, but
`tests/import-graph.test.ts` keeps `runner.ts` strictly under 200 lines, and it
has 193. Keep the import and the check to about four lines, passing
`verifyCtx` as 084's `beginGitGuard` call does. New files stay within 250
lines and new functions within 80.
