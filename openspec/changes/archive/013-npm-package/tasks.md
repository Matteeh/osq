# Tasks

- [x] 1. When npm pack runs, the tarball contains exactly the distribution assets
- [x] 2. When the built bin is invoked directly, it runs with runtime version resolution
- [x] 3. When the tarball is installed into a clean project, scaffolding commands execute
- [x] 4. When a release tag is pushed, the workflow publishes via trusted publishing
- [x] 5. When a release is cut, the changelog documents historical versions