# Task 1 — Package tarball hygiene

## Changed

- `package.json`
  - Added `"sideEffects": false`.
  - Added `"keywords"` (`spec-driven-development`, `coding-agents`, `agentic-engineering`, `watcher`, `cli`).
  - Added `"packageManager": "pnpm@9.15.4"` (aligned with CI's pnpm 9 line).
  - Changed `"prepublishOnly"` from `pnpm build` to `npm run build`.
  - `"files"` already contained `dist` and `templates`; left unchanged.
- `tests/package-hygiene.test.ts` (new)
  - Runs `npm pack --dry-run --json` via `child_process.execFile` from the repo root.
  - Asserts every packed path is `dist/…`, `templates/…`, `README.md`, `LICENSE`, or `package.json`; asserts each of those required assets is present.
  - Asserts no `src/`, `tests/`, `.github/`, `specs/`, `fixture/`, non-`.d.ts` `.ts`, or root config/lockfiles are packed.
  - Asserts manifest `sideEffects: false`, non-empty `keywords`, a pinned `pnpm@` `packageManager`, `files` containing `dist`/`templates`, and `prepublishOnly === "npm run build"`.

## Verified

`node --import tsx --test tests/package-hygiene.test.ts` → 3 pass / 0 fail.

Also ran `pnpm tsc --noEmit` and `pnpm lint` clean (biome reformatted the new test once).

Observed tarball contents: `LICENSE`, `README.md`, `dist/**`, `templates/.env.example`, `package.json` (136 entries). No source, tests, configs, `.github`, or specs leak.

## Deviations

None. `files` already listed `dist` and `templates`, so no change was needed there.

## Drift against features/

None. This is additive package metadata/tooling; it does not alter `features/cli-foundation.md` behavior.

## Notes / next steps

- `templates/` currently contains only `.env.example`; tasks covering `osq init` templates are out of scope for this task.
- Remaining spec-013 tasks: shebang/version runtime behavior, consumer pack smoke test, release workflow, changelog/README docs.
