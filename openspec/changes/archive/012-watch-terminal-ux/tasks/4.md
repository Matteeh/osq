---
title: When the harness stream carries an unrecognised event, it routes to verbose logging
verify: node --import tsx --test tests/stream-unrecognised-events.test.ts
scope: [src/harness/opencode.ts, src/harness/agy.ts, src/watcher/runner.ts, tests/stream-unrecognised-events.test.ts, tests/runner-lifecycle-logging.test.ts, tests/runner-synthesized-result.test.ts]
entry: [src/harness/opencode.ts, src/watcher/runner.ts]
skills: []
---
## Acceptance
- [ ] console.debug is removed from opencode.ts and unrecognised stream events route to logger.verbose
- [ ] agy.ts verifies unrecognised stream events do not write to stdout and route to verbose logging
- [ ] Runner demotes task exited log line from info level to verbose level
- [ ] Runner demotes task result synthesized log line from info level to verbose level
- [ ] events.jsonl lifecycle and synthesized result event persistence remains completely unchanged
- [ ] Existing runner lifecycle and synthesized result tests are updated to assert on verbose output

## Signatures

In `src/harness/opencode.ts`:

```ts
export async function processOpencodeStdoutLine(
  line: string,
  specFolderPath: string,
  taskNumber: string,
  logger?: Logger,
): Promise<void>;
```

## Details

1. In `src/harness/opencode.ts`:
   - Remove `console.debug` at line 389.
   - In `processOpencodeStdoutLine`, route unknown event types to `logger?.verbose(`[opencode] Unknown event type: ${eventObj.type}`)`.
   - Ensure neither stdout nor stderr receives unformatted debug logs.
2. In `src/harness/agy.ts`:
   - Ensure unrecognised stream events or malformed lines are logged to `logger?.verbose` when logger is present, without stdout leakage.
3. In `src/watcher/runner.ts`:
   - In `recordLifecycleEvent`, change the log level for `exited` events: `logger?.verbose(summary)` instead of `logger?.info(summary)`.
   - When synthesizing results, change `logger?.info(`task ${taskNumber} result synthesized...`)` to `logger?.verbose(...)`.
   - The `events.jsonl` append-only stream (`started`, `exited`, `result_written`) must remain completely unchanged.
4. In existing and new test files:
   - Create `tests/stream-unrecognised-events.test.ts` to test unrecognised event routing and verify zero stdout output.
   - In `tests/runner-lifecycle-logging.test.ts`, update assertions that check for `task <n> exited` to use a verbose logger (`createLogger('verbose')`).
   - In `tests/runner-synthesized-result.test.ts`, update assertions checking for `result synthesized` to use a verbose logger (`createLogger('verbose')`).