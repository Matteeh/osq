## Changed

- `writePromptHandoff` in `src/cli/plan-queue.ts` now takes the formatted next
  step and its one line ends with ` — next: <next step>`.
- `planCommand` in `src/cli/plan.ts` computes the handoff step with
  `readNextStep` and `formatNextStep` and passes it to `writePromptHandoff`.
- The catch in `approveCommand` in `src/cli/approve.ts` finds the change with
  `findSpecFolder` (ignoring its error) and prints `Next: <next step>` after the
  error; a missing folder prints nothing more.
- Added `tests/plan-approve-next-step.test.ts` covering the fresh plan handoff
  (one line, ends with `unplanned — osq plan <id>`), the approval refusal of a
  template (prints `Next: unplanned — osq plan <id>` with `process.exit`
  stubbed), and the missing-folder case (no `Next:` line).

## Deviated

None.

## Missing context

None.

## Outside scope

None.

## Next

None. All three acceptance lines are covered.

Touched: src/cli/plan-queue.ts, src/cli/plan.ts, src/cli/approve.ts, tests/plan-approve-next-step.test.ts
