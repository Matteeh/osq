## Changed

- `src/core/spec/digest.ts`: `ApprovalDigest` gains `beforeApproval: string`;
  `buildApprovalDigest` fills it from `parseHumanSteps(body)` while
  `humanSteps` keeps `extractSection(body, 'Human steps')` unchanged.
  `formatApprovalDigest` moved out to `src/core/spec/digest-format.ts` and is
  re-exported from `digest.ts`, so every existing import keeps working. The file
  is 222 lines (250 max).
- Added `src/core/spec/digest-format.ts`: `formatApprovalDigest` prints
  `Before approval, do these first:` and each step line indented two spaces
  right after the goal, only when `beforeApproval` is non-empty; a proposal
  without before-approval steps formats byte-for-byte as before. 35 lines.
- Added `tests/digest-before-approval.test.ts`: builds a temporary change
  through `buildApprovalDigest` and covers "Steps before approval in the
  digest" (block appears before `Tasks:`) plus a `## Human steps` reading
  `None` whose `formatApprovalDigest` output equals the exact pre-change
  baseline.

## Missing context

- None.

## Next

- None; both acceptance lines are covered and the task verify and `pnpm verify`
  pass.

Touched: src/core/spec/digest.ts, src/core/spec/digest-format.ts, tests/digest-before-approval.test.ts
