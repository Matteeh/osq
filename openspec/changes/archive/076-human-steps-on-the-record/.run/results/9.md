## Changed

- `src/core/foundation/init-blocks.ts`: added the `### Before approval` /
  `### After landing` bullet immediately after the `## Human steps` bullet in
  `MANAGED_PLANNER_BLOCK`, and changed the shared-file rule to say `list the
  expected osq retry under `### Before approval` in `## Human steps``.
- `PLANNER.md` and `templates/PLANNER.md`: carry the same two block changes
  between their `OSQ:START`/`OSQ:END` markers, so the managed-block byte
  equality holds.
- `templates/openspec/schemas/osq/schema.yaml` and
  `openspec/schemas/osq/schema.yaml`: the Human steps line now names the two
  subsections, `check`, and `osq verified`; the copies stay byte-identical.
- `tests/human-steps-guidance.test.ts`: asserts the new bullet (and that it
  follows the Human steps bullet), the changed retry line with the old line
  gone, the two files carrying the block, the schema line in both copies, and
  copy equality. It also covers the scenario's `### Before approval`,
  `### After landing`, `check: <command>`, and `osq verified` names.

## Missing context

None.

## Next

None; every acceptance line is implemented and the task and proposal verifies
pass.

Touched: src/core/foundation/init-blocks.ts, PLANNER.md, templates/PLANNER.md, templates/openspec/schemas/osq/schema.yaml, openspec/schemas/osq/schema.yaml, tests/human-steps-guidance.test.ts
