---
title: When a proposal has steps before approval, the approval digest lists them first
verify: node --import tsx --test tests/digest-before-approval.test.ts
verify_starts: red
scope:
  - src/core/spec/digest.ts
  - src/core/spec/digest-format.ts
  - tests/digest-before-approval.test.ts
entry:
  - src/core/spec/digest.ts
  - src/core/spec/human-steps.ts
  - tests/approval-digest.test.ts
skills: []
---
## Acceptance

- [ ] `ApprovalDigest` gains `beforeApproval`, filled by `buildApprovalDigest` with `parseHumanSteps` from task 1's `src/core/spec/human-steps.ts`, and `humanSteps` keeps its current value.
- [ ] `formatApprovalDigest` prints the before-approval block as the spec-lint-and-approve delta's "Digest steps before approval" says, and prints exactly what it prints today when there are no steps before approval.
- [ ] New `tests/digest-before-approval.test.ts` covers "Steps before approval in the digest" through `buildApprovalDigest` on a temporary change, and shows that a proposal whose `## Human steps` reads `None` formats exactly as before.

## Details

`digest.ts` is 246 lines and must stay within 250. If it would not, move
`formatApprovalDigest` into new `src/core/spec/digest-format.ts` and re-export
it from `digest.ts`, so every import keeps working.
