---
title: When a dependency is verification pending or failed, depends_on, the queue and osq plan --next treat it as not landed
verify: node --import tsx --test tests/verification-dependents.test.ts
verify_starts: red
scope:
  - src/core/status/state.ts
  - src/core/status/dependency-readiness.ts
  - src/core/status/queue-state.ts
  - src/core/status/queue-report-detail.ts
  - tests/verification-dependents.test.ts
entry:
  - src/core/status/state.ts
  - src/core/status/queue-state.ts
  - src/core/status/queue-planning.ts
  - src/core/status/verification.ts
  - src/core/status/next-step.ts
  - tests/queue-plan.test.ts
skills: []
---
## Acceptance

- [ ] `isDependencyDone` and the helpers only it needs move from `state.ts` into new `src/core/status/dependency-readiness.ts`, and an archived dependency counts as met only when `readVerification` from task 1 says it is not pending, as "Verification pending dependency" says. `state.ts` imports it and keeps every export.
- [ ] `QueueItemState` gains `verification-pending`. `selectAssociation` derives an archived association as `verification-pending` while its change is pending, and `readQueueState` keeps it out of the landed set, as the modified "Brief queue state projection" says.
- [ ] `queue-report-detail.ts` treats a `verification-pending` row like a `landed` one wherever it reads the archived association.
- [ ] New `tests/verification-dependents.test.ts` covers "Pending dependency": an approved change depending on a pending archived change derives as `blocked`, `readNextStep` reports `blocked`, and after a `passed` event it derives as `pending`. A `failed` outcome keeps it blocked, and an archived dependency without `verification` stays met.
- [ ] The same file covers "Verification pending queue dependency" with `projectQueue` and `prepareQueuePlan`, and shows `formatQueue` printing `[verification-pending]`.

## Details

Write the archived dependency's `.run/events/change.jsonl` in the test, with an
`archived` event carrying `verification: { afterLanding: true, check: null }`.
`tests/queue-plan.test.ts` shows how to write `openspec/queue.md` and briefs
with `queue_item` and `queue_hash`. `queue-planning.ts` needs no change: it
selects by `row.unmetDependencies`. `state.ts` and `queue-state.ts` must stay
within 250 lines.
