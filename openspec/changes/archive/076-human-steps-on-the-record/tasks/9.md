---
title: When a planner writes Human steps, the planner block and the osq schema describe Before approval, After landing, and the check key
verify: node --import tsx --test tests/human-steps-guidance.test.ts
verify_starts: red
scope:
  - src/core/foundation/init-blocks.ts
  - PLANNER.md
  - templates/PLANNER.md
  - templates/openspec/schemas/osq/schema.yaml
  - openspec/schemas/osq/schema.yaml
  - tests/human-steps-guidance.test.ts
entry:
  - src/core/foundation/init-blocks.ts
  - tests/managed-wording.test.ts
  - tests/proposal-format.test.ts
  - tests/doctor.test.ts
skills: []
---
## Acceptance

- [ ] In `MANAGED_PLANNER_BLOCK`, right after the bullet "- Anything a task must not do itself goes under `## Human steps`, which never includes `osq approve`.", add: "- Split `## Human steps` into `### Before approval` and `### After landing`, and write `None` under one with no steps. A step during the run, such as an expected `osq retry`, goes under Before approval. After-landing steps, or `check: <command>` in the proposal frontmatter, keep the change verification pending after it lands, and its dependents wait, until a human runs `osq verified <id> --passed` or `--failed`."
- [ ] In the same block, "list the expected `osq retry` under `## Human steps`" becomes "list the expected `osq retry` under `### Before approval` in `## Human steps`".
- [ ] The Human steps line in both `schema.yaml` copies reads: "- **Human steps**: everything a task must not do itself, never including `osq approve`, under `### Before approval` and `### After landing`; write `None` when there is nothing. After-landing steps or a `check` command keep the change verification pending until `osq verified`." The two copies stay byte-identical.
- [ ] `PLANNER.md` and `templates/PLANNER.md` carry the new block between their `OSQ:START` and `OSQ:END` markers.
- [ ] New `tests/human-steps-guidance.test.ts` asserts the new bullet, the changed retry line, and the schema line, and that both schema copies are equal, which covers "Planner block names the subsections".

## Details

Wrap the block's new prose at the width it already uses; the quoted sentences
are the text, not the line breaks. `tests/managed-wording.test.ts` matches
existing bullets as substrings, and `tests/doctor.test.ts` runs `osq doctor` on
this repository, so the repository copies must equal the constant.
