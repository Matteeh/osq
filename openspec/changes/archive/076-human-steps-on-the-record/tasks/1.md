---
title: When a change folder's state is read, one next-step function names its state and command, and osq status prints it
verify: node --import tsx --test tests/next-step.test.ts
verify_starts: red
scope:
  - src/core/spec/human-steps.ts
  - src/core/status/verification.ts
  - src/core/status/next-step.ts
  - src/core/status/status.ts
  - tests/next-step.test.ts
entry:
  - src/core/status/state.ts
  - src/core/status/status.ts
  - src/core/spec/parser.ts
  - src/core/spec/linter.ts
  - src/core/status/layout.ts
  - src/core/status/inbox.ts
skills: []
---
## Acceptance

- [ ] New `src/core/spec/human-steps.ts` exports `parseHumanSteps(body)` returning `{ beforeApproval, afterLanding }`, `readCheckCommand(frontmatterData)`, and the `VerificationRequirement` type `{ afterLanding: boolean; check: string | null }`, as the spec-lint-and-approve delta's "Human steps sections" says, reusing `extractSection` from `parser.ts`.
- [ ] New `src/core/status/verification.ts` exports `readVerification(folderPath)`, returning `required`, `afterLanding`, `check`, `outcome` (`passed`, `failed`, or null, from the latest `verification_recorded` event), and `checkRanSinceArchive`, plus `listPendingVerifications(archiveDir)`, as "Verification state" in the status-inspection delta says.
- [ ] New `src/core/status/next-step.ts` exports the `NextStepState` and `NextStep` types, `readNextStep(projectRoot, folderPath, config)`, and `formatNextStep(step)` as "Change next step", "Next step commands", and "Next step detail and format" say. A folder directly under the configured archive directory is archived.
- [ ] `readNextStep` decides `unplanned` with `analyzeVerifyCommand` from `linter.ts` and an empty script set, reads the active state through `readChangeFolder` and `deriveSpecState` from `state.ts`, and reads steps before approval with `parseHumanSteps` on the proposal body.
- [ ] `StatusOverview` gains optional `nextSteps`, a record of `NextStep` keyed by active folder name, and optional `pendingVerifications`, a list of `{ folderName, title, next }`. `getStatusOverview` fills both, and `formatStatusOverview` prints them as "Explicit status with next steps" says and prints nothing new when they are absent.
- [ ] New `tests/next-step.test.ts` covers every scenario of "Human steps sections", "Change next step", "Next step commands", "Next step detail and format", "Verification state", and "Explicit status with next steps", with archived changes whose `.run/events/change.jsonl` the test writes itself.

## Details

The event shapes this task reads are written by task 2: the `archived` event's
`data.verification` is a `VerificationRequirement`,
`verification_recorded` carries `data.outcome` and `data.note`, and `check_ran`
carries `data.command` and `data.exitCode`. Read the stream as
`parseEventLines` in `src/core/status/inbox.ts` does, skipping malformed lines.
The latest `archived` event decides whether verification is required.

`StatusOverview` fields must stay optional: `tests/regression-status.test.ts`
builds the overview as a literal. `state.ts` is at 247 lines and belongs to task
3; do not change it. `analyzeVerifyCommand(projectRoot, command, scripts)`
returns `placeholder: true` for the template sentinel.
