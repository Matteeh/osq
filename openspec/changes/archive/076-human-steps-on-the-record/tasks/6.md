---
title: When osq plan hands off a change or osq approve refuses one, it prints the change's next step
verify: node --import tsx --test tests/plan-approve-next-step.test.ts
verify_starts: red
scope:
  - src/cli/plan-queue.ts
  - src/cli/plan.ts
  - src/cli/approve.ts
  - tests/plan-approve-next-step.test.ts
entry:
  - src/cli/plan-queue.ts
  - src/cli/plan.ts
  - src/cli/approve.ts
  - src/core/status/next-step.ts
  - tests/plan-handoff.test.ts
  - tests/approve-confirm.test.ts
skills: []
---
## Acceptance

- [ ] `writePromptHandoff` takes the formatted next step, and its one printed line ends with ` — next: <next step>`, as the cli-foundation delta's "Plan handoff next step" says. `planCommand` computes it with `readNextStep` and `formatNextStep` from task 1.
- [ ] The catch in `approveCommand` prints `Next: <next step>` after the error when the change folder exists, as "Approve refusal next step" says, and prints nothing more when it does not.
- [ ] New `tests/plan-approve-next-step.test.ts` covers "Fresh handoff" through `planCommand` without a session and asserts the output is still one line, and covers "Approving a template" through `approveCommand` with `process.exit` stubbed.

## Details

`tests/plan-handoff.test.ts` and `tests/queue-plan.test.ts` assert exactly one
handoff line containing `ask your planning tool to plan change <slug>`; keep
both true. Find the folder for the refusal with `findSpecFolder` from
`src/core/spec/approve.ts`, ignoring its error.
