---
title: When osq show runs, it prints the change's next step and its verification history
verify: node --import tsx --test tests/show-next-step.test.ts
verify_starts: red
scope:
  - src/core/status/show.ts
  - src/cli/show.ts
  - tests/show-next-step.test.ts
entry:
  - src/core/status/show.ts
  - src/cli/show.ts
  - src/core/status/next-step.ts
  - src/core/status/verification.ts
  - tests/show.test.ts
skills: []
---
## Acceptance

- [ ] `SpecDetails` gains optional `next`, filled by `getSpecDetails` with `readNextStep` from task 1 for active and archived changes and left out for rejected ones, so `osq show --json` carries it.
- [ ] `formatSpecDetails` prints `Next: <formatNextStep>` right after the `Status:` line when `next` is set, and the `Verification:` section the status-inspection delta's "Show next step" describes for an archived change that requires verification.
- [ ] New `tests/show-next-step.test.ts` covers "Archived change with an outcome", a failed outcome, a `check_ran` row, an active unplanned change, and `--json` carrying `next`, writing the archived events itself.

## Details

The event shapes are in task 1's `src/core/status/verification.ts`. Rejected
changes print as today. `show.ts` is on the line-budget allow list; keep new
functions within 80 lines.
