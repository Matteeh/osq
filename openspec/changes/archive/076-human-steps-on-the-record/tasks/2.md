---
title: When a change archives with after-landing steps or a check, the archived event records it, and osq check and osq verified append their events
verify: node --import tsx --test tests/verification-record.test.ts
verify_starts: red
scope:
  - src/watcher/archiver.ts
  - src/core/lifecycle/verification-record.ts
  - src/cli/check.ts
  - src/cli/verified.ts
  - src/cli/index.ts
  - tests/verification-record.test.ts
entry:
  - src/watcher/archiver.ts
  - src/core/lifecycle/reject.ts
  - src/cli/reject.ts
  - src/core/run/verification.ts
  - src/core/status/show.ts
  - src/core/spec/human-steps.ts
  - src/core/status/next-step.ts
skills: []
---
## Acceptance

- [ ] `archiveSpecFolder` reads the proposal before the move with `parseHumanSteps` and `readCheckCommand` from task 1's `src/core/spec/human-steps.ts`, and the `archived` event carries `verification` as the watcher-and-harness delta's "Archived verification requirement" says, typed as `ArchivedEventData & { verification?: VerificationRequirement }` without changing `src/harness/types.ts`.
- [ ] `src/core/lifecycle/verification-record.ts` exports the `check_ran` and `verification_recorded` data types, as "Human verification events" says.
- [ ] New `src/core/lifecycle/verification-record.ts` exports `recordVerification(projectRoot, id, outcome, note, config)` and `runCheck(projectRoot, id, config)`. They find the change with `resolveSpecFolder` from `src/core/status/show.ts`, read its requirement with `readVerification`, refuse as the cli-foundation delta's "Check command" and "Verified command" say, and append straight to `.run/events/change.jsonl`, as `appendRejectedEvent` in `reject.ts` does.
- [ ] `runCheck` runs the command with `runVerificationCommand` and `config.timeouts.verifyTimeoutSeconds` from the project root and records `command`, `exitCode`, `duration`, `timedOut`, and `output`.
- [ ] New `src/cli/check.ts` and `src/cli/verified.ts` follow `src/cli/reject.ts`, print the result and `Next: <formatNextStep>`, and exit as the two requirements say. `src/cli/index.ts` registers `check <id>` and `verified <id>` with `--passed`, `--failed`, and `--note <text>`.
- [ ] New `tests/verification-record.test.ts` archives temporary changes with `archiveSpecFolder` and covers every scenario of "Archived verification requirement", "Human verification events", "Check command", and "Verified command", including that a refusal appends nothing.

## Details

Use a local `node check.cjs` backed by a file in the test's own temp project for
the check command, never the network or this repository's suite. Stub
`process.exit` in the CLI tests as `tests/approve-confirm.test.ts` does.
`archiver.ts` is 195 lines; keep it within 250. Do not change `loop.ts`; the
watcher's `Human steps after completion` print stays as it is.
