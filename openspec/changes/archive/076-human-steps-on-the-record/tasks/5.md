---
title: When the inbox lists changes, it shows unplanned templates as needing planning, flags steps before approval, and lists pending and failed verification
verify: node --import tsx --test tests/inbox-next-step.test.ts
verify_starts: red
scope:
  - src/core/status/inbox.ts
  - src/core/status/inbox-text.ts
  - src/core/status/inbox-projection.ts
  - packages/ui/src/home/labels.ts
  - tests/inbox.test.ts
  - tests/inbox-next-step.test.ts
entry:
  - src/core/status/inbox.ts
  - src/core/status/status.ts
  - src/core/status/next-step.ts
  - src/core/status/verification.ts
  - tests/inbox-stuck.test.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] `NeedsYouKind` gains `planning`, `verification-pending`, and `verification-failed`, and `NeedsYouItem` gains optional `beforeApproval: true`, as the modified "Stable inbox object" says.
- [ ] `projectNeedsYou` reads `overview.nextSteps` from task 1 and emits a `planning` item or an approval item with `beforeApproval` as "Planning inbox items" says. A change missing from `nextSteps` keeps today's approval item.
- [ ] `readInbox` adds one item per entry of `overview.pendingVerifications`, after the active items, as "Verification inbox items" says.
- [ ] `formatInboxText` renders the new lines as those two requirements say, each ending with the item's command.
- [ ] `packages/ui/src/home/labels.ts` labels the new kinds as the web-inspection delta's "Inbox kind labels" says.
- [ ] New `tests/inbox-next-step.test.ts` covers the scenarios of "Planning inbox items", "Verification inbox items", "Steps before approval", and "Planning label" through `readInbox` and `formatInboxText` on temporary projects, and asserts the JSON key order of each new item.
- [ ] In `tests/inbox.test.ts`, only the expected kinds and commands change where a fixture change keeps the placeholder verify, so it now expects a `planning` item with `osq plan <id>` or `osq lint <id>`.

## Details

New item fields appear only on the items that have them, because
`tests/inbox.test.ts` and `tests/inbox-stuck.test.ts` pin the key order
`kind, change, task, command`. Import `needsYouKindLabel` from
`packages/ui/src/home/labels.ts` directly in the new test; its only import is a
type. Run `pnpm typecheck:ui` as well as the tests.
