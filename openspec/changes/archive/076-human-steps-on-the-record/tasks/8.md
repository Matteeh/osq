---
title: When osq report runs, it counts after-landing checks by outcome and the pending ones
verify: node --import tsx --test tests/report-verification.test.ts
verify_starts: red
scope:
  - src/core/report/report.ts
  - src/core/report/record-verification.ts
  - tests/report-verification.test.ts
entry:
  - src/core/report/report.ts
  - src/core/report/record-rework.ts
  - src/core/status/verification.ts
  - tests/report-record-extras.test.ts
  - tests/report-scope-regressions.test.ts
skills: []
---
## Acceptance

- [ ] New `src/core/report/record-verification.ts` counts archived changes that require verification by latest outcome with `readVerification` from task 1, and formats the text line, as the metrics-and-reporting delta's "After-landing verification counts" says.
- [ ] The report's history carries optional `verification`, and the text and JSON show it only when some archived change requires verification.
- [ ] New `tests/report-verification.test.ts` covers "Mixed outcomes" and "No verification required" through `reportCommand` in text and `json`.

## Details

Follow how `record-rework.ts` feeds `collectRework` and `formatRework` into
`report.ts`, including how `toStableMetrics` leaves out empty fields.
`tests/report-scope-regressions.test.ts` pins the `history` key set for a
project with no verification, so the key must be absent there, and
`fixture/report/expected.json` must not change.
