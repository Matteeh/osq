---
title: When a planning prompt is built, the recent repository record follows the brief
verify: node --import tsx --test tests/plan.test.ts tests/codex/plan.test.ts
scope:
  - src/cli/plan.ts
  - tests/plan.test.ts
entry:
  - src/cli/plan.ts
  - src/core/report.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] `buildOpeningPrompt` returns five strict sections in order: complete PLANNER.md, change id/title, capability spec paths, complete brief, and `## This repository's record`.
- [ ] The fifth section calls task 1's shared report derivation and formatter with the loaded config and the 20 most recent canonical archives; no event parsing or outcome aggregation is duplicated in the planning CLI.
- [ ] With at least five measured archived tasks, interactive, resumed, queue-selected, and printed prompts show first-attempt pass rate, at most ten dead lines with change/task title/reason, the largest pass with scope and acceptance sizes, and median task duration after the brief.
- [ ] With fewer than five measured archived tasks, the section contains the too-small sentence and no pass-rate, dead, largest, duration, result, or diff line.
- [ ] `--print` writes the same five-section prompt exclusively to stdout without spawning a harness or appending planning telemetry, while interactive planning passes those exact bytes to the existing adapter path.
- [ ] Existing brief creation, manifest writing, planner selection, queue context from dependency 039, resume behavior, capability ordering, and planning lifecycle semantics remain unchanged.

## Details

Keep `src/cli/plan.ts` thin. Extend `buildOpeningPrompt` with the loaded config or
a preformatted record body, and call the shared repository-record functions
created by task 1 rather than importing parser details or reading task streams
directly. Construct the heading exactly as `## This repository's record` and
append it after `briefContent.trim()` for every path through `planCommand`.

The recent archive scan is read-only and happens after ordinary or queue-driven
change resolution but before print or interactive dispatch. It must not include
the new active change, rejected folders, or active changes. Do not cache the
record in `.run`, the manifest, or memory across invocations; it is a derived
view of current archived files.

Reuse `buildOpeningPrompt`, `planCommand`, `loadConfig`, and task 1's exported
repository-record derivation and formatter. Extend `tests/plan.test.ts` with a
temporary canonical archive copied or built from the checked-in measured
fixture, covering both sufficient and too-small histories and proving strict
section ordering through the direct command and `-print` CLI alias. Keep
`tests/codex/plan.test.ts` frozen as a regression check and preserve the
post-039 `plan --next` context. Run the task verify and `pnpm verify` before
exit.
