# Task 2 result — repository record in the planning prompt

Status: complete. Task verify `node --import tsx --test tests/plan.test.ts
tests/codex/plan.test.ts` passes (19 tests). `pnpm verify` passes
(`pnpm tsc --noEmit`, 988 tests, `biome check src tests`).

## Changed

- `src/cli/plan.ts`
  - Imported `getRepositoryRecord`/`formatRepositoryRecordBody` from
    `../core/report.js` and the base composer as
    `buildOpeningPrompt as buildBaseOpeningPrompt` from `./plan-queue.js`.
  - Added `REPOSITORY_RECORD_HEADING = "## This repository's record"` and
    `OpeningPromptOptions`, and exported a new `buildOpeningPrompt` wrapper that
    appends the fifth section to the base four-section prompt. It calls task 1's
    shared derivation/formatter with the passed `config` (falling back to
    `loadConfig(projectRoot)` for direct callers) and an optional
    preformatted `recordBody`, so no event parsing or outcome aggregation is
    duplicated in the CLI.
  - `planCommand` now passes its already-loaded `config` into
    `buildOpeningPrompt`; all ordinary, resumed, queue-selected, interactive,
    and print paths share the resulting bytes.
  - Kept `export { formatBriefContent } from './plan-queue.js'`; `plan-queue.ts`
    was not modified (out of scope).
- `tests/plan.test.ts`
  - Imports `buildOpeningPrompt` directly and adds `SIZES_FIXTURE`,
    `RECORD_HEADING`, `copyMeasuredArchive`, `recordSection`, and
    `captureStdout` helpers.
  - New tests: five ordered sections with the sufficient record after the brief
    (reusing the checked-in `tests/fixtures/report-sizes` archive), the
    too-small-only section, one identical prompt for interactive/resumed/print
    with no telemetry appended by print, the `-print` CLI alias carrying the
    record, and a queue-selected `plan --next` prompt carrying the record.

## Deviations

- `buildOpeningPrompt` is a thin wrapper in `src/cli/plan.ts` because
  `buildOpeningPrompt` lives in `src/cli/plan-queue.ts`, which is outside this
  task's scope. The wrapper adds the heading and fifth section only; the base
  four-section composer is unchanged.
- For direct `buildOpeningPrompt` calls that omit `config`, the wrapper loads
  config from `projectRoot`; `planCommand` always passes the config it already
  loaded.

## Drift against features/

None. The referenced capability docs are the living OpenSpec specs
(`cli-foundation`, `metrics-and-reporting`); no retired `features/` paths were
read or introduced. Existing brief creation, manifest writing, planner
selection, dependency-039 queue context, resume behavior, capability ordering,
and append-only planning telemetry are untouched and covered by the existing
tests.

## Missing context

None. Task 1's `getRepositoryRecord`, `formatRepositoryRecordBody`, and the
`tests/fixtures/report-sizes` archive were present in the working tree as
described in `.run/results/1.md`.

## Next steps

None for task 2; all acceptance lines are implemented and verified.
