---
title: When a consumer configures Codex, documentation and generated examples explain the supported workflow
verify: node --import tsx --test tests/codex-guidance.test.ts tests/init.test.ts tests/managed-blocks.test.ts tests/setup-block-coexistence.test.ts tests/line-budget.test.ts
scope:
  - README.md
  - .env.example
  - src/core/init.ts
  - tests/codex-guidance.test.ts
entry:
  - README.md
  - src/core/init.ts
tests:
  modify: false
skills: []
---
## Acceptance

- [ ] README lists codex and provides executor and independent planner examples with optional bin/model/effort, correct precedence/default behavior, and a clear explanation that Codex planner.agent is unsupported.
- [ ] README covers installing/authenticating Codex in osq's host environment, shared setup without generated Codex files, doctor/preflight failures, executor workspace-write/never permissions, planner on-request permissions, and scope as a protocol rather than hard confinement.
- [ ] README describes fresh sessions, watcher-owned verification/result synthesis, observed-only token/cost accounting, and an optional live smoke procedure separate from offline checks without claiming an untested minimum CLI version.
- [ ] scaffoldProject generates commented Codex selection/binary/model guidance while retaining the active default harness; repository .env.example mirrors it without secrets, and repeated scaffolding preserves a consumer-edited example.

## Details

Make examples match the approved contract and integration. Do not change this repository's live osq.config.ts, select Codex in active environment settings, run a real model, or approve a change. Model identifiers in examples must be clearly user-supplied placeholders or omitted for native defaults, not recommendations inferred from memory.

Reuse scaffoldProject, DEFAULT_ENV_EXAMPLE, and the existing preservation behavior in `src/core/init.ts`. Retain managed AGENTS/PLANNER instructions because Codex uses the same protocol. Retain `OSQ_HARNESS=agy` as the active example default and show Codex selection as a comment, with no credentials. Explain that an explicit harness in osq.config.ts wins over the environment fallback.

New `tests/codex-guidance.test.ts` exercises scaffoldProject in a disposable fixture, checks the generated example, and verifies a repeat scaffold preserves consumer edits. This is a behavioral scaffold test, not a test matching every README sentence. Existing tests are read-only regression checks. Review README examples against task 1 behavior, retain honest limits on scope/network confinement and observed costs, and stay within the source line budget.
