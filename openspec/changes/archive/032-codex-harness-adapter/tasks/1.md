---
title: When Codex is selected, osq supports setup, verified execution, and interactive planning
verify: node --import tsx --test tests/codex/*.test.ts tests/line-budget.test.ts tests/import-graph.test.ts
scope:
  - src/harness/**
  - src/core/config*.ts
  - src/core/doctor.ts
  - src/core/manifest.ts
  - src/watcher/**
  - src/cli/plan.ts
  - src/index.ts
  - tests/codex/**
entry:
  - src/harness/index.ts
  - src/core/config.ts
  - src/core/doctor.ts
  - src/watcher/loop.ts
  - src/cli/plan.ts
tests:
  modify: false
skills: []
---
## Acceptance

- [ ] Public configuration and loadConfig support CodexConfig bin/model/effort with proposal precedence, register codex, reject invalid settings and unsupported Codex planner.agent, retain the required planner.model, and provide backwards-compatible centrally defaulted preflight/grace timeouts.
- [ ] Real setup and doctor entry points work with the registered adapter; repeated and mixed-harness setup preserves consumer files and foreign blocks without creating Codex files; doctor and watcher preflight probe the same fake binary and fail before task spawn on missing, nonzero, or timed-out probes.
- [ ] The watcher invokes a fresh fake Codex process through the registered adapter with correct cwd, literal argv, JSON mode, workspace-write/never permissions, disabled web search and shell network, model/effort settings, full task context, capability rules, and configured deadlines; process outcomes preserve PID, signal, elapsed time, and diagnostics.
- [ ] Chunked JSONL fixtures produce ordered completed text/tool/file_changed/tokens observations with correct usage subsets, absent cost, relative paths, no duplicated file counts or lifecycle events, tolerance of malformed/unknown records, a flushed final unterminated record, and terminal turn.failed handling even with exit zero.
- [ ] Approved fixture tasks through the watcher/runner preserve supplied results, synthesize missing results from final text, fail with no_result without either, fail with verify_red on independent verify failure, and reach done only after watcher verification; reportCommand JSON consumes normalized usage/file events without double counting.
- [ ] Approval manifests and started events attribute the Codex executor model or default sentinel correctly, and the manifest records configured effort/null; planCommand uses the actual planner harness/model in both brief and invocation for Codex-only and mixed-harness cases while retaining existing planner-manifest semantics.
- [ ] planCommand with the registered adapter launches the fake interactive executable with inherited stdio, existing prompt order, correct cwd/model, workspace-write/on-request permissions, and native effort defaults; existing-change and print paths remain correct and nonzero/signal/spawn failures propagate without breaking existing harness behavior or line/import limits.

## Details

Implement the proposal and two named deltas. Scope permits complete wiring; it is not permission for unrelated refactors. All existing tests are frozen. New test files and executable helpers belong under `tests/codex/`; use disposable copies of `fixture/` as work directories. Write tests for acceptance behavior before implementation.

Reuse getHarnessAdapter and the existing HarnessAdapter port. Codex setup is a no-op because setupCommand already maintains shared instructions and invokes both selected adapters. spawnInteractive and preflight already exist: add no methods. Split Codex prompt/stream/process concerns into small harness modules to meet the line budget. Shared binary/model/config helpers belong in `src/core/config*.ts`, so doctor and manifest never import harness or CLI. Preserve existing harness behavior and precedence.

Reuse spawnWithTimeout, EventStreamParser, asRecord, resolveEventTimestamp, appendHarnessEvent, and relativizeToolSummary. Supply the prompt as one literal argument, including paths/text containing spaces, quotes, and shell metacharacters. Put global approval flags before exec and correctly quote string-valued TOML overrides. Do not copy OpenCode's --agent, --file, or --auto flags. Native Codex reads paths named in the prompt. Supply OSQ_TASK_NUMBER and OSQ_SPEC_FOLDER consistently with other adapters.

Use resolveCapabilityRules and capabilityRuleLines for injected rules. Resolve task/proposal/delta/living/result paths from actual options and configured OpenSpec paths. Include the one-attempt procedure: read prior result, start from task verify, test acceptance, edit scope only, write the result, exit. Do not write markers, checkboxes, living specs, or verification events in the adapter.

Translate completed assistant messages to non-empty text, completed command/MCP items to tool summaries, and successful completed file-change paths to file_changed only. Avoid also emitting edit/write observations that duplicate report counts. Do not treat reasoning text as results. Fixtures should represent the official JSONL structure, not an invented generic schema. Usage comes once per completed turn; cache and reasoning counts are subsets, not additions. A turn.failed is terminal even with process exit zero; a recoverable error followed by success is not. Preserve failure diagnostics in SpawnResult. Flush before returning so result synthesis sees final text. Only the watcher emits started/exited/done/dead.

Watcher startup currently gates preflight on OpenCode: extend it to invoke Codex's optional preflight port, retaining legacy OpenCode fallback. Doctor and adapter must resolve the same binary. Add optional preflight/grace fields with defaults centrally matching current probe and kill-grace values; old timeout object literals must remain type-compatible. Use existing process PID/signal/elapsed/onSpawn handling.

Fix model attribution in `src/watcher/spawn.ts` and `src/core/manifest.ts` as well as configuration. In planCommand, fix both model selection sites: brief creation and interactive launch. Explicit planner.model wins. When planning falls back to a Codex executor with no selected model, use default for brief metadata but omit the CLI model flag. Explicit planners do not inherit executor OSQ_MODEL or effort. Keep manifest.planner as explicit planner.model or null. Defensively reject unsupported interactive agent arguments as well as invalid planner config.

Tests must exercise setupCommand, doctorCommand/runDoctorChecks, watcher startup and runTask, buildManifest, planCommand, and reportCommand with the registered adapter instead of validating only builders. Read existing tests to reuse their fixture patterns and the fake-validator helper without editing them. Disposable fixture approvals may use existing test patterns; never approve or write markers for this real change. The fake binary supports --version, exec, and interactive invocation, captures argv/cwd, emits deterministic chunked JSONL, and simulates exits/timeouts without a model/network/TTY. Inherited stdio can be checked from an outer subprocess using pipes, not a real terminal.

Run the task verify and `pnpm verify` before exit. Record exact pre-existing failures rather than modifying frozen tests or unrelated code. This task contains all shared implementation edits; task 2's scope is disjoint.
