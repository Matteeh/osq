# Task 2 result: Codex consumer guidance

## Changed

- `README.md`
  - Added `codex` to the available-adapters list.
  - Added a "Codex CLI" section under Harnesses: executor config with optional
    `bin`/`model`/`effort`, binary/model/effort precedence and native-default
    behavior, `default` attribution when no model is set, an independent planner
    example, and a clear statement that `planner.agent` is unsupported and
    rejected by validation. Also notes an explicit `osq.config.ts` harness wins
    over the `OSQ_HARNESS` fallback and that an explicit planner never inherits
    executor model/effort.
  - Added sub-sections for setup/authentication prerequisites (installed and
    authenticated in osq's host environment, native auth reused, no generated
    Codex files, shared managed AGENTS block preserving foreign blocks),
    doctor/preflight `--version` probing and failure-before-spawn, executor
    `never`/`workspace-write`/web-search-off/shell-network-off permissions,
    planner `on-request` permissions with native effort, and scope as a protocol
    rather than hard confinement.
  - Added sub-sections for fresh sessions, watcher-owned verification and result
    synthesis, observed-only token/cost accounting, and an optional human-owned
    live smoke procedure explicitly separate from offline checks and stating the
    offline suite does not establish a minimum supported Codex release.

- `src/core/init.ts`
  - `DEFAULT_ENV_EXAMPLE` now keeps `OSQ_HARNESS=agy` as the active default and
    adds commented Codex selection/binary/model/effort guidance. No credentials
    or recommended model identifiers; model is a `<your-codex-model>` placeholder.
  - File is 226 lines, within the 250-line budget.

- `.env.example`
  - Mirrored byte-for-byte from `DEFAULT_ENV_EXAMPLE`.

- `tests/codex-guidance.test.ts` (new)
  - Behavioral scaffold tests in a disposable tmp fixture: agy stays the active
    default, Codex selection/binary/model guidance is present as comments, no
    credential-like values, repository `.env.example` matches the generated file,
    and a consumer-edited example survives repeated scaffolding.
  - Light README checks confirming the required guidance sections exist.

## Deviated

None. `.env.example` and `README.md` are in the declared scope; `src/core/init.ts`
change is limited to `DEFAULT_ENV_EXAMPLE` with existing scaffolding/preservation
behavior reused unchanged.

## Drift against features/

None. The `cli-foundation` consumer-guidance requirement (selection, independent
planner, optional model/effort, precedence, setup/authentication, permissions,
diagnostics, fresh sessions, watcher verification, observed-only metrics) is
satisfied. No living spec was edited, and no retired paths were introduced into
the README.

## Missing context

None blocking. The real Codex CLI is unavailable in this environment, so the
live smoke procedure is documented for a human rather than executed; the task
explicitly requires that separation.

## Verification

- Task verify: `node --import tsx --test tests/codex-guidance.test.ts tests/init.test.ts tests/managed-blocks.test.ts tests/setup-block-coexistence.test.ts tests/line-budget.test.ts` -> 30 tests, 0 fail.
- `pnpm tsc --noEmit` -> clean; `pnpm lint` -> 156 files, no errors.
- Extra regression checks: `tests/changelog-release-docs.test.ts`, `tests/package-hygiene.test.ts`, `tests/init-planner.test.ts` -> 18 tests, 0 fail.

## Next steps

All acceptance lines are complete. Remaining human step is the optional live
Codex smoke check described in the proposal; no code follow-up required.
