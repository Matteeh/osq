# Task 1 Result

## Changed

- `src/core/parser.ts`
  - Added exported `parseCodeOwnership(content: string): string[]`. Locates `### Requirement: Code ownership` by header name, bounds the requirement block at the next level-3/level-2 heading (scenario subheadings stay in-block), and returns the comma-separated path globs from the `<!-- source: ... -->` comment. Falls back to backtick-quoted path-like tokens (`/`, `\`, or `*`) when no source comment exists. Returns `[]` when the header is absent.
  - Added `readTestsModify(data)` helper and `readonly testsModify: boolean` to `TaskData`.
  - `parseTaskMd` now reads `tests.modify` from nested `tests: { modify: boolean }` first, then the flat `tests.modify` key, defaulting to `false`. Non-boolean values yield `false`.
- `templates/proposal.md`
  - Documented the Code ownership requirement structure: header, `<!-- source: ... -->` glob list, and `#### Scenario: Codebase ownership boundaries`, with an example block.
- `tests/parser.test.ts`
  - New `Code ownership parsing` suite (delta spec, living markdown, absent header).
  - `testsModify` cases: omitted default, nested true/false, flat dotted key, non-boolean ignored.
  - Template test asserts `### Requirement: Code ownership` is documented and that `parseCodeOwnership(template)` returns the example globs.

## Verify

`node --import tsx --test tests/parser.test.ts` → 19 pass / 0 fail. `pnpm tsc --noEmit` and `biome check` on the touched files are clean.

## Deviated

- Acceptance line 5 ("accepts tests.modify as boolean or nested") is implemented as flat key `tests.modify: true` plus nested `tests: { modify: true }`. `parseTaskMd` does not surface non-boolean values; invalid types collapse to `false` so schema rejection remains task 2's (linter) responsibility via raw frontmatter.

## Drift against features/

The spec's `features.reads` docs (`features/*.md`) have been migrated; the in-repo references are now `openspec/specs/<capability>.md`. Used those plus the 017 delta specs as the normative source. No behavioral drift.

## Next

Task 2: linter `tests.modify` boolean validation and scope ownership boundary checks.
