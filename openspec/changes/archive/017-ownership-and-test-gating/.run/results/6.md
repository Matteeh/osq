# Task 6 Result

## Changed

- `tests/delta-merge.test.ts`
  - Added a `Baseline living capability code ownership` suite with four tests:
    1. Parses all five capability delta specs (`cli-foundation`,
       `metrics-and-reporting`, `spec-lint-and-approve`, `status-inspection`,
       `watcher-and-harness`) without syntax errors; asserts each declares an
       ADDED `Code ownership` requirement with a `<!-- source: -->` comment and
       the `Codebase ownership boundaries` scenario, and that every ADDED
       requirement round-trips through `parseRequirement`.
    2. Asserts `mergeDelta` cleanly appends `### Requirement: Code ownership`
       to a base spec that already has requirements, preserving the existing
       purpose and requirement order.
    3. Merges each real delta into its real living base spec twice, asserts
       byte-for-byte determinism, emptiness of syntax drift, the declared
       ownership globs, and that the merged ownership block matches the delta
       source byte-for-byte.
    4. Copies the five delta specs and the five living base specs into a temp
       OpenSpec root and runs `applyOpenSpecDeltas`, asserting every resulting
       living capability spec contains the `Code ownership` requirement with its
       declared globs.
  - Added `resolveDeltaRoot()`, which locates the 017 delta specs in the active
    change folder or, once archived, under `openspec/changes/archive/`, so the
    suite stays green after the watcher archives this change.

- `src/core/delta.ts` needed no change. The existing merge engine already
  parses and merges all five deltas into valid capability specs.

## Verify

`node --import tsx --test tests/delta-merge.test.ts` -> 15 pass / 0 fail.
`pnpm tsc --noEmit` clean. `biome check tests/delta-merge.test.ts` clean.

## Drift against features/

The repository `features/` directory is empty; living docs live under
`openspec/specs/`. The on-disk `openspec/specs/<capability>/spec.md` files still
contain migrated legacy prose (no `## Purpose` / `## Requirements`); the code
ownership blocks are written when the watcher archives 017 via
`applyOpenSpecDeltas`. Task 6's tests prove that merge path produces specs with a
`Code ownership` requirement and the declared globs. No behavioral drift.

## Missing context

- The first OpenSpec delta application normalizes each living spec: it takes the
  delta `## Purpose` and rebuilds `## Requirements` from the ADDED blocks. The
  migrated legacy prose in `openspec/specs/<capability>/spec.md` is therefore
  replaced, and the parallel flat `openspec/specs/<capability>.md` copies remain
  stale. This is existing archiver behavior, outside this task's scope.
- `mergeDelta` does not de-duplicate ADDED requirements, so re-applying the same
  delta to an already-merged spec duplicates the block. Normal archive flow
  applies once (and moves the folder), so this is not exercised; noted for
  follow-up if archive retries become possible.

## Next

None. Task 6 is the final task; on completion the watcher's archive step applies
the five deltas and updates the living capability specs.
