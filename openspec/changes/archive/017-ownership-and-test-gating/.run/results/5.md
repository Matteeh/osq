# Task 5 Result

## Changed

- `src/core/state.ts`
  - Added exported `deriveTaskStatus` alias for `deriveTaskState`. It extracts
    the dead reason (including `undeclared_test_change`) from
    `.run/dead/<n>.md` frontmatter, satisfying the acceptance name without
    changing existing callers.
- `src/core/status.ts`
  - Renamed the internal `formatTaskLine` to exported `formatStatusLine`;
    `formatStatusOverview` now calls it. It renders
    `[dead] (reason: undeclared_test_change)` for dead tasks.
- `src/core/show.ts`
  - Added exported `formatShowOutput` alias for `formatSpecDetails`. It renders
    the dead reason plus the `Failure diagnostic:` block that lists the
    modified/deleted test files.
- `src/core/report.ts`
  - Added exported `generateReport` alias for `getMetricsReport`, which
    aggregates every `dead` reason (including `undeclared_test_change`) into
    `failureBreakdown`.
- `tests/show.test.ts`
  - Reworked all fixtures to create change folders under the configured
    `openspec/changes` root (copying `specs/_template`) instead of the
    pre-migration `specs/` root.
  - Added a test that: derives `deadReason === 'undeclared_test_change'` via
    `deriveTaskStatus`, asserts the status line contains
    `(reason: undeclared_test_change)`, and asserts
    `formatShowOutput` prints `undeclared_test_change`, `Failure diagnostic:`,
    and the offending `tests/show.test.ts (modified)` path.
- `tests/report.test.ts`
  - Same `openspec/changes` fixture migration, for both active and archived
    folders.
  - Added a test asserting `generateReport(...).failureBreakdown
    .undeclared_test_change === 1`, that `formatMetricsReport` prints
    `undeclared_test_change: 1`, and that `reportCommand --json` includes the
    reason under `failureBreakdown`.

## Verify

`node --import tsx --test tests/show.test.ts tests/report.test.ts` → 12 pass /
0 fail. `pnpm tsc --noEmit` clean. `biome check` on the six touched files clean.

## Deviated

- Added thin aliases (`deriveTaskStatus`, `formatShowOutput`, `generateReport`)
  plus an export rename (`formatStatusLine`) because the acceptance lines name
  those symbols and none existed. Behavior is unchanged and existing exports
  (`deriveTaskState`, `formatSpecDetails`, `getMetricsReport`) are preserved.
- Tests construct change folders directly under `openspec/changes` rather than
  calling `createNewSpec`, which still defaults to `specs/` while
  `DEFAULT_CONFIG.paths.specs` is `openspec/changes` (016 migration fallout).
  This keeps the task's verify green without editing out-of-scope files.
- CLI calls in tests pass `config: DEFAULT_CONFIG` explicitly so assertions do
  not depend on `loadConfig` resolving `@matteeh/osq` inside a temp project.

## Drift against features/

The repository `features/` directory is empty; the 016 cut-over moved living
docs to `openspec/specs/`. Read the migrated specs (`cli-foundation`,
`metrics-and-reporting`, `spec-lint-and-approve`, `status-inspection`,
`watcher-and-harness`) plus the 017 delta specs. The `status-inspection` delta
"Undeclared test change status inspection" and the `metrics-and-reporting`
delta "Undeclared test change failure metrics" requirements and all their
scenarios are now satisfied by code and tests. No behavioral drift.

## Missing context

- The repo-wide suite still has pre-existing 016 migration failures in
  `state.test.ts` / `status.test.ts` and the `runner-*.test.ts` files, which
  build fixtures under `specs/`. Not caused by and not fixed in this task.
- `Create a new spec` still defaults to `specs/`; that mismatch is the source
  of the above and remains out of scope for 017 task 5.

## Next

Task 6: baseline living capability specs declare `### Requirement: Code
ownership` blocks.
