# Result for Task 3

## Changed
- `src/core/hasher.ts`: Implemented `hashChangeFolder` to compute a deterministic SHA-256 digest of change folder files ignoring `.run/`, normalizing CRLF, and sorting relative file paths. Added `verifyFolderHash`.
- `tests/hasher.test.ts`: Added unit tests verifying determinism, `.run` isolation, content tampering detection, and cross-platform line ending normalization.
