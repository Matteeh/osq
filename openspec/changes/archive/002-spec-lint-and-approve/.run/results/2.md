# Result for Task 2

## Changed
- `src/core/linter.ts`: Implemented `lintChangeFolder` enforcing the specification rules from `osq.config.ts`: max feature writes, single contract table, non-empty delta on feature writes, dependency existence in `specs/` or `specs/archive/`, single verify command without chaining, and acceptance line limit.
- `tests/linter.test.ts`: Added unit tests covering compliant specs and all reject/warn conditions.
