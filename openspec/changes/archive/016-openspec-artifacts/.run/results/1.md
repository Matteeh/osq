# Task 1 result: OpenSpec paths and options typed and defaulted

## Changed

- `src/core/config.ts`
  - Added `openspecRoot` to `OsqPaths`.
  - Added `OpenSpecConfig` (`bin?`, `schema?`) and wired `openspec?` into `OsqConfig` and `OsqUserConfig`.
  - `DEFAULT_CONFIG`: `paths.openspecRoot = 'openspec'`; `openspec = { bin: 'openspec', schema: 'osq' }`.
  - `defineConfig` deep-merges `openspec`, so `loadConfig` resolves custom paths and optional OpenSpec settings.
- `package.json`
  - `devDependencies["@fission-ai/openspec"] = "1.13.1"` (exact pin).
  - `peerDependencies["@fission-ai/openspec"] = ">=1.13.1 <2"`.
  - `peerDependenciesMeta["@fission-ai/openspec"].optional = true` (see Deviations).
- `tests/config.test.ts`
  - Added tests for the devDependency pin, peer range, default `openspecRoot`/`openspec` values, partial OpenSpec merge, and `loadConfig` custom paths + OpenSpec settings resolution.

## Acceptance mapping

- devDependencies exact 1.13.1: test + manifest.
- peerDependencies `>=1.13.1 <2`: test + manifest.
- `OsqPaths` includes specs, archive, features, decisions, templates, openspecRoot: interface + default test.
- Defaults preserve specs, specs/archive, features, decisions, templates: existing assertions retained, `openspecRoot` added.
- `loadConfig` resolves custom paths and optional OpenSpec settings: new test writes `osq.config.ts` with custom `paths` and `openspec`.
- Strict compile without shims: `pnpm tsc --noEmit` clean; no backward-compat aliases added.

## Deviations

- Added `peerDependenciesMeta` marking `@fission-ai/openspec` optional. Required to keep `tests/package-install-smoke.test.ts` green: it installs the packed tarball with `pnpm add --offline` and a stub registry, and a non-optional peer fails resolution (`Failed to resolve @fission-ai/openspec@>=1.13.1 <2 in package mirror`). The peer range itself is still declared as required by the spec.
- Chose `openspec.bin` and `openspec.schema` as the "optional OpenSpec settings" (task text did not enumerate fields). Defaults `openspec` and `osq` match spec sections 4 and 3 respectively.
- Did not export `OpenSpecConfig` from `src/index.ts`; that file is outside task scope.

## Drift against features/

- `features/cli-foundation.md` still documents `paths` without `openspecRoot` and does not mention `openspec` settings. Expected: this task only types/defaults config; runtime still uses legacy `specs`/`features` paths until task 9 cut-over, and the feature doc update is applied by the approved delta at archive time.

## Missing context

- Exact expected OpenSpec option field names (e.g. validator bin, schema) were not specified by this task; inferred from the parent spec's pinned-validator and schema-bundle sections.

## Next

- Task 2: parse `proposal.md` and OpenSpec task lists alongside rewritten templates.
