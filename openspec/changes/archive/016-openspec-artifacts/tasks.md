# Tasks

## 1. Configuration and Dependencies

- [x] 1. When configuration is loaded, OpenSpec paths with runtime options are typed with defaults

## 2. Parsing and Templates

- [x] 2. When change folders are parsed, proposal.md with OpenSpec task lists are supported alongside rewritten templates

## 3. Delta Spec Merging

- [x] 3. When spec deltas are processed, deterministic requirement merging applies ADDED, MODIFIED, REMOVED, RENAMED blocks

## 4. OpenSpec Validator Integration

- [x] 4. When change folders are linted, openspec validate --strict executes while surfacing prefixed findings

## 5. Checkbox Projection

- [x] 5. When task verification passes, checkbox projection is write-only covering the archive path

## 6. Archival Path & Validation

- [x] 6. When specs are archived, folders move to OpenSpec archive paths compatible with validate --archived

## 7. Project Scaffolding

- [x] 7. When osq init runs, openspec/config.yaml with the osq schema bundle is scaffolded

## 8. Migration Command

- [x] 8. When osq migrate openspec runs, legacy change folders plus features migrate to OpenSpec layout

## 9. Layout Cut-over

- [x] 9. When layout cut-over is performed, runtime switches to openspec paths prompting human restart

