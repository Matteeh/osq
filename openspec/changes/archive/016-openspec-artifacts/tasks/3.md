---
title: When spec deltas are processed, deterministic requirement merging applies ADDED, MODIFIED, REMOVED, RENAMED blocks
verify: node --import tsx --test tests/delta-merge.test.ts
scope: [src/core/delta.ts, src/watcher/archiver.ts, tests/delta-merge.test.ts]
entry: [src/core/delta.ts]
skills: []
---
## Acceptance
- [ ] Delta parser extracts ADDED, MODIFIED, REMOVED, and RENAMED requirement blocks
- [ ] Requirement parser extracts requirement names and scenario WHEN/THEN bullets
- [ ] Merge engine executes operations in strict sequence: RENAMED -> REMOVED -> MODIFIED -> ADDED
- [ ] Missing MODIFIED or REMOVED targets in base spec throw a deterministic error
- [ ] Applying deltas to a non-existent capability creates a new spec using delta Purpose
- [ ] Golden-file test asserts byte-for-byte exact rebuilding across all four delta operations
