## Changed

- `maskEvent` in `tests/golden-events.test.ts` now calls a new `maskRepoCounts`
  helper for every `measures` event, replacing `repoFiles` and `repoLines` with
  the shared `MASKED_REPO_COUNT` placeholder (`0`), next to the existing scope
  hash masking.
- Added a unit test (`masks measures repository counts to a stable placeholder`)
  asserting a `measures` event with real repository counts normalizes to the
  placeholder while other fields are untouched.
- Added `still matches the verified golden events when the scaffolded project
  grows`: it appends ten non-empty lines to the scaffolded `PLANNER.md`, runs the
  same verified mock task, and asserts the normalized stream still equals
  `verified.jsonl`.
- Regenerated `tests/fixtures/events/verified.jsonl` and `dead.jsonl` with
  `UPDATE_GOLDEN=1`. `git diff` confirms only `repoFiles` (14 → 0) and
  `repoLines` (518 → 0) changed in each fixture.

## Verification

- Task verify: `node --import tsx --test tests/golden-events.test.ts` — 7 pass, 0 fail.
- Proposal verify: `pnpm verify` — exit 0.

## Next

None.

Touched: tests/golden-events.test.ts, tests/fixtures/events/verified.jsonl, tests/fixtures/events/dead.jsonl
