## Changed

- `observePlanning` in `src/core/web/web-data-observations.ts` no longer declares
  `sessionReported` or assigns it in the token branches. `reported` now
  increments directly inside the `usage.cost !== null` branch, so coverage
  counts exactly the cost-bearing sessions. Token groups, durations, cost, and
  coverage values are unchanged for every input. The function is now 76 lines.
- `tests/function-budget.test.ts` drops the
  `'core/web/web-data-observations.ts#observePlanning'` grandfather entry. No
  other entry changed.

## Verification

- Task verify: `node --import tsx --test tests/function-budget.test.ts tests/web-planning-cost.test.ts tests/web-data-unreported.test.ts` — 6 pass, 0 fail.
- Proposal verify: `pnpm verify` — exit 0.

Touched: src/core/web/web-data-observations.ts, tests/function-budget.test.ts
