---
title: When golden event streams are compared, measures repository counts are masked so fixtures ignore the scaffolded project's size
verify: node --import tsx --test tests/golden-events.test.ts
verify_starts: green
scope:
  - tests/golden-events.test.ts
  - tests/fixtures/events/**
entry:
  - tests/golden-events.test.ts
  - tests/fixtures/events/verified.jsonl
  - tests/fixtures/events/dead.jsonl
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] `maskEvent` in `tests/golden-events.test.ts` replaces `repoLines` and `repoFiles` in every `measures` event with a fixed placeholder, the same way it already masks durations and scope hashes.
- [ ] `tests/fixtures/events/verified.jsonl` and `dead.jsonl` are regenerated with `UPDATE_GOLDEN=1`, and only their `repoLines` and `repoFiles` values change.
- [ ] A new test in `tests/golden-events.test.ts` runs the verified scenario after appending ten lines to the scaffolded project's `PLANNER.md` and asserts the stream still matches `verified.jsonl`.

## Details

Today both fixtures pin the line and file counts of the project that
`scaffoldProject` writes. Any edit to the managed planner block or a template
changes them, which forced 061 task 5 to reflow planner bullets to keep the
line count. Other `measures` fields stay compared as they are. Tests outside
this scope are frozen.
