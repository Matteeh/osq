---
title: When the dashboard observes planning, observePlanning keeps only cost-bearing bookkeeping and leaves the function-budget grandfather list
verify: node --import tsx --test tests/function-budget.test.ts tests/web-planning-cost.test.ts tests/web-data-unreported.test.ts
verify_starts: green
scope:
  - src/core/web/web-data-observations.ts
  - tests/function-budget.test.ts
entry:
  - src/core/web/web-data-observations.ts
  - tests/function-budget.test.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] `observePlanning` drops the `sessionReported` variable and every assignment to it, and counts a session in `reported` exactly when its `usage.cost` is not null. Token groups, durations, cost, and coverage values are unchanged for every input.
- [ ] `observePlanning` is at most 80 lines, and `'core/web/web-data-observations.ts#observePlanning'` is removed from the grandfather list in `tests/function-budget.test.ts`. No other entry in that file changes.
- [ ] `tests/web-planning-cost.test.ts` and `tests/web-data-unreported.test.ts` pass unchanged.

## Details

062 task 8 kept the dead bookkeeping only so the function stayed over 80
lines. The grandfather test also fails when a listed function shrinks under
the budget, and this task owns that file. Removing the lines was measured to
bring the function to 76 lines and break no test. Other tests are frozen.
