# Task 3 result: schema execution authority instructions

## Changed

- `templates/openspec/schemas/osq/schema.yaml`:
  - `artifacts:` -> `id: tasks` instruction now carries an `Execution authority:` block: tasks are executed ONLY by `osq watch` after human approval; archiving is owned exclusively by osq and NEVER `openspec archive`; `tasks.md` checkboxes are strictly written by the osq runner as a write-only projection of runtime verification, preventing bypass of the approval hash; agents MUST NOT execute tasks directly or edit checkboxes.
  - `apply:` instruction now commands agents to execute tasks strictly by running `osq watch`, forbids direct execution, editing `tasks.md` checkboxes, and running `openspec archive` / `openspec apply`, and states checkboxes are updated automatically by the runner on passing independent verification.
- `templates/openspec/config.yaml`:
  - `context` adds an authority paragraph: execution and archive authority belong strictly to osq (executed only by `osq watch`, archiving owned exclusively by osq and never `openspec archive`, checkboxes are write-only runner projections preventing approval-hash bypass).
  - `tasks` rules add: execute tasks exclusively through `osq watch`; never edit `tasks.md` checkboxes and never run `openspec archive`.
- `templates/openspec/schemas/osq/README.md`: added `## Execution and archive authority` documenting that agents must delegate execution to `osq watch`, checkboxes are runner-written write-only projections, and archiving is osq-owned and never done with `openspec archive`.
- `tests/schema-instructions.test.ts` (new): 7 tests asserting each acceptance line, parsing `schema.yaml` and `config.yaml` with `yaml` and checking `tasks.instruction`, `apply.instruction`, `context`, `rules.tasks`, and the README.

## Verification

- `node --import tsx --test tests/schema-instructions.test.ts` -> 7 pass, 0 fail.
- `node --import tsx --test tests/init.test.ts` -> 10 pass, 0 fail (scaffolding of the edited templates still valid).
- `pnpm test` -> 553 pass, 1 skipped, 0 fail.
- `pnpm tsc --noEmit` -> clean.
- `pnpm biome check tests/schema-instructions.test.ts` -> clean.

## Drift against features

- `openspec/specs/spec-lint-and-approve.md` does not yet contain the capability rule "OpenSpec schema execution authority instructions"; the capability rule supplied for this task covers it. Expected feature-doc drift to be applied by the watcher when the delta is approved.
- `openspec/specs/cli-foundation.md` already owns `templates/**` (Code ownership requirement), so no additional ownership change is needed there.

## Missing context

- None. Scope and template paths were sufficient.

## Next

- Task complete; no acceptance line outstanding.
