# Task 4 result: AGENTS.md managed block coexistence

## Changed

- `src/cli/setup.ts`: imported `updateAgentsMd` from `../core/init.js` and call `await updateAgentsMd(cwd)` in `setupCommand()` before adapter setup, so `osq setup` refreshes the `<!-- OSQ:START -->` block.
- `tests/setup-block-coexistence.test.ts` (new): 3 tests covering every acceptance line.
  - `updateAgentsMd` on an `AGENTS.md` containing an OpenSpec managed block and custom notes adds the osq block, keeps exactly one of each marker, and preserves the OpenSpec block and surrounding text byte-for-byte.
  - Repeated `updateAgentsMd` refreshes the osq block in place with no duplication/corruption; the OpenSpec block, outer heading, and custom notes are unchanged.
  - `setupCommand()` end-to-end (temp cwd + `osq.config.ts` selecting the no-op `mock` harness) run twice leaves exactly one OpenSpec and one osq block with identical intact content.

## Deviated

- No code change was needed in `src/core/init.ts`: `updateManagedBlock` already slices only between `OSQ_START_MARKER`/`OSQ_END_MARKER` and re-emits `before`/`after`, so foreign OpenSpec blocks are preserved. Left untouched to satisfy the 250-line budget.
- `src/core/init.ts` remains 249 lines; no compression was required.

## Drift against features

- `openspec/specs/cli-foundation.md` does not yet contain the capability rule "Managed block coexistence"; the capability rules supplied for this task cover it. Expected feature-doc drift to be applied by the watcher when the delta is approved.
- `AGENTS.md` in this repo is the task runner's own instruction file and contains the OSQ block but no OPENSPEC block; not modified.

## Missing context

- None. Scope, entry points, and the referenced feature docs were sufficient.

## Next

- Task complete; no acceptance line outstanding.
