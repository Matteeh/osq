# Task 2 result: doctor validator pin diagnostics

## Changed

- `src/core/doctor.ts`:
  - Imported `OPENSPEC_EXPECTED_VERSION` from `./linter.js`.
  - Added injectable `probeValidator?: (projectRoot: string) => Promise<string>` to `DoctorDependencies`.
  - Added `checkValidator(projectRoot, deps)`: resolves `node_modules/.bin/openspec` (falling back to `openspec` on PATH), reuses the existing `probeVersion` helper (`--version`, 10s timeout) unless `deps.probeValidator` is supplied, and returns:
    - `[ok] validator: pinned 1.13.1` on an exact trimmed match,
    - `[fail] validator: openspec version <ver> differs from pinned 1.13.1` on drift,
    - `[fail] validator: binary unavailable: openspec (<message>)` on error.
  - Appended `await checkValidator(...)` as the 6th entry of `runDoctorChecks`.
  - File is 219 lines (limit 250).
- `tests/doctor.test.ts`:
  - Renamed the healthy test to "passes all six checks", injecting `probeValidator` and asserting names `['config','harness','managed-blocks','locks','archives','validator']`.
  - Added validator pass (pinned, with surrounding whitespace), drift (`9.9.9`), and binary-unavailable (throw/ENOENT) cases.
  - Added `installFakeValidator` helper; CLI `doctorCommand` beforeEach installs it so the default probe path is exercised hermetically, and assertions updated from 5 to 6 checks/lines.
  - Guarded the active-checkout smoke test with `isValidatorAvailable()` and a combined skip reason, so it skips cleanly when no real `openspec` binary is installed.

## Verification

- `node --import tsx --test tests/doctor.test.ts` -> 17 tests, 16 pass, 1 skipped (active checkout, no local validator), 0 fail.
- `pnpm tsc --noEmit` -> clean.
- `pnpm biome check src/core/doctor.ts tests/doctor.test.ts` -> clean.

## Deviated

- The task did not mention the `active repository checkout` test. The osq checkout has no `@fission-ai/openspec` install, so its default validator probe would fail; the test now skips when no validator binary is present (same pattern already used for the harness binary).
- The task did not specify how the `doctorCommand` CLI test would inject a validator. Since `src/cli/doctor.ts` is out of scope and `doctorCommand` does not forward `DoctorDependencies`, the test installs a stub `node_modules/.bin/openspec` script.

## Drift against features

- `openspec/specs/cli-foundation.md` diagnostic scenario still enumerates only `config`, `harness`, `managed-blocks`, `locks`, `archives`. The capability rules supplied for this task already include the validator check, so this is expected feature-doc drift to be applied by the watcher when the delta is approved.

## Missing context

- None. `OPENSPEC_EXPECTED_VERSION` (`1.13.1`) already exists in `src/core/linter.ts`.

## Next

- Task complete; no acceptance line outstanding.
