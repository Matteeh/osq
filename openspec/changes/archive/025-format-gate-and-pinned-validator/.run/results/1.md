# Task 1 result: ADR 004 Pinned OpenSpec Validator

## Changed

- `decisions/004-pinned-openspec-validator.md` (new): Accepted ADR recording the pin of `@fission-ai/openspec` to exact `1.13.1` in `devDependencies` with peer range `>=1.13.1 <2`, local binary `node_modules/.bin/openspec`, validation semantics (`OPENSPEC_TELEMETRY=0`, `--strict`, `--json`, `--no-interactive`), and `osq doctor` drift diagnostics (`[ok] validator: pinned 1.13.1`, `[fail] validator: openspec version <x> differs from pinned 1.13.1`, `[fail] validator: binary unavailable: openspec`, exit 1 on failure).
- `decisions/README.md`: added an Index section referencing ADRs 001, 002, and 004.
- `tests/adr-004.test.ts` (new): asserts ADR file presence, accepted status, dependency pin/binary/peer-range, validation semantics, doctor drift semantics, and README index entry.

## Verification

`node --import tsx --test tests/adr-004.test.ts` → 5 pass, 0 fail.

Also checked `pnpm biome check tests/adr-004.test.ts` (clean) and `pnpm tsc --noEmit` (clean).

## Deviated

- None from the task's stated scope. The README index lists existing ADRs 001 and 002 alongside 004 for consistency; the task only required the 004 entry.

## Notes / next

- The proposal also requires doctor to actually emit the validator line; that is a separate task in this change and is not implemented here. This task only records the ADR and its test.
- README index omits 003 intentionally because `decisions/003-*.md` does not exist in the repository.
