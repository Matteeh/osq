# Task 5 result

## Changed
- `src/core/migrate.ts`:
  - Imports `getSpecsDir` from `./layout.js` alongside `getArchiveDir` and `getChangesDir`.
  - `resolvePaths` now derives `specsTarget` via `getSpecsDir(config.paths.openspecRoot, projectRoot)` instead of `path.join(projectRoot, config.paths.openspecRoot, 'specs')`. `changesTarget`/`archiveTarget` already used `getChangesDir`/`getArchiveDir`.
- `tests/migrate.test.ts`:
  - Imports `getArchiveDir`, `getChangesDir`, `getSpecsDir` from `../src/core/layout.js`.
  - Adds test `resolves every migration target through the canonical layout helpers`, asserting feature docs land under `getSpecsDir(...)/<capability>/spec.md`, active changes under `getChangesDir(...)/<id>`, archives under `getArchiveDir(...)/<id>`, and the sample stub under `getChangesDir(...)/017-sample`.

## Verification
- `node --import tsx --test tests/migrate.test.ts` -> 12 pass / 0 fail.
- `pnpm tsc --noEmit` -> clean.
- `node --import tsx --test tests/line-budget.test.ts` -> pass; `src/core/migrate.ts` still 335 lines and still listed in `ALLOW_LIST`.

## Deviated
- None.

## Drift against features/
- None. Writes only to `cli-foundation` canonical layout authority and `spec-lint-and-approve` (migrate ownership) as declared.

## Missing context
- None.

## Acceptance status
- All five acceptance lines satisfied.
