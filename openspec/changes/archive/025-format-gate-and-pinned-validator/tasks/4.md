---
title: When osq setup executes, managed blocks coexist in AGENTS.md without touching OpenSpec markers
verify: node --import tsx --test tests/setup-block-coexistence.test.ts
scope: [src/cli/setup.ts, src/core/init.ts, tests/setup-block-coexistence.test.ts]
entry: [src/cli/setup.ts, src/core/init.ts]
skills: []
---
## Acceptance
- [ ] src/cli/setup.ts calls updateAgentsMd to refresh the osq managed block in AGENTS.md
- [ ] updateManagedBlock in src/core/init.ts updates only the <!-- OSQ:START --> block and preserves any <!-- OPENSPEC:START --> block
- [ ] Executing setup on an AGENTS.md with an existing OpenSpec block leaves both blocks cleanly formatted
- [ ] Executing setup a second time leaves both blocks intact without duplication or corruption
- [ ] tests/setup-block-coexistence.test.ts asserts both blocks survive repeated setups
- [ ] src/core/init.ts remains under 250 lines

## Details

1. In `src/cli/setup.ts`:
   - Import `updateAgentsMd` from `../core/init.js`.
   - In `setupCommand()`, call `await updateAgentsMd(cwd);` before or after adapter setup.
2. In `src/core/init.ts`:
   - Verify `updateManagedBlock` cleanly replaces only between `OSQ_START_MARKER` and `OSQ_END_MARKER`.
   - If `filePath` contains `<!-- OPENSPEC:START --> ... <!-- OPENSPEC:END -->`, ensure the replacement slice preserves the exact OpenSpec block and all surrounding content.
   - Ensure `src/core/init.ts` line count stays strictly at or under 250 lines (currently 249 lines; compress any formatting if needed).
3. Create `tests/setup-block-coexistence.test.ts`:
   - Setup a temporary directory with an `AGENTS.md` containing:
     ```markdown
     # Project AGENTS

     <!-- OPENSPEC:START -->
     OpenSpec instructions here
     <!-- OPENSPEC:END -->

     Custom user notes
     ```
   - Invoke `updateAgentsMd(tmpDir)` (simulating `osq setup`).
   - Assert `AGENTS.md` contains both `<!-- OPENSPEC:START -->` and `<!-- OSQ:START -->`.
   - Invoke `updateAgentsMd(tmpDir)` a second time.
   - Assert `AGENTS.md` still contains exactly one `<!-- OPENSPEC:START -->` block and exactly one `<!-- OSQ:START -->` block with identical intact content.
4. Run verification command:
   `node --import tsx --test tests/setup-block-coexistence.test.ts`