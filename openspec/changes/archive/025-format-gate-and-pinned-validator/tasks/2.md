---
title: When doctor checks repository health, validator pin is verified against expected version
verify: node --import tsx --test tests/doctor.test.ts
scope: [src/core/doctor.ts, tests/doctor.test.ts]
entry: [src/core/doctor.ts]
skills: []
tests:
  modify: true
---
## Acceptance
- [ ] src/core/doctor.ts defines checkValidator verifying openspec binary matches OPENSPEC_EXPECTED_VERSION (1.13.1)
- [ ] runDoctorChecks executes checkValidator as the 6th diagnostic check
- [ ] doctor outputs [ok] validator: pinned 1.13.1 when installed version matches
- [ ] doctor outputs [fail] validator: openspec version <ver> differs from pinned 1.13.1 and exits 1 on version drift
- [ ] doctor outputs [fail] validator: binary unavailable: openspec (...) when binary is missing
- [ ] tests/doctor.test.ts verifies all validator diagnostic states and reports 6 total checks
- [ ] src/core/doctor.ts remains under 250 lines

## Details

1. In `src/core/doctor.ts`:
   - Import `OPENSPEC_EXPECTED_VERSION` from `./linter.js`.
   - Add injectable `probeValidator?: (projectRoot: string) => Promise<string>` to `DoctorDependencies`.
   - Implement `checkValidator(projectRoot: string, deps: DoctorDependencies): Promise<DoctorCheckResult>`:
     - Check local binary path `node_modules/.bin/openspec` (falling back to `openspec` on PATH).
     - Run `--version` with 10s timeout using `deps.probeValidator` if provided.
     - If output matches `OPENSPEC_EXPECTED_VERSION` (trimmed), return `make('validator', true, `pinned ${OPENSPEC_EXPECTED_VERSION}`)`.
     - If output differs, return `make('validator', false, `openspec version ${version} differs from pinned ${OPENSPEC_EXPECTED_VERSION}`)`.
     - On error, return `make('validator', false, `binary unavailable: openspec (${message})`)`.
   - Update `runDoctorChecks` to append `await checkValidator(projectRoot, deps)` to `checks`.
   - Ensure `src/core/doctor.ts` remains strictly under 250 lines.
2. In `tests/doctor.test.ts`:
   - Update `it('passes all five checks for a healthy repository')` to assert 6 checks: `['config', 'harness', 'managed-blocks', 'locks', 'archives', 'validator']`.
   - Add test case verifying validator pass with pinned version.
   - Add test case verifying validator failure on version drift.
   - Add test case verifying validator failure when binary is missing.
   - Update CLI test `doctorCommand` assertion expecting 6 check lines.
3. Run verification command:
   `node --import tsx --test tests/doctor.test.ts`