---
title: When ADR 004 is accepted, decision records pinned validator drift semantics
verify: node --import tsx --test tests/adr-004.test.ts
scope: [decisions/004-pinned-openspec-validator.md, decisions/README.md, tests/adr-004.test.ts]
entry: [decisions/004-pinned-openspec-validator.md]
skills: []
---
## Acceptance
- [ ] decisions/004-pinned-openspec-validator.md records status Accepted for @fission-ai/openspec pinned to 1.13.1
- [ ] ADR 004 defines exact devDependencies pin, binary path node_modules/.bin/openspec, and peerDependencies range
- [ ] ADR 004 documents validation execution under OPENSPEC_TELEMETRY=0 with --strict, --json, and --no-interactive
- [ ] ADR 004 defines doctor diagnostic verification and drift reporting semantics
- [ ] decisions/README.md indexes ADR 004
- [ ] tests/adr-004.test.ts asserts ADR 004 file presence, index entry, and required architectural sections

## Details

1. Create `decisions/004-pinned-openspec-validator.md`:
   - Title: `# 004. Pinned OpenSpec Validator`
   - Status: `Accepted`
   - Context: AGENTS.md mandates zero runtime dependencies beyond chokidar, yaml, commander, and jiti, requiring an ADR before adding or integrating external tooling. osq acts as a strict execution profile of OpenSpec and validates changes/specs using OpenSpec's official validator CLI.
   - Decision:
     - Pin `@fission-ai/openspec` to exact version `1.13.1` in `devDependencies` (and `>=1.13.1 <2` in peerDependencies).
     - Invoke local binary at `node_modules/.bin/openspec` with environment `OPENSPEC_TELEMETRY=0` and flags `--strict`, `--json`, and `--no-interactive`.
     - `osq doctor` probes `openspec --version` and checks for exact match with `1.13.1`.
     - On drift, doctor outputs `[fail] validator: openspec version <x> differs from pinned 1.13.1` and exits 1.
     - On missing binary, doctor outputs `[fail] validator: binary unavailable: openspec`.
     - On match, doctor outputs `[ok] validator: pinned 1.13.1`.
2. Update `decisions/README.md`:
   - Add entry for `004-pinned-openspec-validator.md`.
3. Create `tests/adr-004.test.ts`:
   - Verify `decisions/004-pinned-openspec-validator.md` exists and contains required sections and `Accepted` status.
   - Verify `decisions/README.md` references ADR 004.
4. Run verification command:
   `node --import tsx --test tests/adr-004.test.ts`