---
title: When migration paths resolve, layout module serves as sole directory authority
verify: node --import tsx --test tests/migrate.test.ts
scope: [src/core/migrate.ts, tests/migrate.test.ts]
entry: [src/core/migrate.ts]
skills: []
tests:
  modify: true
---
## Acceptance
- [ ] src/core/migrate.ts imports getSpecsDir from ./layout.js alongside getChangesDir and getArchiveDir
- [ ] resolvePaths derives specsTarget exclusively via getSpecsDir(config.paths.openspecRoot, projectRoot)
- [ ] resolvePaths derives changesTarget and archiveTarget exclusively via getChangesDir and getArchiveDir
- [ ] tests/migrate.test.ts passes asserting complete migration directory mapping
- [ ] src/core/migrate.ts remains on the grandfathered allow list without regression

## Details

1. In `src/core/migrate.ts`:
   - Update import from `./layout.js`:
     ```ts
     import { getArchiveDir, getChangesDir, getSpecsDir } from './layout.js';
     ```
   - In `resolvePaths(projectRoot: string, config: OsqConfig): MigrationPaths`:
     Update `specsTarget`:
     ```ts
     specsTarget: getSpecsDir(config.paths.openspecRoot, projectRoot),
     ```
   - Ensure all target paths are derived strictly through layout functions without hardcoded `'specs'` concatenation.
2. In `tests/migrate.test.ts`:
   - Ensure existing tests pass and verify that migration paths resolve identically to canonical layout expectations.
3. Run verification command:
   `node --import tsx --test tests/migrate.test.ts`