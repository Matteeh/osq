---
title: When osq commits for a task, one function builds the message with its Osq trailers
verify: node --import tsx --test tests/commit-message.test.ts
verify_starts: red
scope:
  - src/core/run/commit-message.ts
  - tests/commit-message.test.ts
entry:
  - src/harness/types.ts
  - src/core/status/layout.ts
  - src/watcher/outcome.ts
skills: []
---
## Acceptance

- [ ] New `src/core/run/commit-message.ts` exports `readCommitTrailers(changeFolder, taskNumber)` and `formatCommitMessage({ subject, title, outcomeLine, trailers })`, as the watcher-and-harness delta's "Osq commit message" says.
- [ ] Malformed lines in the events file are skipped, and a `started` event without `harness`, `model`, or `osqVersion` strings counts as absent.
- [ ] New `tests/commit-message.test.ts` covers both scenarios; "Trailers parse" runs `git interpret-trailers --parse` on the message.

## Details

Find the events file with `getEventsPath` from `src/core/status/layout.ts`.
The folder name in `Osq-Change` is the change folder's base name. Use `\n`
line endings and end the message with one newline. The module must not import
from `src/watcher/`.
