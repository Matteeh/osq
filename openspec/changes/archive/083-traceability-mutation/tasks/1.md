---
title: When osq.config.ts sets traceability.mutation, osq validates the command and budget
verify: node --import tsx --test tests/mutation-config.test.ts
verify_starts: red
scope:
  - src/core/foundation/config-traceability.ts
  - tests/mutation-config.test.ts
entry:
  - src/core/foundation/config-traceability.ts
  - tests/focused-config.test.ts
skills: []
---
## Acceptance

- [ ] `TraceabilityConfig` gains optional `mutation: MutationConfig`, with `command: string` and `budgetSeconds: number`, and `validateTraceabilityConfig` reads it as the cli-foundation delta's "Mutation configuration" says, keeping it out of the result when unset.
- [ ] New `tests/mutation-config.test.ts` covers both scenarios, each error message, a custom budget, and loading `mutation` from an `osq.config.ts` in a temporary project through `loadConfig`.
- [ ] `tests/traceability-config.test.ts` and `tests/focused-config.test.ts` pass unchanged.

## Details

Model the new field on how change 082 added `focusedTests`. Leaving the key
out when unset keeps every existing deep-equality check of `traceability`
unchanged. Keep the budget default in `config-traceability.ts`, beside the
other traceability defaults. New functions stay within 80 lines.
