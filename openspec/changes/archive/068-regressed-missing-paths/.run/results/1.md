## Changed

- `src/harness/types.ts`: added optional `missingPaths?: string[]` to `RegressedEventData`, documented as the paths a verify named that did not exist.
- `src/watcher/archive-verify.ts`: the `verify_path_missing` regressed event now records `missingPaths: [...missing]` instead of `differingPaths`.
- `tests/archive-verify-path-missing.test.ts`: changed its `differingPaths` assertion to `missingPaths` (the only change).
- `tests/regressed-missing-paths.test.ts`: new test builds a done change whose task verify names `tests/present.cjs`, deletes it, drives `checkAndArchiveSpec`, and asserts the one `regressed` event has reason `verify_path_missing`, `missingPaths` equal to that path, and no `differingPaths` key.

## Missing context

None.

Touched: src/harness/types.ts, src/watcher/archive-verify.ts, tests/archive-verify-path-missing.test.ts, tests/regressed-missing-paths.test.ts
