# Tasks

## 1. Import graph

- [x] 1. When the import graph is built, it records every relative JavaScript and TypeScript import, and the measures fan-in reads it

## 2. Impact lint

- [x] 2. When a change is linted, it warns about frozen tests its tasks reach, undeclared capability reads and writes, and verifies that test nothing in scope
