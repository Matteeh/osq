---
title: When a change is linted, it warns about frozen tests its tasks reach, undeclared capability reads and writes, and verifies that test nothing in scope
verify: node --import tsx --test tests/impact-lint.test.ts
verify_starts: red
scope:
  - src/core/spec/impact-lint.ts
  - src/core/spec/test-impact.ts
  - src/core/spec/capability-impact.ts
  - src/core/spec/linter.ts
  - src/cli/lint.ts
  - src/core/foundation/config.ts
  - README.md
  - tests/impact-lint.test.ts
entry:
  - src/core/spec/linter.ts
  - src/core/spec/lint-findings.ts
  - src/core/spec/import-graph.ts
  - src/core/spec/parser.ts
  - src/core/spec/verify-paths.ts
  - src/core/run/scope.ts
  - tests/lint-findings.test.ts
skills: []
---
## Acceptance

- [ ] `OsqLimits` and `DEFAULT_CONFIG` gain `importGraphDepth: 2` and `maxListedImporters: 8`, as the cli-foundation delta's "Import graph lint limits" says, and the README's lint table lists the four new warnings.
- [ ] New `src/core/spec/test-impact.ts` implements "Frozen test reach warning", "Frozen test warning text", and "Verify without scope test warning" on task 1's graph with `reachImporters` and `reachImports`.
- [ ] New `src/core/spec/capability-impact.ts` reads every living spec's globs with `parseCodeOwnership` and implements "Undeclared capability read warning" and "Capability write without delta warning", matching paths with `scopeCoversPath`.
- [ ] New `src/core/spec/impact-lint.ts` exports one function that runs both modules for a change and returns `LintFinding` warnings. `lintChangeFolder` calls it with its resolved task scopes and the proposal's `features.reads`, and adds each finding as the change's own.
- [ ] `LintOptions` gains optional `importGraph`. `lintCommand` builds the graph once with `buildImportGraph`, skipping the OpenSpec root, and passes it to every change, as "Import graph built once per lint run" says.
- [ ] New `tests/impact-lint.test.ts` lints temporary repositories and covers every scenario of the six spec-lint-and-approve requirements this task implements and of "Import graph lint limits", including the Python-only repository, asserting finding fields, not only text.

## Details

Build the temporary repositories as `tests/lint-findings.test.ts` does, with
`installFakeValidator` and `scaffoldProject`; the fake validator reports
nothing, so only import-graph findings and osq's own checks appear. Give each
repository a living `openspec/specs/<capability>/spec.md` with a `###
Requirement: Code ownership` block when the scenario needs ownership. A rough
version of these four checks, wired into lint in a scratch worktree, changed no
existing test result. Keep `linter.ts` to the call; it is on the line-budget
allow list and `lintChangeFolder` is grandfathered in the function budget. New
functions stay within 80 lines and new files within 250.
