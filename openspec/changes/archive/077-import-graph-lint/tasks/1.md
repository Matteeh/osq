---
title: When the import graph is built, it records every relative JavaScript and TypeScript import, and the measures fan-in reads it
verify: node --import tsx --test tests/import-graph-build.test.ts
verify_starts: red
scope:
  - src/core/spec/import-graph.ts
  - src/watcher/measures.ts
  - tests/import-graph-build.test.ts
entry:
  - src/watcher/measures.ts
  - tests/measures.test.ts
  - src/core/run/scope.ts
skills: []
---
## Acceptance

- [ ] New `src/core/spec/import-graph.ts` exports `buildImportGraph(projectRoot, options)`, returning an `ImportGraph` with `files`, `importsOf(file)`, and `importersOf(file)`, as the spec-lint-and-approve delta's "Import graph" and "Import specifier resolution" say. `options.skip` lists extra repository-relative folders to leave out.
- [ ] The module also exports `reachImporters(graph, files, maxDepth)`, returning each file that imports one of `files` within `maxDepth` levels with its depth, and `reachImports(graph, files)`, returning every file `files` import at any depth.
- [ ] The ignored folder names move from `measures.ts` into `import-graph.ts`, and `gatherRepoCounts` keeps using them.
- [ ] `countImportFanIn` keeps its signature and counts from `buildImportGraph` as the watcher-and-harness delta's "Import fan-in from the shared graph" says. `listTsFiles` and `importSpecifier` go away.
- [ ] New `tests/import-graph-build.test.ts` covers every scenario of "Import graph", "Import specifier resolution", and "Import fan-in from the shared graph", plus `export ... from`, `import type`, dynamic `import()`, `require()`, and depth limits in `reachImporters`.
- [ ] The same file compares `countImportFanIn` on this repository, for `src/core/foundation/config.ts`, `src/core/report/planning.ts`, `src/harness/codex/codex.ts`, `src/core/status/state.ts`, and `src/core/spec/parser.ts`, with a search it writes for whole quoted specifiers with or without a script extension.

## Details

A graph prototype built from these rules matched the whole-specifier search on
all 170 `src/` TypeScript files and built in under 200 ms. Find specifiers with
one regular expression over the file text; there is no parser dependency to
add. `.d.ts` files are not graph files. Leave the OpenSpec root out only when
`options.skip` names it; the watcher's fan-in counts only `src/` anyway.
`tests/measures.test.ts` must pass unchanged. `measures.ts` is 249 lines and
must stay within 250.
