# Task 1 result

## Changed

- `src/harness/types.ts`: added `'done'` and `'dead'` to `HarnessEventType`; exported `DoneEventData` (`readonly task: string`) and `DeadEventData` (`readonly task: string; readonly reason: string`).
- `src/watcher/runner.ts`: added `recordDeadEvent` / `recordDoneEvent` helpers that append to `.run/events/<n>.jsonl` via `appendHarnessEvent`. Wired them into every dead-marker write site (`spec_conflict` missing approved, `spec_conflict` hash mismatch, task-file-missing `crashed`, agent `timeout`/`crashed`, `no_result`, `verify_red`) and the `already_running` failure path, plus the successful `done/<n>` marker write.
- `tests/runner-done-dead-events.test.ts`: new test file (9 tests) covering the type/payload shapes and marker/event pairing for done and each dead reason (no_result, crashed, timeout, verify_red, both spec_conflict variants, already_running).

## Deviated

- The task Details list `already_running` under "every location where `.run/dead/<taskNumber>.md` is written", but the current runner never writes a dead marker for that path. I append the `dead` event for the failed attempt without creating a marker, since writing a marker would wrongly indicate another watcher's run had failed. Test asserts the event only (no marker).
- Added a dead event to the existing task-file-not-found path (`reason: crashed`), which the six-item Details list omitted but which does write a dead marker; needed to satisfy "alongside every dead marker write". That path is effectively unreachable (a missing task file fails the approval hash first), so there is no test for it.
- `reapStaleLocks` in `src/core/lock.ts` also writes dead markers; out of scope for this task and left unchanged.

## Missing context

None.

## Next steps

All acceptance lines are complete. `node --import tsx --test tests/runner-done-dead-events.test.ts` passes (9/9); full `pnpm tsc --noEmit && pnpm test && pnpm lint` passes (186 tests, 0 lint errors).
