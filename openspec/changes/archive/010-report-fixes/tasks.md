# Tasks

- [x] 1. When the runner writes a done or dead marker, matching events are appended to events.jsonl
- [x] 2. When the report counts task states, active specs use deriveSpecState while archived specs use events
- [x] 3. When the report counts failures, every historical dead event is grouped by reason
- [x] 4. When the report sums tokens, neutral categories with cache share percentage are reported
- [x] 5. When events carry cost fields, the report displays summed reported cost
- [x] 6. When the report counts file changes, edit or write tool events are deduplicated by path
- [x] 7. When osq report runs with the json flag, stable structured metrics are emitted
