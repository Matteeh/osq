# Task 1 — ordered filesystem-derived queue item states (attempt 5, scope-regression retry)

## Changed

- No source or test edits. The working tree already satisfies every acceptance
  line. This invocation is a re-record after the pre-spawn scope comparison
  flagged `src/core/queue.ts` as regressed: task 4 decomposed the queue sources
  and rewrote that file into the stable facade that task 1's scope owns.
- Re-ran the task verify and the broader queue/config/line-budget/lint checks
  against the current tree so the watcher records a fresh `done/1` whose scope
  hash matches the facade version of `src/core/queue.ts`.

## Deviated

- None new. Prior deviations still stand: `QueueItem` omits the raw section
  string (the digest is computed from the untouched slice) and the internal
  association type is named `Assoc`. Both are preserved by the task 4
  decomposition and keep the modules within the source-line budget.

## Drift against features

- None. The `status-inspection` read-only parsing and state-projection deltas
  and the `cli-foundation` `osq queue` command delta remain satisfied through
  the facade exports (`parseQueue`, `readQueue`, `projectQueue`, `formatQueue`,
  `getQueuePath`) and the thin CLI wrapper.

## Missing context

- None. All seven acceptance lines are exercised by the 23 passing tests:
  - L1 registration + byte/stat-identical render through `createProgram`.
  - L2 exact heading grammar, single dependency line, verbatim body, and
    `sha256:` digest over the raw section slice.
  - L3 missing file, malformed heading/dependency line, invalid/duplicate slug,
    empty title/body, duplicate/self/forward/unknown dependency all error with
    the queue path and item context.
  - L4 association only via `queue_item` metadata; malformed unrelated folders
    and loose entries cannot hide valid rows.
  - L5 precedence landed, active dead/running/approved/planned, rejected, then
    unplanned, with selected change id and deterministic rejection count.
  - L6 unmet dependencies from archived associations only; drift from the
    selected `queue_hash`; fresh derivation on every call.
  - L7 deterministic text with change ids, rejection counts, unmet deps, and
    drift, formatted through `createProgram`.

## Verification

- `node --import tsx --test tests/queue.test.ts` → **23 pass, 0 fail**.
- `node --import tsx --test tests/queue*.test.ts tests/config-queue.test.ts` →
  **64 pass, 0 fail**.
- `node --import tsx --test tests/line-budget.test.ts` → **1 pass, 0 fail**.
- `pnpm tsc --noEmit` → clean.
- `pnpm lint` → clean (200 files).

## Next steps

- Task 1 is complete at the current facade hash; the watcher re-records `done/1`.
- Forward warning unchanged: task 2 (`src/cli/plan.ts`, `src/cli/index.ts`,
  `src/core/queue.ts`) also regressed when task 4 rewrote the shared facade and
  needs the same retry/re-record pass before task 5 can run.
