# Task 2 — next queue item enters the planning flow (attempt 3, scope-regression retry)

## Changed

- No source or test edits. The task-2 implementation from attempts 1 and 2 is
  already present in the working tree and satisfies every acceptance line.
  This invocation is a re-record after the pre-spawn scope comparison flagged
  `src/cli/plan.ts` and `src/core/queue.ts` as regressed: task 4 decomposed the
  queue sources, and `src/cli/plan.ts` / `src/core/queue.ts` both live in
  task 2's scope. Task 4's decomposition is behavior-preserving for every task
  acceptance line.
- Re-ran the task verify and the broader queue/plan/config/line-budget plus the
  full repository suite against the current tree so the watcher records a fresh
  `done/2` whose scope hashes match the post-task-4 facade version.

## Verification

- Task verify: `node --import tsx --test tests/plan.test.ts tests/queue-plan.test.ts tests/queue-watch.test.ts`
  → **20 pass, 0 fail**.
- `node --import tsx --test tests/queue*.test.ts tests/plan*.test.ts tests/config-queue.test.ts tests/line-budget.test.ts`
  → **86 pass, 0 fail**.
- `pnpm tsc --noEmit` → clean.
- `pnpm lint` → clean (200 files).
- `pnpm verify` → **956 tests, 956 pass, 0 fail**, biome clean. (A first
  `pnpm verify` run hit a transient `npm pack --dry-run` EOF on
  `dist/core/done.js`; the immediate re-run was fully green. No source change
  was involved and `dist/` is not in scope.)

## Deviated

- None new. Prior deviations still stand: task 4 moved the shared plan seeding
  and `formatBriefContent` into `src/cli/plan-queue.ts` and re-exports them from
  `src/cli/plan.ts`, so the established `tests/plan*.test.ts` import path and
  byte behavior are preserved.

## Drift against features/

- None. Acceptance lines 1-7 remain satisfied through the post-task-4 modules:
  - L1 `plan [name] --next [--replan] [--print]` mode validation and ordinary
    plan byte-compatibility (`src/cli/plan-queue.ts` `validatePlanModeOptions`,
    `src/cli/index.ts` registration).
  - L2 deterministic dead/regressed/change failure halt with exact
    `osq retry <id> <task|change>` remediation and no pre-mutation writes.
  - L3 first in-file unplanned item with landed dependencies, active/landed
    skipped, actionable no-op summary without mutation.
  - L4 rejected first eligible refuses without `--replan`; `--replan` creates
    one new active attempt and preserves every rejected folder, marker, event,
    reason, and count.
  - L5 `<next-id>-<queue-slug>` numbering across active, archived, and rejected
    folders; proposal title and landed dependency ids seeded without changing
    the template body.
  - L6 brief body plus planner/date and exact `queue_item`/`queue_hash`
    metadata; existing launch path with landed dependency archive paths and no
    queue-file or later-item exposure.
  - L7 three-item real-CLI/mock-harness lifecycle: print planning, authored
    output, approve, `watch --once`, dead-task halt, retry preservation, and
    eventual landing, one newly planned item per invocation.

## Next steps

- Task 2 is complete at the current facade hashes; the watcher re-records
  `done/2`.
- Task 5's scope (`src/core/report.ts`, `src/cli/report.ts`,
  `src/core/queue-report*.ts`, `tests/report-queue.test.ts`, `fixture/report/**`)
  does not overlap task 2's scope, so this re-record should hold through task 5.
