# Tasks

## 1. Export core

- [ ] 1. When the dashboard is exported, every view's documents are inlined with scrubbed paths beside the built UI

## 2. Serve flag

- [ ] 2. When a user runs osq serve --export <dir>, osq writes the snapshot, reminds them what was scrubbed, and exits without serving
