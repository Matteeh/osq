---
title: When the graph renders, every change gets a mark in change-number order, lane names show in full, and enabled edges are visible
verify: pnpm verify
scope:
  - packages/ui/src/graph/**
  - tests/ui-graph.test.tsx
entry:
  - packages/ui/src/graph/layout.ts
  - packages/ui/src/graph/GraphCanvas.tsx
  - packages/ui/src/graph/relationships.ts
  - packages/ui/src/graph/types.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] `layout.ts` orders archived changes left to right by numeric change id, so a change without a landed time still gets a mark. Active changes keep their right gutter, and rejected changes their toggle.
- [ ] A mark's hover and focus text shows its landed date, or `landed date not recorded` when there is none.
- [ ] The lane label column is as wide as the longest capability name needs, estimated from its character count and the label font size, so no lane name is clipped. `laneLabelWidth` in `GRAPH_GEOMETRY` becomes a minimum, not a fixed width.
- [ ] Dependency and reads paths render with the classes `graph-edge graph-edge-depends` and `graph-edge graph-edge-reads`, which change 056 task 1 styles. The relationships layer draws after the lanes and before the marks.
- [ ] `tests/ui-graph.test.tsx` adds a fixture of undated archived changes that depend on each other and read a capability named `metrics-and-reporting`. It asserts one mark per change in id order, one depends path and one reads path with their classes, and the full lane name in the rendered SVG. Its other assertions change only where they pinned landed-time order.
- [ ] The task's verify command passes, and every file under `packages/ui/src/graph/` stays at or under 250 lines.

## Details

This task's `scope` is authoritative over any injected code-ownership rules.
37 of this repository's 52 archived changes have no landed time, which is why
the graph looked empty. `projectRelationships` already builds the paths; they
were dropped only because their marks were missing, and invisible only because
no stroke was set. Keep every control, the fill encodings, and the lane-label
activation that shows capability text.
