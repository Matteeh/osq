---
title: When a plan prompt is built, it lists every accepted ADR in an Architecture Decisions section
verify: node --import tsx --test tests/plan-decisions.test.ts
verify_starts: red
scope:
  - src/cli/plan-sections.ts
  - src/cli/plan-queue.ts
  - src/cli/plan.ts
  - tests/plan-decisions.test.ts
entry:
  - src/cli/plan-queue.ts
  - src/cli/plan.ts
  - src/core/foundation/decisions.ts
  - tests/managed-wording.test.ts
skills: []
---
## Acceptance

- [ ] New `src/cli/plan-sections.ts` exports `formatCapabilitySpecsSection(projectRoot, openspecRoot)`, holding the `## Capability Specs` code moved out of `buildBaseOpeningPrompt` with byte-identical output, and `formatArchitectureDecisionsSection(projectRoot, config)`, returning the section or null.
- [ ] `formatArchitectureDecisionsSection` implements the cli-foundation delta's "Plan prompt architecture decisions" from `readDecisions`, with the sentence and line format exactly as the requirement gives them.
- [ ] `buildBaseOpeningPrompt` takes an optional `config`, defaulting to `DEFAULT_CONFIG`, and places the section between the capability specs and the brief only when it isn't null.
- [ ] `buildOpeningPrompt` in `plan.ts` resolves the config before building the base prompt and passes it in, so `osq plan` reads the configured decisions folder.
- [ ] New `tests/plan-decisions.test.ts` covers the requirement's scenario, a project with only proposed ADRs, and a configured `paths.decisions`, through `buildOpeningPrompt` with a supplied `recordBody`.

## Details

`plan-queue.ts` is 247 lines and must stay within 250; moving the spec listing
out makes room. `tests/managed-wording.test.ts` must pass unchanged. It calls
`buildBaseOpeningPrompt` without a config on a project without a decisions
folder, where the prompt must stay as it is today. For a project with ADR 007,
applying to `ingress` and `billing`, the line reads:

`- ADR 007: UI framework. Applies to: ingress, billing. Rule: UI components use React. Path: decisions/007-ui-framework.md`

New functions stay within 80 lines and new files within 250.
