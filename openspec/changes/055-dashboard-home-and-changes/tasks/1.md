---
title: When the graph document describes a change, its node carries the number of done tasks
verify: node --import tsx --test tests/web-data-progress.test.ts tests/web-data.test.ts tests/serve.test.ts tests/import-graph.test.ts
scope:
  - src/core/web/web-data-graph.ts
  - src/core/web/web-data-types.ts
  - tests/web-data-progress.test.ts
  - tests/web-data.test.ts
entry:
  - src/core/web/web-data-graph.ts
  - src/core/web/web-data-types.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] `WebChangeNode` gains `doneCount: number` next to `taskCount`, declared in `WebChangeNodeBase`. It counts the change's tasks that have a done marker, manual done included, using the marker derivation `web-data-change.ts` already uses (`deriveSpecState`).
- [ ] Active, archived, and rejected nodes all carry it. A change whose state can't be derived gets `doneCount` 0.
- [ ] New `tests/web-data-progress.test.ts` covers an active change with one of three tasks done, an archived change with every task done, and an archived change without markers, through `getWebGraph`.
- [ ] `tests/web-data.test.ts` changes only where it pins a whole change node, to add `doneCount`.
- [ ] The task's verify command passes.

## Details

This task's `scope` is authoritative over any injected code-ownership rules.
Task 2's changes list is the only consumer. Don't add other per-node counts.
The UI imports `WebGraph` as a type from `web-data-types.ts`, so the new field
reaches it without a UI change.
