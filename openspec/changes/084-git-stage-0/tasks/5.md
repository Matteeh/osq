---
title: When an executor prompt is built, its closing line forbids running git
verify: node --import tsx --test tests/executor-no-git.test.ts
verify_starts: red
tests:
  modify: true
scope:
  - src/harness/prompt.ts
  - tests/executor-no-git.test.ts
  - tests/fixtures/prompts/*.txt
entry:
  - src/harness/prompt.ts
  - tests/harness-prompt-injection.test.ts
skills: []
---
## Acceptance

- [ ] The last line of `buildExecutorPrompt` ends with a sentence telling the agent never to run git, as the watcher-and-harness delta's modified "Shared executor prompt" says.
- [ ] New `tests/executor-no-git.test.ts` covers the "No git" scenario.
- [ ] The golden prompts in `tests/fixtures/prompts/` are regenerated with `UPDATE_GOLDEN=1 node --import tsx --test tests/harness-prompt-injection.test.ts`, and that test passes without the variable.

## Details

Change only the closing `CRITICAL:` line. `EXECUTOR_STEPS` stays as it is,
because it also renders the managed block in AGENTS.md, which gains ADR 003's
rule through the rules block.
