---
title: When verify output or an archive path is recorded, it is relative to the project root
verify: node --import tsx --test tests/relative-paths.test.ts
verify_starts: red
tests:
  modify: true
scope:
  - src/core/run/verification.ts
  - src/watcher/archiver.ts
  - tests/relative-paths.test.ts
  - tests/archiver.test.ts
  - tests/verification-record.test.ts
  - tests/focused-tests.test.ts
  - tests/osq-change-env.test.ts
entry:
  - src/core/run/verification.ts
  - src/core/run/summary.ts
  - src/watcher/archiver.ts
  - tests/osq-change-env.test.ts
skills: []
---
## Acceptance

- [ ] `runVerificationCommand` returns its output passed through `relativizeToolSummary` with its project root, so every gate, event, and marker that reads it gets the relative form.
- [ ] `archiveSpecFolder` records `archivePath` relative to its `projectRoot`, and the comment on `ArchivedEventData` says so.
- [ ] New `tests/relative-paths.test.ts` covers both scenarios of the watcher-and-harness delta's "Relative verify output and archive path" through `runWatcherOnce`, including the dead marker.
- [ ] The absolute expectations in `tests/archiver.test.ts`, `tests/verification-record.test.ts`, `tests/focused-tests.test.ts`, and `tests/osq-change-env.test.ts` become relative. Those tests print `OSQ_CHANGE`, which stays absolute in the environment, so they now expect the change folder relative to the project root.

## Details

Nothing in `src/` reads `archivePath` from an event, and the UI does not
either. Leave existing events and archives untouched. The fingerprint of a dead
marker already replaces the project root, so a relative body fingerprints the
same way.
