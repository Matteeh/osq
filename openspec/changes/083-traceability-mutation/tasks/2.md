---
title: When a task starts, osq records the hash of each tagged function in scope and can find any function's mutation ranges
verify: node --import tsx --test tests/trace-function-ranges.test.ts
verify_starts: red
scope:
  - src/core/trace/function-ranges.ts
  - src/watcher/measures.ts
  - src/harness/types.ts
  - tests/trace-function-ranges.test.ts
entry:
  - src/core/trace/tag-scan.ts
  - src/watcher/measures.ts
  - src/watcher/dependencies.ts
  - src/harness/types.ts
  - tests/measures.test.ts
  - fixture/trace/pricing/src/pricing/quote.ts
skills: []
---
## Acceptance

- [ ] New `src/core/trace/function-ranges.ts` implements the traceability delta's "Function ranges": top-level functions with their ranges, a function's mutation ranges, and the hash of a function's own range text.
- [ ] It also exports the baseline reader for "Function baseline", which takes the project root and a task's scope, and `gatherStartMeasures` adds its result as `functionHashes` through one call beside `readScopedDependencies`, only when it isn't empty.
- [ ] `MeasuresEventData` gains optional `functionHashes: Record<string, string | null>`. `HarnessEventType` and `OsqEventData` gain `mutation_ran` with a typed `MutationRanEventData` holding the fields the watcher-and-harness delta's "Mutation check after a pass" lists, and `survivors` as `{ file, line, column, mutator, replacement }[]`.
- [ ] New `tests/trace-function-ranges.test.ts` covers every scenario of both requirements. It also covers each declaration form, an arrow function with an expression body, a return type with braces, a comment holding `}`, a nested helper chain, a helper called by two exported functions, an unbalanced range reading as unknown, and `gatherStartMeasures` on a temporary scope with and without a tagged function.

## Details

`fixture/trace/pricing/src/pricing/quote.ts` holds exactly the shape of the
first scenario, with `tierPrice` on lines 20–24 and `quote` on lines 33–39, so
read it in the test. It belongs to change 081, so never edit it. `tag-scan.ts` exports only `scanSource` and its types, and it isn't in
this task's scope, so write the declaration patterns here and leave it alone.

`measures.ts` is 225 lines and must stay within 250. `readScopedDependencies`
in `dependencies.ts` is the model for a start baseline read in its own module.
The golden event scenario has an empty scope, so its fixtures don't change. A
trial adding the field and the event type broke no test. New functions stay
within 80 lines and new files within 250.
