---
title: When planners and executors read the managed blocks, they get the ts-paas rules, and the plan prompt labels its spec list
verify: node --import tsx --test tests/managed-wording.test.ts
verify_starts: red
scope:
  - src/core/foundation/init-blocks.ts
  - src/cli/plan-queue.ts
  - AGENTS.md
  - PLANNER.md
  - templates/PLANNER.md
  - .opencode/agent/osq-coder.md
  - tests/fixtures/prompts/
  - tests/managed-wording.test.ts
entry:
  - src/core/foundation/init-blocks.ts
  - src/cli/plan-queue.ts
  - src/core/foundation/init-managed.ts
  - tests/harness-prompt-injection.test.ts
  - tests/init-planner.test.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] Executor step 3 in `EXECUTOR_STEPS` reads: "3. Read a previous result file for this task if present. Run the task's `verify`. Start from what fails. A `verify` that names a file your task creates fails until that file exists, so starting red is expected."
- [ ] In `MANAGED_AGENTS_MD_BODY`, the `tasks.md` line reads: "- Executors never edit `tasks.md` or any file under `.run/` except their result file; those belong to the watcher and the human. Planners write `tasks.md` and the task files."
- [ ] In `MANAGED_PLANNER_BLOCK`, interactive steps 2 and 3 become one step 2: "2. Write the change folder. Stop after the task list only when the human asks to review it first; then reply with the parent spec, the task list (titles only), the capability specs this change will write, and any `## Human steps`, and say the change folder is not written yet."
- [ ] "Either way" gains, after the `osq approve` line: "- Finish by telling the human, in chat, the task titles, that the change folder is written and `osq lint` passes, and the exact `osq approve <id>` to run. The human should not approve before that message."
- [ ] "Parent spec" ends with: "- Anything a task must not do itself goes under `## Human steps`, which never includes `osq approve`.", then "- Guidance a task needs about another capability's code, such as how to test against it, goes into that capability's spec through a delta, not only into the task.", then "- Replacing a requirement's behavior is a REMOVED requirement plus an ADDED one. A MODIFIED requirement must keep every scenario it already has; `osq lint` and archive refuse one that drops any."
- [ ] `buildBaseOpeningPrompt` in `src/cli/plan-queue.ts` puts "All living specs. Read the ones this change writes or whose code it uses." on its own paragraph between `## Capability Specs` and the list, which is otherwise unchanged.
- [ ] New `tests/managed-wording.test.ts` asserts each rule's key phrase in the constants, that `osq init` (`scaffoldProject`) on a temp project whose managed blocks hold older text replaces both, and that `buildBaseOpeningPrompt` output has the sentence and every living spec path. The repository copies and `tests/fixtures/prompts/*.txt` are refreshed to the new constants.

## Details

Wrap the planner block's new prose at the width the block already uses; the
quoted sentences are the text, not the line breaks. Refresh `AGENTS.md`,
`PLANNER.md`, `templates/PLANNER.md`, and `.opencode/agent/osq-coder.md` by
replacing the text between the `OSQ:START` and `OSQ:END` markers with the new
constant, as `tests/init-planner.test.ts` and `tests/managed-blocks.test.ts`
compare them. Regenerate the prompt fixtures from what
`tests/harness-prompt-injection.test.ts` renders; only step 3 may differ.
`tests/doctor.test.ts` runs `osq doctor` on this repository and must pass.
