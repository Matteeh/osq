---
title: When osq new seeds a proposal, its Human steps read None and the schema says they never include osq approve
verify: node --import tsx --test tests/proposal-seed-human-steps.test.ts
verify_starts: red
scope:
  - src/core/foundation/new.ts
  - templates/proposal.md
  - templates/openspec/schemas/osq/
  - openspec/schemas/osq/
  - tests/fixtures/events/
  - tests/proposal-seed-human-steps.test.ts
entry:
  - src/core/foundation/new.ts
  - templates/proposal.md
  - templates/openspec/schemas/osq/schema.yaml
  - tests/proposal-format.test.ts
  - tests/golden-events.test.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] The `## Human steps` body in `templates/proposal.md`, `templates/openspec/schemas/osq/templates/proposal.md`, and the fallback proposal in `src/core/foundation/new.ts` is the single line `None`.
- [ ] The `Human steps` instruction in `templates/openspec/schemas/osq/schema.yaml` reads: "**Human steps**: everything a task must not do itself, never including `osq approve`; write `None` when there is nothing."
- [ ] `openspec/schemas/osq/` holds the same bytes as `templates/openspec/schemas/osq/`.
- [ ] New `tests/proposal-seed-human-steps.test.ts` creates a change with `createNewSpec` and asserts its `## Human steps` section is `None` and the proposal contains no `osq approve`; it asserts the same for `FALLBACK_PROPOSAL_MD`, which `src/core/foundation/new.ts` now exports, and that the schema instruction contains "never including `osq approve`".
- [ ] The golden `measures` events under `tests/fixtures/events/` change only in `proposalWords`.

## Details

`tests/proposal-format.test.ts` compares the repository's schema with the
template copy byte for byte. Regenerate the golden events from what
`tests/golden-events.test.ts` produces. Do not change `loop.ts`; a `None`
section still prints after archive, which is out of scope.
