---
title: When an executor writes its result file, each disclosure heading names its reader and one parser reads the sections
verify: node --import tsx --test tests/result-sections.test.ts
verify_starts: red
scope:
  - src/core/foundation/init-blocks.ts
  - AGENTS.md
  - .opencode/agent/osq-coder.md
  - tests/fixtures/prompts/
  - tests/managed-blocks.test.ts
  - src/core/report/result-sections.ts
  - tests/result-sections.test.ts
entry:
  - src/core/foundation/init-blocks.ts
  - tests/managed-blocks.test.ts
  - tests/harness-prompt-injection.test.ts
tests:
  modify: true
skills: []
---
## Acceptance

- [ ] `RESULT_HEADINGS` lists, in order: `## Changed` "what you changed."; `## Deviated` "for the reviewer: what you did differently from the task, and why."; `## Missing context` "for the planner: what the task lacked that you needed."; `## Outside scope` "for the human: what you found broken outside your scope and left alone."; `## Next` "for unfinished work, the acceptance line to pick up next."
- [ ] `AGENTS.md` and `.opencode/agent/osq-coder.md` hold the new `MANAGED_AGENTS_MD_BODY` between their markers, and `tests/fixtures/prompts/*.txt` hold what `tests/harness-prompt-injection.test.ts` now renders; only the result heading lines differ.
- [ ] New `src/core/report/result-sections.ts` exports `parseResultSections(content)`, returning the text of `changed`, `deviated`, `missingContext`, `outsideScope`, `next`, and `touched`, each null when absent. Headings match after dropping leading `#`s, surrounding spaces, and one trailing colon, ignoring case and collapsing inner spaces; both a `## Touched:` heading and a final `Touched:` line fill `touched`. A section that is empty or only `None`, any case, optional period, is null.
- [ ] The same module exports `readChangeDisclosures(folderPath)`, which reads every `.run/results/<n>.md` in numeric order and returns `{ task, deviated, missingContext, outsideScope }` for each task with at least one non-null disclosure, and `countChangeDisclosures(folderPath)`, which returns how many tasks have each: `{ deviated, missingContext, outsideScope }`.
- [ ] New `tests/result-sections.test.ts` covers `## deviated:` with text, `## Missing context` of `None`, a real `##  Outside Scope`, a `## Touched:` heading, unknown headings ignored, and `readChangeDisclosures` on a temp folder.
- [ ] `tests/managed-blocks.test.ts` changes only its expected heading list.

## Details

`result-sections.ts` imports only `node:` modules. Refresh the repository copies
by replacing the text between the `OSQ:START` and `OSQ:END` markers with the
constant; `tests/doctor.test.ts` runs `osq doctor` on this repository and must
pass.
