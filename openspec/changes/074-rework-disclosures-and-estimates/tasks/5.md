---
title: When a model with recorded planning tokens has no price, osq doctor and osq approve name the missing key
verify: node --import tsx --test tests/planning-price-gaps.test.ts
verify_starts: red
scope:
  - src/core/report/planning-price-gaps.ts
  - src/core/foundation/doctor-prices.ts
  - src/core/foundation/doctor.ts
  - src/core/spec/approve.ts
  - src/cli/approve.ts
  - tests/planning-price-gaps.test.ts
entry:
  - src/core/foundation/doctor.ts
  - src/core/spec/approve.ts
  - src/cli/approve.ts
  - src/core/report/planning-records.ts
  - tests/planning-observed-approve.test.ts
skills: []
---
## Acceptance

- [ ] New `src/core/report/planning-price-gaps.ts` exports `findUnpricedPlanningModels(folders, prices)`: the sorted distinct `plan_started` models of sessions whose exit recorded a slice with non-null input or output tokens, or usage tokens, and that have no key in `prices`. It also exports `formatPriceKey(model)`, returning `planning.prices["<model>"]` with the model JSON-quoted.
- [ ] New `src/core/foundation/doctor-prices.ts` exports a check over every active and archived change that returns null when nothing is unpriced, and otherwise `{ name: 'planning-prices', ok: true, warning: true }` with a message naming every missing key and saying planning cost for those models stays unreported. `runDoctorChecks` appends it after the validator check only when it is not null.
- [ ] `ApproveResult` gains `missingPrices: string[]` from `findUnpricedPlanningModels` over the approved change's folder after observed sessions are appended; `osq approve` prints one line per model naming `formatPriceKey(model)` after the planning notice. Approval itself is unchanged.
- [ ] New `tests/planning-price-gaps.test.ts` covers the finder, the doctor check present and absent through `runDoctorChecks` on temp projects, and `approveSpec` plus the CLI line for a change with recorded `claude-opus-5-5` tokens and no price entry, then with one.

## Details

`doctor.ts` is 241 lines; keep the check in `doctor-prices.ts`. Existing doctor
tests on projects without planning records must see an unchanged check list.
`tests/planning-observed-approve.test.ts` shows how to record observed sessions
at approval.
