---
title: When the golden events test runs, the measures events match the checked-in fixture
verify: node --import tsx --test tests/golden-events.test.ts
scope: [tests/golden-events.test.ts, tests/fixtures/events/verified.jsonl, tests/fixtures/events/dead.jsonl]
entry: [tests/golden-events.test.ts]
skills: []
tests:
  modify: true
---
## Acceptance
- [ ] tests/fixtures/events/verified.jsonl includes measures start and measures end events in the correct position
- [ ] tests/fixtures/events/dead.jsonl includes measures start and measures end events in the correct position
- [ ] golden-events.test.ts maskEvent function masks measures event data fields that are non-deterministic
- [ ] golden events test for verified task passes with updated fixture
- [ ] golden events test for dead task passes with updated fixture

## Details

1. Update `tests/golden-events.test.ts` `maskEvent` function:
   - For `measures` events, mask non-deterministic fields in `data`:
     - `repoFiles` and `repoLines` may vary: replace with stable tokens like `0` or leave as-is since the temp project is deterministic.
     - Actually, since the temp project scaffold is fully controlled, all counts should be deterministic. Leave them as raw values in the fixture.
     - `scopeHashes` values: mask hash strings to `[HASH]` since file content hashes are stable but depend on scope file content in the temp project (which is also controlled, so they should be stable).
     - Actually, re-evaluate: the mock adapter doesn't write scope files, so before/after hashes should be identical and deterministic. The temp project has no src/ directory, so importFanIn is 0 and repo counts are deterministic.

2. Regenerate golden fixtures:
   - Run `UPDATE_GOLDEN=1 node --import tsx --test tests/golden-events.test.ts`.
   - Inspect the generated `verified.jsonl` and `dead.jsonl` to confirm measures events appear.
   - Commit the updated fixtures.

3. Verify:
   - Run `node --import tsx --test tests/golden-events.test.ts` without `UPDATE_GOLDEN` to confirm the fixtures match.

4. If maskEvent needs adjustments for determinism, update it and re-run `UPDATE_GOLDEN=1`.

5. Run verification:
   `node --import tsx --test tests/golden-events.test.ts`
