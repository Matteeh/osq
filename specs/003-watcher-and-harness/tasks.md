# Tasks

- [x] 1. When change folder markers are inspected, state is derived with stale lock reaping
- [x] 2. When a harness adapter is invoked, agent processes are configured before spawning
- [x] 3. When an approved task is dispatched, execution with verification gates enforces rules
- [x] 4. When all tasks complete, deltas are applied before change folders are archived
- [x] 5. When osq watch or setup is invoked, the event loop handles reactive execution
