# Tasks

- [x] 1. When change folder markers are inspected, state is derived with stale lock reaping
- [ ] 2. When a harness adapter is invoked, agent processes are configured before spawning
- [ ] 3. When an approved task is dispatched, execution with verification gates enforces rules
- [ ] 4. When all tasks complete, deltas are applied before change folders are archived
- [ ] 5. When osq watch or setup is invoked, the event loop handles reactive execution
