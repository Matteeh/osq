# Tasks

- [x] 1. When spec files are parsed, YAML frontmatter and markdown sections are extracted
- [x] 2. When a change folder is linted, specification rules from config are enforced
- [ ] 3. When a change folder is hashed, a deterministic digest is computed excluding .run
- [ ] 4. When osq approve is invoked, valid change folders are sealed with an approved marker
