## Changed

- Updated `src/core/config.ts`:
  - Added `OpencodeConfig` interface defining `bin`, `model`, `agent`, and optional `variant`.
  - Added `opencode` field to `OsqConfig` interface.
  - Added `DEFAULT_CONFIG.opencode` with defaults: `bin: "opencode"`, `model: "deepseek/deepseek-flash"`, and `agent: "osq-coder"`.
  - Updated `defineConfig` to merge `opencode` defaults with user overrides.
  - Updated `loadConfig` to support `harness: "opencode"` and propagate `opencode` configuration including optional `OSQ_MODEL` override.
- Created `src/harness/opencode.ts`:
  - Created `OpencodeAdapter` implementing `HarnessAdapter` with `name = 'opencode'`.
- Updated `src/harness/index.ts`:
  - Registered `opencode` in adapters dictionary returning `new OpencodeAdapter()`.
  - Exported `OpencodeAdapter` from `./opencode.js`.
- Updated `README.md`:
  - Documented `opencode` in the available harness adapter list in the `## Harnesses` section.
  - Added a sample configuration block demonstrating how to configure `opencode` in `osq.config.ts`.
- Created `tests/opencode-config.test.ts`:
  - Added tests for all five acceptance criteria covering configuration defaults, custom overrides, `loadConfig`/`defineConfig` support, adapter resolution via `getHarnessAdapter`, and README documentation.

## Next Steps

- Proceed to Task 3 (`specs/008-opencode-harness-adapter/tasks/3.md`): Implement `OpencodeAdapter.setup` to scaffold `.opencode/agent/osq-coder.md` idempotently with subagent mode, strict permissions, AGENTS.md procedure, and document README auto caveat.
