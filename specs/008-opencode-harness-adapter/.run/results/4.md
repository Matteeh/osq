## Changed

- Updated `src/harness/opencode.ts`:
  - Implemented `resolveOpencodeBinary` supporting `OPENCODE_PATH` environment variable, `config.opencode.bin`, and fallback to `'opencode'`.
  - Implemented `buildOpencodePrompt` constructing the positional prompt matching the AGENTS.md protocol, including task file, parent spec, scope, entry, verify command, and the seven execution rules.
  - Implemented `buildOpencodeArgs` and exported `spawnTask` constructing CLI arguments: `run --agent <agent> --auto --format json --dir <projectRoot> --model <model>`, optional `--variant <variant>`, and attaching `--file <task.md>`, `--file <spec.md>`, and `--file <featureDoc>` for features named in task frontmatter or parent `spec.md`.
  - Implemented `OpencodeAdapter.spawn` executing the binary via `spawnWithTimeout`, logging `started` and `exited` events to `.run/events/<n>.jsonl`, and returning structured `SpawnResult` with exit code, timeout flag, signal, and error diagnostics.
- Created `tests/opencode-spawn.test.ts`:
  - Added tests for argument construction with default and customized agent and model.
  - Added tests verifying inclusion of `--variant` when configured and omission when absent.
  - Added tests verifying attachment of `task.md`, `spec.md`, and named feature docs from task frontmatter and parent `spec.md` fallback.
  - Added tests verifying positional prompt content matching AGENTS.md guidelines.
  - Added tests verifying execution with a fake opencode binary for passed flag validation, non-zero exit code propagation, and timeout termination under SIGTERM.

## Next Steps

- Proceed to Task 5 (`specs/008-opencode-harness-adapter/tasks/5.md`): Implement stdout JSON event streaming to parse `step_finish` events into `tokens` events while safely ignoring unknown or unparseable lines.
