---
title: When a task finishes, exactly one outcome line is logged
verify: node --import tsx --test tests/runner-outcome-logging.test.ts
scope: [src/watcher/runner.ts, tests/runner-outcome-logging.test.ts]
entry: [src/watcher/runner.ts]
skills: []
---
## Acceptance
- [ ] formatTaskOutcomeSummary is completely deleted from src/watcher/runner.ts and no longer exists in src/
- [ ] Test imports runner.ts and asserts formatTaskOutcomeSummary is not exported
- [ ] Duplicate call sites in runner.ts are removed and all outcomes route through logOutcome using formatTaskOutcomeLine
- [ ] formatTaskOutcomeLine handles all failure reasons (spec_conflict, already_running, crashed, timeout, no_result, verify_red) and detail strings
- [ ] End-to-end mock harness tests for verified and dead tasks assert exactly one info outcome line each with no (passed) string

## Signatures

In `src/watcher/runner.ts`:

```ts
export function formatTaskOutcomeLine(
  taskNumber: string,
  success: boolean,
  reason: RunTaskFailureReason | undefined,
  elapsedSeconds: number,
  symbols: boolean,
  extra?: string,
): string;
```

## Details

1. In `src/watcher/runner.ts`:
   - Delete `formatTaskOutcomeSummary` definition and export entirely.
   - Update `formatTaskOutcomeLine` signature to accept optional `extra?: string` detail parameter, formatting `${resolveSymbol('✗', '[dead]', symbols)} task ${taskNumber} dead (reason: ${reason}${detail}, elapsed: ${elapsedSeconds}s)`.
   - Update `logOutcome` in `runTask` to accept `(success: boolean, reason?: RunTaskFailureReason, extra?: string)` and pass `extra` to `formatTaskOutcomeLine`.
   - Remove duplicate `logger?.info(formatTaskOutcomeSummary(...))` calls across all completion and failure paths in `runTask` (spec_conflict, crashed, already_running, no_result, verify_red, success), routing exclusively through `logOutcome`.
2. In `tests/runner-outcome-logging.test.ts`:
   - Assert `formatTaskOutcomeSummary` is not exported by `runner.ts`.
   - Run end-to-end mock harness execution for a verified task and a dead task, asserting info-level output contains exactly one outcome line each and no `(passed)` string.