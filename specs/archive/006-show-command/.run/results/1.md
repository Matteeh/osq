## Changed
- Created `src/core/show.ts` with `resolveSpecFolder`, `getSpecDetails`, and `formatSpecDetails` to resolve active and archived specs, extract spec metadata, contract, tasks, results, dead markers, and chronologically sort event streams.
- Created `src/cli/show.ts` implementing `showCommand` for CLI invocation.
- Updated `src/cli/index.ts` to register `show <id>` command in Commander.
- Created `tests/show.test.ts` verifying all 5 acceptance lines:
  - Cross-directory resolution (active and archive)
  - Extraction of spec metadata, tasks, results, and dead markers
  - Chronological event timeline parsing from `.run/events/<n>.jsonl`
  - Formatted inspection printing in `showCommand`
  - Commander CLI command registration

## Drift against features/
- `features/status-inspection.md` does not yet detail `osq show <id>` and its output sections; delta in `specs/006-show-command/spec.md` will be applied upon watcher approval and archiving.

## Next steps
- Task 1 is complete. Ready for watcher verification gate and archiving.
