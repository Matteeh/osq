# Task 5 — Changelog and release documentation

## Changed

- `CHANGELOG.md` (new)
  - `# Changelog` heading with the Keep a Changelog intro line.
  - Single `## [0.1.0] - 2026-09-18` release entry summarising specs 001 through 012 in six
    grouped bullets (core queue 001/002, watcher/locking 003/004, inspection+reporting
    005/006/007/010, adapters 008, observability 009/011, watch UX 012).
- `README.md`
  - New `## Release Procedure` section inserted between `## Commands` and `## Not yet`,
    documenting the 5 steps: bump `package.json` version, add a `CHANGELOG.md` release section,
    `git commit -am "release: v<x.y.z>"`, `git tag v<x.y.z> && git push --tags`, and the
    `release.yml` trusted-publishing follow-through.
- `tests/changelog-release-docs.test.ts` (new)
  - Asserts `CHANGELOG.md` exists, opens with `# Changelog`, and contains `## [0.1.0]`.
  - Asserts the 0.1.0 entry references every spec id from 001 through 012.
  - Asserts `README.md` has a `## Release Procedure` section covering version bump, changelog,
    tag, and push.
  - Asserts the exact commands are present: `git commit`, `git tag`, `git push --tags`.

## Verified

- Task verify `node --import tsx --test tests/changelog-release-docs.test.ts` → 4 pass / 0 fail.
- `pnpm tsc --noEmit` clean.
- `pnpm lint` clean (83 files, no fixes applied).

## Deviated

- None. The README section is inserted before `## Not yet` rather than literally at the file's
  end, keeping the document's existing section flow while still satisfying the "append a
  dedicated section" intent.

## Drift against features/

None. `features/cli-foundation.md` is untouched; the documented `Version Tracking & Release
Procedure` behavior from the spec Delta matches this task's output.

## Missing context / notes

- The signature content for both files was reproduced verbatim, so no ambiguity remained.
- `package.json` already carries `"version": "0.1.0"`, consistent with the changelog entry.
- Spec 013 task 5 is the last task in this spec; no follow-on acceptance line remains.
