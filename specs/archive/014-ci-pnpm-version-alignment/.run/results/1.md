# Task 1 result

## Changed
- `.github/workflows/ci.yml`: `pnpm/action-setup@v4` step no longer declares `with.version: 9`; version is derived from `packageManager` in `package.json`.
- `.github/workflows/release.yml`: same removal of `with.version: 9` from the `pnpm/action-setup@v4` step.
- `tests/release-workflow.test.ts`: added a "pnpm setup version delegation" suite that reads `package.json`, `ci.yml`, and `release.yml`, asserts `packageManager` pins an exact pnpm version, and asserts both workflows use `pnpm/action-setup@v4` with no `with.version`. Updated the existing runner-setup test to assert absence of `with.version` instead of presence.

## Verify
`pnpm verify` exit 0: tsc clean, 332 tests pass / 0 fail, biome lint clean (0 diagnostics).

## Deviated
None. All acceptance lines satisfied.

## Notes
The parent spec Delta mentions updating `features/cli-foundation.md`. Per AGENTS.md, feature docs are only changed by the watcher applying an approved delta, so that was left untouched for the watcher/human gate.
