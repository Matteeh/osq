## Changed

- Updated `src/harness/types.ts`:
  - Added optional `preflight?(projectRoot: string, config: OsqConfig): Promise<void>` method to `HarnessAdapter` interface.
- Updated `src/harness/opencode.ts`:
  - Implemented `preflightOpencode(projectRoot: string, config?: OsqConfig)` to resolve binary via config or `OPENCODE_PATH`, execute `<bin> --version`, print a single error line on missing or failing binary with process exit code 1, and log resolved version string on success.
  - Implemented `preflight` method on `OpencodeAdapter` invoking `preflightOpencode`.
- Updated `src/watcher/loop.ts`:
  - Added preflight verification check in `startWatcher` when harness is `opencode` before any task dispatch or polling loop begins.
- Created `tests/watcher-preflight.test.ts`:
  - Added test suite verifying preflight version checks, failure behavior on missing or failing binary, non-dispatch of tasks when preflight fails, version string logging, and adapter preflight execution.

Completed manually after agy --print-timeout cutoff; see dead/6.md history.
