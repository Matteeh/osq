## Changed

- Created `src/harness/process.ts` exporting `DEFAULT_KILL_GRACE_PERIOD_MS` and `spawnWithTimeout`. It executes child processes with configurable command, args, cwd, and environment, streams stdout and stderr, terminates long-running processes via `SIGTERM` when exceeding `timeoutSeconds`, and escalates to `SIGKILL` after a 5000ms grace period if `SIGTERM` fails to terminate the process.
- Updated `src/harness/agy.ts` to delegate process spawning and timeout signal management to `spawnWithTimeout`, preserving all existing behavior including event recording (`started`, `exited`) and timeout exit code handling.
- Created `tests/harness-process.test.ts` containing tests for all five acceptance criteria:
  1. Spawning child processes with given command, args, cwd, and environment variables.
  2. Terminating processes with `SIGTERM` on timeout.
  3. Escalating to `SIGKILL` after 5000ms grace period when `SIGTERM` is ignored.
  4. Setting `timedOut: true` and returning non-zero exit code (`124`) on timeout.
  5. `AgyAdapter` utilizing `spawnWithTimeout` while preserving timeout behavior and event logging.

## Next Steps

- Proceed to Task 2 (`specs/008-opencode-harness-adapter/tasks/2.md`).
