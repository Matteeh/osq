# Task 5 result

## Changed

- `src/core/report.ts`:
  - Added `export interface CostMetrics { total; perSpec: Record<string, number>; formattedTotal }` and optional `cost?: CostMetrics` on `MetricsReport`.
  - Added `export function formatCost(total)` — `toFixed(2)` (e.g. `$0.15`) at or above one cent, `toFixed(4)` (e.g. `$0.0012`) below one cent.
  - While scanning every spec's `.run/events/*.jsonl`, accumulates any numeric finite `event.data.cost` into `totalCost` and into `perSpecCost[path.basename(folderPath)]`. No internal price table is used; only harness-reported values.
  - The returned `MetricsReport` includes `cost` only when `totalCost > 0` (spread conditional, so the property is omitted, not `undefined`). `perSpec` drops zero/negative entries and sorts keys.
  - `formatMetricsReport` appends a `Cost:` section with `  Reported cost: <formattedTotal>` after Token Usage only when `report.cost.total > 0`; otherwise the section and line are absent.
- `tests/report-cost.test.ts` (new): 7 tests — fixture totals and per-spec breakdown (`0.1533` ±0.0001, `$0.15`, 008 omitted), `CostMetrics` key shape, formatted cost line, sub-cent `$0.0012` formatting, cost-free omission (`report.cost === undefined`, no `Reported cost`/`Cost:` text), and the README disclaimer.
- `README.md`: added an `osq report` paragraph noting the cost values come from harness events and "Reported cost reflects the harness's internal price table rather than the invoice."

## Deviated

- `perSpec` keys are the spec folder basenames (e.g. `009-watcher-observability`), since neither the task nor the spec fixes a key format. `fixture/report/` has only one cost-bearing spec, so collisions between active/archived folders with the same basename are not exercised.
- Cost is read from any event's `data.cost`, not only `tokens` events, matching the task wording ("events carry cost fields"). The fixture puts cost on `tokens` events.

## Drift against features/

- `features/metrics-and-reporting.md` is still the one-line stub; it does not yet document reported cost, `--json`, failure history, or metric sourcing. This is expected: the spec's `## Delta` is applied by the watcher once all tasks complete, and agents must not edit `features/`.
- `features/watcher-and-harness.md` has pre-existing uncommitted edits from task 6/8 workstreams but still lacks the task-6 file-change wording and the done/dead event delta; not in task 5 scope.

## Next steps

All six task 5 acceptance lines are complete and verified. Next unstarted task is 6 (file change metrics, `tests/report-file-changes.test.ts`) — note task 6 also touches `fixture/report/**`, which task 5 did not modify.

## Verify

- `node --import tsx --test tests/report-cost.test.ts` passes (7/7).
- `pnpm tsc --noEmit` passes; `pnpm lint` passes.
- `pnpm test` passes (216/216).
