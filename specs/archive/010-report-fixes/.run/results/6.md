# Task 6 result

## Changed

- `src/core/report.ts`:
  - `FileChangeMetrics` now matches the task signature: `readonly totalChanges`, `readonly uniqueCount`, `readonly uniqueFiles: readonly string[]`, plus backward-compatible `readonly totalEvents`.
  - Added `FILE_CHANGE_TOOLS = new Set(['edit', 'write'])` and helpers `normalizeFilePath` (trims whitespace, strips leading `./`, rejects empty results) and `extractFilePath` (first usable of `summary`, `path`, `filePath`, `file`, `filename`).
  - Event scan now increments `totalFileChanges` for every `tool` event whose `data.tool` lowercases to `edit` or `write`, and adds the normalized path to the unique set when one is present. Read/glob/bash tool events do not count.
  - Legacy `file_changed` handling is retained as a fallback and now also normalizes `file`/`path`/`filePath`/`filename` and entries of `files`.
- `tests/report-file-changes.test.ts` (new): 7 tests covering all six acceptance lines — fixture totals (3 changes, 2 unique, exact sorted paths), fixture event-stream presence of duplicate edit/write paths and an ignored read event, path extraction from all four fields with normalization, case-insensitive tool names, edit/write events with no path still counting toward the total, cross-spec deduplication, and legacy `file_changed` fallback.
- `fixture/report/specs/archive/009-watcher-observability/.run/events/5.jsonl`: appended four `tool` events per the task details (edit `src/harness/opencode.ts`, write `tests/opencode-tool-events.test.ts`, duplicate edit `src/harness/opencode.ts`, read `src/harness/types.ts`).

## Deviated

- The duplicate-path tool events were appended to the end of `5.jsonl` rather than interleaved chronologically. Reporting reads per-type, not order, and the timestamps stay inside the file's existing min/max range, so durations/tokens are unaffected.
- `extractFilePath` also accepts `filename` (used only by legacy `file_changed` events) beyond the four fields named in the task, to preserve existing backward-compatible behavior.

## Drift against features/

- `features/metrics-and-reporting.md` is still the one-line stub and does not yet document file-change derivation from `edit`/`write` tool events. Expected: the spec `## Delta` is applied by the watcher after all tasks complete; agents must not edit `features/`.

## Next steps

All six task 6 acceptance lines are complete and verified. Task 6 also touched `fixture/report/**`, which earlier report tasks did not. Full suite remains green.

## Verify

- `node --import tsx --test tests/report-file-changes.test.ts` passes (7/7).
- `pnpm tsc --noEmit` passes; `pnpm lint` passes.
- `pnpm test` passes (223/223).
