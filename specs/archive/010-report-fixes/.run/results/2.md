# Task 2 result

## Changed

- `fixture/report/specs/archive/008-opencode-harness-adapter/` and `.../009-watcher-observability/`: copied `spec.md`, `tasks/*.md`, `.run/done/*`, `.run/events/*.jsonl` from `specs/archive/`. 14 tasks total.
- `fixture/report/specs/archive/009-watcher-observability/.run/events/1.jsonl`: inserted a `dead` event (`task: "1"`, `reason: "crashed"`) immediately after the first `exited` `exitCode:1` event, and appended a `done` event (`task: "1"`) at the end, producing the dead-then-retried history.
- `src/core/report.ts`: imported `deriveSpecState` + `compareNumericPrefix` from `./state.js`; replaced the manual marker/running/pending loop with:
  - active specs: `deriveSpecState(projectRoot, folderPath)` and tally of `specState.tasks` (`done`/`dead`/`running`/`pending`, dead reason into `deadBreakdown`);
  - archived specs: new `readArchivedTerminalEvents()` scans `.run/events/*.jsonl` in numeric order and keeps the latest `done`/`dead` per task, then falls back to `.run/done/<n>` and `.run/dead/<n>.md` markers, and finally counts tasks as done when neither exists. Archived branches never increment `pendingTasks` or `runningTasks`.
- `tests/report-task-states.test.ts`: new test file covering the fixture counts (2 specs, 14 tasks, 14 done, 0 dead/running/pending, 100%), the dead-before-done event ordering, active-spec tally parity with `deriveSpecState`, archived event derivation, marker fallback, and never-pending behavior.

## Deviated

- Details said to copy only tasks/done/events; I also copied each archived `spec.md` so the fixture folders are well-formed specs. It is not read by the archived path.
- Event terminal lookup is per task (falling back per task to markers), not per spec. Needed because 009 only has `done`/`dead` events for task 1 while tasks 2-8 only have done markers; a spec-level "has events" switch would have misclassified those as pending/done-fallback.
- "Remove the manual marker and status parsing loop" applied to task-status tallying only. Marker parsing remains for (a) the archived fallback and (b) `deadBreakdown` for active specs, since task 3 is the task that moves failure aggregation to `dead` events. Existing `tests/report.test.ts` still passes.

## Drift against features/

`features/metrics-and-reporting.md` is still the placeholder copy; the spec 010 `## Delta` has not been applied yet. No feature files were modified (correct: only the watcher applies approved deltas).

## Next steps

All task 2 acceptance lines are complete. Next unstarted task is 3 (`tests/report-failure-breakdown.test.ts`), which replaces `deadBreakdown` aggregation with a scan of every `dead` event.

## Verify

`node --import tsx --test tests/report-task-states.test.ts` passes (7/7). Full `pnpm tsc --noEmit && pnpm test && pnpm lint` passes (193 tests, 0 lint errors).
