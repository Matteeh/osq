---
title: When the report counts task states, active specs use deriveSpecState while archived specs use events
verify: node --import tsx --test tests/report-task-states.test.ts
scope: [fixture/report/**, src/core/report.ts, tests/report-task-states.test.ts]
entry: [src/core/report.ts]
skills: []
---
## Acceptance
- [ ] fixture/report/ contains archived specs 008 and 009 with one dead-then-retried task in 009 events
- [ ] Custom status parsing in src/core/report.ts is replaced by deriveSpecState for active specs
- [ ] Archived specs derive task status from done and dead events in events.jsonl
- [ ] Archived specs without done or dead events fall back to done and dead marker files
- [ ] Archived specs never report pending tasks under any condition
- [ ] tests/report-task-states.test.ts verifies getMetricsReport reports 14 total tasks, 14 done, 0 pending on fixture/report/

## Signatures

In `src/core/report.ts`:

```ts
import { deriveSpecState } from './state.js';

export async function getMetricsReport(
  projectRoot: string,
  config?: OsqConfig,
): Promise<MetricsReport>;
```

## Details

1. Create test fixture directory `fixture/report/`:
   - Copy `specs/archive/008-opencode-harness-adapter` (tasks 1-6, .run/done/1-6, .run/events/1-6.jsonl) into `fixture/report/specs/archive/008-opencode-harness-adapter`.
   - Copy `specs/archive/009-watcher-observability` (tasks 1-8, .run/done/1-8, .run/events/1-8.jsonl) into `fixture/report/specs/archive/009-watcher-observability`.
   - Edit `fixture/report/specs/archive/009-watcher-observability/.run/events/1.jsonl` to add `{ "type": "dead", "timestamp": "2026-09-17T21:37:46.920Z", "data": { "task": "1", "reason": "crashed" } }` after the first exitCode 1 event, and `{ "type": "done", "timestamp": "2026-09-17T21:51:35.500Z", "data": { "task": "1" } }` at the end.
2. In `src/core/report.ts`, remove the manual marker and status parsing loop.
3. For active specs (`activeFolders`):
   - Call `deriveSpecState(projectRoot, folder.path)`.
   - Tally task states directly from `specState.tasks`: `status === 'done'` increments `doneTasks`, `status === 'dead'` increments `deadTasks`, `status === 'running'` increments `runningTasks`, and `status === 'pending'` increments `pendingTasks`.
4. For archived specs (`archivedFolders`):
   - An archived spec is completed; it must never increment `pendingTasks` or `runningTasks`.
   - Check `.run/events/*.jsonl` for `done` and `dead` events. If present, determine final status per task from the latest terminal event (`done` or `dead`).
   - If a spec has no `done` or `dead` events (legacy specs predating event logging), fall back to checking `.run/done/<task>` and `.run/dead/<task>.md` markers. If markers are absent (such as bootstrapped specs), count tasks as done.
5. In `tests/report-task-states.test.ts`, assert `getMetricsReport` run on `fixture/report/` returns:
   - `specs.total: 2`, `specs.active: 0`, `specs.archived: 2`
   - `tasks.total: 14`, `tasks.done: 14`, `tasks.dead: 0`, `tasks.running: 0`, `tasks.pending: 0`
   - `completionRate: 100`
